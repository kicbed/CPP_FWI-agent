"""SQLite authority for D-003 task identity and immutable runtime records.

The store is deliberately unaware of HTTP, MCP, workers, and dataset paths.
Business validation belongs to :mod:`scientific_runtime.task_service`; this
module provides short atomic transactions and durable read models.
"""

from __future__ import annotations

import copy
import fcntl
import hashlib
import json
import os
import re
import sqlite3
import stat
import time
from dataclasses import dataclass, replace
from datetime import datetime, timedelta, timezone
from pathlib import Path
from typing import Any, Callable, Mapping, Protocol, Sequence

from scientific_runtime_contracts import schema_errors

from .dag_data_binding import (
    DagDataBindingError,
    bind_dag_artifact_input,
)
from .dag_node_cache import (
    NodeCacheIdentity,
    NodeCacheIdentityError,
    build_node_cache_identity,
)
from .dag_scheduler import DagScheduleError, evaluate_dag_readiness


MIGRATIONS_DIRECTORY = Path(__file__).with_name("migrations")
APPLICATION_ID = 0x53525431  # ASCII "SRT1"
SCHEMA_MIGRATIONS_SQL = """
CREATE TABLE schema_migrations (
    version INTEGER PRIMARY KEY,
    name TEXT NOT NULL UNIQUE,
    checksum TEXT NOT NULL,
    applied_at TEXT NOT NULL
)
"""

TASK_STATUSES = frozenset(
    {
        "Draft",
        "NeedsInput",
        "AwaitingApproval",
        "Queued",
        "Running",
        "Waiting",
        "Retrying",
        "Succeeded",
        "Failed",
        "Cancelled",
    }
)

WORKBENCH_MUTATION_OPERATIONS = frozenset(
    {"revise_draft", "persist_plan", "persist_approval", "abandon_task"}
)

TASK_VISIBILITY_OPERATIONS = frozenset({"trash_task", "restore_task"})
TASK_VISIBILITY_VIEWS = frozenset({"active", "trash"})
TASK_PURGE_LOCAL_RUN_STATES = frozenset({"deleted", "not_created"})

WORKER_SUBMISSION_ID = re.compile(r"^submission-[0-9a-f]{64}$")
WORKER_ATTEMPT_ID = re.compile(r"^attempt-[0-9a-f]{32}$")
WORKER_JOB_ID = re.compile(r"^fwi-[0-9]{8}T[0-9]{6}Z-[0-9a-f]{12}$")
WORKER_EVIDENCE_TIMESTAMP = re.compile(
    r"^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}"
    r"(?:\.[0-9]{3}|\.[0-9]{6})?Z$"
)
DOCUMENT_HASH = re.compile(r"^sha256:[0-9a-f]{64}$")
RUNTIME_TIMEOUT_ID = re.compile(r"^timeout-[0-9a-f]{32}$")
RUNTIME_CHECKPOINT_ID = re.compile(r"^checkpoint-[0-9a-f]{32}$")
RUNTIME_RESUME_ID = re.compile(r"^resume-[0-9a-f]{32}$")

ALLOWED_TRANSITIONS: dict[str, frozenset[str]] = {
    "Draft": frozenset({"Draft", "NeedsInput", "AwaitingApproval", "Cancelled"}),
    "NeedsInput": frozenset(
        {"NeedsInput", "Draft", "AwaitingApproval", "Cancelled"}
    ),
    "AwaitingApproval": frozenset({"AwaitingApproval", "Queued", "Cancelled"}),
    "Queued": frozenset({"Running", "Retrying", "Failed", "Cancelled"}),
    "Running": frozenset(
        {"Running", "Waiting", "Retrying", "Succeeded", "Failed", "Cancelled"}
    ),
    "Waiting": frozenset({"Running", "Failed", "Cancelled"}),
    "Retrying": frozenset({"Running", "Failed", "Cancelled"}),
    # Terminal-state retries are reads, not new transitions/events.
    "Succeeded": frozenset(),
    "Failed": frozenset(),
    "Cancelled": frozenset(),
}


class TaskStoreError(RuntimeError):
    """Base class for stable persistence failures."""


class TaskStoreConflict(TaskStoreError):
    """A uniqueness, revision, relationship, or state precondition failed."""


class IdempotencyConflict(TaskStoreConflict):
    """An idempotency key was replayed with a different request."""


class TaskStoreCorruption(TaskStoreError):
    """Persisted data is inconsistent with the migration contract."""


class TaskStoreUnavailable(TaskStoreError):
    """The durable store could not acquire its bounded SQLite lock."""


class RuntimeSupervisorLeaseLost(TaskStoreConflict):
    """A supervisor write no longer owns the exact active fenced lease."""


@dataclass(frozen=True)
class TaskCancellationSnapshot:
    """Durable projection of one exact-attempt runtime cancellation."""

    request_id: str
    task_id: str
    intent_id: str
    attempt_id: str
    reason: str
    requested_at: str
    state: str
    result: str | None = None
    terminal_status: str | None = None
    resolved_at: str | None = None
    adapter_proof: dict[str, Any] | None = None


@dataclass(frozen=True)
class TaskTimeoutSnapshot:
    """Durable projection of one exact-attempt runtime timeout window."""

    timeout_id: str
    task_id: str
    intent_id: str
    attempt_id: str
    wall_time_seconds: int
    started_at: str
    deadline_at: str
    state: str
    result: str | None = None
    failure_code: str | None = None
    terminal_status: str | None = None
    resolved_at: str | None = None
    adapter_proof: dict[str, Any] | None = None


@dataclass(frozen=True)
class TaskCheckpointSnapshot:
    """Bounded public projection of one exact same-attempt checkpoint."""

    checkpoint_id: str
    intent_id: str
    node_id: str
    submission_id: str
    attempt_id: str
    attempt_number: int
    checkpoint_index: int
    completed_updates: int
    checkpoint_manifest_relative_path: str
    checkpoint_manifest_size_bytes: int
    checkpoint_manifest_hash: str
    checkpoint_receipt_record_hash: str
    checkpoint_created_at: str
    state: str
    resume_id: str | None = None
    resume_requested_at: str | None = None
    resume_acknowledged_at: str | None = None


@dataclass(frozen=True)
class TaskSnapshot:
    """Current durable view of one task aggregate."""

    task_id: str
    project_id: str
    principal_id: str
    status: str
    draft: dict[str, Any]
    plan: dict[str, Any] | None
    approval: dict[str, Any] | None
    created_at: str
    updated_at: str
    abandonment: dict[str, Any] | None = None
    visibility_revision: int = 0
    trashed_at: str | None = None
    purge_id: str | None = None
    purge_requested_at: str | None = None
    purged_at: str | None = None
    purge_local_run_state: str | None = None
    cancellation: TaskCancellationSnapshot | None = None
    timeout: TaskTimeoutSnapshot | None = None
    checkpoint: TaskCheckpointSnapshot | None = None


@dataclass(frozen=True)
class TaskSnapshotPage:
    """One stable, scope-bound keyset page of durable task aggregates."""

    snapshots: tuple[TaskSnapshot, ...]
    next_cursor: str | None


@dataclass(frozen=True)
class CreateTaskRecord:
    """Store result indicating whether an idempotent create was replayed."""

    snapshot: TaskSnapshot
    replayed: bool


@dataclass(frozen=True)
class RegistryWriteRecord:
    """Immutable registry document and whether the write was a replay."""

    document: dict[str, Any]
    replayed: bool


@dataclass(frozen=True)
class RegistrySnapshots:
    """Server-owned registry documents read from one SQLite snapshot."""

    datasets: dict[tuple[str, str], dict[str, Any]]
    algorithms: dict[tuple[str, str], dict[str, Any]]


@dataclass(frozen=True)
class ApprovalBudget:
    """Durable task-count budget bound to one immutable approval."""

    task_id: str
    approval_id: str
    max_tasks: int
    tasks_used: int


@dataclass(frozen=True)
class SubmitGateContext:
    """Current server-owned submit inputs pinned by one write transaction."""

    snapshot: TaskSnapshot
    registry: RegistrySnapshots
    budget: ApprovalBudget
    task_budget_already_consumed: bool = False


@dataclass(frozen=True)
class DispatchReconciliationSnapshot:
    """Original reconciliation case and any positive-evidence resolution."""

    failure_code: str
    recorded_at: str
    state: str
    result: str | None = None
    evidence_kind: str | None = None
    resolved_at: str | None = None
    action_classification: str | None = None
    action_failure_code: str | None = None
    action_observed_at: str | None = None


@dataclass(frozen=True)
class DispatchIntentSnapshot:
    """Durable dispatch state projected from immutable intent/claim/outcome rows."""

    intent_id: str
    task_id: str
    plan_id: str
    plan_hash: str
    approval_id: str
    node_id: str
    node_idempotency_key: str
    adapter_id: str
    adapter_version: str
    request: dict[str, Any]
    request_hash: str
    queue_fingerprint: dict[str, Any]
    state: str
    handle: dict[str, Any] | None
    failure_code: str | None
    created_at: str
    dispatch_claimed_at: str | None
    outcome_recorded_at: str | None
    reconciliation: DispatchReconciliationSnapshot | None = None


@dataclass(frozen=True)
class SubmitTaskRecord:
    """Atomic submit result and whether the operation was an exact replay."""

    snapshot: TaskSnapshot
    intent: DispatchIntentSnapshot
    replayed: bool


@dataclass(frozen=True)
class WorkbenchMutationRecord:
    """A durable non-submit Workbench mutation and its stable outcome."""

    task_id: str
    operation: str
    outcome: dict[str, Any]


@dataclass(frozen=True)
class TaskVisibilityMutationRecord:
    """One exact trash/restore outcome and whether it was replayed."""

    snapshot: TaskSnapshot
    replayed: bool


@dataclass(frozen=True)
class TaskPurgeRecord:
    """One durable two-phase purge reservation or completion."""

    snapshot: TaskSnapshot
    purge_id: str
    outcome: dict[str, Any] | None
    replayed: bool


@dataclass(frozen=True)
class RetryExhaustionCleanupProof:
    """Store-authenticated input for no-handle Adapter cleanup.

    This is projected only from the immutable retry-exhaustion audit chain
    after a matching task-purge reservation exists.  It is deliberately not a
    public request contract: the TaskService obtains it from SQLite and passes
    its path-free token directly to the fixed Dispatcher/Adapter boundary.
    """

    purge_id: str
    intent_id: str
    task_id: str
    project_id: str
    principal_id: str
    approval_id: str
    attempt_id: str
    observation_sequence: int
    evidence: dict[str, Any]
    evidence_hash: str
    private_schema_version: str
    private_proof_hash: str
    failure_kind: str
    previous_attempt_id: str
    previous_observation_sequence: int
    previous_private_proof_hash: str
    retry_reserved_at: str
    terminal_event_sequence: int
    terminal_event_hash: str
    exhausted_at: str
    previous_failure_kind: str = "pre_running_launch_failure"
    previous_private_schema_version: str | None = None

    def adapter_token(self) -> dict[str, Any]:
        """Return a canonical purge-bound token for the private state check."""

        worker_exit_lineage = self.previous_failure_kind == "worker_exit"
        if worker_exit_lineage:
            if self.previous_private_schema_version not in {"1.1.0", "1.2.0"}:
                raise TaskStoreCorruption(
                    "worker-exit cleanup proof has an invalid private lineage"
                )
        elif (
            self.previous_failure_kind != "pre_running_launch_failure"
            or self.previous_private_schema_version is not None
        ):
            raise TaskStoreCorruption(
                "pre-running cleanup proof has an invalid private lineage"
            )
        payload = {
            "schema_version": "1.1.0" if worker_exit_lineage else "1.0.0",
            "purge_id": self.purge_id,
            "intent_id": self.intent_id,
            "task_id": self.task_id,
            "project_id": self.project_id,
            "principal_id": self.principal_id,
            "approval_id": self.approval_id,
            "attempt_id": self.attempt_id,
            "attempt_number": 2,
            "observation_sequence": self.observation_sequence,
            "evidence": copy.deepcopy(self.evidence),
            "evidence_hash": self.evidence_hash,
            "private_schema_version": self.private_schema_version,
            "private_proof_hash": self.private_proof_hash,
            "failure_kind": self.failure_kind,
            "previous_attempt_id": self.previous_attempt_id,
            "previous_observation_sequence": self.previous_observation_sequence,
            "previous_private_proof_hash": self.previous_private_proof_hash,
            "retry_reserved_at": self.retry_reserved_at,
            "terminal_event_sequence": self.terminal_event_sequence,
            "terminal_event_hash": self.terminal_event_hash,
            "exhausted_at": self.exhausted_at,
        }
        if worker_exit_lineage:
            payload.update(
                {
                    "previous_failure_kind": self.previous_failure_kind,
                    "previous_private_schema_version": (
                        self.previous_private_schema_version
                    ),
                }
            )
        return {**payload, "proof_hash": encode_document(payload)[1]}


@dataclass(frozen=True)
class RuntimeSupervisorLease:
    """Current durable control-plane supervisor term for one exact scope."""

    project_id: str
    principal_id: str
    fencing_token: int
    owner_id: str
    state: str
    acquired_at: str
    heartbeat_at: str
    expires_at: str


@dataclass(frozen=True)
class RuntimeSupervisorLeaseAcquisition:
    """Atomic result showing whether this exact owner acquired or replayed."""

    lease: RuntimeSupervisorLease
    acquired: bool


@dataclass(frozen=True)
class DagNodeStateSnapshot:
    """Latest durable lifecycle fact for one exact PlanGraph node."""

    task_id: str
    plan_id: str
    plan_hash: str
    node_id: str
    revision: int
    state: str
    recorded_at: str


@dataclass(frozen=True)
class DagNodeStateMapSnapshot:
    """Complete exact node-state map for one initialized multi-node plan."""

    task_id: str
    plan_id: str
    plan_hash: str
    nodes: tuple[DagNodeStateSnapshot, ...]


@dataclass(frozen=True)
class DagNodeClaimCandidate:
    """Term-bound readiness audit that deliberately grants no execution right."""

    node: DagNodeStateSnapshot
    approval_id: str
    project_id: str
    principal_id: str
    fencing_token: int
    owner_id: str
    term_acquired_at: str
    readiness_document_hash: str
    recorded_at: str
    replayed: bool

    @property
    def dispatch_authorized(self) -> bool:
        """A P3 claim candidate is never an Adapter dispatch authorization."""

        return False


@dataclass(frozen=True)
class DagNodeArtifactInput:
    """Exact in-memory artifact material for one declared node-output input."""

    target_input_port: str
    artifact_manifest: Mapping[str, Any]
    artifact_data: bytes


@dataclass(frozen=True)
class DagNodeInputBindingFact:
    """Durable all-input audit fact that deliberately grants no execution right."""

    task_id: str
    plan_id: str
    plan_hash: str
    approval_id: str
    target_node_id: str
    target_node_revision: int
    project_id: str
    principal_id: str
    fencing_token: int
    owner_id: str
    term_acquired_at: str
    claim_readiness_document_hash: str
    binding_document: dict[str, Any]
    binding_document_hash: str
    recorded_at: str
    replayed: bool

    @property
    def dispatch_authorized(self) -> bool:
        """A durable binding remains evidence only until a later admission step."""

        return False


@dataclass(frozen=True)
class DagNodeExecutionAdmission:
    """Atomic DAG-node admission projected through the existing P2 intent."""

    snapshot: TaskSnapshot
    intent: DispatchIntentSnapshot
    input_binding_document_hash: str
    pending_revision: int
    queued_revision: int
    admission_fencing_token: int
    admitted_at: str
    replayed: bool


@dataclass(frozen=True)
class DagNodeTerminalFact:
    """One exact append-only DAG node terminal projection."""

    snapshot: TaskSnapshot
    intent: DispatchIntentSnapshot
    node_revision: int
    state: str
    event_sequence: int
    completion_fencing_token: int
    receipt_document_hash: str | None
    replayed: bool


@dataclass(frozen=True)
class DagNodeCacheCandidate:
    """One exact executed Succeeded source selected by a semantic cache key."""

    cache_entry_id: str
    cache_key_document: dict[str, Any]
    cache_key_hash: str
    source_snapshot: TaskSnapshot
    source_intent: DispatchIntentSnapshot
    source_node_revision: int
    source_admission_document_hash: str
    source_receipt_document_hash: str
    source_receipt_record_hash: str
    source_attempt_id: str
    source_attempt_number: int
    trusted_lineage_document: dict[str, Any]
    trusted_lineage_document_hash: str
    outputs: tuple[dict[str, Any], ...]


@dataclass(frozen=True)
class DagNodeCacheHitFact:
    """One no-Worker Pending-to-Succeeded node cache projection."""

    snapshot: TaskSnapshot
    cache_hit_id: str
    node_id: str
    node_revision: int
    cache_key_hash: str
    source_cache_entry_id: str
    source_receipt_document_hash: str
    output_receipt_document_hash: str
    event_sequence: int
    completion_fencing_token: int
    replayed: bool


@dataclass(frozen=True)
class DagNodeOutputMaterialization:
    """Logical node output plus its exact physical Adapter source."""

    source_node_id: str
    source_output_port: str
    data_type: str
    logical_artifact_manifest: dict[str, Any]
    logical_artifact_manifest_hash: str
    physical_task_id: str
    physical_intent_id: str
    physical_artifact_id: str
    physical_artifact_manifest: dict[str, Any]
    physical_artifact_manifest_hash: str
    trusted_lineage_document: dict[str, Any]
    trusted_lineage_document_hash: str


@dataclass(frozen=True)
class DagScheduleReconciliation:
    """One fenced, convergent dependency/aggregate reconciliation result."""

    snapshot: TaskSnapshot
    node_states: DagNodeStateMapSnapshot
    runnable_node_ids: tuple[str, ...]
    active_node_ids: tuple[str, ...]
    blocked_node_ids: tuple[str, ...]
    aggregate_status: str
    replayed: bool


@dataclass(frozen=True)
class SupervisedDispatchAuthorization:
    """One active Supervisor term authorized to enter Adapter dispatch."""

    intent: DispatchIntentSnapshot
    reason: str
    fencing_token: int
    authorized_at: str
    replayed: bool


@dataclass(frozen=True)
class SupervisedRetryAuthorization:
    """One permanently consumed retry reservation and term delivery audit."""

    intent: DispatchIntentSnapshot
    attempt_number: int
    previous_attempt_id: str
    previous_observation_sequence: int
    evidence_hash: str
    private_proof_hash: str
    failure_kind: str
    fencing_token: int
    reserved_at: str
    authorized_at: str
    reservation_replayed: bool
    authorization_replayed: bool
    private_schema_version: str | None = None
    source_outcome_document_hash: str | None = None
    source_handle_hash: str | None = None
    source_handle: dict[str, Any] | None = None
    retry_event_sequence: int | None = None
    retry_event_hash: str | None = None

    def adapter_token(self) -> dict[str, Any]:
        """Return the stable, path-free authorization consumed by Adapter."""

        return {
            "schema_version": "1.0.0",
            "intent_id": self.intent.intent_id,
            "previous_attempt_id": self.previous_attempt_id,
            "previous_observation_sequence": self.previous_observation_sequence,
            "failure_kind": self.failure_kind,
            "private_proof_hash": self.private_proof_hash,
            "next_attempt_number": self.attempt_number,
            "authorized_at": self.reserved_at,
        }


@dataclass(frozen=True)
class SupervisedWorkerExitRetryExhaustion:
    """Atomic terminalization of attempt 2 after a B2 reservation."""

    snapshot: TaskSnapshot
    intent: DispatchIntentSnapshot
    attempt_id: str
    observation_sequence: int
    evidence_hash: str
    private_proof_hash: str
    private_schema_version: str
    failure_kind: str
    terminal_event_sequence: int
    fencing_token: int
    exhausted_at: str
    replayed: bool


@dataclass(frozen=True)
class SupervisedRetryExhaustion:
    """Atomic terminalization of the exact stopped second attempt."""

    snapshot: TaskSnapshot
    intent: DispatchIntentSnapshot
    attempt_id: str
    observation_sequence: int
    evidence_hash: str
    private_proof_hash: str
    terminal_event_sequence: int
    fencing_token: int
    exhausted_at: str
    replayed: bool


@dataclass(frozen=True)
class SupervisedDispatchReconciliationAuthorization:
    """One term authorized to inspect an exact unresolved dispatch case."""

    intent: DispatchIntentSnapshot
    evidence_kind: str
    fencing_token: int
    authorized_at: str
    replayed: bool


@dataclass(frozen=True)
class SupervisedDispatchReconciliationObservation:
    """One durable, still-action-required reconciliation classification."""

    intent: DispatchIntentSnapshot
    classification: str
    failure_code: str
    observation_sequence: int
    fencing_token: int
    observed_at: str
    replayed: bool


@dataclass(frozen=True)
class SupervisedNegativeDispatchReconciliation:
    """Atomic terminal result for one exact proof that dispatch did not start."""

    snapshot: TaskSnapshot
    intent: DispatchIntentSnapshot
    attempt_id: str
    attempt_number: int
    evidence_hash: str
    private_record_hash: str
    private_proof_hash: str
    terminal_event_sequence: int
    fencing_token: int
    resolved_at: str
    replayed: bool


@dataclass(frozen=True)
class SupervisedPrivateReceiptAdoption:
    """One exact legacy-private receipt adopted by an active term."""

    intent: DispatchIntentSnapshot
    private_schema_version: str
    receipt_record_hash: str
    outcome_document_hash: str
    fencing_token: int
    recorded_at: str
    adopted: bool
    replayed: bool


@dataclass(frozen=True)
class WorkerAttemptProjection:
    """Durable result of one fenced, observation-only Worker sample."""

    intent: DispatchIntentSnapshot
    attempt_id: str
    observation_sequence: int
    document_hash: str
    adopted: bool
    replayed: bool


@dataclass(frozen=True)
class SupervisedCheckpointWait:
    """Atomic checkpoint evidence plus Running-to-Waiting projection."""

    snapshot: TaskSnapshot
    checkpoint: TaskCheckpointSnapshot
    checkpoint_proof: dict[str, Any]
    fencing_token: int
    recorded_at: str
    replayed: bool


@dataclass(frozen=True)
class TaskCheckpointResumeRecord:
    """One internal resume request or its exact acknowledged outcome."""

    snapshot: TaskSnapshot
    checkpoint: TaskCheckpointSnapshot
    resume_id: str
    request_record_hash: str
    replayed: bool


@dataclass(frozen=True)
class SupervisedCheckpointResumeAuthorization:
    """Active-term token for one exact same-attempt resume delivery."""

    checkpoint: TaskCheckpointSnapshot
    task_id: str
    resume_id: str
    resume_request_record_hash: str
    checkpoint_proof_hash: str
    requested_at: str
    authorized_at: str
    fencing_token: int
    replayed: bool

    def adapter_token(self) -> dict[str, Any]:
        """Return the strict path-free token consumed by Dispatcher/Adapter."""

        return {
            "schema_version": "1.0.0",
            "intent_id": self.checkpoint.intent_id,
            "task_id": self.task_id,
            "node_id": self.checkpoint.node_id,
            "submission_id": self.checkpoint.submission_id,
            "attempt_id": self.checkpoint.attempt_id,
            "attempt_number": self.checkpoint.attempt_number,
            "checkpoint_id": self.checkpoint.checkpoint_id,
            "checkpoint_manifest_hash": (
                self.checkpoint.checkpoint_manifest_hash
            ),
            "checkpoint_receipt_record_hash": (
                self.checkpoint.checkpoint_receipt_record_hash
            ),
            "checkpoint_proof_hash": self.checkpoint_proof_hash,
            "resume_id": self.resume_id,
            # The Worker request is one immutable, append-only action.  Bind
            # it to the durable resume admission time so successor terms can
            # safely re-deliver the exact same bytes.  ``authorized_at`` on
            # this dataclass remains the per-term SQLite audit time.
            "authorized_at": self.requested_at,
            "resume_request_record_hash": self.resume_request_record_hash,
        }

@dataclass(frozen=True)
class TaskCancellationRecord:
    """One durable cancel admission and its current exact outcome."""

    snapshot: TaskSnapshot
    cancellation: TaskCancellationSnapshot
    replayed: bool


@dataclass(frozen=True)
class SupervisedCancelAuthorization:
    """One active Supervisor term authorized to deliver an exact cancel."""

    cancellation: TaskCancellationSnapshot
    fencing_token: int
    authorized_at: str
    replayed: bool


@dataclass(frozen=True)
class TaskTimeoutRecord:
    """One durable timeout-window mutation or terminal resolution."""

    snapshot: TaskSnapshot
    timeout: TaskTimeoutSnapshot
    replayed: bool


@dataclass(frozen=True)
class SupervisedTimeoutAuthorization:
    """One active Supervisor term's exact timeout delivery decision."""

    timeout: TaskTimeoutSnapshot
    authorized: bool
    fencing_token: int
    authorized_at: str | None
    replayed: bool


@dataclass(frozen=True)
class _Migration:
    version: int
    path: Path
    text: str
    checksum: str


class TaskStore(Protocol):
    """Storage boundary used by the P1 TaskService."""

    def register_dataset(
        self, *, dataset: Mapping[str, Any], now: str
    ) -> RegistryWriteRecord:
        ...

    def register_algorithm(
        self, *, manifest: Mapping[str, Any], now: str
    ) -> RegistryWriteRecord:
        ...

    def get_dataset(
        self, *, project_id: str, dataset_id: str, version: str
    ) -> dict[str, Any] | None:
        ...

    def list_datasets(self, *, project_id: str) -> list[dict[str, Any]]:
        ...

    def get_algorithm(
        self, *, algorithm_id: str, version: str
    ) -> dict[str, Any] | None:
        ...

    def list_algorithms(self) -> list[dict[str, Any]]:
        ...

    def get_approval_budget(
        self, *, task_id: str, approval_id: str
    ) -> ApprovalBudget | None:
        ...

    def load_registry_snapshots(
        self,
        *,
        project_id: str,
        dataset_keys: Sequence[tuple[str, str]],
        algorithm_keys: Sequence[tuple[str, str]],
    ) -> RegistrySnapshots:
        ...

    def create_task(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        draft: Mapping[str, Any],
        idempotency_key: str,
        request_hash: str,
        now: str,
    ) -> CreateTaskRecord:
        ...

    def lookup_create_task(
        self,
        *,
        project_id: str,
        principal_id: str,
        idempotency_key: str,
        request_hash: str,
    ) -> CreateTaskRecord | None:
        ...

    def lookup_compatible_create_task(
        self,
        *,
        project_id: str,
        principal_id: str,
        idempotency_key: str,
        request_hashes: Sequence[str],
    ) -> CreateTaskRecord | None:
        """Look up one create record against precomputed exact request hashes."""

        ...

    def lookup_submit_task(
        self,
        *,
        project_id: str,
        principal_id: str,
        idempotency_key: str,
        request_hash: str,
    ) -> SubmitTaskRecord | None:
        ...

    def submit_task(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        approval_id: str,
        idempotency_key: str,
        request_hash: str,
        admit: Callable[
            [SubmitGateContext, str],
            tuple[Mapping[str, Any], Mapping[str, Any]],
        ],
        clock: Callable[[], str],
    ) -> SubmitTaskRecord:
        ...

    def get_dispatch_intent(self, task_id: str) -> DispatchIntentSnapshot | None:
        ...

    def get_dispatch_intent_by_id(
        self, intent_id: str
    ) -> DispatchIntentSnapshot | None:
        ...

    def list_dispatch_intents(
        self, task_id: str
    ) -> tuple[DispatchIntentSnapshot, ...]:
        ...

    def record_dispatch_success(
        self,
        *,
        intent_id: str,
        handle: Mapping[str, Any],
        now: str,
    ) -> DispatchIntentSnapshot:
        ...

    def claim_dispatch(
        self, *, intent_id: str, now: str
    ) -> tuple[DispatchIntentSnapshot, bool]:
        ...

    def record_dispatch_reconciliation(
        self,
        *,
        intent_id: str,
        failure_code: str,
        now: str,
    ) -> DispatchIntentSnapshot:
        ...

    def get_task(self, task_id: str) -> TaskSnapshot | None:
        ...

    def list_tasks(
        self,
        *,
        project_id: str,
        principal_id: str,
        cursor: str | None = None,
        limit: int = 20,
        view: str = "active",
    ) -> TaskSnapshotPage:
        ...

    def change_task_visibility(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        operation: str,
        expected_visibility_revision: int,
        idempotency_key: str,
        request_hash: str,
        now: str,
    ) -> TaskVisibilityMutationRecord:
        ...

    def reserve_task_purge(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        expected_visibility_revision: int,
        idempotency_key: str,
        request_hash: str,
        now: str,
    ) -> TaskPurgeRecord:
        ...

    def complete_task_purge(
        self,
        *,
        purge_id: str,
        task_id: str,
        project_id: str,
        principal_id: str,
        local_run_state: str,
        now: str,
    ) -> TaskPurgeRecord:
        ...

    def get_retry_exhaustion_cleanup_proof(
        self,
        *,
        purge_id: str,
        task_id: str,
        project_id: str,
        principal_id: str,
    ) -> RetryExhaustionCleanupProof | None:
        ...

    def lookup_workbench_mutation(
        self,
        *,
        project_id: str,
        principal_id: str,
        operation: str,
        idempotency_key: str,
        request_hash: str,
    ) -> WorkbenchMutationRecord | None:
        ...

    def lookup_compatible_workbench_mutation(
        self,
        *,
        project_id: str,
        principal_id: str,
        operation: str,
        idempotency_key: str,
        request_hashes: Sequence[str],
    ) -> WorkbenchMutationRecord | None:
        """Look up one mutation against precomputed exact request hashes."""

        ...

    def append_draft_revision(
        self,
        *,
        task_id: str,
        expected_revision: int,
        draft: Mapping[str, Any],
        now: str,
        project_id: str | None = None,
        principal_id: str | None = None,
        idempotency_key: str | None = None,
        request_hash: str | None = None,
    ) -> TaskSnapshot:
        ...

    def store_plan(
        self,
        *,
        task_id: str,
        plan: Mapping[str, Any],
        now: str,
        project_id: str | None = None,
        principal_id: str | None = None,
        idempotency_key: str | None = None,
        request_hash: str | None = None,
    ) -> TaskSnapshot:
        ...

    def store_approval(
        self,
        *,
        task_id: str,
        approval: Mapping[str, Any],
        now: str,
        project_id: str | None = None,
        principal_id: str | None = None,
        idempotency_key: str | None = None,
        request_hash: str | None = None,
    ) -> TaskSnapshot:
        ...

    def abandon_task(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        abandonment: Mapping[str, Any],
        idempotency_key: str,
        request_hash: str,
        now: str,
    ) -> tuple[TaskSnapshot, bool]:
        ...

    def acquire_runtime_supervisor_lease(
        self,
        *,
        project_id: str,
        principal_id: str,
        owner_id: str,
        lease_seconds: int,
        clock: Callable[[], str],
    ) -> RuntimeSupervisorLeaseAcquisition:
        ...

    def heartbeat_runtime_supervisor_lease(
        self,
        *,
        lease: RuntimeSupervisorLease,
        lease_seconds: int,
        clock: Callable[[], str],
    ) -> RuntimeSupervisorLease:
        ...

    def release_runtime_supervisor_lease(
        self,
        *,
        lease: RuntimeSupervisorLease,
        clock: Callable[[], str],
    ) -> RuntimeSupervisorLease:
        ...

    def get_runtime_supervisor_lease(
        self, *, project_id: str, principal_id: str
    ) -> RuntimeSupervisorLease | None:
        ...

    def get_dag_node_state_snapshot(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
    ) -> DagNodeStateMapSnapshot | None:
        ...

    def claim_ready_dag_node_candidate(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        expected_plan_hash: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagNodeClaimCandidate:
        ...

    def bind_ready_dag_node_inputs(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        expected_plan_hash: str,
        claim_candidate: DagNodeClaimCandidate,
        artifact_inputs: Sequence[DagNodeArtifactInput],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagNodeInputBindingFact:
        ...

    def admit_ready_dag_node_execution(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        expected_plan_hash: str,
        input_binding: DagNodeInputBindingFact,
        admit: Callable[
            [SubmitGateContext, str],
            tuple[Mapping[str, Any], Mapping[str, Any]],
        ],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagNodeExecutionAdmission:
        ...

    def find_dag_node_cache_candidate(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        input_binding: DagNodeInputBindingFact,
        adapter_id: str,
        adapter_version: str,
        queue_fingerprint: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagNodeCacheCandidate | None:
        ...

    def commit_dag_node_cache_hit(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        input_binding: DagNodeInputBindingFact,
        candidate: DagNodeCacheCandidate,
        artifact_verification: Mapping[str, Any],
        cache_hit_id: str,
        validate: Callable[
            [SubmitGateContext, str],
            tuple[Mapping[str, Any], Mapping[str, Any]],
        ],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagNodeCacheHitFact:
        ...

    def get_dag_node_output_materializations(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        source_node_id: str,
    ) -> tuple[DagNodeOutputMaterialization, ...]:
        ...

    def get_dag_node_input_binding_for_intent(
        self, intent_id: str
    ) -> DagNodeInputBindingFact | None:
        ...

    def is_dag_node_execution_intent(self, intent_id: str) -> bool:
        ...

    def get_active_dag_node_execution(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
    ) -> DagNodeExecutionAdmission | None:
        ...

    def reconcile_dag_node_dependencies(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        expected_plan_hash: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagScheduleReconciliation:
        ...

    def record_supervised_dag_dispatch_reconciliation(
        self,
        *,
        intent_id: str,
        failure_code: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DispatchIntentSnapshot:
        ...

    def commit_dag_node_runtime_transition(
        self,
        *,
        intent_id: str,
        expected_node_state: str,
        event: Mapping[str, Any],
        adapter_status: Mapping[str, Any],
        output_receipt: Mapping[str, Any] | None,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagNodeTerminalFact | TaskSnapshot:
        ...

    def authorize_supervised_dispatch(
        self,
        *,
        intent_id: str,
        reason: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedDispatchAuthorization:
        ...

    def authorize_supervised_retry(
        self,
        *,
        intent_id: str,
        previous_attempt_id: str,
        previous_observation_sequence: int,
        failure_kind: str,
        private_proof_hash: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedRetryAuthorization:
        ...

    def resume_supervised_retry(
        self,
        *,
        intent_id: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedRetryAuthorization:
        ...

    def authorize_supervised_worker_exit_retry(
        self,
        *,
        intent_id: str,
        previous_attempt_id: str,
        previous_observation_sequence: int,
        failure_kind: str,
        private_schema_version: str,
        private_proof_hash: str,
        retry_event: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedRetryAuthorization:
        ...

    def resume_supervised_worker_exit_retry(
        self,
        *,
        intent_id: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedRetryAuthorization:
        ...

    def finalize_supervised_worker_exit_retry_exhaustion(
        self,
        *,
        intent_id: str,
        attempt_id: str,
        observation_sequence: int,
        evidence: Mapping[str, Any],
        private_schema_version: str,
        private_proof_hash: str,
        failure_kind: str,
        terminal_event: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedWorkerExitRetryExhaustion:
        ...

    def finalize_supervised_retry_exhaustion(
        self,
        *,
        intent_id: str,
        attempt_id: str,
        observation_sequence: int,
        evidence: Mapping[str, Any],
        private_schema_version: str,
        private_proof_hash: str,
        failure_kind: str,
        terminal_event: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedRetryExhaustion:
        ...

    def authorize_supervised_dispatch_reconciliation(
        self,
        *,
        intent_id: str,
        evidence_kind: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedDispatchReconciliationAuthorization:
        ...

    def record_supervised_dispatch_reconciliation_observation(
        self,
        *,
        intent_id: str,
        classification: str,
        failure_code: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedDispatchReconciliationObservation:
        ...

    def finalize_supervised_negative_dispatch_reconciliation(
        self,
        *,
        intent_id: str,
        attempt_id: str,
        attempt_number: int,
        adapter_version: str,
        private_schema_version: str,
        private_record_hash: str,
        private_proof_hash: str,
        evidence: Mapping[str, Any],
        terminal_event: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedNegativeDispatchReconciliation:
        ...

    def record_supervised_private_receipt_adoption(
        self,
        *,
        intent_id: str,
        handle: Mapping[str, Any],
        private_schema_version: str,
        receipt_record_hash: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedPrivateReceiptAdoption:
        ...

    def record_supervised_worker_observation(
        self,
        *,
        intent_id: str,
        evidence: Mapping[str, Any],
        handle: Mapping[str, Any] | None,
        replacement_event: Mapping[str, Any] | None = None,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> WorkerAttemptProjection:
        ...

    def record_supervised_checkpoint_wait(
        self,
        *,
        intent_id: str,
        checkpoint_proof: Mapping[str, Any],
        checkpoint_event: Mapping[str, Any],
        waiting_event: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedCheckpointWait:
        ...

    def request_checkpoint_resume(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        idempotency_key: str,
        request_hash: str,
        clock: Callable[[], str],
    ) -> TaskCheckpointResumeRecord:
        ...

    def authorize_supervised_checkpoint_resume(
        self,
        *,
        resume_id: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedCheckpointResumeAuthorization:
        ...

    def complete_supervised_checkpoint_resume(
        self,
        *,
        resume_id: str,
        adapter_proof: Mapping[str, Any],
        running_event: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> TaskCheckpointResumeRecord:
        ...

    def arm_worker_attempt_timeout(
        self,
        *,
        intent_id: str,
        attempt_id: str,
        capability_proof: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> TaskTimeoutRecord:
        ...

    def authorize_supervised_timeout(
        self,
        *,
        timeout_id: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedTimeoutAuthorization:
        ...

    def complete_supervised_timeout(
        self,
        *,
        timeout_id: str,
        result: str,
        terminal_event: Mapping[str, Any] | None,
        adapter_proof: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> TaskTimeoutRecord:
        ...

    def lookup_task_cancellation(
        self,
        *,
        project_id: str,
        principal_id: str,
        idempotency_key: str,
        request_id: str,
        task_id: str,
        reason: str,
        request_hash: str,
    ) -> TaskCancellationRecord | None:
        ...

    def can_request_task_cancellation(self, task_id: str) -> bool:
        ...

    def get_task_cancel_candidate(
        self, task_id: str
    ) -> dict[str, Any] | None:
        ...

    def request_task_cancellation(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        request_id: str,
        reason: str,
        idempotency_key: str,
        request_hash: str,
        build_documents: Callable[
            [TaskSnapshot, DispatchIntentSnapshot, Mapping[str, Any], int, str],
            tuple[Mapping[str, Any], Mapping[str, Any]],
        ],
        clock: Callable[[], str],
    ) -> TaskCancellationRecord:
        ...

    def authorize_supervised_cancel(
        self,
        *,
        request_id: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedCancelAuthorization:
        ...

    def complete_supervised_cancel(
        self,
        *,
        request_id: str,
        result: str,
        terminal_event: Mapping[str, Any] | None,
        adapter_proof: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> TaskCancellationRecord:
        ...

    def commit_runtime_transition(
        self,
        *,
        task_id: str,
        expected_status: str,
        event: Mapping[str, Any],
        now: str | None,
        supervisor_lease: RuntimeSupervisorLease | None = None,
        supervisor_clock: Callable[[], str] | None = None,
    ) -> TaskSnapshot:
        ...

    def list_run_events(
        self, task_id: str, *, after_sequence: int = 0, limit: int = 100
    ) -> list[dict[str, Any]]:
        ...

    def latest_run_event_sequence(self, task_id: str) -> int:
        ...


def encode_document(value: Mapping[str, Any]) -> tuple[str, str]:
    """Return deterministic JSON text and its SHA-256 identity.

    TaskDraft legitimately contains floating-point confidence values, so this
    is intentionally distinct from the integer-only PlanGraph hash profile.
    """

    try:
        text = json.dumps(
            dict(value),
            ensure_ascii=False,
            allow_nan=False,
            separators=(",", ":"),
            sort_keys=True,
        )
    except (TypeError, ValueError) as error:
        raise TaskStoreConflict(f"document is not finite JSON: {error}") from error
    digest = hashlib.sha256(text.encode("utf-8")).hexdigest()
    return text, f"sha256:{digest}"


def _validate_cancel_adapter_proof(
    value: Mapping[str, Any],
    *,
    request_id: str,
    task_id: str,
    attempt_id: str,
    reason: str,
    result: str,
    terminal_status: str | None = None,
    dag_terminal_node_status: str | None = None,
) -> dict[str, Any]:
    """Validate the exact Adapter proof before it can become task truth."""

    proof = dict(value)
    expected_fields = {
        "schema_version",
        "task_id",
        "request_id",
        "reason",
        "state",
        "code",
        "attempt_id",
        "capability_record_hash",
        "request_record_hash",
        "acknowledgement_record_hash",
        "terminal_status",
        "local_run_state",
        "replayed",
        "receipt_record_hash",
        "proof_hash",
    }
    capability_hash = proof.get("capability_record_hash")
    request_hash = proof.get("request_record_hash")
    acknowledgement_hash = proof.get("acknowledgement_record_hash")
    chain = (capability_hash, request_hash, acknowledgement_hash)
    chain_valid = (
        all(
            item is None
            or (isinstance(item, str) and DOCUMENT_HASH.fullmatch(item))
            for item in chain
        )
        and not (capability_hash is None and request_hash is not None)
        and not (request_hash is None and acknowledgement_hash is not None)
    )
    try:
        _, calculated_proof_hash = encode_document(
            {key: item for key, item in proof.items() if key != "proof_hash"}
        )
    except TaskStoreConflict as error:
        raise TaskStoreConflict("task cancel Adapter proof is invalid") from error
    dag_terminal_won = (
        dag_terminal_node_status in {"Succeeded", "Failed"}
        and proof.get("state") == "terminal_won"
        and proof.get("code") == "CANCEL_TERMINAL_WON"
        and proof.get("terminal_status") == dag_terminal_node_status
    )
    final_shape_valid = False
    if result == "cancel_confirmed":
        final_shape_valid = (
            (
                proof.get("state") == "cancelled"
                and proof.get("code") == "CANCEL_COMPLETED"
                and proof.get("terminal_status") == "Cancelled"
                and all(item is not None for item in chain)
            )
            or dag_terminal_won
        )
    elif result == "terminal_preempted":
        final_shape_valid = (
            proof.get("state") == "terminal_won"
            and proof.get("code") == "CANCEL_TERMINAL_WON"
            and proof.get("terminal_status") in {"Succeeded", "Failed"}
        )
    if (
        set(proof) != expected_fields
        or proof.get("schema_version") != "1.0.0"
        or proof.get("task_id") != task_id
        or proof.get("request_id") != request_id
        or proof.get("attempt_id") != attempt_id
        or not isinstance(attempt_id, str)
        or WORKER_ATTEMPT_ID.fullmatch(attempt_id) is None
        or proof.get("reason") != reason
        or reason != "user_requested"
        or not chain_valid
        or not final_shape_valid
        or (
            terminal_status is not None
            and proof.get("terminal_status") != terminal_status
            and not dag_terminal_won
        )
        or proof.get("local_run_state") != "retained"
        or type(proof.get("replayed")) is not bool
        or not isinstance(proof.get("receipt_record_hash"), str)
        or DOCUMENT_HASH.fullmatch(proof["receipt_record_hash"]) is None
        or not isinstance(proof.get("proof_hash"), str)
        or DOCUMENT_HASH.fullmatch(proof["proof_hash"]) is None
        or proof["proof_hash"] != calculated_proof_hash
    ):
        raise TaskStoreConflict("task cancel Adapter proof is invalid")
    return proof


def _validate_timeout_capability_proof(
    value: Mapping[str, Any],
    *,
    attempt_id: str,
    binding_hash: str,
    private_schema_version: str,
) -> dict[str, Any]:
    """Validate the independent exact-stop v2 capability used to arm time."""

    if not isinstance(value, Mapping):
        raise TaskStoreConflict("timeout capability proof is invalid")
    proof = dict(value)
    required = {
        "schema_version",
        "private_schema_version",
        "attempt_id",
        "binding_hash",
        "capability_record_hash",
        "supported_reasons",
        "proof_hash",
    }
    try:
        _, calculated_hash = encode_document(
            {key: item for key, item in proof.items() if key != "proof_hash"}
        )
    except TaskStoreConflict as error:
        raise TaskStoreConflict("timeout capability proof is invalid") from error
    if (
        set(proof) != required
        or proof.get("schema_version") != "2.0.0"
        or private_schema_version not in {"1.1.0", "1.2.0", "1.3.0"}
        or proof.get("private_schema_version") != private_schema_version
        or proof.get("attempt_id") != attempt_id
        or WORKER_ATTEMPT_ID.fullmatch(attempt_id) is None
        or proof.get("binding_hash") != binding_hash
        or not _is_sha256(binding_hash)
        or not _is_sha256(proof.get("capability_record_hash"))
        or proof.get("supported_reasons")
        != ["user_requested", "wall_time_exceeded"]
        or proof.get("proof_hash") != calculated_hash
        or not _is_sha256(proof.get("proof_hash"))
    ):
        raise TaskStoreConflict("timeout capability proof is invalid")
    return proof


def _validate_store_approval_retry_policy(
    approval: Mapping[str, Any], plan: Mapping[str, Any]
) -> None:
    """Enforce the finite-retry grant at the public Store boundary.

    TaskService performs the same product validation, but ``store_approval``
    is itself a public protocol method.  The durable budget trigger must never
    be reachable with an ApprovalDecision 1.1 that targets a historical or
    otherwise unsupported plan.
    """

    version = approval.get("schema_version")
    scope = approval.get("scope")
    policy = scope.get("retry_policy") if isinstance(scope, Mapping) else None
    if version == "1.0.0":
        if isinstance(scope, Mapping) and "retry_policy" in scope:
            raise TaskStoreConflict(
                "ApprovalDecision 1.0 cannot grant retry attempts"
            )
        return
    if version != "1.1.0":
        raise TaskStoreConflict("approval retry policy version is unsupported")

    nodes = plan.get("nodes")
    resources = scope.get("resource_limits") if isinstance(scope, Mapping) else None
    algorithms = scope.get("algorithms") if isinstance(scope, Mapping) else None
    managed_algorithm = (
        nodes[0].get("algorithm")
        if isinstance(nodes, list)
        and nodes
        and isinstance(nodes[0], Mapping)
        else None
    )
    supported_algorithms = (
        {"id": "deepwave.acoustic_fwi", "version": "1.5.0"},
        {"id": "deepwave.acoustic_fwi", "version": "1.6.0"},
    )
    if (
        not isinstance(nodes, list)
        or len(nodes) != 1
        or not isinstance(nodes[0], Mapping)
        or managed_algorithm not in supported_algorithms
        or algorithms != [managed_algorithm]
        or not isinstance(nodes[0].get("resources"), Mapping)
        or not isinstance(resources, Mapping)
        or resources != nodes[0]["resources"]
        or not isinstance(policy, Mapping)
    ):
        raise TaskStoreConflict(
            "ApprovalDecision 1.1 requires the current managed FWI plan"
        )
    wall_time = nodes[0]["resources"].get("wall_time_seconds")
    failures = policy.get("retryable_failure_classes")
    if (
        type(wall_time) is not int
        or wall_time < 1
        or set(policy)
        != {
            "max_attempts",
            "max_concurrent_attempts",
            "max_cumulative_attempt_wall_time_seconds",
            "retryable_failure_classes",
        }
        or policy.get("max_attempts") != 2
        or policy.get("max_concurrent_attempts") != 1
        or policy.get("max_cumulative_attempt_wall_time_seconds")
        != 2 * wall_time
        or not isinstance(failures, list)
        or len(failures) != 2
        or frozenset(failures)
        != {"pre_running_launch_failure", "worker_exit"}
    ):
        raise TaskStoreConflict(
            "ApprovalDecision 1.1 retry policy differs from its plan budget"
        )


def _validate_timeout_adapter_proof(
    value: Mapping[str, Any],
    *,
    timeout: TaskTimeoutSnapshot,
    capability_record_hash: str,
    ready_record_hash: str,
    result: str,
    terminal_status: str | None = None,
) -> dict[str, Any]:
    """Validate an exact Worker timeout proof before it becomes task truth."""

    proof = dict(value)
    expected_fields = {
        "schema_version",
        "task_id",
        "request_id",
        "reason",
        "state",
        "code",
        "attempt_id",
        "wall_time_seconds",
        "started_at",
        "deadline_at",
        "ready_record_hash",
        "capability_record_hash",
        "request_record_hash",
        "acknowledgement_record_hash",
        "terminal_status",
        "terminal_failure_code",
        "local_run_state",
        "replayed",
        "receipt_record_hash",
        "proof_hash",
    }
    request_hash = proof.get("request_record_hash")
    acknowledgement_hash = proof.get("acknowledgement_record_hash")
    chain_valid = (
        (request_hash is None or _is_sha256(request_hash))
        and (acknowledgement_hash is None or _is_sha256(acknowledgement_hash))
        and not (request_hash is None and acknowledgement_hash is not None)
    )
    try:
        _, calculated_proof_hash = encode_document(
            {key: item for key, item in proof.items() if key != "proof_hash"}
        )
    except TaskStoreConflict as error:
        raise TaskStoreConflict("task timeout Adapter proof is invalid") from error

    final_shape_valid = False
    if result == "timeout_confirmed":
        final_shape_valid = (
            proof.get("state") == "timed_out"
            and proof.get("code") == "TIMEOUT_COMPLETED"
            and proof.get("terminal_status") == "Failed"
            and proof.get("terminal_failure_code") == "WALL_TIME_EXCEEDED"
            and request_hash is not None
            and acknowledgement_hash is not None
        )
    elif result == "terminal_preempted":
        natural_code = proof.get("terminal_failure_code")
        natural_status = proof.get("terminal_status")
        final_shape_valid = (
            proof.get("state") == "terminal_won"
            and proof.get("code") == "TIMEOUT_TERMINAL_WON"
            and natural_status in {"Succeeded", "Failed"}
            and natural_code is None
        )
    if (
        set(proof) != expected_fields
        or proof.get("schema_version") != "1.0.0"
        or proof.get("task_id") != timeout.task_id
        or proof.get("request_id") != timeout.timeout_id
        or proof.get("reason") != "wall_time_exceeded"
        or proof.get("attempt_id") != timeout.attempt_id
        or proof.get("wall_time_seconds") != timeout.wall_time_seconds
        or proof.get("started_at") != timeout.started_at
        or proof.get("deadline_at") != timeout.deadline_at
        or proof.get("ready_record_hash") != ready_record_hash
        or proof.get("capability_record_hash") != capability_record_hash
        or not _is_sha256(capability_record_hash)
        or not chain_valid
        or not final_shape_valid
        or (
            terminal_status is not None
            and proof.get("terminal_status") != terminal_status
        )
        or proof.get("local_run_state") != "retained"
        or type(proof.get("replayed")) is not bool
        or not _is_sha256(proof.get("receipt_record_hash"))
        or not _is_sha256(proof.get("proof_hash"))
        or proof.get("proof_hash") != calculated_proof_hash
    ):
        raise TaskStoreConflict("task timeout Adapter proof is invalid")
    return proof


_UNIX_EPOCH = datetime(1970, 1, 1, tzinfo=timezone.utc)


def _runtime_timestamp(value: str) -> tuple[str, int]:
    """Normalize a trusted RFC3339 instant and return integer microseconds."""

    try:
        parsed = datetime.fromisoformat(value.replace("Z", "+00:00"))
    except (AttributeError, ValueError) as error:
        raise TaskStoreConflict("runtime supervisor timestamp is invalid") from error
    if parsed.tzinfo is None:
        raise TaskStoreConflict("runtime supervisor timestamp is invalid")
    parsed = parsed.astimezone(timezone.utc)
    delta = parsed - _UNIX_EPOCH
    microseconds = (
        delta.days * 86_400_000_000
        + delta.seconds * 1_000_000
        + delta.microseconds
    )
    if microseconds < 0:
        raise TaskStoreConflict("runtime supervisor timestamp is invalid")
    canonical = parsed.isoformat(timespec="microseconds").replace("+00:00", "Z")
    return canonical, microseconds


def _runtime_timestamp_from_microseconds(value: int) -> str:
    """Return the canonical RFC3339 spelling of one persisted runtime tick."""

    if type(value) is not int or value < 0:
        raise TaskStoreConflict("runtime supervisor timestamp is invalid")
    try:
        parsed = _UNIX_EPOCH + timedelta(microseconds=value)
    except OverflowError as error:
        raise TaskStoreConflict("runtime supervisor timestamp is invalid") from error
    return parsed.isoformat(timespec="microseconds").replace("+00:00", "Z")


def _runtime_timeout_id(
    *,
    intent_id: str,
    attempt_id: str,
    start_observation_sequence: int,
    wall_time_seconds: int,
) -> str:
    identity = "\x1f".join(
        (
            intent_id,
            attempt_id,
            str(start_observation_sequence),
            str(wall_time_seconds),
        )
    )
    return "timeout-" + hashlib.sha256(identity.encode("utf-8")).hexdigest()[:32]


def _is_sha256(value: Any) -> bool:
    return (
        isinstance(value, str)
        and len(value) == 71
        and value.startswith("sha256:")
        and all(character in "0123456789abcdef" for character in value[7:])
    )


def _is_safe_checkpoint_relative_path(value: Any) -> bool:
    """Reject absolute, platform-dependent, and traversal artifact paths."""

    if (
        not isinstance(value, str)
        or not value
        or len(value.encode("utf-8")) > 512
        or value.startswith("/")
        or "\\" in value
        or "\x00" in value
    ):
        return False
    parts = value.split("/")
    return all(part not in {"", ".", ".."} for part in parts)


def _validate_checkpoint_adapter_proof(
    value: Mapping[str, Any],
    *,
    state: str,
    task_id: str | None = None,
    intent_id: str | None = None,
    node_id: str | None = None,
    submission_id: str | None = None,
    attempt_id: str | None = None,
    attempt_number: int | None = None,
    checkpoint_id: str | None = None,
    checkpoint_proof_hash: str | None = None,
    resume_id: str | None = None,
    resume_request_record_hash: str | None = None,
) -> dict[str, Any]:
    """Validate the path-safe 1.0 checkpoint/resume Adapter proof."""

    if not isinstance(value, Mapping) or state not in {"waiting", "resumed"}:
        raise TaskStoreConflict("checkpoint Adapter proof is invalid")
    proof = dict(value)
    required = {
        "schema_version",
        "task_id",
        "node_id",
        "submission_id",
        "attempt_id",
        "attempt_number",
        "checkpoint_id",
        "checkpoint_index",
        "completed_updates",
        "binding_hash",
        "submission_receipt_record_hash",
        "ready_record_hash",
        "checkpoint_manifest_relative_path",
        "checkpoint_manifest_size_bytes",
        "checkpoint_manifest_hash",
        "checkpoint_receipt_record_hash",
        "checkpoint_created_at",
        "state",
        "checkpoint_proof_hash",
        "resume_id",
        "resume_request_record_hash",
        "resume_acknowledgement_record_hash",
        "resume_acknowledged_at",
        "proof_hash",
    }
    try:
        created_at, _ = _runtime_timestamp(proof.get("checkpoint_created_at"))
        _, calculated_hash = encode_document(
            {key: item for key, item in proof.items() if key != "proof_hash"}
        )
    except (TaskStoreConflict, TypeError) as error:
        raise TaskStoreConflict("checkpoint Adapter proof is invalid") from error
    hashes = (
        proof.get("binding_hash"),
        proof.get("submission_receipt_record_hash"),
        proof.get("ready_record_hash"),
        proof.get("checkpoint_manifest_hash"),
        proof.get("checkpoint_receipt_record_hash"),
        proof.get("proof_hash"),
    )
    common_valid = (
        set(proof) == required
        and proof.get("schema_version") == "1.0.0"
        and isinstance(proof.get("task_id"), str)
        and bool(proof.get("task_id"))
        and isinstance(proof.get("node_id"), str)
        and bool(proof.get("node_id"))
        and isinstance(proof.get("submission_id"), str)
        and WORKER_SUBMISSION_ID.fullmatch(proof["submission_id"]) is not None
        and isinstance(proof.get("attempt_id"), str)
        and WORKER_ATTEMPT_ID.fullmatch(proof["attempt_id"]) is not None
        and type(proof.get("attempt_number")) is int
        and proof["attempt_number"] >= 1
        and isinstance(proof.get("checkpoint_id"), str)
        and RUNTIME_CHECKPOINT_ID.fullmatch(proof["checkpoint_id"]) is not None
        and type(proof.get("checkpoint_index")) is int
        and proof["checkpoint_index"] >= 1
        and type(proof.get("completed_updates")) is int
        and proof["completed_updates"] >= 1
        and all(_is_sha256(item) for item in hashes)
        and _is_safe_checkpoint_relative_path(
            proof.get("checkpoint_manifest_relative_path")
        )
        and type(proof.get("checkpoint_manifest_size_bytes")) is int
        and 1 <= proof["checkpoint_manifest_size_bytes"] <= 16 * 1024 * 1024
        and proof.get("checkpoint_created_at") == created_at
        and proof.get("proof_hash") == calculated_hash
    )
    indexed = {
        "task_id": task_id,
        "node_id": node_id,
        "submission_id": submission_id,
        "attempt_id": attempt_id,
        "attempt_number": attempt_number,
        "checkpoint_id": checkpoint_id,
    }
    exact_valid = all(
        expected is None or proof.get(key) == expected
        for key, expected in indexed.items()
    )
    if intent_id is not None and not isinstance(intent_id, str):
        exact_valid = False
    if state == "waiting":
        state_valid = (
            proof.get("state") == "waiting"
            and proof.get("checkpoint_proof_hash") is None
            and proof.get("resume_id") is None
            and proof.get("resume_request_record_hash") is None
            and proof.get("resume_acknowledgement_record_hash") is None
            and proof.get("resume_acknowledged_at") is None
        )
    else:
        try:
            acknowledged_at, _ = _runtime_timestamp(
                proof.get("resume_acknowledged_at")
            )
        except (TaskStoreConflict, TypeError):
            acknowledged_at = None
        state_valid = (
            proof.get("state") == "resumed"
            and proof.get("checkpoint_proof_hash") == checkpoint_proof_hash
            and _is_sha256(checkpoint_proof_hash)
            and proof.get("resume_id") == resume_id
            and isinstance(resume_id, str)
            and RUNTIME_RESUME_ID.fullmatch(resume_id) is not None
            and proof.get("resume_request_record_hash")
            == resume_request_record_hash
            and _is_sha256(resume_request_record_hash)
            and _is_sha256(proof.get("resume_acknowledgement_record_hash"))
            and proof.get("resume_acknowledged_at") == acknowledged_at
        )
    if not (common_valid and exact_valid and state_valid):
        raise TaskStoreConflict("checkpoint Adapter proof is invalid")
    return proof


def _is_supported_managed_dispatch(intent: DispatchIntentSnapshot) -> bool:
    """Accept only immutable, same-version Adapter/Algorithm managed pairs."""

    version = intent.adapter_version
    return (
        intent.adapter_id == "fwi.deepwave_adapter"
        and version in {"1.4.0", "1.5.0", "1.6.0"}
        and intent.request.get("algorithm")
        == {"id": "deepwave.acoustic_fwi", "version": version}
    )


def _is_checkpoint_capable_managed_dispatch(
    intent: DispatchIntentSnapshot,
) -> bool:
    """Return whether the immutable dispatch pair owns Waiting semantics."""

    return (
        intent.adapter_id == "fwi.deepwave_adapter"
        and intent.adapter_version == "1.6.0"
        and intent.request.get("algorithm")
        == {"id": "deepwave.acoustic_fwi", "version": "1.6.0"}
    )


def _worker_evidence_timestamp(value: Any) -> None:
    if (
        not isinstance(value, str)
        or WORKER_EVIDENCE_TIMESTAMP.fullmatch(value) is None
    ):
        raise TaskStoreConflict("Worker evidence timestamp is invalid")
    try:
        _runtime_timestamp(value)
    except TaskStoreConflict as error:
        raise TaskStoreConflict("Worker evidence timestamp is invalid") from error


def _validated_worker_evidence(evidence: Mapping[str, Any]) -> dict[str, Any]:
    """Validate and canonicalize the private Adapter's path-free evidence."""

    if not isinstance(evidence, Mapping):
        raise TaskStoreConflict("Worker evidence must be an object")
    value = dict(evidence)
    required = {
        "schema_version",
        "submission_id",
        "attempt_id",
        "attempt_number",
        "job_id",
        "request_hash",
        "binding_hash",
        "created_at",
        "ticket",
        "ready",
        "heartbeat",
    }
    if set(value) != required or value.get("schema_version") != "1.0.0":
        raise TaskStoreConflict("Worker evidence fields are invalid")
    submission_id = value.get("submission_id")
    attempt_id = value.get("attempt_id")
    attempt_number = value.get("attempt_number")
    job_id = value.get("job_id")
    if (
        not isinstance(submission_id, str)
        or WORKER_SUBMISSION_ID.fullmatch(submission_id) is None
        or not isinstance(attempt_id, str)
        or WORKER_ATTEMPT_ID.fullmatch(attempt_id) is None
        or type(attempt_number) is not int
        or attempt_number not in {1, 2}
        or not isinstance(job_id, str)
        or WORKER_JOB_ID.fullmatch(job_id) is None
        or not _is_sha256(value.get("request_hash"))
        or not _is_sha256(value.get("binding_hash"))
    ):
        raise TaskStoreConflict("Worker attempt identity is invalid")
    _worker_evidence_timestamp(value.get("created_at"))
    binding_payload = {
        "schema_version": "1.0.0",
        "submission_id": submission_id,
        "attempt_id": attempt_id,
        "attempt_number": attempt_number,
        "job_id": job_id,
        "request_hash": value["request_hash"],
        "created_at": value["created_at"],
    }
    if encode_document(binding_payload)[1] != value["binding_hash"]:
        raise TaskStoreConflict("Worker attempt binding hash is invalid")

    ticket = value.get("ticket")
    ticket_fields = {
        "state",
        "capacity_slot",
        "capacity_generation",
        "worker_pid",
        "updated_at",
        "record_hash",
    }
    if not isinstance(ticket, Mapping) or set(ticket) != ticket_fields:
        raise TaskStoreConflict("Worker launch ticket is invalid")
    ticket = dict(ticket)
    state = ticket.get("state")
    slot = ticket.get("capacity_slot")
    generation = ticket.get("capacity_generation")
    worker_pid = ticket.get("worker_pid")
    if state == "staged":
        projection_valid = slot is None and generation is None and worker_pid is None
    elif state == "leased":
        projection_valid = (
            type(slot) is int
            and slot >= 0
            and type(generation) is int
            and generation >= 1
            and worker_pid is None
        )
    elif state == "spawned":
        projection_valid = (
            type(slot) is int
            and slot >= 0
            and type(generation) is int
            and generation >= 1
            and type(worker_pid) is int
            and worker_pid >= 1
        )
    elif state == "failed":
        projection_valid = worker_pid is None and (
            (slot is None and generation is None)
            or (
                type(slot) is int
                and slot >= 0
                and type(generation) is int
                and generation >= 1
            )
        )
    else:
        projection_valid = False
    if not projection_valid or not _is_sha256(ticket.get("record_hash")):
        raise TaskStoreConflict("Worker launch ticket projection is invalid")
    _worker_evidence_timestamp(ticket.get("updated_at"))
    ticket_payload = {
        **binding_payload,
        "binding_hash": value["binding_hash"],
        "state": state,
        "capacity_slot": slot,
        "capacity_generation": generation,
        "worker_pid": worker_pid,
        "updated_at": ticket["updated_at"],
    }
    if encode_document(ticket_payload)[1] != ticket["record_hash"]:
        raise TaskStoreConflict("Worker launch ticket hash is invalid")

    ready = value.get("ready")
    if ready is not None:
        if not isinstance(ready, Mapping) or set(ready) != {
            "worker_pid",
            "started_at",
            "record_hash",
        }:
            raise TaskStoreConflict("Worker ready evidence is invalid")
        ready = dict(ready)
        if (
            state != "spawned"
            or type(ready.get("worker_pid")) is not int
            or ready["worker_pid"] < 1
            or ready["worker_pid"] != worker_pid
            or not _is_sha256(ready.get("record_hash"))
        ):
            raise TaskStoreConflict("Worker ready identity is invalid")
        _worker_evidence_timestamp(ready.get("started_at"))
        ready_payload = {
            "schema_version": "1.0.0",
            "submission_id": submission_id,
            "attempt_id": attempt_id,
            "attempt_number": attempt_number,
            "binding_hash": value["binding_hash"],
            "job_id": job_id,
            "capacity_slot": slot,
            "capacity_generation": generation,
            "worker_pid": ready["worker_pid"],
            "started_at": ready["started_at"],
        }
        if encode_document(ready_payload)[1] != ready["record_hash"]:
            raise TaskStoreConflict("Worker ready evidence hash is invalid")

    heartbeat = value.get("heartbeat")
    if heartbeat is not None:
        if (
            ready is None
            or not isinstance(heartbeat, Mapping)
            or set(heartbeat)
            != {"sequence", "state", "updated_at", "record_hash"}
        ):
            raise TaskStoreConflict("Worker heartbeat evidence is invalid")
        heartbeat = dict(heartbeat)
        if (
            type(heartbeat.get("sequence")) is not int
            or heartbeat["sequence"] < 1
            or heartbeat.get("state")
            not in {"running", "waiting", "succeeded", "failed", "stopped"}
            or not _is_sha256(heartbeat.get("record_hash"))
        ):
            raise TaskStoreConflict("Worker heartbeat state is invalid")
        _worker_evidence_timestamp(heartbeat.get("updated_at"))
        heartbeat_payload = {
            "schema_version": "1.0.0",
            "submission_id": submission_id,
            "attempt_id": attempt_id,
            "attempt_number": attempt_number,
            "binding_hash": value["binding_hash"],
            "job_id": job_id,
            "capacity_slot": slot,
            "capacity_generation": generation,
            "sequence": heartbeat["sequence"],
            "state": heartbeat["state"],
            "worker_pid": ready["worker_pid"],
            "started_at": ready["started_at"],
            "updated_at": heartbeat["updated_at"],
        }
        if encode_document(heartbeat_payload)[1] != heartbeat["record_hash"]:
            raise TaskStoreConflict("Worker heartbeat evidence hash is invalid")

    if ready is not None and heartbeat is None:
        raise TaskStoreConflict("Worker ready evidence has no heartbeat")

    return {
        **{key: value[key] for key in required - {"ticket", "ready", "heartbeat"}},
        "ticket": ticket,
        "ready": ready,
        "heartbeat": heartbeat,
    }


def _worker_observation_columns(value: Mapping[str, Any]) -> dict[str, Any]:
    """Return the exact relational projection of validated Worker evidence."""

    ticket = value["ticket"]
    ready = value["ready"]
    heartbeat = value["heartbeat"]
    return {
        "attempt_id": value["attempt_id"],
        "ticket_state": ticket["state"],
        "capacity_slot": ticket["capacity_slot"],
        "capacity_generation": ticket["capacity_generation"],
        "ticket_worker_pid": ticket["worker_pid"],
        "ticket_updated_at": ticket["updated_at"],
        "ticket_record_hash": ticket["record_hash"],
        "ready_worker_pid": None if ready is None else ready["worker_pid"],
        "ready_started_at": None if ready is None else ready["started_at"],
        "ready_record_hash": None if ready is None else ready["record_hash"],
        "heartbeat_sequence": (
            None if heartbeat is None else heartbeat["sequence"]
        ),
        "heartbeat_state": None if heartbeat is None else heartbeat["state"],
        "heartbeat_updated_at": (
            None if heartbeat is None else heartbeat["updated_at"]
        ),
        "heartbeat_record_hash": (
            None if heartbeat is None else heartbeat["record_hash"]
        ),
    }


def _decode_worker_observation_row(row: Mapping[str, Any]) -> dict[str, Any]:
    """Decode one observation and prove its JSON/relational views agree."""

    document = _decode_hashed_document(row, label="Worker attempt observation")
    try:
        normalized = _validated_worker_evidence(document)
    except TaskStoreConflict as error:
        raise TaskStoreCorruption(
            "persisted Worker attempt observation is invalid"
        ) from error
    expected = _worker_observation_columns(normalized)
    if any(row[key] != value for key, value in expected.items()):
        raise TaskStoreCorruption(
            "Worker attempt observation columns differ from its evidence"
        )
    return normalized


def _first_durable_running_observation(
    connection: sqlite3.Connection,
    *,
    attempt_id: str,
    project_id: str,
    principal_id: str,
    allow_checkpoint_waiting: bool = False,
) -> tuple[sqlite3.Row, dict[str, Any]] | None:
    """Decode observations in sequence order before selecting the start tick.

    Relational observation columns are only indexes over the hashed document,
    so filtering on those columns first would let a forged earlier document
    hide the real first-running observation.
    """

    observations = connection.execute(
        """
        SELECT * FROM worker_attempt_observations
        WHERE attempt_id = ?
        ORDER BY observation_sequence ASC
        """,
        (attempt_id,),
    )
    expected_sequence = 1
    first_running: tuple[sqlite3.Row, dict[str, Any]] | None = None
    for observation in observations:
        evidence = _decode_worker_observation_row(observation)
        if (
            type(observation["observation_sequence"]) is not int
            or observation["observation_sequence"] != expected_sequence
            or observation["project_id"] != project_id
            or observation["principal_id"] != principal_id
        ):
            raise TaskStoreCorruption(
                "Worker attempt observation audit chain is invalid"
            )
        expected_sequence += 1
        heartbeat_state = (
            None
            if evidence["heartbeat"] is None
            else evidence["heartbeat"]["state"]
        )
        if first_running is None and (
            evidence["ticket"]["state"] == "spawned"
            and evidence["ready"] is not None
            and evidence["heartbeat"] is not None
            and (
                heartbeat_state == "running"
                or (allow_checkpoint_waiting and heartbeat_state == "waiting")
            )
        ):
            first_running = observation, evidence
    return first_running


def _require_worker_attempt_observation_binding(
    attempt: Mapping[str, Any],
    evidence: Mapping[str, Any],
    *,
    intent_id: str,
    task_id: str,
    project_id: str,
    principal_id: str,
) -> None:
    """Bind the mutable relational attempt index back to hashed evidence."""

    expected = {
        "attempt_id": evidence["attempt_id"],
        "intent_id": intent_id,
        "task_id": task_id,
        "project_id": project_id,
        "principal_id": principal_id,
        "attempt_number": evidence["attempt_number"],
        "submission_id": evidence["submission_id"],
        "job_id": evidence["job_id"],
        "adapter_request_hash": evidence["request_hash"],
        "binding_hash": evidence["binding_hash"],
        "created_at": evidence["created_at"],
    }
    if any(attempt[key] != value for key, value in expected.items()):
        raise TaskStoreCorruption(
            "Worker launch attempt differs from its hashed observation"
        )


def _decode_run_event_row(
    row: Mapping[str, Any], *, label: str = "run event"
) -> dict[str, Any]:
    """Decode one RunEvent and prove its JSON/index projections agree."""

    event = _decode_hashed_document(row, label=label)
    indexed = {
        "task_id": row["task_id"],
        "sequence": row["sequence"],
        "event_id": row["event_id"],
        "event_type": row["event_type"],
        "task_status": row["task_status"],
        "node_id": row["node_id"],
        "occurred_at": row["occurred_at"],
    }
    fingerprint = event.get("fingerprint")
    if not isinstance(fingerprint, Mapping):
        raise TaskStoreCorruption(
            f"persisted {label} identity does not match its index"
        )
    try:
        _, fingerprint_hash = encode_document(fingerprint)
    except TaskStoreConflict as error:
        raise TaskStoreCorruption(
            f"persisted {label} fingerprint is invalid"
        ) from error
    if (
        any(event.get(key) != value for key, value in indexed.items())
        or fingerprint_hash != row["fingerprint_hash"]
    ):
        raise TaskStoreCorruption(
            f"persisted {label} identity does not match its index"
        )
    return event


def _validate_timeout_natural_terminal_event(
    event: Mapping[str, Any], *, receipt_job_id: str
) -> None:
    """Require the exact v1.4 Adapter-status bridge for a natural winner.

    The Worker's ``occurred_at`` is intentionally not ordered against the
    Supervisor clock.  Its same-clock binding to ``worker_updated_at`` is part
    of the event itself and can be proved without assuming clock sync.
    """

    terminal_status = event.get("task_status")
    expected_event_type = {
        "Succeeded": "node_succeeded",
        "Failed": "node_failed",
    }.get(terminal_status)
    expected_keys = {
        "schema_version",
        "event_id",
        "sequence",
        "task_id",
        "node_id",
        "event_type",
        "task_status",
        "occurred_at",
        "fingerprint",
        "extensions",
    }
    if terminal_status == "Failed":
        expected_keys.add("error")
    extensions = event.get("extensions")
    adapter_status = (
        extensions.get("org.agent_rpc.adapter_status")
        if isinstance(extensions, Mapping)
        else None
    )
    allowed_stages = {
        "Succeeded": frozenset({"complete"}),
        "Failed": frozenset({"failed", "worker_exit"}),
    }.get(terminal_status, frozenset())
    error_valid = (
        "error" not in event
        if terminal_status == "Succeeded"
        else event.get("error")
        == {
            "code": "worker_failed",
            "message": "FWI Worker reported a failure",
            "retryable": False,
        }
    )
    try:
        _runtime_timestamp(event.get("occurred_at"))
    except TaskStoreConflict as error:
        raise TaskStoreConflict(
            "natural terminal timeout event is invalid"
        ) from error
    if (
        set(event) != expected_keys
        or event.get("schema_version") != "1.0.0"
        or not isinstance(event.get("event_id"), str)
        or not event["event_id"]
        or type(event.get("sequence")) is not int
        or event["sequence"] < 1
        or not isinstance(event.get("task_id"), str)
        or not event["task_id"]
        or not isinstance(event.get("node_id"), str)
        or not event["node_id"]
        or event.get("event_type") != expected_event_type
        or not isinstance(event.get("occurred_at"), str)
        or not event["occurred_at"]
        or not isinstance(event.get("fingerprint"), Mapping)
        or not isinstance(extensions, Mapping)
        or set(extensions) != {"org.agent_rpc.adapter_status"}
        or not isinstance(adapter_status, Mapping)
        or set(adapter_status) != {"job_id", "stage", "worker_updated_at"}
        or adapter_status.get("job_id") != receipt_job_id
        or adapter_status.get("stage") not in allowed_stages
        or adapter_status.get("worker_updated_at") != event.get("occurred_at")
        or not error_valid
    ):
        raise TaskStoreConflict("natural terminal timeout event is invalid")


def _runtime_lease_window(
    clock: Callable[[], str], lease_seconds: int
) -> tuple[str, int, str, int]:
    """Sample a lease clock only after the caller owns its write transaction."""

    if type(lease_seconds) is not int or not 1 <= lease_seconds <= 3600:
        raise TaskStoreConflict("runtime supervisor lease window is invalid")
    if not callable(clock):
        raise TaskStoreConflict("runtime supervisor clock is invalid")
    now, now_us = _runtime_timestamp(clock())
    try:
        expires = (
            _UNIX_EPOCH
            + timedelta(microseconds=now_us)
            + timedelta(seconds=lease_seconds)
        )
    except OverflowError as error:
        raise TaskStoreConflict("runtime supervisor lease window is invalid") from error
    expires_at = expires.isoformat(timespec="microseconds").replace("+00:00", "Z")
    expires_at_us = now_us + lease_seconds * 1_000_000
    return now, now_us, expires_at, expires_at_us


def _request_hash_set(values: str | Sequence[str]) -> frozenset[str]:
    """Validate the bounded exact-hash set used only for version replay.

    This deliberately accepts hashes rather than an unqualified idempotency-key
    lookup: callers must first reconstruct and validate every historical request
    shape they are willing to replay.
    """

    candidates: Sequence[str]
    if isinstance(values, str):
        candidates = (values,)
    elif isinstance(values, Sequence):
        candidates = values
    else:
        raise TaskStoreConflict("compatible request hashes are invalid")
    if not 1 <= len(candidates) <= 8:
        raise TaskStoreConflict("compatible request hashes are invalid")
    result = frozenset(candidates)
    if len(result) != len(candidates) or any(
        not isinstance(value, str)
        or len(value) != 71
        or not value.startswith("sha256:")
        or any(character not in "0123456789abcdef" for character in value[7:])
        for value in result
    ):
        raise TaskStoreConflict("compatible request hashes are invalid")
    return result


def _decode_document(text: str, *, label: str) -> dict[str, Any]:
    try:
        value = json.loads(text)
    except (TypeError, json.JSONDecodeError) as error:
        raise TaskStoreCorruption(f"invalid persisted JSON for {label}") from error
    if not isinstance(value, dict):
        raise TaskStoreCorruption(f"persisted {label} must be a JSON object")
    return value


def _raise_operational_error(error: sqlite3.OperationalError) -> None:
    error_code = getattr(error, "sqlite_errorcode", None)
    message = str(error).lower()
    # SQLITE_BUSY=5 and SQLITE_LOCKED=6. Python 3.10 does not expose the
    # symbolic constants, and extended result codes retain the low byte.
    if (
        (isinstance(error_code, int) and error_code & 0xFF in {5, 6})
        or "database is locked" in message
        or "database is busy" in message
    ):
        raise TaskStoreUnavailable("task store is busy") from error
    raise TaskStoreError("SQLite task-store operation failed") from error


def _decode_hashed_document(
    row: Mapping[str, Any], *, label: str
) -> dict[str, Any]:
    value = _decode_document(row["document_json"], label=label)
    try:
        _, actual_hash = encode_document(value)
    except TaskStoreConflict as error:
        raise TaskStoreCorruption(f"persisted {label} is not finite JSON") from error
    if actual_hash != row["document_hash"]:
        raise TaskStoreCorruption(f"persisted {label} hash does not match its content")
    return value


def _load_migrations() -> tuple[_Migration, ...]:
    migrations: list[_Migration] = []
    for path in sorted(MIGRATIONS_DIRECTORY.glob("[0-9][0-9][0-9][0-9]_*.sql")):
        prefix = path.name.split("_", 1)[0]
        version = int(prefix)
        text = path.read_text(encoding="utf-8")
        migrations.append(
            _Migration(
                version=version,
                path=path,
                text=text,
                checksum=hashlib.sha256(text.encode("utf-8")).hexdigest(),
            )
        )
    if not migrations:
        raise TaskStoreCorruption("no task-store migrations are available")
    versions = [migration.version for migration in migrations]
    if versions != list(range(1, len(migrations) + 1)):
        raise TaskStoreCorruption(
            "task-store migration versions must be contiguous from one"
        )
    return tuple(migrations)


def _migration_statements(text: str) -> Sequence[str]:
    """Split SQL with sqlite's parser, preserving trigger bodies atomically."""

    statements: list[str] = []
    pending = ""
    for line in text.splitlines(keepends=True):
        pending += line
        if sqlite3.complete_statement(pending):
            statement = pending.strip()
            if statement:
                statements.append(statement)
            pending = ""
    if pending.strip():
        raise TaskStoreCorruption("incomplete SQLite migration statement")
    return statements


def _schema_manifest(connection: sqlite3.Connection) -> tuple[tuple[str, ...], ...]:
    rows = connection.execute(
        """
        SELECT type, name, tbl_name, sql
        FROM sqlite_master
        WHERE name NOT LIKE 'sqlite_%'
        ORDER BY type, name
        """
    ).fetchall()
    return tuple(
        (
            str(row["type"]),
            str(row["name"]),
            str(row["tbl_name"]),
            " ".join(str(row["sql"]).split()) if row["sql"] is not None else "",
        )
        for row in rows
    )


def _expected_schema_manifest(
    migrations: Sequence[_Migration],
) -> tuple[tuple[str, ...], ...]:
    connection = sqlite3.connect(":memory:", isolation_level=None)
    connection.row_factory = sqlite3.Row
    try:
        # v21 performs a name-preserving DROP/reCREATE rebuild.  The expected
        # schema is data-free, so build it with FK actions disabled and then
        # validate the finished graph exactly as the file migration does.
        connection.execute("PRAGMA foreign_keys = OFF")
        connection.execute(SCHEMA_MIGRATIONS_SQL)
        for migration in migrations:
            for statement in _migration_statements(migration.text):
                connection.execute(statement)
        connection.execute("PRAGMA foreign_keys = ON")
        if connection.execute("PRAGMA foreign_key_check").fetchone() is not None:
            raise TaskStoreCorruption(
                "expected task-store schema has invalid foreign keys"
            )
        return _schema_manifest(connection)
    finally:
        connection.close()


class SQLiteTaskStore:
    """File-backed SQLite implementation with WAL and per-operation connections."""

    def __init__(self, database_path: str | Path, *, busy_timeout_ms: int = 5000):
        raw_path = Path(database_path).expanduser()
        if str(database_path) == ":memory:":
            raise ValueError("the durable task store cannot use an in-memory database")
        if not raw_path.is_absolute():
            raise ValueError("the durable task database path must be absolute")
        raw_path = Path(os.path.abspath(raw_path))
        if busy_timeout_ms < 1:
            raise ValueError("busy_timeout_ms must be positive")
        if any(part.is_symlink() for part in (raw_path, *raw_path.parents)):
            raise ValueError("the task database path cannot traverse a symbolic link")
        raw_path.parent.mkdir(mode=0o700, parents=True, exist_ok=True)
        if not raw_path.parent.is_dir():
            raise ValueError("the task database parent must be a directory")
        if os.stat(raw_path.parent).st_mode & 0o077:
            raise ValueError("the task database parent must be a dedicated private directory")
        self.database_path = raw_path
        self.busy_timeout_ms = int(busy_timeout_ms)
        self._initialize()

    def _initialize(self) -> None:
        """Serialize first-open inspection without changing an unrelated DB."""

        flags = os.O_RDWR | os.O_CREAT | os.O_CLOEXEC | getattr(os, "O_NOFOLLOW", 0)
        try:
            lock_fd = os.open(self.database_path, flags | os.O_EXCL, 0o600)
        except FileExistsError:
            lock_fd = os.open(
                self.database_path,
                os.O_RDWR | os.O_CLOEXEC | getattr(os, "O_NOFOLLOW", 0),
            )
        locked = False
        try:
            if not stat.S_ISREG(os.fstat(lock_fd).st_mode):
                raise TaskStoreError("task database is not a regular private file")
            deadline = time.monotonic() + self.busy_timeout_ms / 1000
            while not locked:
                try:
                    fcntl.flock(lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
                    locked = True
                except BlockingIOError as error:
                    remaining = deadline - time.monotonic()
                    if remaining <= 0:
                        raise TaskStoreUnavailable(
                            "task store initialization is busy"
                        ) from error
                    time.sleep(min(0.01, remaining))
            self._initialize_locked()
        finally:
            if locked:
                fcntl.flock(lock_fd, fcntl.LOCK_UN)
            os.close(lock_fd)

    def _connect(self, *, require_wal: bool = True) -> sqlite3.Connection:
        connection = sqlite3.connect(
            str(self.database_path),
            timeout=self.busy_timeout_ms / 1000,
            isolation_level=None,
        )
        connection.row_factory = sqlite3.Row
        try:
            connection.execute("PRAGMA foreign_keys = ON")
            connection.execute(f"PRAGMA busy_timeout = {self.busy_timeout_ms}")
            connection.execute("PRAGMA synchronous = FULL")
            if connection.execute("PRAGMA foreign_keys").fetchone()[0] != 1:
                raise TaskStoreError("SQLite foreign key enforcement is unavailable")
            if require_wal:
                journal_mode = str(
                    connection.execute("PRAGMA journal_mode").fetchone()[0]
                ).lower()
                if journal_mode != "wal":
                    raise TaskStoreError(
                        "SQLite WAL mode is required for the task store"
                    )
        except sqlite3.OperationalError as error:
            connection.close()
            _raise_operational_error(error)
        except sqlite3.DatabaseError as error:
            connection.close()
            raise TaskStoreCorruption("task database is not valid SQLite") from error
        except Exception:
            connection.close()
            raise
        return connection

    def arm_worker_attempt_timeout(
        self,
        *,
        intent_id: str,
        attempt_id: str,
        capability_proof: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> TaskTimeoutRecord:
        """Arm one exact attempt from its first durable ready/running sample."""

        if (
            not isinstance(intent_id, str)
            or not intent_id
            or not isinstance(attempt_id, str)
            or WORKER_ATTEMPT_ID.fullmatch(attempt_id) is None
            or not isinstance(capability_proof, Mapping)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("Worker timeout window is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            recorded_at, recorded_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            if intent is None:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("timeout target task cannot be read")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=recorded_at_us,
                action="Worker timeout window",
            )
            if (
                task["status"] not in {"Queued", "Running"}
                or intent.state != "dispatched"
                or intent.handle is None
                or not _is_supported_managed_dispatch(intent)
            ):
                raise TaskStoreConflict(
                    "timeout window requires a current managed dispatch"
                )
            if connection.execute(
                "SELECT 1 FROM task_purge_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("purged task cannot arm a timeout")
            attempt = connection.execute(
                """
                SELECT * FROM worker_launch_attempts
                WHERE intent_id = ?
                ORDER BY attempt_number DESC LIMIT 1
                """,
                (intent_id,),
            ).fetchone()
            if attempt is None or attempt["attempt_id"] != attempt_id:
                raise TaskStoreConflict(
                    "timeout window requires the latest exact Worker attempt"
                )
            worker_exit_retry_attempt = connection.execute(
                """
                SELECT 1 FROM worker_exit_retry_reservations
                WHERE intent_id = ? AND attempt_number = ?
                """,
                (intent_id, int(attempt["attempt_number"])),
            ).fetchone() is not None
            proof = _validate_timeout_capability_proof(
                capability_proof,
                attempt_id=attempt_id,
                binding_hash=attempt["binding_hash"],
                private_schema_version=(
                    "1.3.0"
                    if worker_exit_retry_attempt
                    else (
                        "1.2.0"
                        if int(attempt["attempt_number"]) == 2
                        else "1.1.0"
                    )
                ),
            )
            proof_json, proof_document_hash = encode_document(proof)
            first_running = _first_durable_running_observation(
                connection,
                attempt_id=attempt_id,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                allow_checkpoint_waiting=(
                    _is_checkpoint_capable_managed_dispatch(intent)
                ),
            )
            if first_running is None:
                raise TaskStoreConflict(
                    "timeout window requires durable ready/live evidence"
                )
            observation, observation_evidence = first_running
            _require_worker_attempt_observation_binding(
                attempt,
                observation_evidence,
                intent_id=intent_id,
                task_id=task_id,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
            )
            heartbeat_state = observation_evidence["heartbeat"]["state"]
            start_source = (
                observation_evidence["ready"]["started_at"]
                if heartbeat_state == "waiting"
                else observation["observed_at"]
            )
            try:
                started_at, started_at_us = _runtime_timestamp(start_source)
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "timeout start observation time is invalid"
                ) from error
            if (
                (
                    heartbeat_state != "waiting"
                    and started_at != start_source
                )
                or recorded_at_us < started_at_us
            ):
                raise TaskStoreConflict("timeout window clock is inconsistent")
            wall_time_seconds = intent.request.get("resources", {}).get(
                "wall_time_seconds"
            )
            if (
                type(wall_time_seconds) is not int
                or not 1 <= wall_time_seconds <= 86_400
            ):
                raise TaskStoreCorruption(
                    "dispatch intent wall-time policy is invalid"
                )
            deadline_at_us = started_at_us + wall_time_seconds * 1_000_000
            deadline_at = _runtime_timestamp_from_microseconds(deadline_at_us)
            timeout_id = _runtime_timeout_id(
                intent_id=intent_id,
                attempt_id=attempt_id,
                start_observation_sequence=observation[
                    "observation_sequence"
                ],
                wall_time_seconds=wall_time_seconds,
            )
            existing = connection.execute(
                """
                SELECT timeout_id, capability_proof_hash
                FROM worker_attempt_timeout_windows
                WHERE attempt_id = ?
                """,
                (attempt_id,),
            ).fetchone()
            if existing is not None:
                if (
                    existing["timeout_id"] != timeout_id
                    or existing["capability_proof_hash"]
                    != proof_document_hash
                ):
                    raise TaskStoreConflict(
                        "Worker attempt already has another timeout window"
                    )
                snapshot = self._load_snapshot(connection, task_id)
                if snapshot is None or snapshot.timeout is None:
                    raise TaskStoreCorruption("timeout window replay cannot be read")
                connection.commit()
                return TaskTimeoutRecord(
                    snapshot=snapshot,
                    timeout=snapshot.timeout,
                    replayed=True,
                )

            window = {
                "schema_version": "1.0.0",
                "timeout_id": timeout_id,
                "task_id": task_id,
                "intent_id": intent_id,
                "attempt_id": attempt_id,
                "reason": "wall_time_exceeded",
                "wall_time_seconds": wall_time_seconds,
                "start_observation_sequence": observation[
                    "observation_sequence"
                ],
                "started_at": started_at,
                "deadline_at": deadline_at,
                "ready_record_hash": observation["ready_record_hash"],
                "running_heartbeat_record_hash": observation[
                    "heartbeat_record_hash"
                ],
                "capability_proof": proof,
                "recorded_at": recorded_at,
                "extensions": {},
            }
            document_json, document_hash = encode_document(window)
            connection.execute(
                """
                INSERT INTO worker_attempt_timeout_windows(
                    timeout_id, task_id, project_id, principal_id,
                    intent_id, attempt_id, start_observation_sequence,
                    wall_time_seconds, started_at, started_at_us,
                    deadline_at, deadline_at_us, ready_record_hash,
                    running_heartbeat_record_hash, capability_record_hash,
                    capability_proof_json, capability_proof_hash,
                    document_json, document_hash, recorded_fencing_token,
                    recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?,
                          ?, ?, ?, ?, ?)
                """,
                (
                    timeout_id,
                    task_id,
                    task["project_id"],
                    task["principal_id"],
                    intent_id,
                    attempt_id,
                    observation["observation_sequence"],
                    wall_time_seconds,
                    started_at,
                    started_at_us,
                    deadline_at,
                    deadline_at_us,
                    observation["ready_record_hash"],
                    observation["heartbeat_record_hash"],
                    proof["capability_record_hash"],
                    proof_json,
                    proof_document_hash,
                    document_json,
                    document_hash,
                    supervisor_lease.fencing_token,
                    recorded_at,
                    recorded_at_us,
                ),
            )
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None or snapshot.timeout is None:
                raise TaskStoreCorruption("timeout window cannot be read")
            connection.commit()
            return TaskTimeoutRecord(
                snapshot=snapshot,
                timeout=snapshot.timeout,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "Worker timeout window lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "Worker timeout window conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    @staticmethod
    def _dag_aggregate_status(readiness: Any) -> str:
        if readiness.all_nodes_succeeded:
            return "Succeeded"
        if (
            readiness.active_node_ids
            or readiness.runnable_node_ids
            or readiness.waiting_node_ids
        ):
            return "Running"
        if (
            readiness.failed_node_ids
            or readiness.cancelled_node_ids
            or readiness.blocked_nodes
        ):
            return "Failed"
        raise TaskStoreCorruption("DAG aggregate has no terminal or runnable case")

    def _materialize_dag_blocked_nodes(
        self,
        connection: sqlite3.Connection,
        *,
        task_id: str,
        plan: Mapping[str, Any],
        approval_id: str,
        project_id: str,
        principal_id: str,
        supervisor_lease: RuntimeSupervisorLease,
        recorded_at: str,
        recorded_at_us: int,
    ) -> tuple[str, ...]:
        state_map = self._load_dag_node_state_map(
            connection, task_id=task_id, plan=plan
        )
        if state_map is None:
            raise TaskStoreConflict(
                "DAG dependency reconciliation requires initialized states"
            )
        states_by_id = {node.node_id: node for node in state_map.nodes}
        try:
            readiness = evaluate_dag_readiness(
                plan,
                node_states={node.node_id: node.state for node in state_map.nodes},
            )
        except DagScheduleError as error:
            raise TaskStoreCorruption(
                "persisted DAG state cannot be reconciled"
            ) from error
        materialized: list[str] = []
        for blocked in readiness.blocked_nodes:
            node = states_by_id[blocked.node_id]
            if node.state == "Blocked":
                continue
            if node.state != "Pending":
                raise TaskStoreCorruption(
                    "pure readiness blocked a non-Pending DAG node"
                )
            blocker_document = {
                "schema_version": "1.0.0",
                "task_id": task_id,
                "plan_id": plan["plan_id"],
                "plan_hash": plan["plan_hash"],
                "approval_id": approval_id,
                "node_id": node.node_id,
                "previous_revision": node.revision,
                "blocked_revision": node.revision + 1,
                "blocked_by_node_ids": list(blocked.blocked_by_node_ids),
                "supervisor_term": {
                    "fencing_token": supervisor_lease.fencing_token,
                    "owner_id": supervisor_lease.owner_id,
                    "acquired_at": supervisor_lease.acquired_at,
                },
                "recorded_at": recorded_at,
            }
            blocker_json, blocker_hash = encode_document(blocker_document)
            connection.execute(
                """
                INSERT INTO dag_node_scheduler_transition_facts(
                    task_id, plan_id, plan_hash, approval_id, node_id,
                    previous_revision, previous_state, node_revision, state,
                    blocker_document_json, blocker_document_hash,
                    project_id, principal_id, fencing_token, owner_id,
                    term_acquired_at, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, 'Pending', ?, 'Blocked', ?, ?,
                          ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    plan["plan_id"],
                    plan["plan_hash"],
                    approval_id,
                    node.node_id,
                    node.revision,
                    node.revision + 1,
                    blocker_json,
                    blocker_hash,
                    project_id,
                    principal_id,
                    supervisor_lease.fencing_token,
                    supervisor_lease.owner_id,
                    supervisor_lease.acquired_at,
                    recorded_at,
                    recorded_at_us,
                ),
            )
            connection.execute(
                """
                INSERT INTO dag_node_state_events(
                    task_id, plan_id, plan_hash, node_id, revision,
                    previous_state, state, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, 'Pending', 'Blocked', ?, ?)
                """,
                (
                    task_id,
                    plan["plan_id"],
                    plan["plan_hash"],
                    node.node_id,
                    node.revision + 1,
                    recorded_at,
                    recorded_at_us,
                ),
            )
            materialized.append(node.node_id)
        return tuple(materialized)

    def reconcile_dag_node_dependencies(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        expected_plan_hash: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagScheduleReconciliation:
        if (
            not task_id
            or not project_id
            or not principal_id
            or not _is_sha256(expected_plan_hash)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("DAG dependency reconciliation is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            recorded_at, recorded_at_us = _runtime_timestamp(supervisor_clock())
            snapshot = self._load_snapshot(connection, task_id)
            if (
                snapshot is None
                or snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
            ):
                raise TaskStoreConflict("task does not exist in the requested scope")
            plan = snapshot.plan
            approval = snapshot.approval
            if (
                snapshot.status not in {
                    "AwaitingApproval", "Queued", "Running", "Waiting",
                    "Failed", "Succeeded"
                }
                or not isinstance(plan, Mapping)
                or len(plan.get("nodes", [])) <= 1
                or plan.get("plan_hash") != expected_plan_hash
                or not isinstance(approval, Mapping)
                or approval.get("decision") != "approved"
                or approval.get("plan_id") != plan.get("plan_id")
                or approval.get("plan_hash") != expected_plan_hash
            ):
                raise TaskStoreConflict(
                    "DAG reconciliation requires the exact approved runtime plan"
                )
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=project_id,
                principal_id=principal_id,
                supplied=supervisor_lease,
                now_us=recorded_at_us,
                action="DAG dependency reconciliation",
            )
            existing_states = self._load_dag_node_state_map(
                connection, task_id=task_id, plan=plan
            )
            if existing_states is None:
                if snapshot.status != "AwaitingApproval":
                    raise TaskStoreCorruption(
                        "runtime DAG Task lost its initialized node states"
                    )
                for node in sorted(plan["nodes"], key=lambda value: value["node_id"]):
                    connection.execute(
                        """
                        INSERT INTO dag_node_state_events(
                            task_id, plan_id, plan_hash, node_id, revision,
                            previous_state, state, recorded_at, recorded_at_us
                        ) VALUES (?, ?, ?, ?, 1, NULL, 'Pending', ?, ?)
                        """,
                        (
                            task_id,
                            plan["plan_id"],
                            plan["plan_hash"],
                            node["node_id"],
                            recorded_at,
                            recorded_at_us,
                        ),
                    )
            blocked_node_ids = self._materialize_dag_blocked_nodes(
                connection,
                task_id=task_id,
                plan=plan,
                approval_id=approval["approval_id"],
                project_id=project_id,
                principal_id=principal_id,
                supervisor_lease=supervisor_lease,
                recorded_at=recorded_at,
                recorded_at_us=recorded_at_us,
            )
            node_states = self._load_dag_node_state_map(
                connection, task_id=task_id, plan=plan
            )
            if node_states is None:
                raise TaskStoreCorruption("reconciled DAG state map cannot be read")
            readiness = evaluate_dag_readiness(
                plan,
                node_states={node.node_id: node.state for node in node_states.nodes},
            )
            aggregate_status = self._dag_aggregate_status(readiness)
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None:
                raise TaskStoreCorruption("reconciled DAG Task cannot be read")
            connection.commit()
            return DagScheduleReconciliation(
                snapshot=snapshot,
                node_states=node_states,
                runnable_node_ids=readiness.runnable_node_ids,
                active_node_ids=readiness.active_node_ids,
                blocked_node_ids=blocked_node_ids,
                aggregate_status=aggregate_status,
                replayed=not blocked_node_ids,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "DAG dependency reconciliation lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "DAG dependency reconciliation conflicts with durable state"
            ) from error
        finally:
            connection.close()

    def _require_current_dag_node_execution(
        self,
        connection: sqlite3.Connection,
        *,
        intent: DispatchIntentSnapshot,
        allowed_node_states: frozenset[str],
    ) -> sqlite3.Row | None:
        """Fail closed if a DAG-tagged P2 intent drifts from its admission."""

        admission = self._load_dag_node_execution_admission(
            connection, intent_id=intent.intent_id
        )
        if admission is None:
            return None
        task = connection.execute(
            """
            SELECT task.status, task.project_id, task.principal_id,
                   task.current_plan_id, task.current_approval_id,
                   approval.decision, approval.plan_id AS approval_plan_id,
                   approval.plan_hash AS approval_plan_hash
            FROM tasks AS task
            JOIN approvals AS approval
              ON approval.task_id = task.task_id
             AND approval.approval_id = task.current_approval_id
            WHERE task.task_id = ?
            """,
            (intent.task_id,),
        ).fetchone()
        binding = connection.execute(
            """
            SELECT 1 FROM dag_node_input_binding_facts
            WHERE task_id = ? AND plan_id = ? AND approval_id = ?
              AND target_node_id = ? AND target_node_revision = ?
              AND fencing_token = ? AND owner_id = ?
              AND term_acquired_at = ? AND binding_document_hash = ?
            """,
            (
                admission["task_id"],
                admission["plan_id"],
                admission["approval_id"],
                admission["node_id"],
                admission["pending_revision"],
                admission["input_fencing_token"],
                admission["input_owner_id"],
                admission["input_term_acquired_at"],
                admission["input_binding_document_hash"],
            ),
        ).fetchone()
        latest_state = connection.execute(
            """
            SELECT revision, state FROM dag_node_state_events
            WHERE task_id = ? AND plan_id = ? AND node_id = ?
            ORDER BY revision DESC LIMIT 1
            """,
            (intent.task_id, intent.plan_id, intent.node_id),
        ).fetchone()
        if (
            admission["intent_id"] != intent.intent_id
            or admission["plan_id"] != intent.plan_id
            or admission["plan_hash"] != intent.plan_hash
            or admission["approval_id"] != intent.approval_id
            or admission["node_id"] != intent.node_id
            or admission["node_idempotency_key"]
            != intent.node_idempotency_key
            or admission["max_node_attempts"] != 1
            or task is None
            or task["current_plan_id"] != intent.plan_id
            or task["current_approval_id"] != intent.approval_id
            or task["decision"] != "approved"
            or task["approval_plan_id"] != intent.plan_id
            or task["approval_plan_hash"] != intent.plan_hash
            or task["project_id"] != admission["project_id"]
            or task["principal_id"] != admission["principal_id"]
            or binding is None
            or latest_state is None
            or latest_state["state"] not in allowed_node_states
            or int(latest_state["revision"])
            != (
                int(admission["queued_revision"])
                if latest_state["state"] == "Queued"
                else int(admission["queued_revision"]) + 1
            )
        ):
            raise TaskStoreConflict(
                "DAG node execution no longer matches its exact admission"
            )
        return admission

    def finalize_supervised_worker_exit_retry_exhaustion(
        self,
        *,
        intent_id: str,
        attempt_id: str,
        observation_sequence: int,
        evidence: Mapping[str, Any],
        private_schema_version: str,
        private_proof_hash: str,
        failure_kind: str,
        terminal_event: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedWorkerExitRetryExhaustion:
        """Atomically close attempt 2 without ever authorizing attempt 3."""

        if (
            not isinstance(intent_id, str)
            or not intent_id
            or WORKER_ATTEMPT_ID.fullmatch(str(attempt_id)) is None
            or type(observation_sequence) is not int
            or observation_sequence < 1
            or private_schema_version != "1.3.0"
            or not _is_sha256(private_proof_hash)
            or failure_kind not in {"pre_running_launch_failure", "worker_exit"}
            or not isinstance(terminal_event, Mapping)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("Worker-exit retry exhaustion is invalid")
        normalized = _validated_worker_evidence(evidence)
        evidence_json, evidence_hash = encode_document(normalized)
        event = copy.deepcopy(dict(terminal_event))
        event_json, event_hash = encode_document(event)

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            exhausted_at, exhausted_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            reservation = connection.execute(
                """
                SELECT * FROM worker_exit_retry_reservations
                WHERE intent_id = ? AND attempt_number = 2
                """,
                (intent_id,),
            ).fetchone()
            if reservation is None:
                raise TaskStoreConflict("Worker-exit retry reservation is absent")
            task_id = reservation["task_id"]
            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("retry target task cannot be read")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=exhausted_at_us,
                action="Worker-exit retry exhaustion",
            )
            existing = connection.execute(
                """
                SELECT * FROM worker_exit_retry_exhaustions
                WHERE intent_id = ? AND attempt_number = 2
                """,
                (intent_id,),
            ).fetchone()
            if existing is not None:
                if (
                    existing["attempt_id"] != attempt_id
                    or existing["observation_sequence"] != observation_sequence
                    or existing["evidence_hash"] != evidence_hash
                    or existing["private_schema_version"]
                    != private_schema_version
                    or existing["private_proof_hash"] != private_proof_hash
                    or existing["failure_kind"] != failure_kind
                    or existing["terminal_event_hash"] != event_hash
                ):
                    raise TaskStoreConflict(
                        "Worker-exit retry exhaustion conflicts with durable state"
                    )
                snapshot = self._load_snapshot(connection, task_id)
                intent = self._load_dispatch_intent(
                    connection, intent_id=intent_id
                )
                if snapshot is None or intent is None or snapshot.status != "Failed":
                    raise TaskStoreCorruption(
                        "Worker-exit retry exhaustion cannot be read"
                    )
                connection.commit()
                return SupervisedWorkerExitRetryExhaustion(
                    snapshot=snapshot,
                    intent=intent,
                    attempt_id=attempt_id,
                    observation_sequence=observation_sequence,
                    evidence_hash=evidence_hash,
                    private_proof_hash=private_proof_hash,
                    private_schema_version=private_schema_version,
                    failure_kind=failure_kind,
                    terminal_event_sequence=existing[
                        "terminal_event_sequence"
                    ],
                    fencing_token=existing["fencing_token"],
                    exhausted_at=existing["exhausted_at"],
                    replayed=True,
                )
            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            if intent is None:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            attempt = connection.execute(
                """
                SELECT * FROM worker_launch_attempts
                WHERE intent_id = ? ORDER BY attempt_number DESC LIMIT 1
                """,
                (intent_id,),
            ).fetchone()
            observation = connection.execute(
                """
                SELECT * FROM worker_attempt_observations
                WHERE attempt_id = ? AND observation_sequence = ?
                """,
                (attempt_id, observation_sequence),
            ).fetchone()
            latest_observation = connection.execute(
                """
                SELECT MAX(observation_sequence)
                FROM worker_attempt_observations WHERE attempt_id = ?
                """,
                (attempt_id,),
            ).fetchone()[0]
            replacement = connection.execute(
                """
                SELECT * FROM worker_exit_retry_dispatch_replacements
                WHERE intent_id = ? AND attempt_number = 2
                """,
                (intent_id,),
            ).fetchone()
            pre_running = failure_kind == "pre_running_launch_failure"
            ticket = normalized["ticket"]
            if (
                attempt is None
                or attempt["attempt_id"] != attempt_id
                or int(attempt["attempt_number"]) != 2
                or attempt["intent_id"] != intent_id
                or observation is None
                or observation["document_hash"] != evidence_hash
                or int(latest_observation) != observation_sequence
                or normalized["attempt_id"] != attempt_id
                or normalized["attempt_number"] != 2
                or (
                    pre_running
                    and (
                        task["status"] != "Retrying"
                        or intent.state != "retrying"
                        or replacement is not None
                        or ticket["state"] != "failed"
                        or ticket["worker_pid"] is not None
                        or normalized["ready"] is not None
                        or normalized["heartbeat"] is not None
                    )
                )
                or (
                    not pre_running
                    and (
                        task["status"] != "Running"
                        or intent.state != "dispatched"
                        or replacement is None
                        or replacement["attempt_id"] != attempt_id
                        or ticket["state"] != "spawned"
                        or normalized["ready"] is None
                        or normalized["heartbeat"] is None
                        or normalized["heartbeat"]["state"] != "running"
                    )
                )
            ):
                raise TaskStoreConflict(
                    "retry exhaustion requires exact current attempt 2 evidence"
                )
            if connection.execute(
                "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("cancellation owns the terminal race")
            if connection.execute(
                "SELECT 1 FROM task_timeout_outcomes WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("timeout owns the terminal race")
            if connection.execute(
                """
                SELECT 1
                FROM worker_attempt_timeout_windows AS timeout
                JOIN supervised_timeout_attempts AS delivery
                  ON delivery.timeout_id = timeout.timeout_id
                WHERE timeout.attempt_id = ?
                """,
                (attempt_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("timeout delivery owns the terminal race")
            next_sequence = int(
                connection.execute(
                    "SELECT COALESCE(MAX(sequence), 0) + 1 FROM run_events "
                    "WHERE task_id = ?",
                    (task_id,),
                ).fetchone()[0]
            )
            extension = event.get("extensions", {}).get(
                "org.agent_rpc.retry_exhaustion"
            )
            expected_extension = {
                "intent_id": intent_id,
                "attempt_id": attempt_id,
                "attempt_number": 2,
                "observation_sequence": observation_sequence,
                "evidence_hash": evidence_hash,
                "private_schema_version": "1.3.0",
                "private_proof_hash": private_proof_hash,
                "failure_kind": failure_kind,
                "max_attempts": 2,
            }
            if (
                event.get("sequence") != next_sequence
                or event.get("task_id") != task_id
                or event.get("node_id") != intent.node_id
                or event.get("event_type") != "node_failed"
                or event.get("task_status") != "Failed"
                or event.get("error", {}).get("code") != "retry_exhausted"
                or event.get("error", {}).get("retryable") is not False
                or event.get("fingerprint") != intent.queue_fingerprint
                or extension != expected_extension
                or (
                    pre_running
                    and event.get("occurred_at") != ticket["updated_at"]
                )
            ):
                raise TaskStoreConflict("retry exhaustion event is invalid")
            _, fingerprint_hash = encode_document(event["fingerprint"])
            connection.execute(
                """
                INSERT INTO run_events(
                    task_id, sequence, event_id, event_type, task_status,
                    node_id, fingerprint_hash, document_json, document_hash,
                    occurred_at, recorded_at
                ) VALUES (?, ?, ?, 'node_failed', 'Failed', ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    next_sequence,
                    event["event_id"],
                    intent.node_id,
                    fingerprint_hash,
                    event_json,
                    event_hash,
                    event["occurred_at"],
                    exhausted_at,
                ),
            )
            connection.execute(
                """
                INSERT INTO supervised_run_event_commits(
                    task_id, sequence, project_id, principal_id,
                    fencing_token, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    next_sequence,
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    exhausted_at,
                    exhausted_at_us,
                ),
            )
            connection.execute(
                "UPDATE tasks SET status = 'Failed', updated_at = ? "
                "WHERE task_id = ?",
                (exhausted_at, task_id),
            )
            connection.execute(
                """
                INSERT INTO worker_exit_retry_exhaustions(
                    intent_id, attempt_number, task_id, project_id,
                    principal_id, approval_id, attempt_id,
                    observation_sequence, evidence_hash,
                    private_schema_version, private_proof_hash, failure_kind,
                    max_attempts, terminal_event_sequence,
                    terminal_event_hash, fencing_token,
                    exhausted_at, exhausted_at_us
                ) VALUES (?, 2, ?, ?, ?, ?, ?, ?, ?, '1.3.0', ?, ?, 2,
                          ?, ?, ?, ?, ?)
                """,
                (
                    intent_id,
                    task_id,
                    task["project_id"],
                    task["principal_id"],
                    intent.approval_id,
                    attempt_id,
                    observation_sequence,
                    evidence_hash,
                    private_proof_hash,
                    failure_kind,
                    next_sequence,
                    event_hash,
                    supervisor_lease.fencing_token,
                    exhausted_at,
                    exhausted_at_us,
                ),
            )
            snapshot = self._load_snapshot(connection, task_id)
            stored_intent = self._load_dispatch_intent(
                connection, intent_id=intent_id
            )
            if snapshot is None or stored_intent is None or snapshot.status != "Failed":
                raise TaskStoreCorruption(
                    "Worker-exit retry exhaustion cannot be read"
                )
            connection.commit()
            return SupervisedWorkerExitRetryExhaustion(
                snapshot=snapshot,
                intent=stored_intent,
                attempt_id=attempt_id,
                observation_sequence=observation_sequence,
                evidence_hash=evidence_hash,
                private_proof_hash=private_proof_hash,
                private_schema_version=private_schema_version,
                failure_kind=failure_kind,
                terminal_event_sequence=next_sequence,
                fencing_token=supervisor_lease.fencing_token,
                exhausted_at=exhausted_at,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "retry exhaustion lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "retry exhaustion conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    @staticmethod
    def _load_worker_exit_retry_source_handle(
        connection: sqlite3.Connection, reservation: sqlite3.Row
    ) -> dict[str, Any]:
        rows = connection.execute(
            """
            SELECT document_json, document_hash FROM (
                SELECT outcome.document_json, outcome.document_hash
                FROM dispatch_outcomes AS outcome
                WHERE outcome.intent_id = ? AND outcome.outcome = 'dispatched'
                UNION ALL
                SELECT resolution.effective_outcome_json AS document_json,
                       resolution.effective_outcome_hash AS document_hash
                FROM dispatch_reconciliation_resolutions AS resolution
                WHERE resolution.intent_id = ?
                  AND resolution.result = 'dispatched'
            ) WHERE document_hash = ?
            """,
            (
                reservation["intent_id"],
                reservation["intent_id"],
                reservation["source_outcome_document_hash"],
            ),
        ).fetchall()
        if len(rows) != 1:
            raise TaskStoreCorruption(
                "worker-exit retry source outcome cannot be reconstructed"
            )
        outcome = _decode_hashed_document(
            rows[0], label="worker-exit retry source outcome"
        )
        handle = outcome.get("handle") if isinstance(outcome, dict) else None
        try:
            _, handle_hash = encode_document(handle)
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "worker-exit retry source handle is invalid"
            ) from error
        if (
            outcome.get("status") != "dispatched"
            or not isinstance(handle, dict)
            or handle_hash != reservation["source_handle_hash"]
        ):
            raise TaskStoreCorruption(
                "worker-exit retry source handle changed"
            )
        return copy.deepcopy(handle)

    def authorize_supervised_worker_exit_retry(
        self,
        *,
        intent_id: str,
        previous_attempt_id: str,
        previous_observation_sequence: int,
        failure_kind: str,
        private_schema_version: str,
        private_proof_hash: str,
        retry_event: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedRetryAuthorization:
        """Atomically retire attempt 1 and reserve the only B2 replacement."""

        if (
            not isinstance(intent_id, str)
            or not intent_id
            or WORKER_ATTEMPT_ID.fullmatch(str(previous_attempt_id)) is None
            or type(previous_observation_sequence) is not int
            or previous_observation_sequence < 1
            or failure_kind != "worker_exit"
            or private_schema_version not in {"1.1.0", "1.2.0"}
            or not _is_sha256(private_proof_hash)
            or not isinstance(retry_event, Mapping)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("supervised Worker-exit retry is invalid")
        event = copy.deepcopy(dict(retry_event))
        event_json, event_hash = encode_document(event)

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            authorized_at, authorized_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("retry target task cannot be read")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=authorized_at_us,
                action="supervised Worker-exit retry",
            )

            existing = connection.execute(
                """
                SELECT * FROM worker_exit_retry_reservations
                WHERE intent_id = ? AND attempt_number = 2
                """,
                (intent_id,),
            ).fetchone()
            if existing is not None:
                if (
                    existing["previous_attempt_id"] != previous_attempt_id
                    or existing["previous_observation_sequence"]
                    != previous_observation_sequence
                    or existing["failure_kind"] != failure_kind
                    or existing["private_schema_version"]
                    != private_schema_version
                    or existing["private_proof_hash"] != private_proof_hash
                    or existing["retry_event_hash"] != event_hash
                ):
                    raise TaskStoreConflict(
                        "Worker-exit retry reservation conflicts with durable state"
                    )
                source_handle = self._load_worker_exit_retry_source_handle(
                    connection, existing
                )
                delivery = connection.execute(
                    """
                    INSERT OR IGNORE INTO supervised_worker_exit_retry_attempts(
                        intent_id, attempt_number, project_id, principal_id,
                        fencing_token, authorized_at, authorized_at_us
                    ) VALUES (?, 2, ?, ?, ?, ?, ?)
                    """,
                    (
                        intent_id,
                        task["project_id"],
                        task["principal_id"],
                        supervisor_lease.fencing_token,
                        authorized_at,
                        authorized_at_us,
                    ),
                )
                intent = self._load_dispatch_intent(
                    connection, intent_id=intent_id
                )
                if intent is None or intent.state != "retrying":
                    raise TaskStoreConflict(
                        "Worker-exit retry is no longer awaiting delivery"
                    )
                connection.commit()
                return SupervisedRetryAuthorization(
                    intent=intent,
                    attempt_number=2,
                    previous_attempt_id=previous_attempt_id,
                    previous_observation_sequence=previous_observation_sequence,
                    evidence_hash=existing["evidence_hash"],
                    private_proof_hash=private_proof_hash,
                    failure_kind="worker_exit",
                    fencing_token=supervisor_lease.fencing_token,
                    reserved_at=existing["reserved_at"],
                    authorized_at=authorized_at,
                    reservation_replayed=True,
                    authorization_replayed=delivery.rowcount == 0,
                    private_schema_version=private_schema_version,
                    source_outcome_document_hash=existing[
                        "source_outcome_document_hash"
                    ],
                    source_handle_hash=existing["source_handle_hash"],
                    source_handle=source_handle,
                    retry_event_sequence=existing["retry_event_sequence"],
                    retry_event_hash=existing["retry_event_hash"],
                )

            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            if (
                intent is None
                or intent.state != "dispatched"
                or intent.handle is None
                or task["status"] not in {"Queued", "Running"}
                or intent.adapter_id != "fwi.deepwave_adapter"
                or intent.adapter_version not in {"1.5.0", "1.6.0"}
                or intent.request.get("algorithm")
                != {
                    "id": "deepwave.acoustic_fwi",
                    "version": intent.adapter_version,
                }
            ):
                raise TaskStoreConflict(
                    "retry requires a current post-ready managed dispatch"
                )
            budget = connection.execute(
                """
                SELECT * FROM approval_retry_budgets
                WHERE task_id = ? AND approval_id = ?
                """,
                (task_id, intent.approval_id),
            ).fetchone()
            if (
                budget is None
                or int(budget["max_attempts"]) != 2
                or int(budget["max_concurrent_attempts"]) != 1
                or budget["retryable_failure_classes_json"]
                != '["pre_running_launch_failure","worker_exit"]'
            ):
                raise TaskStoreConflict("approval does not authorize finite retry")
            attempt = connection.execute(
                """
                SELECT * FROM worker_launch_attempts
                WHERE intent_id = ? ORDER BY attempt_number DESC LIMIT 1
                """,
                (intent_id,),
            ).fetchone()
            observation = connection.execute(
                """
                SELECT * FROM worker_attempt_observations
                WHERE attempt_id = ? AND observation_sequence = ?
                """,
                (previous_attempt_id, previous_observation_sequence),
            ).fetchone()
            latest_sequence = connection.execute(
                """
                SELECT MAX(observation_sequence)
                FROM worker_attempt_observations WHERE attempt_id = ?
                """,
                (previous_attempt_id,),
            ).fetchone()[0]
            if (
                attempt is None
                or attempt["attempt_id"] != previous_attempt_id
                or int(attempt["attempt_number"]) != 1
                or observation is None
                or observation["ticket_state"] != "spawned"
                or observation["ticket_worker_pid"] is None
                or observation["ready_record_hash"] is None
                or observation["heartbeat_state"] != "running"
                or observation["heartbeat_record_hash"] is None
                or int(latest_sequence) != previous_observation_sequence
            ):
                raise TaskStoreConflict(
                    "retry requires exact post-ready attempt 1 evidence"
                )
            for table in (
                "task_cancel_requests",
                "task_purge_requests",
                "task_timeout_outcomes",
            ):
                if connection.execute(
                    f"SELECT 1 FROM {table} WHERE task_id = ?", (task_id,)
                ).fetchone() is not None:
                    raise TaskStoreConflict(
                        "runtime control already owns the retry target"
                    )
            elapsed_or_authorized_timeout = connection.execute(
                """
                SELECT 1
                FROM worker_attempt_timeout_windows AS timeout
                LEFT JOIN supervised_timeout_attempts AS delivery
                  ON delivery.timeout_id = timeout.timeout_id
                WHERE timeout.attempt_id = ?
                  AND (delivery.timeout_id IS NOT NULL
                       OR timeout.deadline_at_us <= ?)
                LIMIT 1
                """,
                (previous_attempt_id, authorized_at_us),
            ).fetchone()
            if elapsed_or_authorized_timeout is not None:
                raise TaskStoreConflict("timeout owns the exact attempt stop race")

            source = connection.execute(
                """
                SELECT outcome_document_json, outcome_document_hash
                FROM effective_dispatched_intents WHERE intent_id = ?
                """,
                (intent_id,),
            ).fetchone()
            if source is None:
                raise TaskStoreConflict("effective dispatch receipt is unavailable")
            source_outcome = _decode_hashed_document(
                {
                    "document_json": source["outcome_document_json"],
                    "document_hash": source["outcome_document_hash"],
                },
                label="worker-exit retry source outcome",
            )
            source_handle = source_outcome.get("handle")
            try:
                _, source_handle_hash = encode_document(source_handle)
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "effective dispatch handle is invalid"
                ) from error
            if source_handle != intent.handle:
                raise TaskStoreConflict("effective dispatch handle changed")

            next_sequence = int(
                connection.execute(
                    "SELECT COALESCE(MAX(sequence), 0) + 1 FROM run_events "
                    "WHERE task_id = ?",
                    (task_id,),
                ).fetchone()[0]
            )
            extension = event.get("extensions", {}).get(
                "org.agent_rpc.worker_exit_retry"
            )
            expected_extension = {
                "intent_id": intent_id,
                "attempt_number": 2,
                "previous_attempt_id": previous_attempt_id,
                "previous_observation_sequence": previous_observation_sequence,
                "evidence_hash": observation["document_hash"],
                "private_schema_version": private_schema_version,
                "private_proof_hash": private_proof_hash,
                "failure_kind": "worker_exit",
                "max_attempts": 2,
                "source_outcome_document_hash": source[
                    "outcome_document_hash"
                ],
                "source_handle_hash": source_handle_hash,
            }
            if (
                event.get("task_id") != task_id
                or event.get("sequence") != next_sequence
                or event.get("event_type") != "node_retrying"
                or event.get("task_status") != "Retrying"
                or event.get("node_id") != intent.node_id
                or event.get("fingerprint") != intent.queue_fingerprint
                or extension != expected_extension
            ):
                raise TaskStoreConflict("Worker-exit retry event is invalid")
            _, fingerprint_hash = encode_document(event["fingerprint"])
            connection.execute(
                """
                INSERT INTO run_events(
                    task_id, sequence, event_id, event_type, task_status,
                    node_id, fingerprint_hash, document_json, document_hash,
                    occurred_at, recorded_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    next_sequence,
                    event["event_id"],
                    "node_retrying",
                    "Retrying",
                    intent.node_id,
                    fingerprint_hash,
                    event_json,
                    event_hash,
                    event["occurred_at"],
                    authorized_at,
                ),
            )
            connection.execute(
                """
                INSERT INTO supervised_run_event_commits(
                    task_id, sequence, project_id, principal_id,
                    fencing_token, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    next_sequence,
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    authorized_at,
                    authorized_at_us,
                ),
            )
            connection.execute(
                "UPDATE tasks SET status = 'Retrying', updated_at = ? "
                "WHERE task_id = ?",
                (authorized_at, task_id),
            )
            connection.execute(
                """
                INSERT INTO worker_exit_retry_reservations(
                    intent_id, attempt_number, task_id, project_id,
                    principal_id, approval_id, previous_attempt_id,
                    previous_observation_sequence, evidence_hash,
                    private_schema_version, private_proof_hash, failure_kind,
                    source_outcome_document_hash, source_handle_hash,
                    retry_event_sequence, retry_event_hash,
                    first_fencing_token, reserved_at, reserved_at_us
                ) VALUES (?, 2, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'worker_exit',
                          ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    intent_id,
                    task_id,
                    task["project_id"],
                    task["principal_id"],
                    intent.approval_id,
                    previous_attempt_id,
                    previous_observation_sequence,
                    observation["document_hash"],
                    private_schema_version,
                    private_proof_hash,
                    source["outcome_document_hash"],
                    source_handle_hash,
                    next_sequence,
                    event_hash,
                    supervisor_lease.fencing_token,
                    authorized_at,
                    authorized_at_us,
                ),
            )
            connection.execute(
                """
                INSERT INTO supervised_worker_exit_retry_attempts(
                    intent_id, attempt_number, project_id, principal_id,
                    fencing_token, authorized_at, authorized_at_us
                ) VALUES (?, 2, ?, ?, ?, ?, ?)
                """,
                (
                    intent_id,
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    authorized_at,
                    authorized_at_us,
                ),
            )
            stored_intent = self._load_dispatch_intent(
                connection, task_id=task_id
            )
            if stored_intent is None or stored_intent.state != "retrying":
                raise TaskStoreCorruption(
                    "Worker-exit retry reservation cannot be read"
                )
            connection.commit()
            return SupervisedRetryAuthorization(
                intent=stored_intent,
                attempt_number=2,
                previous_attempt_id=previous_attempt_id,
                previous_observation_sequence=previous_observation_sequence,
                evidence_hash=observation["document_hash"],
                private_proof_hash=private_proof_hash,
                failure_kind="worker_exit",
                fencing_token=supervisor_lease.fencing_token,
                reserved_at=authorized_at,
                authorized_at=authorized_at,
                reservation_replayed=False,
                authorization_replayed=False,
                private_schema_version=private_schema_version,
                source_outcome_document_hash=source["outcome_document_hash"],
                source_handle_hash=source_handle_hash,
                source_handle=copy.deepcopy(source_handle),
                retry_event_sequence=next_sequence,
                retry_event_hash=event_hash,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "Worker-exit retry lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "Worker-exit retry conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def resume_supervised_worker_exit_retry(
        self,
        *,
        intent_id: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedRetryAuthorization:
        """Redeliver the immutable B2 token after a Supervisor restart."""

        if (
            not isinstance(intent_id, str)
            or not intent_id
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("supervised Worker-exit retry is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            authorized_at, authorized_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            reservation = connection.execute(
                """
                SELECT * FROM worker_exit_retry_reservations
                WHERE intent_id = ? AND attempt_number = 2
                """,
                (intent_id,),
            ).fetchone()
            if reservation is None:
                raise TaskStoreConflict("Worker-exit retry reservation is absent")
            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (reservation["task_id"],),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("retry target task cannot be read")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=authorized_at_us,
                action="supervised Worker-exit retry",
            )
            intent = self._load_dispatch_intent(
                connection, task_id=reservation["task_id"]
            )
            if (
                intent is None
                or intent.state != "retrying"
                or task["status"] != "Retrying"
                or connection.execute(
                    "SELECT 1 FROM worker_exit_retry_exhaustions "
                    "WHERE intent_id = ?",
                    (intent_id,),
                ).fetchone()
                is not None
            ):
                raise TaskStoreConflict(
                    "Worker-exit retry is no longer awaiting delivery"
                )
            source_handle = self._load_worker_exit_retry_source_handle(
                connection, reservation
            )
            delivery = connection.execute(
                """
                INSERT OR IGNORE INTO supervised_worker_exit_retry_attempts(
                    intent_id, attempt_number, project_id, principal_id,
                    fencing_token, authorized_at, authorized_at_us
                ) VALUES (?, 2, ?, ?, ?, ?, ?)
                """,
                (
                    intent_id,
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    authorized_at,
                    authorized_at_us,
                ),
            )
            connection.commit()
            return SupervisedRetryAuthorization(
                intent=intent,
                attempt_number=2,
                previous_attempt_id=reservation["previous_attempt_id"],
                previous_observation_sequence=reservation[
                    "previous_observation_sequence"
                ],
                evidence_hash=reservation["evidence_hash"],
                private_proof_hash=reservation["private_proof_hash"],
                failure_kind="worker_exit",
                fencing_token=supervisor_lease.fencing_token,
                reserved_at=reservation["reserved_at"],
                authorized_at=authorized_at,
                reservation_replayed=True,
                authorization_replayed=delivery.rowcount == 0,
                private_schema_version=reservation["private_schema_version"],
                source_outcome_document_hash=reservation[
                    "source_outcome_document_hash"
                ],
                source_handle_hash=reservation["source_handle_hash"],
                source_handle=source_handle,
                retry_event_sequence=reservation["retry_event_sequence"],
                retry_event_hash=reservation["retry_event_hash"],
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "Worker-exit retry lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "Worker-exit retry delivery conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def authorize_supervised_dispatch_reconciliation(
        self,
        *,
        intent_id: str,
        evidence_kind: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedDispatchReconciliationAuthorization:
        """Authorize one positive-evidence inspection without dispatching."""

        if evidence_kind not in {
            "managed_worker_receipt",
            "private_receipt",
        }:
            raise TaskStoreConflict(
                "dispatch reconciliation evidence kind is invalid"
            )
        if not isinstance(supervisor_lease, RuntimeSupervisorLease):
            raise TaskStoreConflict("runtime supervisor lease is invalid")
        if not callable(supervisor_clock):
            raise TaskStoreConflict("runtime supervisor clock is invalid")

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            authorized_at, authorized_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            if intent is None:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            outcome = connection.execute(
                """
                SELECT outcome, document_hash FROM dispatch_outcomes
                WHERE intent_id = ?
                """,
                (intent_id,),
            ).fetchone()
            if task is None or outcome is None:
                raise TaskStoreCorruption(
                    "dispatch reconciliation case cannot be read"
                )
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=authorized_at_us,
                action="dispatch reconciliation",
            )
            self._require_current_dag_node_execution(
                connection,
                intent=intent,
                allowed_node_states=frozenset({"Queued"}),
            )
            if (
                task["status"] != "Queued"
                or outcome["outcome"] != "reconciliation_required"
                or not _is_supported_managed_dispatch(intent)
            ):
                raise TaskStoreConflict(
                    "dispatch intent is not eligible for reconciliation"
                )
            if connection.execute(
                "SELECT 1 FROM task_purge_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("purged task cannot be reconciled")
            if connection.execute(
                "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "task with a cancel request cannot be reconciled"
                )

            existing = connection.execute(
                """
                SELECT * FROM supervised_dispatch_reconciliation_attempts
                WHERE intent_id = ? AND fencing_token = ?
                  AND evidence_kind = ?
                """,
                (
                    intent_id,
                    supervisor_lease.fencing_token,
                    evidence_kind,
                ),
            ).fetchone()
            if existing is not None:
                try:
                    stored_at, stored_at_us = _runtime_timestamp(
                        existing["authorized_at"]
                    )
                except TaskStoreConflict as error:
                    raise TaskStoreCorruption(
                        "dispatch reconciliation authorization time is invalid"
                    ) from error
                if (
                    existing["project_id"] != task["project_id"]
                    or existing["principal_id"] != task["principal_id"]
                    or existing["source_outcome_hash"]
                    != outcome["document_hash"]
                    or stored_at != existing["authorized_at"]
                    or stored_at_us != existing["authorized_at_us"]
                ):
                    raise TaskStoreCorruption(
                        "dispatch reconciliation authorization is inconsistent"
                    )
                connection.commit()
                return SupervisedDispatchReconciliationAuthorization(
                    intent=intent,
                    evidence_kind=evidence_kind,
                    fencing_token=supervisor_lease.fencing_token,
                    authorized_at=stored_at,
                    replayed=True,
                )
            if (
                intent.state != "reconciliation_required"
                or intent.reconciliation is None
                or intent.reconciliation.state != "required"
            ):
                raise TaskStoreConflict(
                    "dispatch reconciliation was already resolved"
                )
            connection.execute(
                """
                INSERT INTO supervised_dispatch_reconciliation_attempts(
                    intent_id, project_id, principal_id, fencing_token,
                    evidence_kind, source_outcome_hash,
                    authorized_at, authorized_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    intent_id,
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    evidence_kind,
                    outcome["document_hash"],
                    authorized_at,
                    authorized_at_us,
                ),
            )
            connection.commit()
            return SupervisedDispatchReconciliationAuthorization(
                intent=intent,
                evidence_kind=evidence_kind,
                fencing_token=supervisor_lease.fencing_token,
                authorized_at=authorized_at,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "dispatch reconciliation lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "dispatch reconciliation authorization conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def record_supervised_dispatch_reconciliation_observation(
        self,
        *,
        intent_id: str,
        classification: str,
        failure_code: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedDispatchReconciliationObservation:
        """Persist one bounded unresolved classification without side effects."""

        if (
            not isinstance(intent_id, str)
            or not intent_id
            or classification not in {"transient", "uncertain"}
            or not isinstance(failure_code, str)
            or not 1 <= len(failure_code) <= 128
            or not failure_code.replace("_", "").isalnum()
            or failure_code.upper() != failure_code
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict(
                "dispatch reconciliation observation is invalid"
            )

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            observed_at, observed_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            task = connection.execute(
                "SELECT project_id, principal_id, status FROM tasks "
                "WHERE task_id = ?",
                (task_id,),
            ).fetchone()
            outcome = connection.execute(
                "SELECT outcome, document_hash FROM dispatch_outcomes "
                "WHERE intent_id = ?",
                (intent_id,),
            ).fetchone()
            if intent is None or task is None or outcome is None:
                raise TaskStoreCorruption(
                    "dispatch reconciliation case cannot be read"
                )
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=observed_at_us,
                action="dispatch reconciliation observation",
            )
            self._require_current_dag_node_execution(
                connection,
                intent=intent,
                allowed_node_states=frozenset({"Queued"}),
            )
            if (
                task["status"] != "Queued"
                or outcome["outcome"] != "reconciliation_required"
                or intent.state != "reconciliation_required"
                or intent.reconciliation is None
                or intent.reconciliation.state != "required"
            ):
                raise TaskStoreConflict(
                    "dispatch reconciliation observation requires an unresolved case"
                )
            if connection.execute(
                "SELECT 1 FROM task_purge_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("purged task cannot be reconciled")
            if connection.execute(
                "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "task with a cancel request cannot be reconciled"
                )

            latest = connection.execute(
                """
                SELECT * FROM dispatch_reconciliation_observations
                WHERE intent_id = ? ORDER BY observation_sequence DESC LIMIT 1
                """,
                (intent_id,),
            ).fetchone()
            if (
                latest is not None
                and latest["classification"] == classification
                and latest["failure_code"] == failure_code
            ):
                document = _decode_hashed_document(
                    latest, label="dispatch reconciliation observation"
                )
                expected_document = {
                    "schema_version": "1.0.0",
                    "intent_id": intent_id,
                    "task_id": task_id,
                    "source_outcome_hash": outcome["document_hash"],
                    "classification": classification,
                    "failure_code": failure_code,
                    "observed_at": latest["observed_at"],
                    "extensions": {},
                }
                try:
                    stored_at, stored_at_us = _runtime_timestamp(
                        latest["observed_at"]
                    )
                except TaskStoreConflict as error:
                    raise TaskStoreCorruption(
                        "dispatch reconciliation observation time is invalid"
                    ) from error
                if (
                    document != expected_document
                    or latest["task_id"] != task_id
                    or latest["project_id"] != task["project_id"]
                    or latest["principal_id"] != task["principal_id"]
                    or latest["source_outcome_hash"] != outcome["document_hash"]
                    or latest["evidence_kind"] is not None
                    or stored_at != latest["observed_at"]
                    or stored_at_us != latest["observed_at_us"]
                ):
                    raise TaskStoreCorruption(
                        "dispatch reconciliation observation is inconsistent"
                    )
                connection.commit()
                return SupervisedDispatchReconciliationObservation(
                    intent=intent,
                    classification=classification,
                    failure_code=failure_code,
                    observation_sequence=int(latest["observation_sequence"]),
                    fencing_token=int(latest["fencing_token"]),
                    observed_at=stored_at,
                    replayed=True,
                )

            sequence = 1 if latest is None else int(
                latest["observation_sequence"]
            ) + 1
            document = {
                "schema_version": "1.0.0",
                "intent_id": intent_id,
                "task_id": task_id,
                "source_outcome_hash": outcome["document_hash"],
                "classification": classification,
                "failure_code": failure_code,
                "observed_at": observed_at,
                "extensions": {},
            }
            document_json, document_hash = encode_document(document)
            connection.execute(
                """
                INSERT INTO dispatch_reconciliation_observations(
                    intent_id, observation_sequence, task_id, project_id,
                    principal_id, source_outcome_hash, classification,
                    failure_code, evidence_kind, attempt_id, attempt_number,
                    adapter_version, private_schema_version,
                    private_record_hash, private_proof_hash,
                    evidence_json, evidence_hash, fencing_token,
                    observed_at, observed_at_us, document_json, document_hash
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, NULL, NULL, NULL,
                          NULL, NULL, NULL, NULL, NULL, NULL, ?, ?, ?, ?, ?)
                """,
                (
                    intent_id,
                    sequence,
                    task_id,
                    task["project_id"],
                    task["principal_id"],
                    outcome["document_hash"],
                    classification,
                    failure_code,
                    supervisor_lease.fencing_token,
                    observed_at,
                    observed_at_us,
                    document_json,
                    document_hash,
                ),
            )
            stored_intent = self._load_dispatch_intent(
                connection, task_id=task_id
            )
            if (
                stored_intent is None
                or stored_intent.state != "reconciliation_required"
                or stored_intent.reconciliation is None
                or stored_intent.reconciliation.action_classification
                != classification
                or stored_intent.reconciliation.action_failure_code
                != failure_code
            ):
                raise TaskStoreCorruption(
                    "dispatch reconciliation observation cannot be read"
                )
            connection.commit()
            return SupervisedDispatchReconciliationObservation(
                intent=stored_intent,
                classification=classification,
                failure_code=failure_code,
                observation_sequence=sequence,
                fencing_token=supervisor_lease.fencing_token,
                observed_at=observed_at,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "dispatch reconciliation observation lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "dispatch reconciliation observation conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def finalize_supervised_negative_dispatch_reconciliation(
        self,
        *,
        intent_id: str,
        attempt_id: str,
        attempt_number: int,
        adapter_version: str,
        private_schema_version: str,
        private_record_hash: str,
        private_proof_hash: str,
        evidence: Mapping[str, Any],
        terminal_event: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedNegativeDispatchReconciliation:
        """Atomically fail a Task after exact proof that dispatch never started."""

        if (
            not isinstance(intent_id, str)
            or not intent_id
            or not isinstance(attempt_id, str)
            or WORKER_ATTEMPT_ID.fullmatch(attempt_id) is None
            or attempt_number != 1
            or (adapter_version, private_schema_version)
            not in {
                ("1.4.0", "1.1.0"),
                ("1.5.0", "1.2.0"),
                ("1.6.0", "1.2.0"),
            }
            or not _is_sha256(private_record_hash)
            or not _is_sha256(private_proof_hash)
            or not isinstance(terminal_event, Mapping)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict(
                "negative dispatch reconciliation proof is invalid"
            )
        normalized = _validated_worker_evidence(evidence)
        if (
            normalized["attempt_id"] != attempt_id
            or normalized["attempt_number"] != 1
            or normalized["ticket"]["state"]
            not in {"staged", "leased", "spawned", "failed"}
            or normalized["ready"] is not None
            or normalized["heartbeat"] is not None
        ):
            raise TaskStoreConflict(
                "negative reconciliation requires exact pre-running evidence"
            )
        evidence_json, evidence_hash = encode_document(normalized)
        proof_payload = {
            "schema_version": "1.0.0",
            "result": "not_dispatched",
            "evidence_kind": "managed_pre_running_failure",
            "adapter_version": adapter_version,
            "private_schema_version": private_schema_version,
            "private_record_hash": private_record_hash,
            "attempt_id": attempt_id,
            "attempt_number": attempt_number,
            "evidence_hash": evidence_hash,
        }
        if encode_document(proof_payload)[1] != private_proof_hash:
            raise TaskStoreConflict(
                "negative reconciliation private proof hash is invalid"
            )
        event = copy.deepcopy(dict(terminal_event))
        event_json, event_hash = encode_document(event)

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            resolved_at, resolved_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            task = connection.execute(
                "SELECT project_id, principal_id, status, current_approval_id "
                "FROM tasks WHERE task_id = ?",
                (task_id,),
            ).fetchone()
            outcome = connection.execute(
                "SELECT outcome, document_hash FROM dispatch_outcomes "
                "WHERE intent_id = ?",
                (intent_id,),
            ).fetchone()
            if intent is None or task is None or outcome is None:
                raise TaskStoreCorruption(
                    "negative reconciliation case cannot be read"
                )
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=resolved_at_us,
                action="negative dispatch reconciliation",
            )
            dag_admission = self._require_current_dag_node_execution(
                connection,
                intent=intent,
                allowed_node_states=frozenset({"Queued", "Failed"}),
            )
            dag_execution = dag_admission is not None
            dag_plan_document: dict[str, Any] | None = None
            projected_task_status = "Failed"
            if dag_execution:
                plan_row = connection.execute(
                    """
                    SELECT document_json, document_hash FROM plans
                    WHERE task_id = ? AND plan_id = ?
                    """,
                    (task_id, intent.plan_id),
                ).fetchone()
                if plan_row is None:
                    raise TaskStoreCorruption(
                        "negative reconciliation DAG plan cannot be read"
                    )
                dag_plan_document = _decode_hashed_document(
                    plan_row, label="negative reconciliation DAG plan"
                )
                state_map = self._load_dag_node_state_map(
                    connection, task_id=task_id, plan=dag_plan_document
                )
                if state_map is None:
                    raise TaskStoreCorruption(
                        "negative reconciliation DAG states cannot be read"
                    )
                prospective_states = {
                    node.node_id: node.state for node in state_map.nodes
                }
                prospective_states[intent.node_id] = "Failed"
                try:
                    prospective_readiness = evaluate_dag_readiness(
                        dag_plan_document, node_states=prospective_states
                    )
                except DagScheduleError as error:
                    raise TaskStoreCorruption(
                        "negative reconciliation aggregate cannot be evaluated"
                    ) from error
                projected_task_status = self._dag_aggregate_status(
                    prospective_readiness
                )

            existing = connection.execute(
                "SELECT * FROM dispatch_reconciliation_negative_resolutions "
                "WHERE intent_id = ?",
                (intent_id,),
            ).fetchone()
            if existing is not None:
                observation = connection.execute(
                    """
                    SELECT * FROM dispatch_reconciliation_observations
                    WHERE intent_id = ? AND observation_sequence = ?
                    """,
                    (intent_id, existing["observation_sequence"]),
                ).fetchone()
                stored_event = connection.execute(
                    "SELECT document_hash FROM run_events "
                    "WHERE task_id = ? AND sequence = ?",
                    (task_id, existing["terminal_event_sequence"]),
                ).fetchone()
                if (
                    observation is None
                    or stored_event is None
                    or observation["attempt_id"] != attempt_id
                    or observation["attempt_number"] != attempt_number
                    or observation["adapter_version"] != adapter_version
                    or observation["private_schema_version"]
                    != private_schema_version
                    or observation["private_record_hash"] != private_record_hash
                    or observation["private_proof_hash"] != private_proof_hash
                    or observation["evidence_hash"] != evidence_hash
                    or observation["evidence_json"] != evidence_json
                    or existing["terminal_event_hash"] != event_hash
                    or stored_event["document_hash"] != event_hash
                    or existing["approval_budget_refunded"] != 0
                    or task["status"] != projected_task_status
                ):
                    raise TaskStoreConflict(
                        "negative reconciliation replay differs from its outcome"
                    )
                stored_intent = self._load_dispatch_intent(
                    connection, intent_id=intent_id
                )
                snapshot = self._load_snapshot(connection, task_id)
                if (
                    stored_intent is None
                    or stored_intent.state != "not_dispatched"
                    or snapshot is None
                    or snapshot.status != projected_task_status
                ):
                    raise TaskStoreCorruption(
                        "negative reconciliation replay cannot be read"
                    )
                stored_at, stored_at_us = _runtime_timestamp(
                    existing["resolved_at"]
                )
                if stored_at_us != existing["resolved_at_us"]:
                    raise TaskStoreCorruption(
                        "negative reconciliation time is inconsistent"
                    )
                connection.commit()
                return SupervisedNegativeDispatchReconciliation(
                    snapshot=snapshot,
                    intent=stored_intent,
                    attempt_id=attempt_id,
                    attempt_number=attempt_number,
                    evidence_hash=evidence_hash,
                    private_record_hash=private_record_hash,
                    private_proof_hash=private_proof_hash,
                    terminal_event_sequence=int(
                        existing["terminal_event_sequence"]
                    ),
                    fencing_token=int(existing["fencing_token"]),
                    resolved_at=stored_at,
                    replayed=True,
                )

            budget = connection.execute(
                "SELECT max_tasks, tasks_used FROM approval_budgets "
                "WHERE task_id = ? AND approval_id = ?",
                (task_id, intent.approval_id),
            ).fetchone()
            if (
                (
                    task["status"] != "Queued"
                    if not dag_execution
                    else task["status"] not in {"Queued", "Running"}
                )
                or task["current_approval_id"] != intent.approval_id
                or outcome["outcome"] != "reconciliation_required"
                or intent.state != "reconciliation_required"
                or intent.reconciliation is None
                or intent.reconciliation.state != "required"
                or intent.adapter_id != "fwi.deepwave_adapter"
                or intent.adapter_version != adapter_version
                or intent.request.get("algorithm")
                != {"id": "deepwave.acoustic_fwi", "version": adapter_version}
                or budget is None
                or int(budget["tasks_used"]) < 1
                or int(budget["tasks_used"]) > int(budget["max_tasks"])
            ):
                raise TaskStoreConflict(
                    "negative reconciliation requires an unresolved managed dispatch"
                )
            if connection.execute(
                "SELECT 1 FROM dispatch_reconciliation_resolutions "
                "WHERE intent_id = ?",
                (intent_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "positive reconciliation already owns the outcome"
                )
            for table, message in (
                ("worker_retry_reservations", "retry owns the dispatch outcome"),
                (
                    "worker_exit_retry_reservations",
                    "Worker-exit retry owns the dispatch outcome",
                ),
            ):
                if connection.execute(
                    f"SELECT 1 FROM {table} WHERE intent_id = ?", (intent_id,)
                ).fetchone() is not None:
                    raise TaskStoreConflict(message)
            if connection.execute(
                "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "task cancellation owns the terminal transition"
                )
            if connection.execute(
                "SELECT 1 FROM task_purge_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "purged task cannot be negatively reconciled"
                )

            expected_sequence = connection.execute(
                "SELECT COALESCE(MAX(sequence), 0) + 1 FROM run_events "
                "WHERE task_id = ?",
                (task_id,),
            ).fetchone()[0]
            extension = {
                "intent_id": intent_id,
                "attempt_id": attempt_id,
                "attempt_number": attempt_number,
                "evidence_hash": evidence_hash,
                "adapter_version": adapter_version,
                "private_schema_version": private_schema_version,
                "private_record_hash": private_record_hash,
                "private_proof_hash": private_proof_hash,
                "result": "not_dispatched",
            }
            _, identity_hash = encode_document(
                {
                    "intent_id": intent_id,
                    "attempt_id": attempt_id,
                    "evidence_hash": evidence_hash,
                    "private_proof_hash": private_proof_hash,
                    "event_type": "node_failed",
                    "sequence": event.get("sequence"),
                }
            )
            expected_fields = {
                "schema_version",
                "event_id",
                "sequence",
                "task_id",
                "node_id",
                "event_type",
                "task_status",
                "error",
                "occurred_at",
                "fingerprint",
                "extensions",
            }
            if (
                set(event) != expected_fields
                or event.get("schema_version") != "1.0.0"
                or event.get("event_id")
                != "event-" + identity_hash.removeprefix("sha256:")[:32]
                or event.get("sequence") != expected_sequence
                or event.get("task_id") != task_id
                or event.get("node_id") != intent.node_id
                or event.get("event_type") != "node_failed"
                or event.get("task_status") != projected_task_status
                or event.get("error")
                != {
                    "code": "dispatch_not_started",
                    "message": "FWI Worker did not reach its running boundary",
                    "retryable": False,
                }
                or event.get("occurred_at") != normalized["ticket"]["updated_at"]
                or event.get("fingerprint") != intent.queue_fingerprint
                or event.get("extensions")
                != {"org.agent_rpc.dispatch_reconciliation": extension}
            ):
                raise TaskStoreConflict(
                    "negative reconciliation terminal event is invalid"
                )
            _, occurred_at_us = _runtime_timestamp(event["occurred_at"])
            if occurred_at_us > resolved_at_us:
                raise TaskStoreConflict(
                    "negative reconciliation terminal event violates causal order"
                )

            latest_observation = connection.execute(
                "SELECT COALESCE(MAX(observation_sequence), 0) FROM "
                "dispatch_reconciliation_observations WHERE intent_id = ?",
                (intent_id,),
            ).fetchone()[0]
            observation_sequence = int(latest_observation) + 1
            observation_document = {
                "schema_version": "1.0.0",
                "intent_id": intent_id,
                "task_id": task_id,
                "source_outcome_hash": outcome["document_hash"],
                "classification": "exact_negative",
                "failure_code": "DISPATCH_NOT_STARTED",
                "evidence": proof_payload,
                "observed_at": resolved_at,
                "extensions": {},
            }
            observation_json, observation_hash = encode_document(
                observation_document
            )
            connection.execute(
                """
                INSERT INTO dispatch_reconciliation_observations(
                    intent_id, observation_sequence, task_id, project_id,
                    principal_id, source_outcome_hash, classification,
                    failure_code, evidence_kind, attempt_id, attempt_number,
                    adapter_version, private_schema_version,
                    private_record_hash, private_proof_hash,
                    evidence_json, evidence_hash, fencing_token,
                    observed_at, observed_at_us, document_json, document_hash
                ) VALUES (?, ?, ?, ?, ?, ?, 'exact_negative',
                          'DISPATCH_NOT_STARTED',
                          'managed_pre_running_failure', ?, 1, ?, ?, ?, ?,
                          ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    intent_id,
                    observation_sequence,
                    task_id,
                    task["project_id"],
                    task["principal_id"],
                    outcome["document_hash"],
                    attempt_id,
                    adapter_version,
                    private_schema_version,
                    private_record_hash,
                    private_proof_hash,
                    evidence_json,
                    evidence_hash,
                    supervisor_lease.fencing_token,
                    resolved_at,
                    resolved_at_us,
                    observation_json,
                    observation_hash,
                ),
            )
            _, fingerprint_hash = encode_document(event["fingerprint"])
            connection.execute(
                """
                INSERT INTO run_events(
                    task_id, sequence, event_id, event_type, task_status,
                    node_id, fingerprint_hash, document_json, document_hash,
                    occurred_at, recorded_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    event["sequence"],
                    event["event_id"],
                    event["event_type"],
                    event["task_status"],
                    event["node_id"],
                    fingerprint_hash,
                    event_json,
                    event_hash,
                    event["occurred_at"],
                    resolved_at,
                ),
            )
            connection.execute(
                """
                INSERT INTO supervised_run_event_commits(
                    task_id, sequence, project_id, principal_id,
                    fencing_token, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    event["sequence"],
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    resolved_at,
                    resolved_at_us,
                ),
            )
            if dag_execution:
                assert dag_admission is not None
                node_revision = int(dag_admission["queued_revision"]) + 1
                adapter_status_json, adapter_status_hash = encode_document(
                    proof_payload
                )
                connection.execute(
                    """
                    INSERT INTO dag_node_terminal_facts(
                        intent_id, task_id, plan_id, plan_hash, approval_id,
                        node_id, input_binding_node_revision,
                        input_binding_document_hash, input_fencing_token,
                        input_owner_id, input_term_acquired_at,
                        node_revision, node_state, event_sequence, event_hash,
                        attempt_id, attempt_number, worker_observation_sequence,
                        worker_observation_hash, dispatch_handle_json,
                        dispatch_handle_hash,
                        dispatch_outcome_document_hash,
                        adapter_status_json, adapter_status_hash,
                        project_id, principal_id, completion_fencing_token,
                        completion_owner_id, completion_term_acquired_at,
                        receipt_document_json, receipt_document_hash,
                        recorded_at, recorded_at_us
                    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'Failed',
                              ?, ?, NULL, NULL, NULL, NULL, NULL, NULL, ?, ?, ?, ?, ?, ?,
                              ?, ?, NULL, NULL, ?, ?)
                    """,
                    (
                        intent_id,
                        task_id,
                        intent.plan_id,
                        intent.plan_hash,
                        intent.approval_id,
                        intent.node_id,
                        dag_admission["pending_revision"],
                        dag_admission["input_binding_document_hash"],
                        dag_admission["input_fencing_token"],
                        dag_admission["input_owner_id"],
                        dag_admission["input_term_acquired_at"],
                        node_revision,
                        event["sequence"],
                        event_hash,
                        outcome["document_hash"],
                        adapter_status_json,
                        adapter_status_hash,
                        task["project_id"],
                        task["principal_id"],
                        supervisor_lease.fencing_token,
                        supervisor_lease.owner_id,
                        supervisor_lease.acquired_at,
                        resolved_at,
                        resolved_at_us,
                    ),
                )
                connection.execute(
                    """
                    INSERT INTO dag_node_execution_transition_facts(
                        intent_id, node_revision, previous_state, state,
                        event_sequence, event_hash, reason,
                        project_id, principal_id, fencing_token,
                        owner_id, term_acquired_at, recorded_at, recorded_at_us
                    ) VALUES (?, ?, 'Queued', 'Failed', ?, ?,
                              'dispatch_not_started', ?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        intent_id,
                        node_revision,
                        event["sequence"],
                        event_hash,
                        task["project_id"],
                        task["principal_id"],
                        supervisor_lease.fencing_token,
                        supervisor_lease.owner_id,
                        supervisor_lease.acquired_at,
                        resolved_at,
                        resolved_at_us,
                    ),
                )
                connection.execute(
                    """
                    INSERT INTO dag_node_state_events(
                        task_id, plan_id, plan_hash, node_id, revision,
                        previous_state, state, recorded_at, recorded_at_us
                    ) VALUES (?, ?, ?, ?, ?, 'Queued', 'Failed', ?, ?)
                    """,
                    (
                        task_id,
                        intent.plan_id,
                        intent.plan_hash,
                        intent.node_id,
                        node_revision,
                        resolved_at,
                        resolved_at_us,
                    ),
                )
                assert dag_plan_document is not None
                self._materialize_dag_blocked_nodes(
                    connection,
                    task_id=task_id,
                    plan=dag_plan_document,
                    approval_id=intent.approval_id,
                    project_id=task["project_id"],
                    principal_id=task["principal_id"],
                    supervisor_lease=supervisor_lease,
                    recorded_at=resolved_at,
                    recorded_at_us=resolved_at_us,
                )
                reconciled_states = self._load_dag_node_state_map(
                    connection, task_id=task_id, plan=dag_plan_document
                )
                if reconciled_states is None:
                    raise TaskStoreCorruption(
                        "negative reconciliation DAG result cannot be read"
                    )
                reconciled_readiness = evaluate_dag_readiness(
                    dag_plan_document,
                    node_states={
                        node.node_id: node.state
                        for node in reconciled_states.nodes
                    },
                )
                if (
                    self._dag_aggregate_status(reconciled_readiness)
                    != projected_task_status
                ):
                    raise TaskStoreCorruption(
                        "negative reconciliation blocker aggregate changed"
                    )
            resolution_document = {
                "schema_version": "1.0.0",
                "intent_id": intent_id,
                "task_id": task_id,
                "source_outcome_hash": outcome["document_hash"],
                "result": "not_dispatched",
                "terminal_status": projected_task_status,
                "failure_code": "DISPATCH_NOT_STARTED",
                "evidence": {
                    **proof_payload,
                    "observation_sequence": observation_sequence,
                },
                "terminal_event_sequence": event["sequence"],
                "approval_budget": {
                    "tasks_used": int(budget["tasks_used"]),
                    "refunded": False,
                },
                "resolved_at": resolved_at,
                "extensions": {},
            }
            resolution_json, resolution_hash = encode_document(
                resolution_document
            )
            connection.execute(
                """
                INSERT INTO dispatch_reconciliation_negative_resolutions(
                    intent_id, task_id, project_id, principal_id, approval_id,
                    source_outcome_hash, result, evidence_kind,
                    observation_sequence, terminal_event_sequence,
                    terminal_event_hash, approval_tasks_used,
                    approval_budget_refunded, document_json, document_hash,
                    fencing_token, resolved_at, resolved_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, 'not_dispatched',
                          'managed_pre_running_failure', ?, ?, ?, ?, 0,
                          ?, ?, ?, ?, ?)
                """,
                (
                    intent_id,
                    task_id,
                    task["project_id"],
                    task["principal_id"],
                    intent.approval_id,
                    outcome["document_hash"],
                    observation_sequence,
                    event["sequence"],
                    event_hash,
                    int(budget["tasks_used"]),
                    resolution_json,
                    resolution_hash,
                    supervisor_lease.fencing_token,
                    resolved_at,
                    resolved_at_us,
                ),
            )
            connection.execute(
                "UPDATE tasks SET status = ?, updated_at = ? WHERE task_id = ?",
                (projected_task_status, resolved_at, task_id),
            )
            heartbeat = connection.execute(
                """
                UPDATE runtime_supervisor_leases
                SET heartbeat_at = ?, heartbeat_at_us = ?
                WHERE project_id = ? AND principal_id = ?
                  AND fencing_token = ? AND owner_id = ?
                  AND acquired_at = ?
                """,
                (
                    resolved_at,
                    resolved_at_us,
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    supervisor_lease.owner_id,
                    supervisor_lease.acquired_at,
                ),
            )
            if heartbeat.rowcount != 1:
                raise RuntimeSupervisorLeaseLost(
                    "negative reconciliation lost its runtime supervisor lease"
                )
            snapshot = self._load_snapshot(connection, task_id)
            closed_intent = self._load_dispatch_intent(
                connection, intent_id=intent_id
            )
            if (
                snapshot is None
                or snapshot.status != projected_task_status
                or closed_intent is None
                or closed_intent.state != "not_dispatched"
            ):
                raise TaskStoreCorruption(
                    "negative reconciliation outcome cannot be read"
                )
            connection.commit()
            return SupervisedNegativeDispatchReconciliation(
                snapshot=snapshot,
                intent=closed_intent,
                attempt_id=attempt_id,
                attempt_number=attempt_number,
                evidence_hash=evidence_hash,
                private_record_hash=private_record_hash,
                private_proof_hash=private_proof_hash,
                terminal_event_sequence=event["sequence"],
                fencing_token=supervisor_lease.fencing_token,
                resolved_at=resolved_at,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "negative reconciliation lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "negative reconciliation conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def authorize_supervised_timeout(
        self,
        *,
        timeout_id: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedTimeoutAuthorization:
        """Authorize the active term only once the durable deadline is due."""

        if (
            not isinstance(timeout_id, str)
            or RUNTIME_TIMEOUT_ID.fullmatch(timeout_id) is None
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("supervised timeout authorization is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            authorized_at, authorized_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            row = connection.execute(
                """
                SELECT timeout_id, task_id, project_id, principal_id,
                       intent_id, attempt_id, deadline_at_us
                FROM worker_attempt_timeout_windows
                WHERE timeout_id = ?
                """,
                (timeout_id,),
            ).fetchone()
            if row is None:
                raise TaskStoreConflict("Worker timeout window does not exist")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=row["project_id"],
                principal_id=row["principal_id"],
                supplied=supervisor_lease,
                now_us=authorized_at_us,
                action="supervised timeout",
            )
            snapshot = self._load_snapshot(connection, row["task_id"])
            if snapshot is None or snapshot.timeout is None:
                raise TaskStoreCorruption("Worker timeout window cannot be read")
            timeout = snapshot.timeout
            if timeout.timeout_id != timeout_id:
                raise TaskStoreCorruption("Worker timeout identity changed")
            if timeout.state == "suppressed":
                raise TaskStoreConflict(
                    "task cancellation already owns the exact stop slot"
                )
            if timeout.state in {"timed_out", "superseded", "not_triggered"}:
                connection.commit()
                return SupervisedTimeoutAuthorization(
                    timeout=timeout,
                    authorized=False,
                    fencing_token=supervisor_lease.fencing_token,
                    authorized_at=None,
                    replayed=True,
                )
            existing = connection.execute(
                """
                SELECT authorized_at, authorized_at_us
                FROM supervised_timeout_attempts
                WHERE timeout_id = ? AND fencing_token = ?
                """,
                (timeout_id, supervisor_lease.fencing_token),
            ).fetchone()
            if existing is not None:
                try:
                    stored_at, stored_us = _runtime_timestamp(
                        existing["authorized_at"]
                    )
                except TaskStoreConflict as error:
                    raise TaskStoreCorruption(
                        "supervised timeout authorization time is invalid"
                    ) from error
                if (
                    stored_at != existing["authorized_at"]
                    or stored_us != existing["authorized_at_us"]
                ):
                    raise TaskStoreCorruption(
                        "supervised timeout authorization time is inconsistent"
                    )
                connection.commit()
                return SupervisedTimeoutAuthorization(
                    timeout=timeout,
                    authorized=True,
                    fencing_token=supervisor_lease.fencing_token,
                    authorized_at=stored_at,
                    replayed=True,
                )
            if authorized_at_us < row["deadline_at_us"]:
                connection.commit()
                return SupervisedTimeoutAuthorization(
                    timeout=timeout,
                    authorized=False,
                    fencing_token=supervisor_lease.fencing_token,
                    authorized_at=None,
                    replayed=False,
                )
            connection.execute(
                """
                INSERT INTO supervised_timeout_attempts(
                    timeout_id, project_id, principal_id, intent_id,
                    attempt_id, fencing_token, action,
                    authorized_at, authorized_at_us
                ) VALUES (?, ?, ?, ?, ?, ?,
                          'deliver_exact_attempt_timeout', ?, ?)
                """,
                (
                    timeout_id,
                    row["project_id"],
                    row["principal_id"],
                    row["intent_id"],
                    row["attempt_id"],
                    supervisor_lease.fencing_token,
                    authorized_at,
                    authorized_at_us,
                ),
            )
            snapshot = self._load_snapshot(connection, row["task_id"])
            if snapshot is None or snapshot.timeout is None:
                raise TaskStoreCorruption(
                    "authorized Worker timeout cannot be read"
                )
            connection.commit()
            return SupervisedTimeoutAuthorization(
                timeout=snapshot.timeout,
                authorized=True,
                fencing_token=supervisor_lease.fencing_token,
                authorized_at=authorized_at,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "supervised timeout lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "supervised timeout authorization conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def complete_supervised_timeout(
        self,
        *,
        timeout_id: str,
        result: str,
        terminal_event: Mapping[str, Any] | None,
        adapter_proof: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> TaskTimeoutRecord:
        """Commit exact timeout failure or its natural terminal winner."""

        if (
            not isinstance(timeout_id, str)
            or RUNTIME_TIMEOUT_ID.fullmatch(timeout_id) is None
            or result not in {"timeout_confirmed", "terminal_preempted"}
            or not isinstance(adapter_proof, Mapping)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("task timeout completion is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            resolved_at, resolved_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            row = connection.execute(
                """
                SELECT timeout.*, attempt.binding_hash
                FROM worker_attempt_timeout_windows AS timeout
                JOIN worker_launch_attempts AS attempt
                  ON attempt.attempt_id = timeout.attempt_id
                WHERE timeout.timeout_id = ?
                """,
                (timeout_id,),
            ).fetchone()
            if row is None:
                raise TaskStoreConflict("Worker timeout window does not exist")
            timeout = TaskTimeoutSnapshot(
                timeout_id=row["timeout_id"],
                task_id=row["task_id"],
                intent_id=row["intent_id"],
                attempt_id=row["attempt_id"],
                wall_time_seconds=row["wall_time_seconds"],
                started_at=row["started_at"],
                deadline_at=row["deadline_at"],
                state="requested",
            )
            proof = _validate_timeout_adapter_proof(
                adapter_proof,
                timeout=timeout,
                capability_record_hash=row["capability_record_hash"],
                ready_record_hash=row["ready_record_hash"],
                result=result,
            )
            proof_json, proof_hash = encode_document(proof)
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=row["project_id"],
                principal_id=row["principal_id"],
                supplied=supervisor_lease,
                now_us=resolved_at_us,
                action="task timeout completion",
            )
            existing = connection.execute(
                """
                SELECT result, adapter_proof_hash
                FROM task_timeout_outcomes WHERE timeout_id = ?
                """,
                (timeout_id,),
            ).fetchone()
            if existing is not None:
                if (
                    existing["result"] != result
                    or existing["adapter_proof_hash"] != proof_hash
                ):
                    raise TaskStoreConflict(
                        "task timeout was completed with another outcome"
                    )
                snapshot = self._load_snapshot(connection, row["task_id"])
                if snapshot is None or snapshot.timeout is None:
                    raise TaskStoreCorruption("task timeout outcome cannot be read")
                connection.commit()
                return TaskTimeoutRecord(
                    snapshot=snapshot,
                    timeout=snapshot.timeout,
                    replayed=True,
                )
            authorization = connection.execute(
                """
                SELECT authorized_at, authorized_at_us
                FROM supervised_timeout_attempts
                WHERE timeout_id = ? AND fencing_token = ?
                """,
                (timeout_id, supervisor_lease.fencing_token),
            ).fetchone()
            if authorization is None:
                raise TaskStoreConflict(
                    "task timeout completion lacks current-term authorization"
                )
            try:
                authorized_at, authorized_at_us = _runtime_timestamp(
                    authorization["authorized_at"]
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "supervised timeout authorization time is invalid"
                ) from error
            if (
                authorized_at != authorization["authorized_at"]
                or authorized_at_us != authorization["authorized_at_us"]
                or authorized_at_us < row["deadline_at_us"]
            ):
                raise TaskStoreCorruption(
                    "supervised timeout authorization time is inconsistent"
                )
            if (
                resolved_at_us < row["deadline_at_us"]
                or resolved_at_us < authorized_at_us
            ):
                raise TaskStoreConflict(
                    "task timeout completion precedes its durable authorization"
                )
            if connection.execute(
                "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                (row["task_id"],),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "task cancellation owns the exact stop slot"
                )
            task = connection.execute(
                "SELECT status FROM tasks WHERE task_id = ?",
                (row["task_id"],),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("task timeout target cannot be read")
            intent = self._load_dispatch_intent(
                connection, task_id=row["task_id"]
            )
            if (
                intent is None
                or intent.intent_id != row["intent_id"]
                or intent.handle is None
            ):
                raise TaskStoreCorruption(
                    "task timeout lost its dispatch receipt"
                )

            event: dict[str, Any]
            if result == "timeout_confirmed":
                if task["status"] not in {"Queued", "Running", "Waiting"}:
                    raise TaskStoreConflict(
                        "timeout confirmation lost the terminal race"
                    )
                if terminal_event is None:
                    raise TaskStoreConflict(
                        "timeout confirmation requires its terminal event"
                    )
                event = dict(terminal_event)
                next_sequence = connection.execute(
                    """
                    SELECT COALESCE(MAX(sequence), 0) + 1
                    FROM run_events WHERE task_id = ?
                    """,
                    (row["task_id"],),
                ).fetchone()[0]
                expected_extension = {
                    "timeout_id": timeout_id,
                    "attempt_id": row["attempt_id"],
                    "wall_time_seconds": row["wall_time_seconds"],
                    "started_at": row["started_at"],
                    "deadline_at": row["deadline_at"],
                    "failure_code": "WALL_TIME_EXCEEDED",
                    "proof_hash": proof["proof_hash"],
                }
                error = event.get("error")
                try:
                    _, event_occurred_at_us = _runtime_timestamp(
                        event.get("occurred_at")
                    )
                except TaskStoreConflict as error:
                    raise TaskStoreConflict(
                        "task timeout terminal event is invalid"
                    ) from error
                if (
                    event.get("schema_version") != "1.0.0"
                    or not isinstance(event.get("event_id"), str)
                    or not event["event_id"]
                    or event.get("task_id") != row["task_id"]
                    or event.get("sequence") != next_sequence
                    or event.get("event_type") != "node_failed"
                    or event.get("task_status") != "Failed"
                    or event.get("node_id") != intent.node_id
                    or event.get("fingerprint")
                    != intent.handle["fingerprint"]
                    or not isinstance(error, Mapping)
                    or error.get("code") != "wall_time_exceeded"
                    or error.get("message")
                    != "FWI Worker exceeded its wall-time limit"
                    or error.get("retryable") is not False
                    or not isinstance(event.get("occurred_at"), str)
                    or not event["occurred_at"]
                    or event.get("extensions")
                    != {"org.agent_rpc.timeout": expected_extension}
                ):
                    raise TaskStoreConflict(
                        "task timeout terminal event is invalid"
                    )
                if (
                    event_occurred_at_us < row["deadline_at_us"]
                    or event_occurred_at_us < authorized_at_us
                    or event_occurred_at_us > resolved_at_us
                ):
                    raise TaskStoreConflict(
                        "task timeout terminal event violates its causal order"
                    )
                event_json, event_hash = encode_document(event)
                _, fingerprint_hash = encode_document(event["fingerprint"])
                connection.execute(
                    """
                    INSERT INTO run_events(
                        task_id, sequence, event_id, event_type, task_status,
                        node_id, fingerprint_hash, document_json,
                        document_hash, occurred_at, recorded_at
                    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        row["task_id"],
                        next_sequence,
                        event["event_id"],
                        event["event_type"],
                        event["task_status"],
                        event["node_id"],
                        fingerprint_hash,
                        event_json,
                        event_hash,
                        event["occurred_at"],
                        resolved_at,
                    ),
                )
                connection.execute(
                    "UPDATE tasks SET status = 'Failed', updated_at = ? "
                    "WHERE task_id = ?",
                    (resolved_at, row["task_id"]),
                )
                terminal_status = "Failed"
                failure_code = "WALL_TIME_EXCEEDED"
                terminal_sequence = next_sequence
                connection.execute(
                    """
                    INSERT INTO supervised_run_event_commits(
                        task_id, sequence, project_id, principal_id,
                        fencing_token, recorded_at, recorded_at_us
                    ) VALUES (?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        row["task_id"],
                        next_sequence,
                        row["project_id"],
                        row["principal_id"],
                        supervisor_lease.fencing_token,
                        resolved_at,
                        resolved_at_us,
                    ),
                )
            else:
                failure_code = None
                if terminal_event is None:
                    if task["status"] not in {"Succeeded", "Failed"}:
                        raise TaskStoreConflict(
                            "terminal-preempted timeout requires a natural terminal task"
                        )
                    terminal_status = task["status"]
                    terminal = connection.execute(
                        """
                        SELECT task_id, sequence, event_id, event_type,
                               task_status, node_id, fingerprint_hash,
                               occurred_at, document_json, document_hash
                        FROM run_events WHERE task_id = ?
                        ORDER BY sequence DESC LIMIT 1
                        """,
                        (row["task_id"],),
                    ).fetchone()
                    expected_type = (
                        "node_succeeded"
                        if terminal_status == "Succeeded"
                        else "node_failed"
                    )
                    if (
                        terminal is None
                        or terminal["event_type"] != expected_type
                        or terminal["task_status"] != terminal_status
                    ):
                        raise TaskStoreCorruption(
                            "natural timeout winner is inconsistent"
                        )
                    natural_event = _decode_run_event_row(
                        terminal, label="natural timeout winner event"
                    )
                    if (
                        natural_event.get("node_id") != intent.node_id
                        or natural_event.get("fingerprint")
                        != intent.handle["fingerprint"]
                    ):
                        raise TaskStoreCorruption(
                            "natural timeout winner lost its dispatch binding"
                        )
                    try:
                        _validate_timeout_natural_terminal_event(
                            natural_event,
                            receipt_job_id=intent.handle["job_id"],
                        )
                    except TaskStoreConflict as error:
                        raise TaskStoreCorruption(
                            "natural timeout winner is inconsistent"
                        ) from error
                    terminal_sequence = terminal["sequence"]
                else:
                    if task["status"] not in {"Queued", "Running", "Waiting"}:
                        raise TaskStoreConflict(
                            "terminal-preempted timeout lost the terminal race"
                        )
                    event = dict(terminal_event)
                    terminal_status = event.get("task_status")
                    expected_type = {
                        "Succeeded": "node_succeeded",
                        "Failed": "node_failed",
                    }.get(terminal_status)
                    next_sequence = connection.execute(
                        """
                        SELECT COALESCE(MAX(sequence), 0) + 1
                        FROM run_events WHERE task_id = ?
                        """,
                        (row["task_id"],),
                    ).fetchone()[0]
                    if (
                        event.get("schema_version") != "1.0.0"
                        or not isinstance(event.get("event_id"), str)
                        or not event["event_id"]
                        or expected_type is None
                        or event.get("task_id") != row["task_id"]
                        or event.get("sequence") != next_sequence
                        or event.get("event_type") != expected_type
                        or event.get("node_id") != intent.node_id
                        or event.get("fingerprint")
                        != intent.handle["fingerprint"]
                        or (
                            terminal_status
                            not in ALLOWED_TRANSITIONS.get(
                                task["status"], frozenset()
                            )
                            and not (
                                task["status"] == "Waiting"
                                and terminal_status == "Succeeded"
                            )
                        )
                        or not isinstance(event.get("occurred_at"), str)
                        or not event["occurred_at"]
                    ):
                        raise TaskStoreConflict(
                            "natural terminal timeout event is invalid"
                        )
                    _validate_timeout_natural_terminal_event(
                        event, receipt_job_id=intent.handle["job_id"]
                    )
                    event_json, event_hash = encode_document(event)
                    _, fingerprint_hash = encode_document(
                        event["fingerprint"]
                    )
                    connection.execute(
                        """
                        INSERT INTO run_events(
                            task_id, sequence, event_id, event_type,
                            task_status, node_id, fingerprint_hash,
                            document_json, document_hash, occurred_at,
                            recorded_at
                        ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                        """,
                        (
                            row["task_id"],
                            next_sequence,
                            event["event_id"],
                            event["event_type"],
                            terminal_status,
                            event["node_id"],
                            fingerprint_hash,
                            event_json,
                            event_hash,
                            event["occurred_at"],
                            resolved_at,
                        ),
                    )
                    connection.execute(
                        "UPDATE tasks SET status = ?, updated_at = ? "
                        "WHERE task_id = ?",
                        (terminal_status, resolved_at, row["task_id"]),
                    )
                    terminal_sequence = next_sequence
                    connection.execute(
                        """
                        INSERT INTO supervised_run_event_commits(
                            task_id, sequence, project_id, principal_id,
                            fencing_token, recorded_at, recorded_at_us
                        ) VALUES (?, ?, ?, ?, ?, ?, ?)
                        """,
                        (
                            row["task_id"],
                            next_sequence,
                            row["project_id"],
                            row["principal_id"],
                            supervisor_lease.fencing_token,
                            resolved_at,
                            resolved_at_us,
                        ),
                    )
            if proof["terminal_status"] != terminal_status:
                raise TaskStoreConflict(
                    "task timeout Adapter proof contradicts the terminal outcome"
                )
            outcome = {
                "schema_version": "1.0.0",
                "request_id": timeout_id,
                "task_id": row["task_id"],
                "result": result,
                "terminal_status": terminal_status,
                "failure_code": failure_code,
                "adapter_proof": proof,
                "resolved_at": resolved_at,
                "extensions": {},
            }
            outcome_json, outcome_hash = encode_document(outcome)
            connection.execute(
                """
                INSERT INTO task_timeout_outcomes(
                    timeout_id, task_id, project_id, principal_id,
                    intent_id, attempt_id, result, terminal_status,
                    failure_code, terminal_event_sequence,
                    adapter_proof_json, adapter_proof_hash,
                    document_json, document_hash, fencing_token,
                    resolved_at, resolved_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    timeout_id,
                    row["task_id"],
                    row["project_id"],
                    row["principal_id"],
                    row["intent_id"],
                    row["attempt_id"],
                    result,
                    terminal_status,
                    failure_code,
                    terminal_sequence,
                    proof_json,
                    proof_hash,
                    outcome_json,
                    outcome_hash,
                    supervisor_lease.fencing_token,
                    resolved_at,
                    resolved_at_us,
                ),
            )
            heartbeat = connection.execute(
                """
                UPDATE runtime_supervisor_leases
                SET heartbeat_at = ?, heartbeat_at_us = ?
                WHERE project_id = ? AND principal_id = ?
                  AND fencing_token = ? AND owner_id = ?
                  AND acquired_at = ?
                """,
                (
                    resolved_at,
                    resolved_at_us,
                    row["project_id"],
                    row["principal_id"],
                    supervisor_lease.fencing_token,
                    supervisor_lease.owner_id,
                    supervisor_lease.acquired_at,
                ),
            )
            if heartbeat.rowcount != 1:
                raise RuntimeSupervisorLeaseLost(
                    "task timeout completion lost its supervisor lease"
                )
            snapshot = self._load_snapshot(connection, row["task_id"])
            if snapshot is None or snapshot.timeout is None:
                raise TaskStoreCorruption("task timeout outcome cannot be read")
            connection.commit()
            return TaskTimeoutRecord(
                snapshot=snapshot,
                timeout=snapshot.timeout,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "task timeout completion lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "task timeout completion conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def lookup_task_cancellation(
        self,
        *,
        project_id: str,
        principal_id: str,
        idempotency_key: str,
        request_id: str,
        task_id: str,
        reason: str,
        request_hash: str,
    ) -> TaskCancellationRecord | None:
        """Look up only one exact scope/key cancellation replay."""

        connection = self._connect()
        try:
            row = connection.execute(
                """
                SELECT request_id, task_id, reason, request_hash
                FROM task_cancel_requests
                WHERE project_id = ? AND principal_id = ?
                  AND idempotency_key = ?
                """,
                (project_id, principal_id, idempotency_key),
            ).fetchone()
            if row is None:
                return None
            if (
                row["request_id"] != request_id
                or row["task_id"] != task_id
                or row["reason"] != reason
                or row["request_hash"] != request_hash
            ):
                raise IdempotencyConflict(
                    "idempotency key was already used for another cancel request"
                )
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None or snapshot.cancellation is None:
                raise TaskStoreCorruption("task cancel replay cannot be read")
            return TaskCancellationRecord(
                snapshot=snapshot,
                cancellation=snapshot.cancellation,
                replayed=True,
            )
        except sqlite3.OperationalError as error:
            _raise_operational_error(error)
        finally:
            connection.close()

    def can_request_task_cancellation(self, task_id: str) -> bool:
        """Return whether the durable view has one cancellable exact attempt."""

        return self.get_task_cancel_candidate(task_id) is not None

    def get_task_cancel_candidate(
        self, task_id: str
    ) -> dict[str, Any] | None:
        """Read the latest exact attempt eligible for cancel admission."""

        if not isinstance(task_id, str) or not task_id:
            return None
        connection = self._connect()
        try:
            row = connection.execute(
                """
                SELECT observation.*
                FROM tasks AS task
                JOIN dispatch_intents AS intent
                  ON intent.task_id = task.task_id
                JOIN effective_dispatched_intents AS dispatch
                  ON dispatch.intent_id = intent.intent_id
                JOIN worker_launch_attempts AS attempt
                  ON attempt.intent_id = intent.intent_id
                JOIN worker_attempt_observations AS observation
                  ON observation.attempt_id = attempt.attempt_id
                WHERE task.task_id = ?
                  AND task.status IN ('Queued', 'Running', 'Waiting')
                  AND intent.adapter_id = 'fwi.deepwave_adapter'
                  AND intent.adapter_version IN ('1.4.0', '1.5.0', '1.6.0')
                  AND json_extract(
                      intent.request_json, '$.request.algorithm.id'
                  ) = 'deepwave.acoustic_fwi'
                  AND json_extract(
                      intent.request_json, '$.request.algorithm.version'
                  ) = intent.adapter_version
                  AND observation.observation_sequence = (
                      SELECT MAX(latest.observation_sequence)
                      FROM worker_attempt_observations AS latest
                      WHERE latest.attempt_id = attempt.attempt_id
                  )
                  AND attempt.attempt_number = (
                      SELECT MAX(latest_attempt.attempt_number)
                      FROM worker_launch_attempts AS latest_attempt
                      WHERE latest_attempt.intent_id = intent.intent_id
                  )
                  AND observation.ticket_state = 'spawned'
                  AND observation.ready_record_hash IS NOT NULL
                  AND (
                      observation.heartbeat_state = 'running'
                      OR (
                          intent.adapter_version = '1.6.0'
                          AND observation.heartbeat_state = 'waiting'
                      )
                  )
                  AND observation.heartbeat_record_hash IS NOT NULL
                  AND NOT EXISTS (
                      SELECT 1 FROM task_cancel_requests AS cancel
                      WHERE cancel.task_id = task.task_id
                  )
                  AND NOT EXISTS (
                      SELECT 1 FROM task_purge_requests AS purge
                      WHERE purge.task_id = task.task_id
                  )
                  AND NOT EXISTS (
                      SELECT 1
                      FROM worker_attempt_timeout_windows AS timeout
                      JOIN supervised_timeout_attempts AS delivery
                        ON delivery.timeout_id = timeout.timeout_id
                      WHERE timeout.task_id = task.task_id
                  )
                LIMIT 1
                """,
                (task_id,),
            ).fetchone()
            return None if row is None else _decode_worker_observation_row(row)
        except sqlite3.OperationalError as error:
            _raise_operational_error(error)
        finally:
            connection.close()

    def request_task_cancellation(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        request_id: str,
        reason: str,
        idempotency_key: str,
        request_hash: str,
        build_documents: Callable[
            [TaskSnapshot, DispatchIntentSnapshot, Mapping[str, Any], int, str],
            tuple[Mapping[str, Any], Mapping[str, Any]],
        ],
        clock: Callable[[], str],
    ) -> TaskCancellationRecord:
        """Atomically admit one exact running-attempt cancellation request."""

        strings = (
            task_id,
            project_id,
            principal_id,
            request_id,
            idempotency_key,
            request_hash,
        )
        if (
            any(not isinstance(value, str) or not value for value in strings)
            or reason != "user_requested"
            or not callable(build_documents)
            or not callable(clock)
        ):
            raise TaskStoreConflict("task cancel request is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            existing = connection.execute(
                """
                SELECT request_id, task_id, reason, request_hash
                FROM task_cancel_requests
                WHERE project_id = ? AND principal_id = ?
                  AND idempotency_key = ?
                """,
                (project_id, principal_id, idempotency_key),
            ).fetchone()
            if existing is not None:
                if (
                    existing["request_id"] != request_id
                    or existing["task_id"] != task_id
                    or existing["reason"] != reason
                    or existing["request_hash"] != request_hash
                ):
                    raise IdempotencyConflict(
                        "idempotency key was already used for another cancel request"
                    )
                snapshot = self._load_snapshot(connection, task_id)
                if snapshot is None or snapshot.cancellation is None:
                    raise TaskStoreCorruption(
                        "task cancel replay cannot be read"
                    )
                connection.commit()
                return TaskCancellationRecord(
                    snapshot=snapshot,
                    cancellation=snapshot.cancellation,
                    replayed=True,
                )

            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreConflict("task does not exist")
            if (
                task["project_id"] != project_id
                or task["principal_id"] != principal_id
            ):
                raise TaskStoreConflict("task cancel crosses task scope")
            if task["status"] not in {"Queued", "Running", "Waiting"}:
                raise TaskStoreConflict(
                    "only a queued, running, or waiting task can be cancelled"
                )
            if connection.execute(
                "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("task already has another cancel request")
            if connection.execute(
                "SELECT 1 FROM task_purge_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("purged task cannot be cancelled")
            if connection.execute(
                """
                SELECT 1
                FROM worker_attempt_timeout_windows AS timeout
                JOIN supervised_timeout_attempts AS delivery
                  ON delivery.timeout_id = timeout.timeout_id
                WHERE timeout.task_id = ?
                LIMIT 1
                """,
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "authorized timeout already owns the exact stop slot"
                )
            intent = self._load_dispatch_intent(connection, task_id=task_id)
            if (
                intent is None
                or intent.state != "dispatched"
                or intent.handle is None
                or not _is_supported_managed_dispatch(intent)
            ):
                raise TaskStoreConflict(
                    "task cancellation requires a current dispatched receipt"
                )
            observation = connection.execute(
                """
                SELECT observation.*
                FROM worker_launch_attempts AS attempt
                JOIN worker_attempt_observations AS observation
                  ON observation.attempt_id = attempt.attempt_id
                WHERE attempt.intent_id = ?
                ORDER BY attempt.attempt_number DESC,
                         observation.observation_sequence DESC
                LIMIT 1
                """,
                (intent.intent_id,),
            ).fetchone()
            if observation is None:
                raise TaskStoreConflict(
                    "task cancellation requires exact Worker evidence"
                )
            evidence = _decode_worker_observation_row(observation)
            if (
                evidence["ticket"]["state"] != "spawned"
                or evidence["ready"] is None
                or evidence["heartbeat"] is None
                or (
                    evidence["heartbeat"]["state"] != "running"
                    and not (
                        evidence["heartbeat"]["state"] == "waiting"
                        and _is_checkpoint_capable_managed_dispatch(intent)
                    )
                )
            ):
                raise TaskStoreConflict(
                    "task cancellation requires an exact running attempt"
                )
            next_sequence = connection.execute(
                """
                SELECT COALESCE(MAX(sequence), 0) + 1
                FROM run_events WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()[0]
            requested_at, _ = _runtime_timestamp(clock())
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None:
                raise TaskStoreCorruption("task cancel target cannot be read")
            request_value, event_value = build_documents(
                snapshot, intent, evidence, next_sequence, requested_at
            )
            request = dict(request_value)
            event = dict(event_value)
            expected_request = {
                "schema_version": "1.0.0",
                "request_id": request_id,
                "task_id": task_id,
                "intent_id": intent.intent_id,
                "attempt_id": evidence["attempt_id"],
                "reason": reason,
                "actor": {"type": "user", "id": principal_id},
                "requested_at": requested_at,
                "extensions": {},
            }
            if request != expected_request:
                raise TaskStoreConflict("task cancel document is invalid")
            if (
                event.get("task_id") != task_id
                or event.get("sequence") != next_sequence
                or event.get("event_type") != "cancel_requested"
                or event.get("task_status") != task["status"]
                or event.get("node_id") != intent.node_id
                or event.get("occurred_at") != requested_at
                or event.get("fingerprint") != intent.handle["fingerprint"]
            ):
                raise TaskStoreConflict("task cancel event is invalid")
            request_json, request_document_hash = encode_document(request)
            event_json, event_hash = encode_document(event)
            _, fingerprint_hash = encode_document(event["fingerprint"])
            connection.execute(
                """
                INSERT INTO run_events(
                    task_id, sequence, event_id, event_type, task_status,
                    node_id, fingerprint_hash, document_json, document_hash,
                    occurred_at, recorded_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    next_sequence,
                    event["event_id"],
                    event["event_type"],
                    event["task_status"],
                    event["node_id"],
                    fingerprint_hash,
                    event_json,
                    event_hash,
                    event["occurred_at"],
                    requested_at,
                ),
            )
            connection.execute(
                """
                INSERT INTO task_cancel_requests(
                    request_id, task_id, project_id, principal_id,
                    intent_id, attempt_id, reason, idempotency_key,
                    request_hash, request_event_sequence,
                    document_json, document_hash, requested_at, recorded_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    request_id,
                    task_id,
                    project_id,
                    principal_id,
                    intent.intent_id,
                    evidence["attempt_id"],
                    reason,
                    idempotency_key,
                    request_hash,
                    next_sequence,
                    request_json,
                    request_document_hash,
                    requested_at,
                    requested_at,
                ),
            )
            connection.execute(
                "UPDATE tasks SET updated_at = ? WHERE task_id = ?",
                (requested_at, task_id),
            )
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None or snapshot.cancellation is None:
                raise TaskStoreCorruption("task cancel request cannot be read")
            connection.commit()
            return TaskCancellationRecord(
                snapshot=snapshot,
                cancellation=snapshot.cancellation,
                replayed=False,
            )
        except (IdempotencyConflict, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict(
                "task cancel request conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def authorize_supervised_cancel(
        self,
        *,
        request_id: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedCancelAuthorization:
        """Authorize one active term to enter the Adapter cancel state machine."""

        if (
            not isinstance(request_id, str)
            or not request_id
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("supervised cancel authorization is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            authorized_at, authorized_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            row = connection.execute(
                """
                SELECT task_id, project_id, principal_id, intent_id, attempt_id
                FROM task_cancel_requests WHERE request_id = ?
                """,
                (request_id,),
            ).fetchone()
            if row is None:
                raise TaskStoreConflict("task cancel request does not exist")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=row["project_id"],
                principal_id=row["principal_id"],
                supplied=supervisor_lease,
                now_us=authorized_at_us,
                action="supervised cancel",
            )
            snapshot = self._load_snapshot(connection, row["task_id"])
            if snapshot is None or snapshot.cancellation is None:
                raise TaskStoreCorruption("task cancel request cannot be read")
            if snapshot.cancellation.state != "requested":
                connection.commit()
                return SupervisedCancelAuthorization(
                    cancellation=snapshot.cancellation,
                    fencing_token=supervisor_lease.fencing_token,
                    authorized_at=authorized_at,
                    replayed=True,
                )
            existing = connection.execute(
                """
                SELECT authorized_at, authorized_at_us
                FROM supervised_cancel_attempts
                WHERE request_id = ? AND fencing_token = ?
                """,
                (request_id, supervisor_lease.fencing_token),
            ).fetchone()
            replayed = existing is not None
            if existing is not None:
                authorized_at, stored_us = _runtime_timestamp(
                    existing["authorized_at"]
                )
                if stored_us != existing["authorized_at_us"]:
                    raise TaskStoreCorruption(
                        "supervised cancel authorization time is inconsistent"
                    )
            else:
                connection.execute(
                    """
                    INSERT INTO supervised_cancel_attempts(
                        request_id, project_id, principal_id, intent_id,
                        attempt_id, fencing_token, action,
                        authorized_at, authorized_at_us
                    ) VALUES (?, ?, ?, ?, ?, ?,
                              'deliver_exact_attempt_cancel', ?, ?)
                    """,
                    (
                        request_id,
                        row["project_id"],
                        row["principal_id"],
                        row["intent_id"],
                        row["attempt_id"],
                        supervisor_lease.fencing_token,
                        authorized_at,
                        authorized_at_us,
                    ),
                )
            connection.commit()
            return SupervisedCancelAuthorization(
                cancellation=snapshot.cancellation,
                fencing_token=supervisor_lease.fencing_token,
                authorized_at=authorized_at,
                replayed=replayed,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict(
                "supervised cancel authorization conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def _load_exact_dag_cancel_terminal_winner(
        self,
        connection: sqlite3.Connection,
        *,
        task_id: str,
        intent_id: str,
        attempt_id: str,
    ) -> dict[str, Any] | None:
        """Validate the natural terminal fact for one cancelled DAG attempt."""

        terminal = connection.execute(
            "SELECT * FROM dag_node_terminal_facts WHERE intent_id = ?",
            (intent_id,),
        ).fetchone()
        if terminal is None:
            return None
        admission = connection.execute(
            "SELECT * FROM dag_node_execution_admissions WHERE intent_id = ?",
            (intent_id,),
        ).fetchone()
        transition = connection.execute(
            """
            SELECT * FROM dag_node_execution_transition_facts
            WHERE intent_id = ? AND node_revision = ?
            """,
            (intent_id, terminal["node_revision"]),
        ).fetchone()
        latest_state = connection.execute(
            """
            SELECT * FROM dag_node_state_events
            WHERE task_id = ? AND plan_id = ? AND node_id = ?
            ORDER BY revision DESC LIMIT 1
            """,
            (task_id, terminal["plan_id"], terminal["node_id"]),
        ).fetchone()
        event = connection.execute(
            "SELECT * FROM run_events WHERE task_id = ? AND sequence = ?",
            (task_id, terminal["event_sequence"]),
        ).fetchone()
        try:
            adapter_status = _decode_document(
                terminal["adapter_status_json"],
                label="DAG cancel natural-terminal Adapter status",
            )
            adapter_status_json, adapter_status_hash = encode_document(adapter_status)
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "DAG cancel natural-terminal proof is invalid"
            ) from error
        expected_event_type = {
            "Succeeded": "node_succeeded",
            "Failed": "node_failed",
        }.get(terminal["node_state"])
        if (
            admission is None
            or transition is None
            or latest_state is None
            or event is None
            or terminal["task_id"] != task_id
            or terminal["intent_id"] != intent_id
            or terminal["attempt_id"] != attempt_id
            or terminal["attempt_number"] != 1
            or terminal["node_state"] not in {"Succeeded", "Failed"}
            or admission["task_id"] != task_id
            or admission["intent_id"] != intent_id
            or admission["plan_id"] != terminal["plan_id"]
            or admission["plan_hash"] != terminal["plan_hash"]
            or admission["approval_id"] != terminal["approval_id"]
            or admission["node_id"] != terminal["node_id"]
            or transition["node_revision"] != terminal["node_revision"]
            or transition["state"] != terminal["node_state"]
            or transition["event_sequence"] != terminal["event_sequence"]
            or transition["event_hash"] != terminal["event_hash"]
            or latest_state["revision"] != terminal["node_revision"]
            or latest_state["state"] != terminal["node_state"]
            or event["document_hash"] != terminal["event_hash"]
            or event["event_type"] != expected_event_type
            or event["node_id"] != terminal["node_id"]
            or event["task_status"] not in {"Running", "Succeeded", "Failed"}
            or adapter_status_json != terminal["adapter_status_json"]
            or adapter_status_hash != terminal["adapter_status_hash"]
            or adapter_status.get("task_id") != task_id
            or adapter_status.get("node_id") != terminal["node_id"]
            or adapter_status.get("status") != terminal["node_state"]
            or adapter_status.get("terminal") is not True
        ):
            raise TaskStoreCorruption(
                "DAG cancel natural-terminal fact is inconsistent"
            )
        return {
            "node_state": terminal["node_state"],
            "node_id": terminal["node_id"],
            "event_sequence": int(terminal["event_sequence"]),
            "event_type": event["event_type"],
            "aggregate_status": event["task_status"],
        }

    def complete_supervised_cancel(
        self,
        *,
        request_id: str,
        result: str,
        terminal_event: Mapping[str, Any] | None,
        adapter_proof: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> TaskCancellationRecord:
        """Commit cancellation or its natural-terminal winner under one term."""

        if (
            not isinstance(request_id, str)
            or not request_id
            or result not in {"cancel_confirmed", "terminal_preempted"}
            or not isinstance(adapter_proof, Mapping)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("task cancel completion is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            resolved_at, resolved_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            row = connection.execute(
                """
                SELECT task_id, project_id, principal_id, intent_id,
                       attempt_id, reason
                FROM task_cancel_requests WHERE request_id = ?
                """,
                (request_id,),
            ).fetchone()
            if row is None:
                raise TaskStoreConflict("task cancel request does not exist")
            dag_intent = connection.execute(
                "SELECT 1 FROM dag_node_execution_admissions WHERE intent_id = ?",
                (row["intent_id"],),
            ).fetchone() is not None
            dag_winner = self._load_exact_dag_cancel_terminal_winner(
                connection,
                task_id=row["task_id"],
                intent_id=row["intent_id"],
                attempt_id=row["attempt_id"],
            )
            dag_terminal_won = (
                adapter_proof.get("state") == "terminal_won"
                and dag_winner is not None
            )
            proof = _validate_cancel_adapter_proof(
                adapter_proof,
                request_id=request_id,
                task_id=row["task_id"],
                attempt_id=row["attempt_id"],
                reason=row["reason"],
                result=result,
                dag_terminal_node_status=(
                    dag_winner["node_state"] if dag_terminal_won else None
                ),
            )
            if (
                dag_intent
                and proof.get("state") == "terminal_won"
                and not dag_terminal_won
            ):
                raise TaskStoreConflict(
                    "DAG terminal-won cancellation lacks its exact node terminal fact"
                )
            proof_json, proof_hash = encode_document(proof)
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=row["project_id"],
                principal_id=row["principal_id"],
                supplied=supervisor_lease,
                now_us=resolved_at_us,
                action="task cancel completion",
            )
            existing = connection.execute(
                """
                SELECT result, adapter_proof_hash
                FROM task_cancel_outcomes WHERE request_id = ?
                """,
                (request_id,),
            ).fetchone()
            if existing is not None:
                if (
                    existing["result"] != result
                    or existing["adapter_proof_hash"] != proof_hash
                ):
                    raise TaskStoreConflict(
                        "task cancel was completed with another outcome"
                    )
                snapshot = self._load_snapshot(connection, row["task_id"])
                if snapshot is None or snapshot.cancellation is None:
                    raise TaskStoreCorruption("task cancel outcome cannot be read")
                connection.commit()
                return TaskCancellationRecord(
                    snapshot=snapshot,
                    cancellation=snapshot.cancellation,
                    replayed=True,
                )
            authorization = connection.execute(
                """
                SELECT 1 FROM supervised_cancel_attempts
                WHERE request_id = ? AND fencing_token = ?
                """,
                (request_id, supervisor_lease.fencing_token),
            ).fetchone()
            if authorization is None:
                raise TaskStoreConflict(
                    "task cancel completion lacks current-term authorization"
                )
            task = connection.execute(
                "SELECT status FROM tasks WHERE task_id = ?",
                (row["task_id"],),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("task cancel target cannot be read")
            if result == "cancel_confirmed":
                if task["status"] not in {"Queued", "Running", "Waiting"}:
                    raise TaskStoreConflict(
                        "cancel confirmation lost the terminal race"
                    )
                if dag_terminal_won and (
                    task["status"] != "Running"
                    or dag_winner["aggregate_status"] != "Running"
                    or dag_winner["event_sequence"]
                    != connection.execute(
                        "SELECT COALESCE(MAX(sequence), 0) FROM run_events "
                        "WHERE task_id = ?",
                        (row["task_id"],),
                    ).fetchone()[0]
                ):
                    raise TaskStoreConflict(
                        "DAG terminal-won cancellation lacks a Running aggregate"
                    )
                if terminal_event is None:
                    raise TaskStoreConflict(
                        "cancel confirmation requires its terminal event"
                    )
                event = dict(terminal_event)
                next_sequence = connection.execute(
                    """
                    SELECT COALESCE(MAX(sequence), 0) + 1
                    FROM run_events WHERE task_id = ?
                    """,
                    (row["task_id"],),
                ).fetchone()[0]
                intent = self._load_dispatch_intent(
                    connection, intent_id=row["intent_id"]
                )
                if (
                    intent is None
                    or intent.intent_id != row["intent_id"]
                    or intent.handle is None
                    or event.get("task_id") != row["task_id"]
                    or event.get("sequence") != next_sequence
                    or event.get("event_type") != "task_cancelled"
                    or event.get("task_status") != "Cancelled"
                    or event.get("node_id") != intent.node_id
                    or event.get("fingerprint") != intent.handle["fingerprint"]
                ):
                    raise TaskStoreConflict("task cancel terminal event is invalid")
                event_json, event_hash = encode_document(event)
                _, fingerprint_hash = encode_document(event["fingerprint"])
                connection.execute(
                    """
                    INSERT INTO run_events(
                        task_id, sequence, event_id, event_type, task_status,
                        node_id, fingerprint_hash, document_json, document_hash,
                        occurred_at, recorded_at
                    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        row["task_id"],
                        next_sequence,
                        event["event_id"],
                        event["event_type"],
                        event["task_status"],
                        event["node_id"],
                        fingerprint_hash,
                        event_json,
                        event_hash,
                        event["occurred_at"],
                        resolved_at,
                    ),
                )
                connection.execute(
                    "UPDATE tasks SET status = 'Cancelled', updated_at = ? "
                    "WHERE task_id = ?",
                    (resolved_at, row["task_id"]),
                )
                terminal_status = "Cancelled"
                terminal_sequence = next_sequence
                connection.execute(
                    """
                    INSERT INTO supervised_run_event_commits(
                        task_id, sequence, project_id, principal_id,
                        fencing_token, recorded_at, recorded_at_us
                    ) VALUES (?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        row["task_id"],
                        next_sequence,
                        row["project_id"],
                        row["principal_id"],
                        supervisor_lease.fencing_token,
                        resolved_at,
                        resolved_at_us,
                    ),
                )
            else:
                if terminal_event is None:
                    if task["status"] not in {"Succeeded", "Failed"}:
                        raise TaskStoreConflict(
                            "terminal-preempted cancel requires a natural terminal task"
                        )
                    terminal_status = task["status"]
                    terminal = connection.execute(
                        """
                        SELECT sequence, event_type, task_status
                        FROM run_events WHERE task_id = ?
                        ORDER BY sequence DESC LIMIT 1
                        """,
                        (row["task_id"],),
                    ).fetchone()
                    expected_type = (
                        dag_winner["event_type"]
                        if dag_terminal_won
                        else (
                            "node_succeeded"
                            if terminal_status == "Succeeded"
                            else "node_failed"
                        )
                    )
                    if (
                        terminal is None
                        or terminal["event_type"] != expected_type
                        or terminal["task_status"] != terminal_status
                        or (
                            dag_terminal_won
                            and (
                                dag_winner["aggregate_status"] != terminal_status
                                or dag_winner["event_sequence"]
                                != terminal["sequence"]
                            )
                        )
                    ):
                        raise TaskStoreCorruption(
                            "natural terminal cancel winner is inconsistent"
                        )
                    terminal_sequence = terminal["sequence"]
                else:
                    if dag_intent:
                        raise TaskStoreConflict(
                            "DAG terminal-preempted cancellation requires a "
                            "committed node terminal fact"
                        )
                    if task["status"] not in {"Queued", "Running", "Waiting"}:
                        raise TaskStoreConflict(
                            "terminal-preempted cancel lost the terminal race"
                        )
                    event = dict(terminal_event)
                    terminal_status = event.get("task_status")
                    expected_type = {
                        "Succeeded": "node_succeeded",
                        "Failed": "node_failed",
                    }.get(terminal_status)
                    next_sequence = connection.execute(
                        """
                        SELECT COALESCE(MAX(sequence), 0) + 1
                        FROM run_events WHERE task_id = ?
                        """,
                        (row["task_id"],),
                    ).fetchone()[0]
                    intent = self._load_dispatch_intent(
                        connection, intent_id=row["intent_id"]
                    )
                    if (
                        expected_type is None
                        or intent is None
                        or intent.intent_id != row["intent_id"]
                        or intent.handle is None
                        or event.get("task_id") != row["task_id"]
                        or event.get("sequence") != next_sequence
                        or event.get("event_type") != expected_type
                        or event.get("node_id") != intent.node_id
                        or event.get("fingerprint")
                        != intent.handle["fingerprint"]
                        or (
                            terminal_status
                            not in ALLOWED_TRANSITIONS.get(
                                task["status"], frozenset()
                            )
                            and not (
                                task["status"] == "Waiting"
                                and terminal_status == "Succeeded"
                            )
                        )
                    ):
                        raise TaskStoreConflict(
                            "natural terminal cancel event is invalid"
                        )
                    event_json, event_hash = encode_document(event)
                    _, fingerprint_hash = encode_document(
                        event["fingerprint"]
                    )
                    connection.execute(
                        """
                        INSERT INTO run_events(
                            task_id, sequence, event_id, event_type,
                            task_status, node_id, fingerprint_hash,
                            document_json, document_hash, occurred_at,
                            recorded_at
                        ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                        """,
                        (
                            row["task_id"],
                            next_sequence,
                            event["event_id"],
                            event["event_type"],
                            terminal_status,
                            event["node_id"],
                            fingerprint_hash,
                            event_json,
                            event_hash,
                            event["occurred_at"],
                            resolved_at,
                        ),
                    )
                    connection.execute(
                        "UPDATE tasks SET status = ?, updated_at = ? "
                        "WHERE task_id = ?",
                        (terminal_status, resolved_at, row["task_id"]),
                    )
                    terminal_sequence = next_sequence
                    connection.execute(
                        """
                        INSERT INTO supervised_run_event_commits(
                            task_id, sequence, project_id, principal_id,
                            fencing_token, recorded_at, recorded_at_us
                        ) VALUES (?, ?, ?, ?, ?, ?, ?)
                        """,
                        (
                            row["task_id"],
                            next_sequence,
                            row["project_id"],
                            row["principal_id"],
                            supervisor_lease.fencing_token,
                            resolved_at,
                            resolved_at_us,
                        ),
                    )
            if proof["terminal_status"] != terminal_status and not dag_terminal_won:
                raise TaskStoreConflict(
                    "task cancel Adapter proof contradicts the terminal outcome"
                )
            outcome = {
                "schema_version": "1.0.0",
                "request_id": request_id,
                "task_id": row["task_id"],
                "result": result,
                "terminal_status": terminal_status,
                "adapter_proof": proof,
                "resolved_at": resolved_at,
                "extensions": {},
            }
            outcome_json, outcome_hash = encode_document(outcome)
            connection.execute(
                """
                INSERT INTO task_cancel_outcomes(
                    request_id, task_id, project_id, principal_id,
                    intent_id, attempt_id, result, terminal_status,
                    terminal_event_sequence, adapter_proof_json,
                    adapter_proof_hash, document_json, document_hash,
                    fencing_token, resolved_at, resolved_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    request_id,
                    row["task_id"],
                    row["project_id"],
                    row["principal_id"],
                    row["intent_id"],
                    row["attempt_id"],
                    result,
                    terminal_status,
                    terminal_sequence,
                    proof_json,
                    proof_hash,
                    outcome_json,
                    outcome_hash,
                    supervisor_lease.fencing_token,
                    resolved_at,
                    resolved_at_us,
                ),
            )
            heartbeat = connection.execute(
                """
                UPDATE runtime_supervisor_leases
                SET heartbeat_at = ?, heartbeat_at_us = ?
                WHERE project_id = ? AND principal_id = ?
                  AND fencing_token = ? AND owner_id = ?
                  AND acquired_at = ?
                """,
                (
                    resolved_at,
                    resolved_at_us,
                    row["project_id"],
                    row["principal_id"],
                    supervisor_lease.fencing_token,
                    supervisor_lease.owner_id,
                    supervisor_lease.acquired_at,
                ),
            )
            if heartbeat.rowcount != 1:
                raise RuntimeSupervisorLeaseLost(
                    "task cancel completion lost its supervisor lease"
                )
            snapshot = self._load_snapshot(connection, row["task_id"])
            if snapshot is None or snapshot.cancellation is None:
                raise TaskStoreCorruption("task cancel outcome cannot be read")
            connection.commit()
            return TaskCancellationRecord(
                snapshot=snapshot,
                cancellation=snapshot.cancellation,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "task cancel completion lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "task cancel completion conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def _initialize_locked(self) -> None:
        migrations = _load_migrations()
        latest_version = migrations[-1].version
        expected_manifest = _expected_schema_manifest(migrations)
        connection = self._connect(require_wal=False)
        foreign_keys_temporarily_disabled = False
        try:
            preflight_user_version = int(
                connection.execute("PRAGMA user_version").fetchone()[0]
            )
            preflight_application_id = int(
                connection.execute("PRAGMA application_id").fetchone()[0]
            )
            preflight_objects = connection.execute(
                """
                SELECT type, name FROM sqlite_master
                WHERE name NOT LIKE 'sqlite_%'
                ORDER BY type, name
                """
            ).fetchall()
            if preflight_objects and preflight_application_id != APPLICATION_ID:
                raise TaskStoreError(
                    "refusing to claim an existing non-task SQLite database"
                )
            if not preflight_objects and (
                preflight_user_version != 0 or preflight_application_id != 0
            ):
                raise TaskStoreError(
                    "task database migration metadata is inconsistent"
                )

            journal_mode = connection.execute("PRAGMA journal_mode = WAL").fetchone()[0]
            if str(journal_mode).lower() != "wal":
                raise TaskStoreError("SQLite WAL mode is required for the task store")

            if preflight_user_version < 21 <= latest_version:
                connection.execute("PRAGMA foreign_keys = OFF")
                if connection.execute("PRAGMA foreign_keys").fetchone()[0] != 0:
                    raise TaskStoreError(
                        "task database migration cannot disable foreign keys"
                    )
                foreign_keys_temporarily_disabled = True

            connection.execute("BEGIN IMMEDIATE")
            user_version = int(
                connection.execute("PRAGMA user_version").fetchone()[0]
            )
            application_id = int(
                connection.execute("PRAGMA application_id").fetchone()[0]
            )
            existing_objects = connection.execute(
                """
                SELECT type, name FROM sqlite_master
                WHERE name NOT LIKE 'sqlite_%'
                ORDER BY type, name
                """
            ).fetchall()
            first_install = not existing_objects
            if first_install:
                if user_version != 0 or application_id != 0:
                    raise TaskStoreError(
                        "task database migration metadata is inconsistent"
                    )
                connection.execute(SCHEMA_MIGRATIONS_SQL)
            else:
                if application_id != APPLICATION_ID:
                    raise TaskStoreError(
                        "refusing to claim an existing non-task SQLite database"
                    )
                if not any(
                    row["type"] == "table" and row["name"] == "schema_migrations"
                    for row in existing_objects
                ):
                    raise TaskStoreError(
                        "task database migration metadata is inconsistent"
                    )
            applied_rows = connection.execute(
                """
                SELECT version, name, checksum
                FROM schema_migrations ORDER BY version ASC
                """
            ).fetchall()
            applied_versions = [int(row["version"]) for row in applied_rows]
            if (
                any(version > latest_version for version in applied_versions)
                or user_version > latest_version
            ):
                raise TaskStoreError(
                    "task database was created by a newer migration version"
                )
            expected_applied_versions = list(range(1, user_version + 1))
            if applied_versions != expected_applied_versions:
                raise TaskStoreError(
                    "task database migration metadata is inconsistent"
                )
            migrations_by_version = {
                migration.version: migration for migration in migrations
            }
            for row in applied_rows:
                migration = migrations_by_version[int(row["version"])]
                if row["name"] != migration.path.name:
                    raise TaskStoreError(
                        "task database migration metadata is inconsistent"
                    )
                if row["checksum"] != migration.checksum:
                    raise TaskStoreError(
                        "applied task-store migration checksum changed"
                    )

            for migration in migrations[user_version:]:
                for statement in _migration_statements(migration.text):
                    connection.execute(statement)
                connection.execute(
                    """
                    INSERT INTO schema_migrations(version, name, checksum, applied_at)
                    VALUES (?, ?, ?, strftime('%Y-%m-%dT%H:%M:%fZ', 'now'))
                    """,
                    (migration.version, migration.path.name, migration.checksum),
                )
                connection.execute(f"PRAGMA user_version = {migration.version}")
            if first_install:
                connection.execute(f"PRAGMA application_id = {APPLICATION_ID}")
            if int(connection.execute("PRAGMA user_version").fetchone()[0]) != latest_version:
                raise TaskStoreError(
                    "task database migration metadata is inconsistent"
                )
            if _schema_manifest(connection) != expected_manifest:
                raise TaskStoreCorruption(
                    "task database schema does not match the applied migration"
                )
            quick_check = [
                str(row[0]) for row in connection.execute("PRAGMA quick_check").fetchall()
            ]
            if quick_check != ["ok"]:
                raise TaskStoreCorruption("SQLite quick_check failed for the task store")
            if connection.execute("PRAGMA foreign_key_check").fetchone() is not None:
                raise TaskStoreCorruption(
                    "SQLite foreign_key_check failed for the task store"
                )
            connection.commit()
            if foreign_keys_temporarily_disabled:
                connection.execute("PRAGMA foreign_keys = ON")
                if connection.execute("PRAGMA foreign_keys").fetchone()[0] != 1:
                    raise TaskStoreError(
                        "task database migration cannot restore foreign keys"
                    )
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.DatabaseError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreCorruption(
                "task database schema or integrity check failed"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            if not connection.in_transaction and foreign_keys_temporarily_disabled:
                connection.execute("PRAGMA foreign_keys = ON")
            connection.close()
        if self.database_path.is_symlink() or not self.database_path.is_file():
            raise TaskStoreError("task database is not a regular private file")
        os.chmod(self.database_path, 0o600)

    def journal_mode(self) -> str:
        connection = self._connect()
        try:
            return str(connection.execute("PRAGMA journal_mode").fetchone()[0]).lower()
        finally:
            connection.close()

    def migration_version(self) -> int:
        connection = self._connect()
        try:
            return int(connection.execute("PRAGMA user_version").fetchone()[0])
        finally:
            connection.close()

    @staticmethod
    def _dataset_core_hash(dataset: Mapping[str, Any]) -> str:
        core = dict(dataset)
        core.pop("access_scope", None)
        _, core_hash = encode_document(core)
        return core_hash

    def _load_dataset_registration(
        self, connection: sqlite3.Connection, row: Mapping[str, Any]
    ) -> dict[str, Any]:
        dataset = _decode_hashed_document(row, label="dataset registration")
        indexed = {
            "id": row["dataset_id"],
            "version": row["version"],
            "content_hash": row["content_hash"],
            "data_type": row["data_type"],
        }
        if any(dataset.get(key) != value for key, value in indexed.items()):
            raise TaskStoreCorruption(
                "persisted dataset identity does not match its index"
            )
        access_scope = dataset.get("access_scope")
        if (
            not isinstance(access_scope, dict)
            or access_scope.get("project_id") != row["project_id"]
        ):
            raise TaskStoreCorruption(
                "persisted dataset project does not match its index"
            )
        version_row = connection.execute(
            """
            SELECT content_hash, data_type, core_hash
            FROM dataset_versions
            WHERE dataset_id = ? AND version = ?
            """,
            (row["dataset_id"], row["version"]),
        ).fetchone()
        if version_row is None:
            raise TaskStoreCorruption(
                "dataset catalog entry references a missing version"
            )
        if (
            version_row["content_hash"] != row["content_hash"]
            or version_row["data_type"] != row["data_type"]
            or version_row["core_hash"] != self._dataset_core_hash(dataset)
        ):
            raise TaskStoreCorruption(
                "persisted dataset version does not match its catalog entry"
            )
        return dataset

    def register_dataset(
        self, *, dataset: Mapping[str, Any], now: str
    ) -> RegistryWriteRecord:
        document_json, document_hash = encode_document(dataset)
        core_hash = self._dataset_core_hash(dataset)
        project_id = dataset["access_scope"]["project_id"]
        identity = (
            dataset["id"],
            dataset["version"],
            dataset["content_hash"],
            dataset["data_type"],
        )
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            version_row = connection.execute(
                """
                SELECT content_hash, data_type, core_hash
                FROM dataset_versions
                WHERE dataset_id = ? AND version = ?
                """,
                identity[:2],
            ).fetchone()
            if version_row is None:
                connection.execute(
                    """
                    INSERT INTO dataset_versions(
                        dataset_id, version, content_hash, data_type,
                        core_hash, first_registered_at
                    ) VALUES (?, ?, ?, ?, ?, ?)
                    """,
                    (*identity, core_hash, now),
                )
            elif (
                version_row["content_hash"] != identity[2]
                or version_row["data_type"] != identity[3]
                or version_row["core_hash"] != core_hash
            ):
                raise TaskStoreConflict(
                    "dataset id/version already identifies different immutable content"
                )

            existing = connection.execute(
                """
                SELECT project_id, dataset_id, version, content_hash, data_type,
                       document_json, document_hash
                FROM dataset_catalog
                WHERE project_id = ? AND dataset_id = ? AND version = ?
                """,
                (project_id, *identity[:2]),
            ).fetchone()
            if existing is not None:
                if existing["document_hash"] != document_hash:
                    raise TaskStoreConflict(
                        "dataset project/version already has a different access snapshot"
                    )
                document = self._load_dataset_registration(connection, existing)
                connection.commit()
                return RegistryWriteRecord(document=document, replayed=True)

            connection.execute(
                """
                INSERT INTO dataset_catalog(
                    project_id, dataset_id, version, content_hash, data_type,
                    document_json, document_hash, registered_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (project_id, *identity, document_json, document_hash, now),
            )
            inserted = connection.execute(
                """
                SELECT project_id, dataset_id, version, content_hash, data_type,
                       document_json, document_hash
                FROM dataset_catalog
                WHERE project_id = ? AND dataset_id = ? AND version = ?
                """,
                (project_id, *identity[:2]),
            ).fetchone()
            if inserted is None:
                raise TaskStoreCorruption(
                    "newly registered dataset cannot be read"
                )
            document = self._load_dataset_registration(connection, inserted)
            connection.commit()
            return RegistryWriteRecord(document=document, replayed=False)
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict(
                "dataset registration conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def register_algorithm(
        self, *, manifest: Mapping[str, Any], now: str
    ) -> RegistryWriteRecord:
        document_json, document_hash = encode_document(manifest)
        identity = (manifest["id"], manifest["version"])
        allowlisted = int(bool(manifest["security"]["allowlisted"]))
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            existing = connection.execute(
                """
                SELECT algorithm_id, version, allowlisted,
                       document_json, document_hash
                FROM algorithm_registry
                WHERE algorithm_id = ? AND version = ?
                """,
                identity,
            ).fetchone()
            if existing is not None:
                if existing["document_hash"] != document_hash:
                    raise TaskStoreConflict(
                        "algorithm id/version already identifies another manifest"
                    )
                document = self._load_algorithm_registration(existing)
                connection.commit()
                return RegistryWriteRecord(document=document, replayed=True)
            connection.execute(
                """
                INSERT INTO algorithm_registry(
                    algorithm_id, version, allowlisted,
                    document_json, document_hash, registered_at
                ) VALUES (?, ?, ?, ?, ?, ?)
                """,
                (*identity, allowlisted, document_json, document_hash, now),
            )
            inserted = connection.execute(
                """
                SELECT algorithm_id, version, allowlisted,
                       document_json, document_hash
                FROM algorithm_registry
                WHERE algorithm_id = ? AND version = ?
                """,
                identity,
            ).fetchone()
            if inserted is None:
                raise TaskStoreCorruption(
                    "newly registered algorithm cannot be read"
                )
            document = self._load_algorithm_registration(inserted)
            connection.commit()
            return RegistryWriteRecord(document=document, replayed=False)
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict(
                "algorithm registration conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    @staticmethod
    def _load_algorithm_registration(row: Mapping[str, Any]) -> dict[str, Any]:
        manifest = _decode_hashed_document(row, label="algorithm registration")
        security = manifest.get("security")
        if (
            manifest.get("id") != row["algorithm_id"]
            or manifest.get("version") != row["version"]
            or not isinstance(security, dict)
            or int(bool(security.get("allowlisted"))) != row["allowlisted"]
        ):
            raise TaskStoreCorruption(
                "persisted algorithm identity does not match its index"
            )
        return manifest

    def get_dataset(
        self, *, project_id: str, dataset_id: str, version: str
    ) -> dict[str, Any] | None:
        connection = self._connect()
        try:
            row = connection.execute(
                """
                SELECT project_id, dataset_id, version, content_hash, data_type,
                       document_json, document_hash
                FROM dataset_catalog
                WHERE project_id = ? AND dataset_id = ? AND version = ?
                """,
                (project_id, dataset_id, version),
            ).fetchone()
            if row is None:
                return None
            return self._load_dataset_registration(connection, row)
        finally:
            connection.close()

    def list_datasets(self, *, project_id: str) -> list[dict[str, Any]]:
        connection = self._connect()
        try:
            rows = connection.execute(
                """
                SELECT project_id, dataset_id, version, content_hash, data_type,
                       document_json, document_hash
                FROM dataset_catalog
                WHERE project_id = ?
                ORDER BY dataset_id ASC, version ASC
                """,
                (project_id,),
            ).fetchall()
            return [
                self._load_dataset_registration(connection, row) for row in rows
            ]
        finally:
            connection.close()

    def get_algorithm(
        self, *, algorithm_id: str, version: str
    ) -> dict[str, Any] | None:
        connection = self._connect()
        try:
            row = connection.execute(
                """
                SELECT algorithm_id, version, allowlisted,
                       document_json, document_hash
                FROM algorithm_registry
                WHERE algorithm_id = ? AND version = ?
                """,
                (algorithm_id, version),
            ).fetchone()
            if row is None:
                return None
            return self._load_algorithm_registration(row)
        finally:
            connection.close()

    def list_algorithms(self) -> list[dict[str, Any]]:
        connection = self._connect()
        try:
            rows = connection.execute(
                """
                SELECT algorithm_id, version, allowlisted,
                       document_json, document_hash
                FROM algorithm_registry
                ORDER BY algorithm_id ASC, version ASC
                """
            ).fetchall()
            return [self._load_algorithm_registration(row) for row in rows]
        finally:
            connection.close()

    def _load_registry_snapshots(
        self,
        connection: sqlite3.Connection,
        *,
        project_id: str,
        dataset_keys: Sequence[tuple[str, str]],
        algorithm_keys: Sequence[tuple[str, str]],
    ) -> RegistrySnapshots:
        datasets: dict[tuple[str, str], dict[str, Any]] = {}
        for key in dict.fromkeys(dataset_keys):
            row = connection.execute(
                """
                SELECT project_id, dataset_id, version, content_hash, data_type,
                       document_json, document_hash
                FROM dataset_catalog
                WHERE project_id = ? AND dataset_id = ? AND version = ?
                """,
                (project_id, *key),
            ).fetchone()
            if row is not None:
                datasets[key] = self._load_dataset_registration(connection, row)
        algorithms: dict[tuple[str, str], dict[str, Any]] = {}
        for key in dict.fromkeys(algorithm_keys):
            row = connection.execute(
                """
                SELECT algorithm_id, version, allowlisted,
                       document_json, document_hash
                FROM algorithm_registry
                WHERE algorithm_id = ? AND version = ?
                """,
                key,
            ).fetchone()
            if row is not None:
                algorithms[key] = self._load_algorithm_registration(row)
        return RegistrySnapshots(datasets=datasets, algorithms=algorithms)

    def load_registry_snapshots(
        self,
        *,
        project_id: str,
        dataset_keys: Sequence[tuple[str, str]],
        algorithm_keys: Sequence[tuple[str, str]],
    ) -> RegistrySnapshots:
        connection = self._connect()
        try:
            # A deferred read transaction pins one consistent WAL snapshot for
            # all requested records.  Future submit code can call the internal
            # helper from its BEGIN IMMEDIATE gate/queue transaction.
            connection.execute("BEGIN")
            snapshots = self._load_registry_snapshots(
                connection,
                project_id=project_id,
                dataset_keys=dataset_keys,
                algorithm_keys=algorithm_keys,
            )
            connection.commit()
            return snapshots
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def get_approval_budget(
        self, *, task_id: str, approval_id: str
    ) -> ApprovalBudget | None:
        connection = self._connect()
        try:
            return self._load_approval_budget(
                connection, task_id=task_id, approval_id=approval_id
            )
        finally:
            connection.close()

    @staticmethod
    def _load_approval_budget(
        connection: sqlite3.Connection, *, task_id: str, approval_id: str
    ) -> ApprovalBudget | None:
        row = connection.execute(
            """
            SELECT budget.task_id, budget.approval_id,
                   budget.max_tasks, budget.tasks_used,
                   approval.document_json, approval.document_hash
            FROM approval_budgets AS budget
            JOIN approvals AS approval
              ON approval.task_id = budget.task_id
             AND approval.approval_id = budget.approval_id
            WHERE budget.task_id = ? AND budget.approval_id = ?
            """,
            (task_id, approval_id),
        ).fetchone()
        if row is None:
            return None
        approval = _decode_hashed_document(row, label="approval")
        scope = approval.get("scope")
        if (
            approval.get("approval_id") != row["approval_id"]
            or not isinstance(scope, dict)
            or type(row["max_tasks"]) is not int
            or type(row["tasks_used"]) is not int
            or scope.get("max_tasks") != row["max_tasks"]
            or row["tasks_used"] < 0
            or row["tasks_used"] > row["max_tasks"]
        ):
            raise TaskStoreCorruption(
                "approval budget does not match its decision"
            )
        return ApprovalBudget(
            task_id=row["task_id"],
            approval_id=row["approval_id"],
            max_tasks=row["max_tasks"],
            tasks_used=row["tasks_used"],
        )

    @staticmethod
    def _load_workbench_mutation(
        connection: sqlite3.Connection,
        *,
        project_id: str,
        principal_id: str,
        operation: str,
        idempotency_key: str,
        request_hash: str | Sequence[str],
    ) -> WorkbenchMutationRecord | None:
        acceptable_hashes = _request_hash_set(request_hash)
        if operation not in WORKBENCH_MUTATION_OPERATIONS:
            raise TaskStoreConflict("workbench mutation operation is invalid")
        row = connection.execute(
            """
            SELECT task_id, request_hash,
                   outcome_json AS document_json,
                   outcome_hash AS document_hash
            FROM workbench_mutations
            WHERE project_id = ? AND principal_id = ?
              AND operation = ? AND idempotency_key = ?
            """,
            (project_id, principal_id, operation, idempotency_key),
        ).fetchone()
        if row is None:
            return None
        if row["request_hash"] not in acceptable_hashes:
            raise IdempotencyConflict(
                "idempotency key was already used for another workbench request"
            )
        outcome = _decode_hashed_document(row, label="workbench mutation outcome")
        valid_outcome = False
        if operation == "revise_draft":
            valid_outcome = (
                set(outcome) == {"task_id", "draft_id", "draft_revision"}
                and isinstance(outcome.get("draft_id"), str)
                and bool(outcome["draft_id"])
                and type(outcome.get("draft_revision")) is int
                and outcome["draft_revision"] >= 1
            )
        elif operation == "persist_plan":
            valid_outcome = (
                set(outcome) == {"task_id", "plan_id", "plan_hash"}
                and isinstance(outcome.get("plan_id"), str)
                and bool(outcome["plan_id"])
                and isinstance(outcome.get("plan_hash"), str)
                and len(outcome["plan_hash"]) == 71
                and outcome["plan_hash"].startswith("sha256:")
                and all(character in "0123456789abcdef" for character in outcome["plan_hash"][7:])
            )
        elif operation == "persist_approval":
            valid_outcome = (
                set(outcome) == {"task_id", "approval_id", "decision"}
                and isinstance(outcome.get("approval_id"), str)
                and bool(outcome["approval_id"])
                and outcome.get("decision") in {"approved", "rejected"}
            )
        elif operation == "abandon_task":
            valid_outcome = (
                set(outcome) == {"task_id", "status"}
                and outcome.get("status") == "Cancelled"
            )
        if not valid_outcome or outcome.get("task_id") != row["task_id"]:
            raise TaskStoreCorruption(
                "workbench mutation outcome is invalid"
            )
        return WorkbenchMutationRecord(
            task_id=row["task_id"], operation=operation, outcome=outcome
        )

    @staticmethod
    def _record_workbench_mutation(
        connection: sqlite3.Connection,
        *,
        project_id: str,
        principal_id: str,
        operation: str,
        idempotency_key: str,
        request_hash: str,
        task_id: str,
        outcome: Mapping[str, Any],
        now: str,
    ) -> None:
        if operation not in WORKBENCH_MUTATION_OPERATIONS:
            raise TaskStoreConflict("workbench mutation operation is invalid")
        outcome_json, outcome_hash = encode_document(outcome)
        connection.execute(
            """
            INSERT INTO workbench_mutations(
                project_id, principal_id, operation, idempotency_key,
                request_hash, task_id, outcome_json, outcome_hash, created_at
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
            """,
            (
                project_id,
                principal_id,
                operation,
                idempotency_key,
                request_hash,
                task_id,
                outcome_json,
                outcome_hash,
                now,
            ),
        )

    @staticmethod
    def _mutation_arguments(
        *,
        project_id: str | None,
        principal_id: str | None,
        idempotency_key: str | None,
        request_hash: str | None,
    ) -> tuple[str, str, str, str] | None:
        values = (project_id, principal_id, idempotency_key, request_hash)
        if all(value is None for value in values):
            return None
        if not all(isinstance(value, str) and value for value in values):
            raise TaskStoreConflict(
                "workbench mutation identity must be provided as one complete set"
            )
        return values  # type: ignore[return-value]

    def lookup_workbench_mutation(
        self,
        *,
        project_id: str,
        principal_id: str,
        operation: str,
        idempotency_key: str,
        request_hash: str,
    ) -> WorkbenchMutationRecord | None:
        connection = self._connect()
        try:
            connection.execute("BEGIN")
            record = self._load_workbench_mutation(
                connection,
                project_id=project_id,
                principal_id=principal_id,
                operation=operation,
                idempotency_key=idempotency_key,
                request_hash=request_hash,
            )
            if record is None:
                connection.commit()
                return None
            snapshot = self._load_snapshot(connection, record.task_id)
            if (
                snapshot is None
                or snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
            ):
                raise TaskStoreCorruption(
                    "workbench mutation crosses its task scope"
                )
            connection.commit()
            return record
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def lookup_compatible_workbench_mutation(
        self,
        *,
        project_id: str,
        principal_id: str,
        operation: str,
        idempotency_key: str,
        request_hashes: Sequence[str],
    ) -> WorkbenchMutationRecord | None:
        """Read a scoped mutation only if one exact historical hash matches."""

        acceptable_hashes = _request_hash_set(request_hashes)
        connection = self._connect()
        try:
            connection.execute("BEGIN")
            record = self._load_workbench_mutation(
                connection,
                project_id=project_id,
                principal_id=principal_id,
                operation=operation,
                idempotency_key=idempotency_key,
                request_hash=tuple(acceptable_hashes),
            )
            if record is None:
                connection.commit()
                return None
            snapshot = self._load_snapshot(connection, record.task_id)
            if (
                snapshot is None
                or snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
            ):
                raise TaskStoreCorruption(
                    "workbench mutation crosses its task scope"
                )
            connection.commit()
            return record
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def create_task(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        draft: Mapping[str, Any],
        idempotency_key: str,
        request_hash: str,
        now: str,
    ) -> CreateTaskRecord:
        if draft.get("status") not in {"Draft", "NeedsInput", "AwaitingApproval"}:
            raise TaskStoreConflict(
                "task must be created in a pre-runtime draft status"
            )
        document_json, document_hash = encode_document(draft)
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            replay = connection.execute(
                """
                SELECT request_hash, task_id
                FROM idempotency_records
                WHERE project_id = ? AND principal_id = ?
                  AND operation = 'create_task' AND idempotency_key = ?
                """,
                (project_id, principal_id, idempotency_key),
            ).fetchone()
            if replay is not None:
                if replay["request_hash"] != request_hash:
                    raise IdempotencyConflict(
                        "idempotency key was already used for another create request"
                    )
                snapshot = self._load_snapshot(connection, replay["task_id"])
                if snapshot is None:
                    raise TaskStoreCorruption(
                        "idempotency record references a missing task"
                    )
                if (
                    snapshot.project_id != project_id
                    or snapshot.principal_id != principal_id
                ):
                    raise TaskStoreCorruption(
                        "idempotency record crosses its project or principal scope"
                    )
                connection.commit()
                return CreateTaskRecord(snapshot=snapshot, replayed=True)

            connection.execute(
                """
                INSERT INTO tasks(
                    task_id, project_id, principal_id, status,
                    created_at, updated_at
                ) VALUES (?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    project_id,
                    principal_id,
                    draft["status"],
                    now,
                    now,
                ),
            )
            connection.execute(
                """
                INSERT INTO draft_revisions(
                    task_id, draft_id, revision, document_json,
                    document_hash, recorded_at
                ) VALUES (?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    draft["draft_id"],
                    draft["revision"],
                    document_json,
                    document_hash,
                    now,
                ),
            )
            connection.execute(
                """
                UPDATE tasks
                SET current_draft_id = ?, current_draft_revision = ?
                WHERE task_id = ?
                """,
                (draft["draft_id"], draft["revision"], task_id),
            )
            response_json, _ = encode_document({"task_id": task_id})
            connection.execute(
                """
                INSERT INTO idempotency_records(
                    project_id, principal_id, operation, idempotency_key,
                    request_hash, task_id, response_json, created_at
                ) VALUES (?, ?, 'create_task', ?, ?, ?, ?, ?)
                """,
                (
                    project_id,
                    principal_id,
                    idempotency_key,
                    request_hash,
                    task_id,
                    response_json,
                    now,
                ),
            )
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None:
                raise TaskStoreCorruption("newly inserted task cannot be read")
            connection.commit()
            return CreateTaskRecord(snapshot=snapshot, replayed=False)
        except (IdempotencyConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict("task creation conflicts with durable state") from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def lookup_create_task(
        self,
        *,
        project_id: str,
        principal_id: str,
        idempotency_key: str,
        request_hash: str,
    ) -> CreateTaskRecord | None:
        """Read an existing create mapping before allocating a new task ID.

        ``create_task`` repeats the lookup in its write transaction, so this
        optimization does not weaken concurrent idempotency.
        """

        connection = self._connect()
        try:
            replay = connection.execute(
                """
                SELECT request_hash, task_id
                FROM idempotency_records
                WHERE project_id = ? AND principal_id = ?
                  AND operation = 'create_task' AND idempotency_key = ?
                """,
                (project_id, principal_id, idempotency_key),
            ).fetchone()
            if replay is None:
                return None
            if replay["request_hash"] != request_hash:
                raise IdempotencyConflict(
                    "idempotency key was already used for another create request"
                )
            snapshot = self._load_snapshot(connection, replay["task_id"])
            if snapshot is None:
                raise TaskStoreCorruption(
                    "idempotency record references a missing task"
                )
            if (
                snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
            ):
                raise TaskStoreCorruption(
                    "idempotency record crosses its project or principal scope"
                )
            return CreateTaskRecord(snapshot=snapshot, replayed=True)
        finally:
            connection.close()

    def lookup_compatible_create_task(
        self,
        *,
        project_id: str,
        principal_id: str,
        idempotency_key: str,
        request_hashes: Sequence[str],
    ) -> CreateTaskRecord | None:
        """Read a scoped create only if one exact historical hash matches."""

        acceptable_hashes = _request_hash_set(request_hashes)
        connection = self._connect()
        try:
            connection.execute("BEGIN")
            replay = connection.execute(
                """
                SELECT request_hash, task_id
                FROM idempotency_records
                WHERE project_id = ? AND principal_id = ?
                  AND operation = 'create_task' AND idempotency_key = ?
                """,
                (project_id, principal_id, idempotency_key),
            ).fetchone()
            if replay is None:
                connection.commit()
                return None
            if replay["request_hash"] not in acceptable_hashes:
                raise IdempotencyConflict(
                    "idempotency key was already used for another create request"
                )
            snapshot = self._load_snapshot(connection, replay["task_id"])
            if snapshot is None:
                raise TaskStoreCorruption(
                    "idempotency record references a missing task"
                )
            if (
                snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
            ):
                raise TaskStoreCorruption(
                    "idempotency record crosses its project or principal scope"
                )
            connection.commit()
            return CreateTaskRecord(snapshot=snapshot, replayed=True)
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def _load_submit_replay(
        self,
        connection: sqlite3.Connection,
        *,
        project_id: str,
        principal_id: str,
        idempotency_key: str,
        request_hash: str,
    ) -> SubmitTaskRecord | None:
        row = connection.execute(
            """
            SELECT record.request_hash, record.task_id, record.response_json,
                   record.created_at AS record_created_at,
                   link.request_hash AS link_request_hash,
                   link.task_id AS link_task_id, link.intent_id,
                   link.created_at AS link_created_at
            FROM idempotency_records AS record
            LEFT JOIN submit_idempotency_links AS link
              ON link.project_id = record.project_id
             AND link.principal_id = record.principal_id
             AND link.operation = record.operation
             AND link.idempotency_key = record.idempotency_key
            WHERE record.project_id = ? AND record.principal_id = ?
              AND record.operation = 'submit_task'
              AND record.idempotency_key = ?
            """,
            (project_id, principal_id, idempotency_key),
        ).fetchone()
        if row is None:
            return None
        if row["request_hash"] != request_hash:
            raise IdempotencyConflict(
                "idempotency key was already used for another submit request"
            )
        if (
            row["intent_id"] is None
            or row["link_request_hash"] != row["request_hash"]
            or row["link_task_id"] != row["task_id"]
            or row["link_created_at"] != row["record_created_at"]
        ):
            raise TaskStoreCorruption(
                "submit idempotency record is not bound to its dispatch intent"
            )
        response = _decode_document(
            row["response_json"], label="submit idempotency response"
        )
        if response != {
            "intent_id": row["intent_id"],
            "task_id": row["task_id"],
        }:
            raise TaskStoreCorruption(
                "submit idempotency response differs from its typed link"
            )
        snapshot = self._load_snapshot(connection, row["task_id"])
        intent = self._load_dispatch_intent(connection, task_id=row["task_id"])
        if snapshot is None or intent is None:
            raise TaskStoreCorruption(
                "submit idempotency record references missing durable state"
            )
        if intent.intent_id != row["intent_id"]:
            raise TaskStoreCorruption(
                "submit idempotency record references another dispatch intent"
            )
        if intent.created_at != row["record_created_at"]:
            raise TaskStoreCorruption(
                "submit idempotency record time differs from its dispatch intent"
            )
        return SubmitTaskRecord(snapshot=snapshot, intent=intent, replayed=True)

    def lookup_submit_task(
        self,
        *,
        project_id: str,
        principal_id: str,
        idempotency_key: str,
        request_hash: str,
    ) -> SubmitTaskRecord | None:
        connection = self._connect()
        try:
            return self._load_submit_replay(
                connection,
                project_id=project_id,
                principal_id=principal_id,
                idempotency_key=idempotency_key,
                request_hash=request_hash,
            )
        finally:
            connection.close()

    def submit_task(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        approval_id: str,
        idempotency_key: str,
        request_hash: str,
        admit: Callable[
            [SubmitGateContext, str],
            tuple[Mapping[str, Any], Mapping[str, Any]],
        ],
        clock: Callable[[], str],
    ) -> SubmitTaskRecord:
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            replay = self._load_submit_replay(
                connection,
                project_id=project_id,
                principal_id=principal_id,
                idempotency_key=idempotency_key,
                request_hash=request_hash,
            )
            if replay is not None:
                connection.commit()
                return replay

            snapshot = self._load_snapshot(connection, task_id)
            if (
                snapshot is None
                or snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
            ):
                raise TaskStoreConflict("task does not exist in the requested scope")
            if snapshot.status != "AwaitingApproval":
                raise TaskStoreConflict("task is not awaiting approval")
            if snapshot.plan is None or snapshot.approval is None:
                raise TaskStoreConflict("task has no current plan and approval")
            if snapshot.approval.get("approval_id") != approval_id:
                raise TaskStoreConflict("approval is not current for this task")
            budget = self._load_approval_budget(
                connection, task_id=task_id, approval_id=approval_id
            )
            if budget is None:
                raise TaskStoreCorruption("current approval has no durable budget")
            try:
                dataset_keys = [
                    (dataset["id"], dataset["version"])
                    for dataset in snapshot.draft["datasets"]
                ]
                algorithm_keys = [
                    (
                        snapshot.draft["algorithm"]["id"],
                        snapshot.draft["algorithm"]["version"],
                    )
                ]
                for node in snapshot.plan["nodes"]:
                    algorithm_keys.append(
                        (node["algorithm"]["id"], node["algorithm"]["version"])
                    )
                    for binding in node["inputs"]:
                        dataset = binding.get("dataset")
                        if not isinstance(dataset, Mapping):
                            raise TaskStoreConflict(
                                "P1 submission does not accept node-output inputs"
                            )
                        dataset_keys.append((dataset["id"], dataset["version"]))
            except (KeyError, TypeError) as error:
                raise TaskStoreCorruption(
                    "current submit documents cannot identify registry records"
                ) from error
            registry = self._load_registry_snapshots(
                connection,
                project_id=project_id,
                dataset_keys=dataset_keys,
                algorithm_keys=algorithm_keys,
            )
            now = clock()
            intent, queued_event = admit(
                SubmitGateContext(
                    snapshot=snapshot,
                    registry=registry,
                    budget=budget,
                ),
                now,
            )
            intent_json, intent_hash = encode_document(intent)
            queue_fingerprint = intent.get("queue_fingerprint")
            if not isinstance(queue_fingerprint, Mapping):
                raise TaskStoreConflict("dispatch intent has no queue fingerprint")
            _, fingerprint_hash = encode_document(queue_fingerprint)
            event_json, event_hash = encode_document(queued_event)
            _, event_fingerprint_hash = encode_document(
                queued_event.get("fingerprint", {})
            )

            if len(snapshot.plan.get("nodes", [])) != 1:
                raise TaskStoreConflict(
                    "P1 submission requires exactly one plan node"
                )
            node = snapshot.plan["nodes"][0]
            indexed_intent = {
                "task_id": task_id,
                "plan_id": snapshot.plan["plan_id"],
                "plan_hash": snapshot.plan["plan_hash"],
                "approval_id": approval_id,
                "node_id": node["node_id"],
                "node_idempotency_key": node["idempotency_key"],
                "created_at": now,
            }
            adapter = intent.get("adapter")
            if (
                intent.get("schema_version") != "1.0.0"
                or any(intent.get(key) != value for key, value in indexed_intent.items())
                or not isinstance(intent.get("intent_id"), str)
                or not isinstance(adapter, Mapping)
                or set(adapter) != {"id", "version"}
                or not isinstance(intent.get("request"), Mapping)
                or queued_event.get("schema_version") != "1.0.0"
                or queued_event.get("sequence") != 1
                or queued_event.get("task_id") != task_id
                or queued_event.get("event_type") != "task_queued"
                or queued_event.get("task_status") != "Queued"
                or queued_event.get("occurred_at") != now
                or queued_event.get("node_id") is not None
                or queued_event.get("fingerprint") != queue_fingerprint
                or event_fingerprint_hash != fingerprint_hash
            ):
                raise TaskStoreConflict(
                    "dispatch intent or queued event differs from current state"
                )

            consumed = connection.execute(
                """
                UPDATE approval_budgets
                SET tasks_used = tasks_used + 1, updated_at = ?
                WHERE task_id = ? AND approval_id = ?
                  AND tasks_used < max_tasks
                """,
                (now, task_id, approval_id),
            )
            if consumed.rowcount != 1:
                raise TaskStoreConflict("approval task budget is exhausted")
            connection.execute(
                """
                INSERT INTO dispatch_intents(
                    intent_id, task_id, plan_id, plan_hash, approval_id,
                    node_id, node_idempotency_key, adapter_id,
                    adapter_version, request_json, request_hash,
                    fingerprint_hash, created_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    intent["intent_id"],
                    task_id,
                    snapshot.plan["plan_id"],
                    snapshot.plan["plan_hash"],
                    approval_id,
                    node["node_id"],
                    node["idempotency_key"],
                    adapter["id"],
                    adapter["version"],
                    intent_json,
                    intent_hash,
                    fingerprint_hash,
                    now,
                ),
            )
            connection.execute(
                """
                INSERT INTO run_events(
                    task_id, sequence, event_id, event_type, task_status,
                    node_id, fingerprint_hash, document_json, document_hash,
                    occurred_at, recorded_at
                ) VALUES (?, 1, ?, 'task_queued', 'Queued', NULL, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    queued_event["event_id"],
                    event_fingerprint_hash,
                    event_json,
                    event_hash,
                    queued_event["occurred_at"],
                    now,
                ),
            )
            connection.execute(
                "UPDATE tasks SET status = 'Queued', updated_at = ? WHERE task_id = ?",
                (now, task_id),
            )
            response_json, _ = encode_document(
                {"intent_id": intent["intent_id"], "task_id": task_id}
            )
            connection.execute(
                """
                INSERT INTO idempotency_records(
                    project_id, principal_id, operation, idempotency_key,
                    request_hash, task_id, response_json, created_at
                ) VALUES (?, ?, 'submit_task', ?, ?, ?, ?, ?)
                """,
                (
                    project_id,
                    principal_id,
                    idempotency_key,
                    request_hash,
                    task_id,
                    response_json,
                    now,
                ),
            )
            connection.execute(
                """
                INSERT INTO submit_idempotency_links(
                    project_id, principal_id, operation, idempotency_key,
                    request_hash, task_id, intent_id, created_at
                ) VALUES (?, ?, 'submit_task', ?, ?, ?, ?, ?)
                """,
                (
                    project_id,
                    principal_id,
                    idempotency_key,
                    request_hash,
                    task_id,
                    intent["intent_id"],
                    now,
                ),
            )
            queued_snapshot = self._load_snapshot(connection, task_id)
            stored_intent = self._load_dispatch_intent(
                connection, task_id=task_id
            )
            if queued_snapshot is None or stored_intent is None:
                raise TaskStoreCorruption("submitted task cannot be read")
            connection.commit()
            return SubmitTaskRecord(
                snapshot=queued_snapshot,
                intent=stored_intent,
                replayed=False,
            )
        except (IdempotencyConflict, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict("task submission conflicts with durable state") from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def get_task(self, task_id: str) -> TaskSnapshot | None:
        connection = self._connect()
        try:
            # Pin every query used to assemble the aggregate to one WAL read
            # snapshot. Without this transaction a concurrent RunEvent commit
            # could make the task row and latest-event row come from different
            # SQLite snapshots and look corrupt for one read.
            connection.execute("BEGIN")
            snapshot = self._load_snapshot(connection, task_id)
            connection.commit()
            return snapshot
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def list_tasks(
        self,
        *,
        project_id: str,
        principal_id: str,
        cursor: str | None = None,
        limit: int = 20,
        view: str = "active",
    ) -> TaskSnapshotPage:
        """List one immutable-creation-order page inside an exact task scope.

        ``cursor`` is the task identity returned as the previous page's
        ``next_cursor``.  The application/HTTP layers may encode that identity,
        but the store always resolves it inside the requested scope before it
        can influence the keyset boundary.
        """

        if (
            not isinstance(project_id, str)
            or not project_id
            or not isinstance(principal_id, str)
            or not principal_id
        ):
            raise TaskStoreConflict("task list scope is invalid")
        if cursor is not None and (not isinstance(cursor, str) or not cursor):
            raise TaskStoreConflict("task list cursor is invalid")
        if type(limit) is not int or not 1 <= limit <= 50:
            raise TaskStoreConflict("task list limit is invalid")
        if view not in TASK_VISIBILITY_VIEWS:
            raise TaskStoreConflict("task list view is invalid")

        connection = self._connect()
        try:
            # Resolve the cursor and every aggregate from one WAL snapshot.
            # Ordering uses immutable created_at + task_id rather than mutable
            # updated_at, so progress events cannot move a task between pages.
            connection.execute("BEGIN")
            parameters: list[Any] = [project_id, principal_id]
            visibility_state = "active" if view == "active" else "trashed"
            parameters.append(visibility_state)
            boundary = ""
            if cursor is not None:
                cursor_row = connection.execute(
                    """
                    SELECT tasks.task_id, tasks.created_at
                    FROM tasks
                    LEFT JOIN task_visibility AS visibility
                      ON visibility.task_id = tasks.task_id
                    WHERE tasks.task_id = ?
                      AND tasks.project_id = ?
                      AND tasks.principal_id = ?
                      AND COALESCE(visibility.state, 'active') = ?
                      AND NOT EXISTS (
                          SELECT 1 FROM task_purge_outcomes AS purge_outcome
                          WHERE purge_outcome.task_id = tasks.task_id
                      )
                    """,
                    (cursor, project_id, principal_id, visibility_state),
                ).fetchone()
                if cursor_row is None:
                    raise TaskStoreConflict("task list cursor is invalid")
                cursor_snapshot = self._load_snapshot(connection, cursor)
                if (
                    cursor_snapshot is None
                    or cursor_snapshot.project_id != project_id
                    or cursor_snapshot.principal_id != principal_id
                    or cursor_snapshot.created_at != cursor_row["created_at"]
                ):
                    raise TaskStoreCorruption(
                        "task list cursor differs from its durable aggregate"
                    )
                boundary = """
                  AND (
                        tasks.created_at < ?
                        OR (tasks.created_at = ? AND tasks.task_id < ?)
                  )
                """
                parameters.extend(
                    [
                        cursor_snapshot.created_at,
                        cursor_snapshot.created_at,
                        cursor,
                    ]
                )
            parameters.append(limit + 1)
            rows = connection.execute(
                f"""
                SELECT tasks.task_id
                FROM tasks
                LEFT JOIN task_visibility AS visibility
                  ON visibility.task_id = tasks.task_id
                WHERE tasks.project_id = ? AND tasks.principal_id = ?
                  AND COALESCE(visibility.state, 'active') = ?
                  AND NOT EXISTS (
                      SELECT 1 FROM task_purge_outcomes AS purge_outcome
                      WHERE purge_outcome.task_id = tasks.task_id
                  )
                {boundary}
                ORDER BY tasks.created_at DESC, tasks.task_id DESC
                LIMIT ?
                """,
                parameters,
            ).fetchall()
            page_rows = rows[:limit]
            snapshots: list[TaskSnapshot] = []
            for row in page_rows:
                snapshot = self._load_snapshot(connection, row["task_id"])
                if snapshot is None:
                    raise TaskStoreCorruption("listed task aggregate is missing")
                if (
                    snapshot.project_id != project_id
                    or snapshot.principal_id != principal_id
                ):
                    raise TaskStoreCorruption("listed task crossed its durable scope")
                snapshots.append(snapshot)
            next_cursor = (
                str(page_rows[-1]["task_id"])
                if len(rows) > limit and page_rows
                else None
            )
            connection.commit()
            return TaskSnapshotPage(
                snapshots=tuple(snapshots), next_cursor=next_cursor
            )
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def _load_task_visibility(
        self, connection: sqlite3.Connection, row: Mapping[str, Any]
    ) -> tuple[int, str | None]:
        task_id = str(row["task_id"])
        projection = connection.execute(
            """
            SELECT project_id, principal_id, state, revision,
                   trashed_at, updated_at
            FROM task_visibility WHERE task_id = ?
            """,
            (task_id,),
        ).fetchone()
        events = connection.execute(
            """
            SELECT project_id, principal_id, revision, event_id, action,
                   previous_state, state, trashed_at, document_json,
                   document_hash, occurred_at, recorded_at
            FROM task_visibility_events
            WHERE task_id = ? ORDER BY revision ASC
            """,
            (task_id,),
        ).fetchall()
        if projection is None:
            mutation = connection.execute(
                "SELECT 1 FROM task_visibility_mutations WHERE task_id = ? LIMIT 1",
                (task_id,),
            ).fetchone()
            if events or mutation is not None:
                raise TaskStoreCorruption(
                    "task visibility history lacks its current projection"
                )
            return 0, None

        if (
            projection["project_id"] != row["project_id"]
            or projection["principal_id"] != row["principal_id"]
            or type(projection["revision"]) is not int
            or projection["revision"] < 1
            or len(events) != projection["revision"]
        ):
            raise TaskStoreCorruption("task visibility projection is invalid")

        previous_state = "active"
        trashed_at: str | None = None
        for expected_revision, event_row in enumerate(events, start=1):
            document = _decode_hashed_document(
                event_row, label="task visibility event"
            )
            expected_action = "trashed" if previous_state == "active" else "restored"
            next_state = "trashed" if expected_action == "trashed" else "active"
            next_trashed_at = (
                event_row["occurred_at"] if next_state == "trashed" else None
            )
            if (
                event_row["project_id"] != row["project_id"]
                or event_row["principal_id"] != row["principal_id"]
                or event_row["revision"] != expected_revision
                or event_row["action"] != expected_action
                or event_row["previous_state"] != previous_state
                or event_row["state"] != next_state
                or event_row["trashed_at"] != next_trashed_at
                or set(document)
                != {
                    "schema_version",
                    "event_id",
                    "task_id",
                    "revision",
                    "action",
                    "previous_state",
                    "state",
                    "trashed_at",
                    "actor",
                    "occurred_at",
                    "extensions",
                }
                or document.get("schema_version") != "1.0.0"
                or document.get("event_id") != event_row["event_id"]
                or document.get("task_id") != task_id
                or document.get("revision") != expected_revision
                or document.get("action") != expected_action
                or document.get("previous_state") != previous_state
                or document.get("state") != next_state
                or document.get("trashed_at") != next_trashed_at
                or document.get("actor")
                != {"type": "user", "id": row["principal_id"]}
                or document.get("occurred_at") != event_row["occurred_at"]
                or document.get("extensions") != {}
            ):
                raise TaskStoreCorruption("task visibility event is invalid")
            previous_state = next_state
            trashed_at = next_trashed_at

        if (
            projection["state"] != previous_state
            or projection["trashed_at"] != trashed_at
            or projection["updated_at"] != events[-1]["occurred_at"]
        ):
            raise TaskStoreCorruption(
                "task visibility projection differs from its event history"
            )
        return int(projection["revision"]), trashed_at

    def _load_task_purge(
        self,
        connection: sqlite3.Connection,
        row: Mapping[str, Any],
        *,
        visibility_revision: int,
        trashed_at: str | None,
    ) -> tuple[str | None, str | None, str | None, str | None, dict[str, Any] | None]:
        """Decode one immutable purge request/outcome overlay."""

        task_id = str(row["task_id"])
        request_row = connection.execute(
            """
            SELECT purge_id, task_id, project_id, principal_id,
                   visibility_revision, request_hash,
                   document_json, document_hash, requested_at, recorded_at
            FROM task_purge_requests WHERE task_id = ?
            """,
            (task_id,),
        ).fetchone()
        if request_row is None:
            dangling = connection.execute(
                """
                SELECT 1 FROM task_purge_outcomes WHERE task_id = ?
                UNION ALL
                SELECT 1 FROM task_purge_idempotency WHERE task_id = ?
                LIMIT 1
                """,
                (task_id, task_id),
            ).fetchone()
            if dangling is not None:
                raise TaskStoreCorruption(
                    "task purge state lacks its immutable request"
                )
            return None, None, None, None, None

        request = _decode_hashed_document(
            request_row, label="task purge request"
        )
        purge_id = request_row["purge_id"]
        if (
            not isinstance(purge_id, str)
            or not purge_id.startswith("purge-")
            or len(purge_id) != 38
            or any(character not in "0123456789abcdef" for character in purge_id[6:])
            or request_row["task_id"] != task_id
            or request_row["project_id"] != row["project_id"]
            or request_row["principal_id"] != row["principal_id"]
            or request_row["visibility_revision"] != visibility_revision
            or trashed_at is None
            or not isinstance(request_row["request_hash"], str)
            or not request_row["request_hash"]
            or not isinstance(request_row["requested_at"], str)
            or request_row["recorded_at"] != request_row["requested_at"]
            or set(request)
            != {
                "schema_version",
                "purge_id",
                "task_id",
                "visibility_revision",
                "purge_state",
                "actor",
                "requested_at",
                "extensions",
            }
            or request.get("schema_version") != "1.0.0"
            or request.get("purge_id") != purge_id
            or request.get("task_id") != task_id
            or request.get("visibility_revision") != visibility_revision
            or request.get("purge_state") != "pending"
            or request.get("actor")
            != {"type": "user", "id": row["principal_id"]}
            or request.get("requested_at") != request_row["requested_at"]
            or request.get("extensions") != {}
        ):
            raise TaskStoreCorruption("task purge request is invalid")

        aliases = connection.execute(
            """
            SELECT project_id, principal_id, operation, idempotency_key,
                   request_hash, purge_id, task_id,
                   visibility_revision, created_at
            FROM task_purge_idempotency
            WHERE purge_id = ? ORDER BY idempotency_key ASC
            """,
            (purge_id,),
        ).fetchall()
        if not aliases:
            raise TaskStoreCorruption(
                "task purge request lacks an idempotency binding"
            )
        for alias in aliases:
            if (
                alias["project_id"] != row["project_id"]
                or alias["principal_id"] != row["principal_id"]
                or alias["operation"] != "purge_task"
                or not isinstance(alias["idempotency_key"], str)
                or not alias["idempotency_key"]
                or alias["request_hash"] != request_row["request_hash"]
                or alias["purge_id"] != purge_id
                or alias["task_id"] != task_id
                or alias["visibility_revision"] != visibility_revision
                or not isinstance(alias["created_at"], str)
            ):
                raise TaskStoreCorruption(
                    "task purge idempotency binding is invalid"
                )

        outcome_row = connection.execute(
            """
            SELECT purge_id, task_id, project_id, principal_id,
                   visibility_revision, local_run_state,
                   document_json, document_hash, purged_at, recorded_at
            FROM task_purge_outcomes WHERE purge_id = ?
            """,
            (purge_id,),
        ).fetchone()
        if outcome_row is None:
            return purge_id, request_row["requested_at"], None, None, None

        outcome = _decode_hashed_document(
            outcome_row, label="task purge outcome"
        )
        if (
            outcome_row["task_id"] != task_id
            or outcome_row["project_id"] != row["project_id"]
            or outcome_row["principal_id"] != row["principal_id"]
            or outcome_row["visibility_revision"] != visibility_revision
            or outcome_row["local_run_state"] not in TASK_PURGE_LOCAL_RUN_STATES
            or not isinstance(outcome_row["purged_at"], str)
            or outcome_row["recorded_at"] != outcome_row["purged_at"]
            or set(outcome)
            != {
                "task_id",
                "purge_id",
                "purge_state",
                "purged_at",
                "local_run_state",
                "audit_retained",
            }
            or outcome.get("task_id") != task_id
            or outcome.get("purge_id") != purge_id
            or outcome.get("purge_state") != "purged"
            or outcome.get("purged_at") != outcome_row["purged_at"]
            or outcome.get("local_run_state") != outcome_row["local_run_state"]
            or outcome.get("audit_retained") is not True
        ):
            raise TaskStoreCorruption("task purge outcome is invalid")
        return (
            purge_id,
            request_row["requested_at"],
            outcome_row["purged_at"],
            outcome_row["local_run_state"],
            outcome,
        )

    def _load_task_cancellation(
        self, connection: sqlite3.Connection, task_id: str
    ) -> TaskCancellationSnapshot | None:
        row = connection.execute(
            """
            SELECT request.request_id, request.task_id, request.project_id,
                   request.principal_id, request.intent_id, request.attempt_id,
                   request.reason, request.request_event_sequence,
                   request.document_json, request.document_hash,
                   request.requested_at,
                   outcome.result, outcome.terminal_status,
                   outcome.terminal_event_sequence,
                   outcome.adapter_proof_json, outcome.adapter_proof_hash,
                   outcome.document_json AS outcome_json,
                   outcome.document_hash AS outcome_hash,
                   outcome.resolved_at
            FROM task_cancel_requests AS request
            LEFT JOIN task_cancel_outcomes AS outcome
              ON outcome.request_id = request.request_id
            WHERE request.task_id = ?
            """,
            (task_id,),
        ).fetchone()
        if row is None:
            return None
        request = _decode_hashed_document(row, label="task cancel request")
        actor = request.get("actor")
        if (
            set(request)
            != {
                "schema_version",
                "request_id",
                "task_id",
                "intent_id",
                "attempt_id",
                "reason",
                "actor",
                "requested_at",
                "extensions",
            }
            or request.get("schema_version") != "1.0.0"
            or request.get("request_id") != row["request_id"]
            or request.get("task_id") != row["task_id"]
            or request.get("intent_id") != row["intent_id"]
            or request.get("attempt_id") != row["attempt_id"]
            or request.get("reason") != row["reason"]
            or request.get("requested_at") != row["requested_at"]
            or actor != {"type": "user", "id": row["principal_id"]}
            or request.get("extensions") != {}
            or row["reason"] != "user_requested"
        ):
            raise TaskStoreCorruption(
                "task cancel request differs from its immutable index"
            )
        event = connection.execute(
            """
            SELECT event_type, task_status FROM run_events
            WHERE task_id = ? AND sequence = ?
            """,
            (task_id, row["request_event_sequence"]),
        ).fetchone()
        if event is None or event["event_type"] != "cancel_requested":
            raise TaskStoreCorruption(
                "task cancel request lacks its admission event"
            )

        if row["result"] is None:
            return TaskCancellationSnapshot(
                request_id=row["request_id"],
                task_id=row["task_id"],
                intent_id=row["intent_id"],
                attempt_id=row["attempt_id"],
                reason=row["reason"],
                requested_at=row["requested_at"],
                state="requested",
            )

        try:
            adapter_proof = _decode_document(
                row["adapter_proof_json"], label="task cancel adapter proof"
            )
            _, adapter_proof_hash = encode_document(adapter_proof)
            dag_winner = (
                self._load_exact_dag_cancel_terminal_winner(
                    connection,
                    task_id=row["task_id"],
                    intent_id=row["intent_id"],
                    attempt_id=row["attempt_id"],
                )
                if adapter_proof.get("state") == "terminal_won"
                else None
            )
            dag_terminal_won = dag_winner is not None
            adapter_proof = _validate_cancel_adapter_proof(
                adapter_proof,
                request_id=row["request_id"],
                task_id=row["task_id"],
                attempt_id=row["attempt_id"],
                reason=row["reason"],
                result=row["result"],
                terminal_status=row["terminal_status"],
                dag_terminal_node_status=(
                    dag_winner["node_state"] if dag_terminal_won else None
                ),
            )
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "task cancel adapter proof is invalid"
            ) from error
        if adapter_proof_hash != row["adapter_proof_hash"]:
            raise TaskStoreCorruption(
                "task cancel adapter proof hash does not match its content"
            )
        outcome_row = {
            "document_json": row["outcome_json"],
            "document_hash": row["outcome_hash"],
        }
        outcome = _decode_hashed_document(
            outcome_row, label="task cancel outcome"
        )
        expected_state = {
            "cancel_confirmed": ("Cancelled", "cancelled"),
            "terminal_preempted": (row["terminal_status"], "superseded"),
        }.get(row["result"])
        if (
            expected_state is None
            or row["terminal_status"] != expected_state[0]
            or set(outcome)
            != {
                "schema_version",
                "request_id",
                "task_id",
                "result",
                "terminal_status",
                "adapter_proof",
                "resolved_at",
                "extensions",
            }
            or outcome.get("schema_version") != "1.0.0"
            or outcome.get("request_id") != row["request_id"]
            or outcome.get("task_id") != row["task_id"]
            or outcome.get("result") != row["result"]
            or outcome.get("terminal_status") != row["terminal_status"]
            or outcome.get("adapter_proof") != adapter_proof
            or outcome.get("resolved_at") != row["resolved_at"]
            or outcome.get("extensions") != {}
        ):
            raise TaskStoreCorruption(
                "task cancel outcome differs from its immutable index"
            )
        terminal = connection.execute(
            """
            SELECT sequence, event_type, task_status FROM run_events
            WHERE task_id = ? AND sequence = ?
            """,
            (task_id, row["terminal_event_sequence"]),
        ).fetchone()
        latest_sequence = connection.execute(
            "SELECT COALESCE(MAX(sequence), 0) FROM run_events WHERE task_id = ?",
            (task_id,),
        ).fetchone()[0]
        expected_event_type = (
            "task_cancelled"
            if row["result"] == "cancel_confirmed"
            else (
                dag_winner["event_type"]
                if dag_terminal_won
                else (
                    "node_succeeded"
                    if row["terminal_status"] == "Succeeded"
                    else "node_failed"
                )
            )
        )
        dag_outcome_consistent = not dag_terminal_won or (
            (
                row["result"] == "cancel_confirmed"
                and row["terminal_status"] == "Cancelled"
                and dag_winner["aggregate_status"] == "Running"
                and dag_winner["event_sequence"] + 1
                == row["terminal_event_sequence"]
            )
            or (
                row["result"] == "terminal_preempted"
                and dag_winner["aggregate_status"] == row["terminal_status"]
                and dag_winner["event_sequence"]
                == row["terminal_event_sequence"]
            )
        )
        if (
            terminal is None
            or terminal["task_status"] != row["terminal_status"]
            or terminal["event_type"] != expected_event_type
            or terminal["sequence"] != latest_sequence
            or not dag_outcome_consistent
        ):
            raise TaskStoreCorruption(
                "task cancel outcome lacks its exact terminal event"
            )
        return TaskCancellationSnapshot(
            request_id=row["request_id"],
            task_id=row["task_id"],
            intent_id=row["intent_id"],
            attempt_id=row["attempt_id"],
            reason=row["reason"],
            requested_at=row["requested_at"],
            state=expected_state[1],
            result=row["result"],
            terminal_status=row["terminal_status"],
            resolved_at=row["resolved_at"],
            adapter_proof=adapter_proof,
        )

    def _load_task_timeout(
        self,
        connection: sqlite3.Connection,
        task_id: str,
        *,
        task_status: str,
    ) -> TaskTimeoutSnapshot | None:
        row = connection.execute(
            """
            SELECT timeout.*,
                   attempt.attempt_number, attempt.binding_hash,
                   outcome.result, outcome.terminal_status,
                   outcome.failure_code, outcome.terminal_event_sequence,
                   outcome.adapter_proof_json,
                   outcome.adapter_proof_hash,
                   outcome.document_json AS outcome_json,
                   outcome.document_hash AS outcome_hash,
                   outcome.resolved_at,
                   outcome.resolved_at_us AS outcome_resolved_at_us,
                   outcome.fencing_token AS outcome_fencing_token,
                   outcome_delivery.authorized_at AS outcome_authorized_at,
                   outcome_delivery.authorized_at_us
                       AS outcome_authorized_at_us,
                   outcome_delivery.fencing_token
                       AS outcome_authorized_fencing_token,
                   (SELECT COUNT(*) FROM supervised_timeout_attempts AS delivery
                    WHERE delivery.timeout_id = timeout.timeout_id)
                       AS authorization_count,
                   EXISTS(
                       SELECT 1 FROM task_cancel_requests AS cancel
                       WHERE cancel.task_id = timeout.task_id
                   ) AS has_cancel_request,
                   EXISTS(
                       SELECT 1
                       FROM worker_exit_retry_reservations AS retry
                       WHERE retry.intent_id = timeout.intent_id
                         AND retry.attempt_number = attempt.attempt_number
                   ) AS is_worker_exit_retry
            FROM worker_attempt_timeout_windows AS timeout
            JOIN worker_launch_attempts AS attempt
              ON attempt.attempt_id = timeout.attempt_id
            LEFT JOIN task_timeout_outcomes AS outcome
              ON outcome.timeout_id = timeout.timeout_id
            LEFT JOIN supervised_timeout_attempts AS outcome_delivery
              ON outcome_delivery.timeout_id = outcome.timeout_id
             AND outcome_delivery.fencing_token = outcome.fencing_token
            WHERE timeout.task_id = ?
              AND NOT EXISTS (
                  SELECT 1
                  FROM worker_exit_retry_timeout_retirements AS retirement
                  WHERE retirement.timeout_id = timeout.timeout_id
              )
            ORDER BY attempt.attempt_number DESC
            LIMIT 1
            """,
            (task_id,),
        ).fetchone()
        if row is None:
            return None

        capability_proof = _decode_document(
            row["capability_proof_json"], label="timeout capability proof"
        )
        try:
            capability_proof = _validate_timeout_capability_proof(
                capability_proof,
                attempt_id=row["attempt_id"],
                binding_hash=row["binding_hash"],
                private_schema_version=(
                    "1.3.0"
                    if bool(row["is_worker_exit_retry"])
                    else (
                        "1.2.0"
                        if int(row["attempt_number"]) == 2
                        else "1.1.0"
                    )
                ),
            )
            _, capability_proof_hash = encode_document(capability_proof)
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "persisted timeout capability proof is invalid"
            ) from error
        if capability_proof_hash != row["capability_proof_hash"]:
            raise TaskStoreCorruption(
                "timeout capability proof hash does not match its content"
            )

        window = _decode_hashed_document(row, label="Worker timeout window")
        expected_window = {
            "schema_version": "1.0.0",
            "timeout_id": row["timeout_id"],
            "task_id": row["task_id"],
            "intent_id": row["intent_id"],
            "attempt_id": row["attempt_id"],
            "reason": "wall_time_exceeded",
            "wall_time_seconds": row["wall_time_seconds"],
            "start_observation_sequence": row[
                "start_observation_sequence"
            ],
            "started_at": row["started_at"],
            "deadline_at": row["deadline_at"],
            "ready_record_hash": row["ready_record_hash"],
            "running_heartbeat_record_hash": row[
                "running_heartbeat_record_hash"
            ],
            "capability_proof": capability_proof,
            "recorded_at": row["recorded_at"],
            "extensions": {},
        }
        try:
            started_at, started_at_us = _runtime_timestamp(row["started_at"])
            deadline_at, deadline_at_us = _runtime_timestamp(row["deadline_at"])
            recorded_at, recorded_at_us = _runtime_timestamp(row["recorded_at"])
        except TaskStoreConflict as error:
            raise TaskStoreCorruption("persisted timeout time is invalid") from error
        if (
            window != expected_window
            or RUNTIME_TIMEOUT_ID.fullmatch(row["timeout_id"]) is None
            or row["capability_record_hash"]
            != capability_proof["capability_record_hash"]
            or started_at != row["started_at"]
            or started_at_us != row["started_at_us"]
            or deadline_at != row["deadline_at"]
            or deadline_at_us != row["deadline_at_us"]
            or recorded_at != row["recorded_at"]
            or recorded_at_us != row["recorded_at_us"]
            or row["deadline_at_us"]
            != row["started_at_us"] + row["wall_time_seconds"] * 1_000_000
            or row["recorded_at_us"] < row["started_at_us"]
            or row["timeout_id"]
            != _runtime_timeout_id(
                intent_id=row["intent_id"],
                attempt_id=row["attempt_id"],
                start_observation_sequence=row["start_observation_sequence"],
                wall_time_seconds=row["wall_time_seconds"],
            )
        ):
            raise TaskStoreCorruption(
                "timeout window differs from its immutable index"
            )

        intent = self._load_dispatch_intent(connection, task_id=task_id)
        allow_checkpoint_waiting = (
            intent is not None
            and _is_checkpoint_capable_managed_dispatch(intent)
        )
        first_running = _first_durable_running_observation(
            connection,
            attempt_id=row["attempt_id"],
            project_id=row["project_id"],
            principal_id=row["principal_id"],
            allow_checkpoint_waiting=allow_checkpoint_waiting,
        )
        observation, observation_evidence = (
            (None, None) if first_running is None else first_running
        )
        attempt_row = connection.execute(
            "SELECT * FROM worker_launch_attempts WHERE attempt_id = ?",
            (row["attempt_id"],),
        ).fetchone()
        if observation_evidence is not None and attempt_row is not None:
            _require_worker_attempt_observation_binding(
                attempt_row,
                observation_evidence,
                intent_id=row["intent_id"],
                task_id=task_id,
                project_id=row["project_id"],
                principal_id=row["principal_id"],
            )
        heartbeat_state = (
            None
            if observation_evidence is None
            or observation_evidence["heartbeat"] is None
            else observation_evidence["heartbeat"]["state"]
        )
        start_source = (
            None
            if observation is None or observation_evidence is None
            else (
                observation_evidence["ready"]["started_at"]
                if heartbeat_state == "waiting"
                else observation["observed_at"]
            )
        )
        try:
            observation_at, observation_at_us = (
                (None, None)
                if observation is None
                else _runtime_timestamp(observation["observed_at"])
            )
            source_at, source_at_us = (
                (None, None)
                if start_source is None
                else _runtime_timestamp(start_source)
            )
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "timeout source observation time is invalid"
            ) from error
        if (
            observation is None
            or observation_evidence is None
            or attempt_row is None
            or observation_evidence["ticket"]["state"] != "spawned"
            or observation_evidence["ready"] is None
            or observation_evidence["heartbeat"] is None
            or heartbeat_state not in {"running", "waiting"}
            or (heartbeat_state == "waiting" and not allow_checkpoint_waiting)
            or type(observation["observation_sequence"]) is not int
            or observation["observation_sequence"]
            != row["start_observation_sequence"]
            or observation["ready_record_hash"] != row["ready_record_hash"]
            or observation["heartbeat_state"] != heartbeat_state
            or observation["heartbeat_record_hash"]
            != row["running_heartbeat_record_hash"]
            or observation_at != observation["observed_at"]
            or observation_at_us != observation["observed_at_us"]
            or (
                heartbeat_state != "waiting"
                and source_at != start_source
            )
            or source_at != row["started_at"]
            or source_at_us != row["started_at_us"]
            or observation_at_us < source_at_us
            or intent is None
            or intent.intent_id != row["intent_id"]
            or not _is_supported_managed_dispatch(intent)
            or intent.request.get("resources", {}).get("wall_time_seconds")
            != row["wall_time_seconds"]
        ):
            raise TaskStoreCorruption(
                "timeout window lost its exact Worker or intent binding"
            )

        authorization_rows = connection.execute(
            """
            SELECT fencing_token, authorized_at, authorized_at_us
            FROM supervised_timeout_attempts
            WHERE timeout_id = ?
            ORDER BY fencing_token ASC
            """,
            (row["timeout_id"],),
        ).fetchall()
        for authorization in authorization_rows:
            try:
                authorized_at, authorized_at_us = _runtime_timestamp(
                    authorization["authorized_at"]
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "supervised timeout authorization time is invalid"
                ) from error
            if (
                type(authorization["fencing_token"]) is not int
                or authorization["fencing_token"] < 1
                or authorized_at != authorization["authorized_at"]
                or authorized_at_us != authorization["authorized_at_us"]
                or authorized_at_us < row["deadline_at_us"]
            ):
                raise TaskStoreCorruption(
                    "supervised timeout authorization time is inconsistent"
                )
        authorization_count = len(authorization_rows)
        if authorization_count != int(row["authorization_count"]):
            raise TaskStoreCorruption(
                "supervised timeout authorization count is inconsistent"
            )
        has_cancel_request = bool(row["has_cancel_request"])
        if row["result"] is None:
            if authorization_count > 0 and has_cancel_request:
                raise TaskStoreCorruption(
                    "timeout and cancellation both own the exact stop slot"
                )
            if authorization_count > 0:
                if task_status not in {"Queued", "Running", "Waiting"}:
                    raise TaskStoreCorruption(
                        "pending timeout has an unresolved terminal task"
                    )
                state = "requested"
            elif has_cancel_request:
                state = "suppressed"
            elif task_status in {"Queued", "Running", "Waiting"}:
                state = "armed"
            elif task_status in {"Succeeded", "Failed"}:
                state = "not_triggered"
            elif task_status == "Cancelled":
                raise TaskStoreCorruption(
                    "timeout window was cancelled without a cancel request"
                )
            else:
                raise TaskStoreCorruption(
                    "timeout window has an incompatible task status"
                )
            return TaskTimeoutSnapshot(
                timeout_id=row["timeout_id"],
                task_id=row["task_id"],
                intent_id=row["intent_id"],
                attempt_id=row["attempt_id"],
                wall_time_seconds=row["wall_time_seconds"],
                started_at=row["started_at"],
                deadline_at=row["deadline_at"],
                state=state,
            )

        if authorization_count < 1 or has_cancel_request:
            raise TaskStoreCorruption(
                "timeout outcome lacks exclusive delivery authorization"
            )
        try:
            resolved_at, resolved_at_us = _runtime_timestamp(
                row["resolved_at"]
            )
            authorized_at, authorized_at_us = _runtime_timestamp(
                row["outcome_authorized_at"]
            )
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "task timeout outcome causal time is invalid"
            ) from error
        if (
            resolved_at != row["resolved_at"]
            or resolved_at_us != row["outcome_resolved_at_us"]
            or authorized_at != row["outcome_authorized_at"]
            or authorized_at_us != row["outcome_authorized_at_us"]
            or type(row["outcome_fencing_token"]) is not int
            or row["outcome_fencing_token"] < 1
            or row["outcome_authorized_fencing_token"]
            != row["outcome_fencing_token"]
            or resolved_at_us < authorized_at_us
            or authorized_at_us < row["deadline_at_us"]
        ):
            raise TaskStoreCorruption(
                "task timeout outcome violates its causal order"
            )
        base_timeout = TaskTimeoutSnapshot(
            timeout_id=row["timeout_id"],
            task_id=row["task_id"],
            intent_id=row["intent_id"],
            attempt_id=row["attempt_id"],
            wall_time_seconds=row["wall_time_seconds"],
            started_at=row["started_at"],
            deadline_at=row["deadline_at"],
            state="requested",
        )
        try:
            adapter_proof = _decode_document(
                row["adapter_proof_json"], label="task timeout Adapter proof"
            )
            _, adapter_proof_hash = encode_document(adapter_proof)
            adapter_proof = _validate_timeout_adapter_proof(
                adapter_proof,
                timeout=base_timeout,
                capability_record_hash=row["capability_record_hash"],
                ready_record_hash=row["ready_record_hash"],
                result=row["result"],
                terminal_status=row["terminal_status"],
            )
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "task timeout Adapter proof is invalid"
            ) from error
        if adapter_proof_hash != row["adapter_proof_hash"]:
            raise TaskStoreCorruption(
                "task timeout Adapter proof hash does not match its content"
            )
        outcome = _decode_hashed_document(
            {
                "document_json": row["outcome_json"],
                "document_hash": row["outcome_hash"],
            },
            label="task timeout outcome",
        )
        expected_state = {
            "timeout_confirmed": ("Failed", "WALL_TIME_EXCEEDED", "timed_out"),
            "terminal_preempted": (row["terminal_status"], None, "superseded"),
        }.get(row["result"])
        if (
            expected_state is None
            or row["terminal_status"] != expected_state[0]
            or row["failure_code"] != expected_state[1]
            or task_status != row["terminal_status"]
            or outcome
            != {
                "schema_version": "1.0.0",
                "request_id": row["timeout_id"],
                "task_id": row["task_id"],
                "result": row["result"],
                "terminal_status": row["terminal_status"],
                "failure_code": row["failure_code"],
                "adapter_proof": adapter_proof,
                "resolved_at": row["resolved_at"],
                "extensions": {},
            }
        ):
            raise TaskStoreCorruption(
                "task timeout outcome differs from its immutable index"
            )
        terminal = connection.execute(
            """
            SELECT task_id, sequence, event_id, event_type, task_status,
                   node_id, fingerprint_hash, occurred_at,
                   document_json, document_hash
            FROM run_events WHERE task_id = ? AND sequence = ?
            """,
            (task_id, row["terminal_event_sequence"]),
        ).fetchone()
        if terminal is None or terminal["task_status"] != row["terminal_status"]:
            raise TaskStoreCorruption(
                "task timeout outcome lacks its exact terminal event"
            )
        terminal_event = _decode_run_event_row(
            terminal, label="task timeout terminal event"
        )
        if (
            intent.handle is None
            or terminal_event.get("node_id") != intent.node_id
            or terminal_event.get("fingerprint")
            != intent.handle["fingerprint"]
        ):
            raise TaskStoreCorruption(
                "task timeout terminal event lost its dispatch binding"
            )
        if row["result"] == "timeout_confirmed":
            try:
                _, event_occurred_at_us = _runtime_timestamp(
                    terminal_event.get("occurred_at")
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "task timeout terminal event time is invalid"
                ) from error
            expected_extension = {
                "timeout_id": row["timeout_id"],
                "attempt_id": row["attempt_id"],
                "wall_time_seconds": row["wall_time_seconds"],
                "started_at": row["started_at"],
                "deadline_at": row["deadline_at"],
                "failure_code": "WALL_TIME_EXCEEDED",
                "proof_hash": adapter_proof["proof_hash"],
            }
            error = terminal_event.get("error")
            valid_terminal = (
                terminal["event_type"] == "node_failed"
                and isinstance(error, Mapping)
                and error.get("code") == "wall_time_exceeded"
                and error.get("message")
                == "FWI Worker exceeded its wall-time limit"
                and error.get("retryable") is False
                and terminal_event.get("extensions")
                == {"org.agent_rpc.timeout": expected_extension}
                and event_occurred_at_us >= row["deadline_at_us"]
                and event_occurred_at_us >= authorized_at_us
                and event_occurred_at_us <= resolved_at_us
            )
        else:
            try:
                _validate_timeout_natural_terminal_event(
                    terminal_event,
                    receipt_job_id=intent.handle["job_id"],
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "task timeout outcome terminal event is inconsistent"
                ) from error
            valid_terminal = True
        if not valid_terminal:
            raise TaskStoreCorruption(
                "task timeout outcome terminal event is inconsistent"
            )
        return TaskTimeoutSnapshot(
            timeout_id=row["timeout_id"],
            task_id=row["task_id"],
            intent_id=row["intent_id"],
            attempt_id=row["attempt_id"],
            wall_time_seconds=row["wall_time_seconds"],
            started_at=row["started_at"],
            deadline_at=row["deadline_at"],
            state=expected_state[2],
            result=row["result"],
            failure_code=row["failure_code"],
            terminal_status=row["terminal_status"],
            resolved_at=row["resolved_at"],
            adapter_proof=adapter_proof,
        )

    def _load_task_checkpoint(
        self,
        connection: sqlite3.Connection,
        task_id: str,
        *,
        task_status: str,
        checkpoint_id: str | None = None,
    ) -> TaskCheckpointSnapshot | None:
        """Decode the latest or one exact checkpoint without private paths."""

        query = """
            SELECT checkpoint.*,
                   request.resume_id, request.document_json AS request_json,
                   request.document_hash AS request_document_hash,
                   request.requested_at,
                   outcome.adapter_proof_json AS resumed_proof_json,
                   outcome.adapter_proof_hash AS resumed_proof_hash,
                   outcome.resume_acknowledged_at,
                   outcome.running_event_sequence
            FROM worker_checkpoint_waits AS checkpoint
            LEFT JOIN task_checkpoint_resume_requests AS request
              ON request.checkpoint_id = checkpoint.checkpoint_id
            LEFT JOIN task_checkpoint_resume_outcomes AS outcome
              ON outcome.resume_id = request.resume_id
            WHERE checkpoint.task_id = ?
        """
        parameters: tuple[Any, ...] = (task_id,)
        if checkpoint_id is None:
            query += " ORDER BY checkpoint.waiting_event_sequence DESC LIMIT 1"
        else:
            query += " AND checkpoint.checkpoint_id = ? LIMIT 1"
            parameters = (task_id, checkpoint_id)
        row = connection.execute(query, parameters).fetchone()
        if row is None:
            if checkpoint_id is None and task_status == "Waiting":
                raise TaskStoreCorruption(
                    "Waiting task lacks an exact checkpoint"
                )
            return None
        try:
            waiting_proof = _decode_document(
                row["proof_json"], label="checkpoint Adapter proof"
            )
            waiting_proof = _validate_checkpoint_adapter_proof(
                waiting_proof,
                state="waiting",
                task_id=task_id,
                node_id=row["node_id"],
                submission_id=row["submission_id"],
                attempt_id=row["attempt_id"],
                attempt_number=row["attempt_number"],
                checkpoint_id=row["checkpoint_id"],
            )
            created_at, created_at_us = _runtime_timestamp(
                row["checkpoint_created_at"]
            )
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "checkpoint Adapter proof is invalid"
            ) from error
        indexed = {
            "node_id": row["node_id"],
            "submission_id": row["submission_id"],
            "attempt_id": row["attempt_id"],
            "attempt_number": row["attempt_number"],
            "checkpoint_index": row["checkpoint_index"],
            "completed_updates": row["completed_updates"],
            "checkpoint_manifest_relative_path": row[
                "checkpoint_manifest_relative_path"
            ],
            "checkpoint_manifest_size_bytes": row[
                "checkpoint_manifest_size_bytes"
            ],
            "binding_hash": row["binding_hash"],
            "submission_receipt_record_hash": row[
                "submission_receipt_record_hash"
            ],
            "ready_record_hash": row["ready_record_hash"],
            "checkpoint_manifest_hash": row["checkpoint_manifest_hash"],
            "checkpoint_receipt_record_hash": row[
                "checkpoint_receipt_record_hash"
            ],
            "checkpoint_created_at": row["checkpoint_created_at"],
        }
        if (
            waiting_proof["proof_hash"] != row["proof_hash"]
            or created_at != row["checkpoint_created_at"]
            or created_at_us != row["checkpoint_created_at_us"]
            or any(waiting_proof.get(key) != value for key, value in indexed.items())
        ):
            raise TaskStoreCorruption(
                "checkpoint proof differs from its immutable index"
            )

        resume_id = row["resume_id"]
        state = "waiting"
        requested_at = None
        acknowledged_at = None
        if resume_id is not None:
            try:
                request = _decode_document(
                    row["request_json"], label="checkpoint resume request"
                )
                _, request_document_hash = encode_document(request)
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "checkpoint resume request is invalid"
                ) from error
            if (
                RUNTIME_RESUME_ID.fullmatch(resume_id) is None
                or request_document_hash != row["request_document_hash"]
                or request
                != {
                    "schema_version": "1.0.0",
                    "resume_id": resume_id,
                    "task_id": task_id,
                    "checkpoint_id": row["checkpoint_id"],
                    "action": "resume_exact_checkpoint",
                    "requested_at": row["requested_at"],
                    "extensions": {},
                }
            ):
                raise TaskStoreCorruption(
                    "checkpoint resume request differs from its index"
                )
            requested_at = row["requested_at"]
            state = "resume_requested"
        if row["resumed_proof_json"] is not None:
            try:
                resumed_proof = _decode_document(
                    row["resumed_proof_json"],
                    label="checkpoint resume Adapter proof",
                )
                authorization = connection.execute(
                    """
                    SELECT authorization_json
                    FROM supervised_checkpoint_resume_authorizations AS auth
                    JOIN task_checkpoint_resume_outcomes AS outcome
                      ON outcome.resume_id = auth.resume_id
                     AND outcome.fencing_token = auth.fencing_token
                    WHERE outcome.resume_id = ?
                    """,
                    (resume_id,),
                ).fetchone()
                if authorization is None:
                    raise TaskStoreCorruption(
                        "checkpoint resume outcome lacks authorization"
                    )
                token = _decode_document(
                    authorization["authorization_json"],
                    label="checkpoint resume authorization",
                )
                resumed_proof = _validate_checkpoint_adapter_proof(
                    resumed_proof,
                    state="resumed",
                    task_id=task_id,
                    node_id=row["node_id"],
                    submission_id=row["submission_id"],
                    attempt_id=row["attempt_id"],
                    attempt_number=row["attempt_number"],
                    checkpoint_id=row["checkpoint_id"],
                    checkpoint_proof_hash=row["proof_hash"],
                    resume_id=resume_id,
                    resume_request_record_hash=token.get(
                        "resume_request_record_hash"
                    ),
                )
                _, resumed_hash = encode_document(resumed_proof)
                acknowledged_at, _ = _runtime_timestamp(
                    row["resume_acknowledged_at"]
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "checkpoint resume Adapter proof is invalid"
                ) from error
            if (
                resumed_hash != row["resumed_proof_hash"]
                or acknowledged_at != resumed_proof["resume_acknowledged_at"]
            ):
                raise TaskStoreCorruption(
                    "checkpoint resume proof differs from its index"
                )
            state = "resumed"
        elif task_status not in {"Waiting"}:
            state = "superseded"
        if checkpoint_id is None and task_status == "Waiting" and state not in {
            "waiting",
            "resume_requested",
        }:
            raise TaskStoreCorruption(
                "Waiting task checkpoint lifecycle is inconsistent"
            )
        if (
            checkpoint_id is None
            and task_status == "Running"
            and state not in {"resumed"}
        ):
            raise TaskStoreCorruption(
                "Running task has an unresolved latest checkpoint"
            )
        return TaskCheckpointSnapshot(
            checkpoint_id=row["checkpoint_id"],
            intent_id=row["intent_id"],
            node_id=row["node_id"],
            submission_id=row["submission_id"],
            attempt_id=row["attempt_id"],
            attempt_number=row["attempt_number"],
            checkpoint_index=row["checkpoint_index"],
            completed_updates=row["completed_updates"],
            checkpoint_manifest_relative_path=row[
                "checkpoint_manifest_relative_path"
            ],
            checkpoint_manifest_size_bytes=row[
                "checkpoint_manifest_size_bytes"
            ],
            checkpoint_manifest_hash=row["checkpoint_manifest_hash"],
            checkpoint_receipt_record_hash=row[
                "checkpoint_receipt_record_hash"
            ],
            checkpoint_created_at=row["checkpoint_created_at"],
            state=state,
            resume_id=resume_id,
            resume_requested_at=requested_at,
            resume_acknowledged_at=acknowledged_at,
        )

    def _load_snapshot(
        self, connection: sqlite3.Connection, task_id: str
    ) -> TaskSnapshot | None:
        row = connection.execute(
            "SELECT * FROM tasks WHERE task_id = ?", (task_id,)
        ).fetchone()
        if row is None:
            return None
        cancellation = self._load_task_cancellation(connection, task_id)
        timeout = self._load_task_timeout(
            connection, task_id, task_status=row["status"]
        )
        checkpoint = self._load_task_checkpoint(
            connection, task_id, task_status=row["status"]
        )
        if cancellation is not None:
            if cancellation.state == "requested":
                pending_status_valid = row["status"] in {
                    "Queued",
                    "Running",
                    "Waiting",
                }
                if not pending_status_valid and row["status"] in {
                    "Succeeded",
                    "Failed",
                }:
                    dag_winner = self._load_exact_dag_cancel_terminal_winner(
                        connection,
                        task_id=task_id,
                        intent_id=cancellation.intent_id,
                        attempt_id=cancellation.attempt_id,
                    )
                    latest_sequence = connection.execute(
                        "SELECT COALESCE(MAX(sequence), 0) FROM run_events "
                        "WHERE task_id = ?",
                        (task_id,),
                    ).fetchone()[0]
                    pending_status_valid = (
                        dag_winner is not None
                        and dag_winner["aggregate_status"] == row["status"]
                        and dag_winner["event_sequence"] == latest_sequence
                    )
                if not pending_status_valid:
                    raise TaskStoreCorruption(
                        "pending cancellation has an unresolved terminal task"
                    )
            if (
                cancellation.state == "cancelled"
                and row["status"] != "Cancelled"
            ):
                raise TaskStoreCorruption(
                    "confirmed cancellation differs from task status"
                )
            if (
                cancellation.state == "superseded"
                and row["status"] != cancellation.terminal_status
            ):
                raise TaskStoreCorruption(
                    "superseded cancellation differs from task status"
                )
        visibility_revision, trashed_at = self._load_task_visibility(
            connection, row
        )
        (
            purge_id,
            purge_requested_at,
            purged_at,
            purge_local_run_state,
            _,
        ) = self._load_task_purge(
            connection,
            row,
            visibility_revision=visibility_revision,
            trashed_at=trashed_at,
        )
        abandonment_row = connection.execute(
            """
            SELECT task_id, project_id, principal_id,
                   document_json, document_hash, abandoned_at
            FROM task_abandonments WHERE task_id = ?
            """,
            (task_id,),
        ).fetchone()
        abandonment = None
        if abandonment_row is not None:
            abandonment = _decode_hashed_document(
                abandonment_row, label="task abandonment"
            )
            actor = abandonment.get("actor")
            if (
                set(abandonment)
                != {
                    "schema_version",
                    "task_id",
                    "previous_status",
                    "status",
                    "reason",
                    "actor",
                    "abandoned_at",
                    "extensions",
                }
                or abandonment.get("schema_version") != "1.0.0"
                or abandonment.get("task_id") != row["task_id"]
                or abandonment.get("previous_status")
                not in {"Draft", "NeedsInput", "AwaitingApproval"}
                or abandonment.get("status") != "Cancelled"
                or abandonment.get("reason") != "user_discarded_draft"
                or actor
                != {"type": "user", "id": abandonment_row["principal_id"]}
                or abandonment.get("abandoned_at")
                != abandonment_row["abandoned_at"]
                or abandonment.get("extensions") != {}
                or abandonment_row["project_id"] != row["project_id"]
                or abandonment_row["principal_id"] != row["principal_id"]
            ):
                raise TaskStoreCorruption(
                    "task abandonment differs from its immutable index"
                )
        pre_runtime_abandoned = row["status"] == "Cancelled" and abandonment is not None
        runtime_cancelled = (
            row["status"] == "Cancelled"
            and cancellation is not None
            and cancellation.result == "cancel_confirmed"
        )
        if abandonment is not None and runtime_cancelled:
            raise TaskStoreCorruption(
                "task cannot be both abandoned and runtime-cancelled"
            )
        if row["status"] == "Cancelled" and not (
            pre_runtime_abandoned or runtime_cancelled
        ):
            raise TaskStoreCorruption(
                "Cancelled task lacks abandonment or runtime cancellation evidence"
            )
        if row["status"] != "Cancelled" and abandonment is not None:
            raise TaskStoreCorruption(
                "task abandonment does not match the Cancelled state"
            )
        if row["current_draft_id"] is None or row["current_draft_revision"] is None:
            raise TaskStoreCorruption("task has no current draft revision")
        draft_row = connection.execute(
            """
            SELECT draft_id, revision, document_json, document_hash
            FROM draft_revisions
            WHERE task_id = ? AND draft_id = ? AND revision = ?
            """,
            (
                task_id,
                row["current_draft_id"],
                row["current_draft_revision"],
            ),
        ).fetchone()
        if draft_row is None:
            raise TaskStoreCorruption("current draft revision is missing")
        draft = _decode_hashed_document(draft_row, label="draft")
        if (
            draft.get("draft_id") != draft_row["draft_id"]
            or draft.get("revision") != draft_row["revision"]
        ):
            raise TaskStoreCorruption("persisted draft identity does not match its index")
        if row["status"] in {"Draft", "NeedsInput", "AwaitingApproval"}:
            if draft.get("status") != row["status"]:
                raise TaskStoreCorruption(
                    "task status does not match its current pre-runtime draft"
                )
        elif pre_runtime_abandoned:
            if draft.get("status") != abandonment.get("previous_status"):
                raise TaskStoreCorruption(
                    "abandoned task does not retain its final pre-runtime draft"
                )
        elif draft.get("status") != "AwaitingApproval":
            raise TaskStoreCorruption(
                "runtime task does not retain an AwaitingApproval draft"
            )
        plan = None
        if row["current_plan_id"] is not None:
            plan_row = connection.execute(
                """
                SELECT plan_id, draft_id, draft_revision, plan_hash,
                       document_json, document_hash
                FROM plans WHERE task_id = ? AND plan_id = ?
                """,
                (task_id, row["current_plan_id"]),
            ).fetchone()
            if plan_row is None:
                raise TaskStoreCorruption("current plan is missing")
            plan = _decode_hashed_document(plan_row, label="plan")
            if (
                plan.get("plan_id") != plan_row["plan_id"]
                or plan.get("plan_hash") != plan_row["plan_hash"]
                or plan.get("draft")
                != {
                    "draft_id": plan_row["draft_id"],
                    "revision": plan_row["draft_revision"],
                }
            ):
                raise TaskStoreCorruption(
                    "persisted plan identity does not match its index"
                )
            if plan.get("draft") != {
                "draft_id": row["current_draft_id"],
                "revision": row["current_draft_revision"],
            }:
                raise TaskStoreCorruption(
                    "current plan does not bind the current draft revision"
                )
        approval = None
        if row["current_approval_id"] is not None:
            approval_row = connection.execute(
                """
                SELECT approval_id, plan_id, plan_hash, decision,
                       document_json, document_hash
                FROM approvals
                WHERE task_id = ? AND approval_id = ?
                """,
                (task_id, row["current_approval_id"]),
            ).fetchone()
            if approval_row is None:
                raise TaskStoreCorruption("current approval is missing")
            approval = _decode_hashed_document(approval_row, label="approval")
            if (
                approval.get("approval_id") != approval_row["approval_id"]
                or approval.get("plan_id") != approval_row["plan_id"]
                or approval.get("plan_hash") != approval_row["plan_hash"]
                or approval.get("decision") != approval_row["decision"]
            ):
                raise TaskStoreCorruption(
                    "persisted approval identity does not match its index"
                )
            if (
                plan is None
                or approval.get("plan_id") != plan.get("plan_id")
                or approval.get("plan_hash") != plan.get("plan_hash")
            ):
                raise TaskStoreCorruption(
                    "current approval does not bind the current plan"
                )
            budget = connection.execute(
                """
                SELECT max_tasks, tasks_used
                FROM approval_budgets
                WHERE task_id = ? AND approval_id = ?
                """,
                (task_id, approval_row["approval_id"]),
            ).fetchone()
            approval_scope = approval.get("scope")
            if (
                budget is None
                or not isinstance(approval_scope, dict)
                or type(budget["max_tasks"]) is not int
                or type(budget["tasks_used"]) is not int
                or budget["max_tasks"] != approval_scope.get("max_tasks")
                or budget["tasks_used"] < 0
                or budget["tasks_used"] > budget["max_tasks"]
            ):
                raise TaskStoreCorruption(
                    "current approval budget does not match its decision"
                )
        if row["status"] in {
            "Queued",
            "Running",
            "Waiting",
            "Retrying",
            "Succeeded",
            "Failed",
        } or runtime_cancelled:
            if (
                plan is None
                or approval is None
                or approval.get("decision") != "approved"
            ):
                raise TaskStoreCorruption(
                    "submitted task lacks its approved current plan"
                )
        event_summary = connection.execute(
            """
            SELECT COUNT(*) AS event_count,
                   COALESCE(MIN(sequence), 0) AS first_sequence,
                   COALESCE(MAX(sequence), 0) AS last_sequence
            FROM run_events WHERE task_id = ?
            """,
            (task_id,),
        ).fetchone()
        runtime_status = (
            row["status"] not in {"Draft", "NeedsInput", "AwaitingApproval"}
            and not pre_runtime_abandoned
        )
        if not runtime_status and event_summary["event_count"] != 0:
            raise TaskStoreCorruption("pre-runtime task unexpectedly has run events")
        intent_bindings = connection.execute(
            """
            SELECT intent_id, plan_id, plan_hash, approval_id
            FROM dispatch_intents WHERE task_id = ? ORDER BY intent_id
            """,
            (task_id,),
        ).fetchall()
        has_cache_hit_table = connection.execute(
            "SELECT 1 FROM sqlite_master WHERE type = 'table' "
            "AND name = 'dag_node_cache_hit_facts'"
        ).fetchone() is not None
        cache_hit_bindings = (
            connection.execute(
                """
                SELECT target_plan_id, target_plan_hash, target_approval_id,
                       event_sequence, event_hash, cache_hit_id
                FROM dag_node_cache_hit_facts
                WHERE target_task_id = ? ORDER BY event_sequence ASC
                """,
                (task_id,),
            ).fetchall()
            if has_cache_hit_table
            else []
        )
        if not runtime_status and (intent_bindings or cache_hit_bindings):
            raise TaskStoreCorruption(
                "pre-runtime task unexpectedly has a dispatch intent"
            )
        if runtime_status:
            if (
                (not intent_bindings and not cache_hit_bindings)
                or plan is None
                or approval is None
                or any(
                    binding["plan_id"] != plan.get("plan_id")
                    or binding["plan_hash"] != plan.get("plan_hash")
                    or binding["approval_id"] != approval.get("approval_id")
                    for binding in intent_bindings
                )
                or any(
                    binding["target_plan_id"] != plan.get("plan_id")
                    or binding["target_plan_hash"] != plan.get("plan_hash")
                    or binding["target_approval_id"]
                    != approval.get("approval_id")
                    for binding in cache_hit_bindings
                )
            ):
                raise TaskStoreCorruption(
                    "runtime task lacks its current dispatch intent"
                )
            if (
                event_summary["event_count"] == 0
                or event_summary["first_sequence"] != 1
                or event_summary["event_count"] != event_summary["last_sequence"]
            ):
                raise TaskStoreCorruption(
                    "runtime task does not have a contiguous event history"
                )
            first_event = connection.execute(
                """
                SELECT event_type, task_status FROM run_events
                WHERE task_id = ? ORDER BY sequence ASC LIMIT 1
                """,
                (task_id,),
            ).fetchone()
            latest_event = connection.execute(
                """
                SELECT task_status FROM run_events
                WHERE task_id = ? ORDER BY sequence DESC LIMIT 1
                """,
                (task_id,),
            ).fetchone()
            cache_started = (
                bool(cache_hit_bindings)
                and cache_hit_bindings[0]["event_sequence"] == 1
            )
            if not (
                (
                    first_event["event_type"] == "task_queued"
                    and first_event["task_status"] == "Queued"
                    and not cache_started
                )
                or (
                    first_event["event_type"] == "node_succeeded"
                    and first_event["task_status"] in {"Running", "Succeeded"}
                    and cache_started
                )
            ):
                raise TaskStoreCorruption(
                    "runtime event history has no exact execution-start fact"
                )
            if latest_event["task_status"] != row["status"]:
                raise TaskStoreCorruption(
                    "task status does not match its latest run event"
                )
            for binding in intent_bindings:
                if self._load_dispatch_intent(
                    connection, intent_id=binding["intent_id"]
                ) is None:
                    raise TaskStoreCorruption(
                        "runtime task dispatch intent cannot be decoded"
                    )
        return TaskSnapshot(
            task_id=row["task_id"],
            project_id=row["project_id"],
            principal_id=row["principal_id"],
            status=row["status"],
            draft=draft,
            plan=plan,
            approval=approval,
            created_at=row["created_at"],
            updated_at=row["updated_at"],
            abandonment=abandonment,
            visibility_revision=visibility_revision,
            trashed_at=trashed_at,
            purge_id=purge_id,
            purge_requested_at=purge_requested_at,
            purged_at=purged_at,
            purge_local_run_state=purge_local_run_state,
            cancellation=cancellation,
            timeout=timeout,
            checkpoint=checkpoint,
        )

    def _load_task_purge_record(
        self,
        connection: sqlite3.Connection,
        *,
        task_id: str,
        replayed: bool,
    ) -> TaskPurgeRecord:
        snapshot = self._load_snapshot(connection, task_id)
        if snapshot is None or snapshot.purge_id is None:
            raise TaskStoreCorruption("task purge reservation cannot be read")
        outcome = None
        if snapshot.purged_at is not None:
            outcome_row = connection.execute(
                """
                SELECT document_json, document_hash
                FROM task_purge_outcomes WHERE purge_id = ?
                """,
                (snapshot.purge_id,),
            ).fetchone()
            if outcome_row is None:
                raise TaskStoreCorruption("task purge outcome cannot be read")
            outcome = _decode_hashed_document(
                outcome_row, label="task purge outcome"
            )
        return TaskPurgeRecord(
            snapshot=snapshot,
            purge_id=snapshot.purge_id,
            outcome=outcome,
            replayed=replayed,
        )

    @staticmethod
    def _select_task_dispatch_intent_id(
        connection: sqlite3.Connection, *, task_id: str
    ) -> str | None:
        """Select the unique active DAG intent, or its latest durable history."""

        rows = connection.execute(
            """
            SELECT intent.intent_id, admission.admitted_at_us,
                   admission.intent_id AS admission_intent_id,
                   state.state AS latest_node_state
            FROM dispatch_intents AS intent
            LEFT JOIN dag_node_execution_admissions AS admission
              ON admission.intent_id = intent.intent_id
            LEFT JOIN dag_node_state_events AS state
              ON state.task_id = admission.task_id
             AND state.plan_id = admission.plan_id
             AND state.node_id = admission.node_id
             AND NOT EXISTS (
                 SELECT 1 FROM dag_node_state_events AS later
                 WHERE later.task_id = state.task_id
                   AND later.plan_id = state.plan_id
                   AND later.node_id = state.node_id
                   AND later.revision > state.revision
             )
            WHERE intent.task_id = ?
            ORDER BY admission.admitted_at_us ASC, intent.intent_id ASC
            """,
            (task_id,),
        ).fetchall()
        if not rows:
            return None
        if len(rows) == 1:
            return str(rows[0]["intent_id"])
        if any(row["admission_intent_id"] is None for row in rows):
            raise TaskStoreCorruption(
                "multiple dispatch intents lack exact DAG admissions"
            )
        active = [
            row
            for row in rows
            if row["latest_node_state"]
            in {"Queued", "Running", "Waiting", "Retrying"}
        ]
        if len(active) > 1:
            raise TaskStoreCorruption("DAG task has multiple active dispatch intents")
        if active:
            return str(active[0]["intent_id"])
        return str(rows[-1]["intent_id"])

    def _load_dispatch_intent(
        self,
        connection: sqlite3.Connection,
        *,
        task_id: str | None = None,
        intent_id: str | None = None,
    ) -> DispatchIntentSnapshot | None:
        if (task_id is None) == (intent_id is None):
            raise TaskStoreCorruption(
                "dispatch intent loader requires one exact selector"
            )
        if intent_id is None:
            assert task_id is not None
            intent_id = self._select_task_dispatch_intent_id(
                connection, task_id=task_id
            )
            if intent_id is None:
                return None
        row = connection.execute(
            """
            SELECT intent.intent_id, intent.task_id, intent.plan_id,
                   intent.plan_hash, intent.approval_id, intent.node_id,
                   intent.node_idempotency_key, intent.adapter_id,
                   intent.adapter_version, intent.request_json AS document_json,
                   intent.request_hash AS document_hash,
                   intent.fingerprint_hash, intent.created_at,
                   attempt.claimed_at AS dispatch_claimed_at,
                   outcome.outcome, outcome.document_json AS outcome_json,
                   outcome.document_hash AS outcome_hash,
                   outcome.recorded_at AS outcome_recorded_at,
                   resolution.result AS reconciliation_result,
                   resolution.source_outcome_hash AS resolution_source_hash,
                   resolution.evidence_kind AS resolution_evidence_kind,
                   resolution.handle_json AS resolution_handle_json,
                   resolution.handle_hash AS resolution_handle_hash,
                   resolution.evidence_record_hash,
                   resolution.attempt_id AS resolution_attempt_id,
                   resolution.observation_sequence
                       AS resolution_observation_sequence,
                   resolution.private_schema_version
                       AS resolution_private_schema_version,
                   resolution.receipt_record_hash
                       AS resolution_receipt_record_hash,
                   resolution.effective_outcome_json,
                   resolution.effective_outcome_hash,
                   resolution.document_json AS resolution_json,
                   resolution.document_hash AS resolution_hash,
                   resolution.fencing_token AS resolution_fencing_token,
                   resolution.resolved_at,
                   resolution.resolved_at_us,
                   exhaustion.attempt_number AS exhaustion_attempt_number,
                   exhaustion.terminal_event_sequence
                       AS exhaustion_terminal_event_sequence,
                   exhaustion.terminal_event_hash
                       AS exhaustion_terminal_event_hash,
                   exhaustion.exhausted_at AS exhaustion_recorded_at
            FROM dispatch_intents AS intent
            LEFT JOIN dispatch_attempts AS attempt
              ON attempt.intent_id = intent.intent_id
            LEFT JOIN dispatch_outcomes AS outcome
              ON outcome.intent_id = intent.intent_id
            LEFT JOIN dispatch_reconciliation_resolutions AS resolution
              ON resolution.intent_id = intent.intent_id
            LEFT JOIN worker_retry_exhaustions AS exhaustion
              ON exhaustion.intent_id = intent.intent_id
             AND exhaustion.attempt_number = 2
            WHERE intent.intent_id = ?
            """,
            (intent_id,),
        ).fetchone()
        if row is None:
            return None
        document = _decode_hashed_document(row, label="dispatch intent")
        required = {
            "schema_version",
            "intent_id",
            "task_id",
            "plan_id",
            "plan_hash",
            "approval_id",
            "node_id",
            "node_idempotency_key",
            "adapter",
            "request",
            "queue_fingerprint",
            "created_at",
        }
        if set(document) != required:
            raise TaskStoreCorruption("dispatch intent fields are inconsistent")
        adapter = document.get("adapter")
        request = document.get("request")
        fingerprint = document.get("queue_fingerprint")
        indexed = {
            "intent_id": row["intent_id"],
            "task_id": row["task_id"],
            "plan_id": row["plan_id"],
            "plan_hash": row["plan_hash"],
            "approval_id": row["approval_id"],
            "node_id": row["node_id"],
            "node_idempotency_key": row["node_idempotency_key"],
            "created_at": row["created_at"],
        }
        if (
            document.get("schema_version") != "1.0.0"
            or any(document.get(key) != value for key, value in indexed.items())
            or not isinstance(adapter, dict)
            or set(adapter) != {"id", "version"}
            or adapter.get("id") != row["adapter_id"]
            or adapter.get("version") != row["adapter_version"]
            or not isinstance(request, dict)
            or not isinstance(fingerprint, dict)
        ):
            raise TaskStoreCorruption("dispatch intent identity is inconsistent")
        request_fields = {
            "task_id",
            "node_id",
            "plan_hash",
            "idempotency_key",
            "project_id",
            "principal_id",
            "algorithm",
            "dataset",
            "task_type",
            "parameters",
            "resources",
            "normalized_config_hash",
        }
        task_identity = connection.execute(
            """
            SELECT task.project_id, task.principal_id, task.status,
                   task.current_plan_id, task.current_approval_id,
                   plan.plan_hash AS durable_plan_hash,
                   node.idempotency_key AS durable_node_key
            FROM tasks AS task
            JOIN plans AS plan
              ON plan.task_id = task.task_id AND plan.plan_id = ?
            JOIN plan_node_idempotency AS node
              ON node.task_id = task.task_id
             AND node.plan_id = plan.plan_id AND node.node_id = ?
            WHERE task.task_id = ?
            """,
            (row["plan_id"], row["node_id"], row["task_id"]),
        ).fetchone()
        if (
            set(request) != request_fields
            or task_identity is None
            or task_identity["current_plan_id"] != row["plan_id"]
            or task_identity["current_approval_id"] != row["approval_id"]
            or task_identity["durable_plan_hash"] != row["plan_hash"]
            or task_identity["durable_node_key"] != row["node_idempotency_key"]
            or request.get("task_id") != row["task_id"]
            or request.get("node_id") != row["node_id"]
            or request.get("plan_hash") != row["plan_hash"]
            or request.get("idempotency_key") != row["node_idempotency_key"]
            or request.get("project_id") != task_identity["project_id"]
            or request.get("principal_id") != task_identity["principal_id"]
        ):
            raise TaskStoreCorruption(
                "dispatch intent request differs from durable task state"
            )
        documents = connection.execute(
            """
            SELECT draft.document_json AS draft_json,
                   draft.document_hash AS draft_hash,
                   plan.document_json AS plan_json,
                   plan.document_hash AS plan_document_hash,
                   queued.document_json AS queued_json,
                   queued.document_hash AS queued_hash,
                   queued.fingerprint_hash AS queued_fingerprint_hash,
                   queued.event_type AS queued_type,
                   queued.task_status AS queued_status,
                   queued.node_id AS queued_node_id
            FROM tasks AS task
            JOIN draft_revisions AS draft
              ON draft.task_id = task.task_id
             AND draft.draft_id = task.current_draft_id
             AND draft.revision = task.current_draft_revision
            JOIN plans AS plan
              ON plan.task_id = task.task_id
             AND plan.plan_id = task.current_plan_id
            JOIN run_events AS queued
              ON queued.task_id = task.task_id AND queued.sequence = 1
            WHERE task.task_id = ?
            """,
            (row["task_id"],),
        ).fetchone()
        if documents is None:
            raise TaskStoreCorruption(
                "dispatch intent lacks its durable draft, plan, or queued event"
            )
        draft = _decode_hashed_document(
            {
                "document_json": documents["draft_json"],
                "document_hash": documents["draft_hash"],
            },
            label="dispatch draft",
        )
        plan = _decode_hashed_document(
            {
                "document_json": documents["plan_json"],
                "document_hash": documents["plan_document_hash"],
            },
            label="dispatch plan",
        )
        queued_event = _decode_hashed_document(
            {
                "document_json": documents["queued_json"],
                "document_hash": documents["queued_hash"],
            },
            label="queued event",
        )
        dag_admission = self._load_dag_node_execution_admission(
            connection, intent_id=row["intent_id"]
        )
        dag_run = connection.execute(
            """
            SELECT first_intent_id FROM dag_task_execution_runs
            WHERE task_id = ?
            """,
            (row["task_id"],),
        ).fetchone()
        if dag_admission is not None and dag_run is None:
            raise TaskStoreCorruption("DAG dispatch intent lacks its Task run anchor")
        first_dag_intent = (
            dag_admission is not None
            and dag_run is not None
            and dag_run["first_intent_id"] == row["intent_id"]
        )
        has_cache_hit_table = connection.execute(
            "SELECT 1 FROM sqlite_master WHERE type = 'table' "
            "AND name = 'dag_node_cache_hit_facts'"
        ).fetchone() is not None
        cache_start = (
            connection.execute(
                """
                SELECT cache_hit_id, event_sequence, event_hash
                FROM dag_node_cache_hit_facts
                WHERE target_task_id = ?
                ORDER BY event_sequence ASC LIMIT 1
                """,
                (row["task_id"],),
            ).fetchone()
            if has_cache_hit_table
            else None
        )
        cache_started_dag = (
            dag_admission is not None
            and cache_start is not None
            and int(cache_start["event_sequence"]) == 1
        )
        try:
            if dag_admission is None:
                if len(plan["nodes"]) != 1:
                    raise TaskStoreCorruption(
                        "dispatch plan no longer has exactly one node"
                    )
                durable_node = plan["nodes"][0]
            else:
                matching_nodes = [
                    value
                    for value in plan["nodes"]
                    if value.get("node_id") == row["node_id"]
                ]
                if (
                    len(plan["nodes"]) <= 1
                    or len(matching_nodes) != 1
                    or dag_admission["intent_id"] != row["intent_id"]
                    or dag_admission["plan_id"] != row["plan_id"]
                    or dag_admission["plan_hash"] != row["plan_hash"]
                    or dag_admission["approval_id"] != row["approval_id"]
                    or dag_admission["node_id"] != row["node_id"]
                    or dag_admission["node_idempotency_key"]
                    != row["node_idempotency_key"]
                    or dag_admission["project_id"]
                    != task_identity["project_id"]
                    or dag_admission["principal_id"]
                    != task_identity["principal_id"]
                ):
                    raise TaskStoreCorruption(
                        "DAG dispatch intent differs from its admission"
                    )
                durable_node = matching_nodes[0]
            expected_request_core = {
                "task_id": row["task_id"],
                "node_id": durable_node["node_id"],
                "plan_hash": plan["plan_hash"],
                "idempotency_key": durable_node["idempotency_key"],
                "project_id": task_identity["project_id"],
                "principal_id": task_identity["principal_id"],
                "algorithm": durable_node["algorithm"],
                "task_type": plan["task_type"],
                "parameters": durable_node["parameters"],
                "resources": durable_node["resources"],
            }
            if dag_admission is None:
                if len(durable_node["inputs"]) != 1:
                    raise TaskStoreCorruption(
                        "dispatch plan no longer has exactly one input"
                    )
                input_identity = durable_node["inputs"][0]["dataset"]
                durable_dataset = next(
                    value
                    for value in draft["datasets"]
                    if all(
                        value[key] == input_identity[key]
                        for key in ("id", "version", "content_hash", "data_type")
                    )
                )
                expected_request_core["dataset"] = durable_dataset
        except (KeyError, StopIteration, TypeError) as error:
            raise TaskStoreCorruption(
                "dispatch intent cannot be reconstructed from durable state"
            ) from error
        request_without_config_hash = {
            key: value
            for key, value in request.items()
            if key != "normalized_config_hash"
        }
        if (
            any(
                request_without_config_hash.get(key) != value
                for key, value in expected_request_core.items()
            )
            or (
                dag_admission is None
                and set(request_without_config_hash) != set(expected_request_core)
            )
            or not isinstance(request_without_config_hash.get("dataset"), Mapping)
        ):
            raise TaskStoreCorruption(
                "dispatch intent request payload differs from current plan"
            )
        try:
            _, fingerprint_hash = encode_document(fingerprint)
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "dispatch intent fingerprint is not finite JSON"
            ) from error
        if fingerprint_hash != row["fingerprint_hash"]:
            raise TaskStoreCorruption(
                "dispatch intent fingerprint hash does not match"
            )
        if (
            fingerprint.get("algorithm") != request.get("algorithm")
            or fingerprint.get("seed") != request.get("parameters", {}).get("seed")
            or fingerprint.get("hardware", {}).get("device")
            != request.get("resources", {}).get("device")
            or fingerprint.get("normalized_config_hash")
            != request.get("normalized_config_hash")
            or fingerprint.get("input_hashes")
            != [request.get("dataset", {}).get("content_hash")]
            or (
                (
                    dag_admission is None
                    or (first_dag_intent and not cache_started_dag)
                )
                and (
                    documents["queued_type"] != "task_queued"
                    or documents["queued_status"] != "Queued"
                    or documents["queued_node_id"] is not None
                    or queued_event.get("sequence") != 1
                    or queued_event.get("task_id") != row["task_id"]
                    or queued_event.get("fingerprint") != fingerprint
                    or documents["queued_fingerprint_hash"] != fingerprint_hash
                )
            )
        ):
            raise TaskStoreCorruption(
                "dispatch intent fingerprint differs from its request or queued event"
            )
        if first_dag_intent and not cache_started_dag:
            token = row["intent_id"].removeprefix("dispatch-")
            expected_extension = {
                "agent_rpc.dispatch": {
                    "state": "pending",
                    "fingerprint_basis": "adapter_preflight",
                    "worker_runtime_started": False,
                },
                "org.agent_rpc.dag_execution": {
                    "node_id": row["node_id"],
                    "pending_revision": dag_admission["pending_revision"],
                    "queued_revision": dag_admission["queued_revision"],
                    "input_binding_document_hash": dag_admission[
                        "input_binding_document_hash"
                    ],
                    "max_node_attempts": 1,
                },
            }
            if (
                queued_event.get("event_id") != f"event-dag-queued-{token}"
                or queued_event.get("event_type") != "task_queued"
                or queued_event.get("task_status") != "Queued"
                or queued_event.get("node_id") is not None
                or queued_event.get("occurred_at") != row["created_at"]
                or queued_event.get("extensions") != expected_extension
            ):
                raise TaskStoreCorruption(
                    "DAG queued event differs from its exact admission"
                )
        if cache_started_dag and (
            documents["queued_type"] != "node_succeeded"
            or documents["queued_node_id"] is None
            or documents["queued_hash"] != cache_start["event_hash"]
            or queued_event.get("extensions", {})
            .get("org.agent_rpc.node_cache", {})
            .get("cache_hit_id")
            != cache_start["cache_hit_id"]
        ):
            raise TaskStoreCorruption(
                "DAG cache-start event differs from its run anchor"
            )

        state = "pending"
        handle: dict[str, Any] | None = None
        failure_code: str | None = None
        dispatch_claimed_at: str | None = None
        outcome_recorded_at: str | None = None
        reconciliation: DispatchReconciliationSnapshot | None = None
        if row["dispatch_claimed_at"] is not None:
            state = "dispatching"
            dispatch_claimed_at = str(row["dispatch_claimed_at"])
        if row["outcome"] is not None:
            outcome_row = {
                "document_json": row["outcome_json"],
                "document_hash": row["outcome_hash"],
            }
            outcome_document = _decode_hashed_document(
                outcome_row, label="dispatch outcome"
            )
            state = str(row["outcome"])
            outcome_recorded_at = str(row["outcome_recorded_at"])
            if (
                outcome_document.get("status") != state
                or outcome_document.get("recorded_at") != outcome_recorded_at
            ):
                raise TaskStoreCorruption("dispatch outcome identity is inconsistent")
            if state == "dispatched":
                if set(outcome_document) != {"status", "handle", "recorded_at"}:
                    raise TaskStoreCorruption(
                        "successful dispatch outcome fields are inconsistent"
                    )
                value = outcome_document.get("handle")
                if not isinstance(value, dict):
                    raise TaskStoreCorruption("dispatch handle must be an object")
                handle = value
            elif state == "reconciliation_required":
                if set(outcome_document) != {
                    "status",
                    "failure_code",
                    "recorded_at",
                }:
                    raise TaskStoreCorruption(
                        "failed dispatch outcome fields are inconsistent"
                    )
                value = outcome_document.get("failure_code")
                if (
                    not isinstance(value, str)
                    or not value
                    or len(value) > 128
                    or not value.replace("_", "").isalnum()
                    or value.upper() != value
                ):
                    raise TaskStoreCorruption("dispatch failure code is invalid")
                failure_code = value
                reconciliation = DispatchReconciliationSnapshot(
                    failure_code=value,
                    recorded_at=outcome_recorded_at,
                    state="required",
                )
            else:
                raise TaskStoreCorruption("dispatch outcome state is invalid")

        observation_rows = connection.execute(
            """
            SELECT * FROM dispatch_reconciliation_observations
            WHERE intent_id = ? ORDER BY observation_sequence
            """,
            (row["intent_id"],),
        ).fetchall()
        negative_resolution = connection.execute(
            "SELECT * FROM dispatch_reconciliation_negative_resolutions "
            "WHERE intent_id = ?",
            (row["intent_id"],),
        ).fetchone()
        if (
            (observation_rows or negative_resolution is not None)
            and (state != "reconciliation_required" or reconciliation is None)
        ):
            raise TaskStoreCorruption(
                "reconciliation observations differ from their source outcome"
            )
        if (
            negative_resolution is not None
            and row["reconciliation_result"] is not None
        ):
            raise TaskStoreCorruption(
                "positive and negative reconciliation outcomes conflict"
            )

        exact_observation: tuple[
            sqlite3.Row, dict[str, Any], dict[str, Any]
        ] | None = None
        latest_action: sqlite3.Row | None = None
        for expected_sequence, observation in enumerate(
            observation_rows, start=1
        ):
            try:
                observed_at, observed_at_us = _runtime_timestamp(
                    observation["observed_at"]
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "dispatch reconciliation observation time is invalid"
                ) from error
            observation_document = _decode_hashed_document(
                observation, label="dispatch reconciliation observation"
            )
            term = connection.execute(
                """
                SELECT owner_id, acquired_at, acquired_at_us
                FROM runtime_supervisor_terms
                WHERE project_id = ? AND principal_id = ?
                  AND fencing_token = ?
                """,
                (
                    observation["project_id"],
                    observation["principal_id"],
                    observation["fencing_token"],
                ),
            ).fetchone()
            if (
                observation["observation_sequence"] != expected_sequence
                or observation["task_id"] != row["task_id"]
                or observation["project_id"] != task_identity["project_id"]
                or observation["principal_id"]
                != task_identity["principal_id"]
                or observation["source_outcome_hash"] != row["outcome_hash"]
                or type(observation["fencing_token"]) is not int
                or observation["fencing_token"] < 1
                or observed_at != observation["observed_at"]
                or observed_at_us != observation["observed_at_us"]
                or term is None
                or term["acquired_at_us"] > observed_at_us
            ):
                raise TaskStoreCorruption(
                    "dispatch reconciliation observation identity is inconsistent"
                )
            classification = observation["classification"]
            if classification in {"transient", "uncertain"}:
                if (
                    not isinstance(observation["failure_code"], str)
                    or not observation["failure_code"]
                    or len(observation["failure_code"]) > 128
                    or not observation["failure_code"]
                    .replace("_", "")
                    .isalnum()
                    or observation["failure_code"].upper()
                    != observation["failure_code"]
                    or any(
                        observation[field] is not None
                        for field in (
                            "evidence_kind",
                            "attempt_id",
                            "attempt_number",
                            "adapter_version",
                            "private_schema_version",
                            "private_record_hash",
                            "private_proof_hash",
                            "evidence_json",
                            "evidence_hash",
                        )
                    )
                    or observation_document
                    != {
                        "schema_version": "1.0.0",
                        "intent_id": row["intent_id"],
                        "task_id": row["task_id"],
                        "source_outcome_hash": row["outcome_hash"],
                        "classification": classification,
                        "failure_code": observation["failure_code"],
                        "observed_at": observed_at,
                        "extensions": {},
                    }
                ):
                    raise TaskStoreCorruption(
                        "dispatch reconciliation action is inconsistent"
                    )
                if exact_observation is not None:
                    raise TaskStoreCorruption(
                        "negative reconciliation has a later action observation"
                    )
                latest_action = observation
                continue

            if classification != "exact_negative" or exact_observation is not None:
                raise TaskStoreCorruption(
                    "dispatch reconciliation observation classification is invalid"
                )
            try:
                evidence = _decode_hashed_document(
                    {
                        "document_json": observation["evidence_json"],
                        "document_hash": observation["evidence_hash"],
                    },
                    label="negative dispatch evidence",
                )
                normalized_evidence = _validated_worker_evidence(evidence)
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "negative dispatch evidence is invalid"
                ) from error
            proof_payload = {
                "schema_version": "1.0.0",
                "result": "not_dispatched",
                "evidence_kind": "managed_pre_running_failure",
                "adapter_version": observation["adapter_version"],
                "private_schema_version": observation[
                    "private_schema_version"
                ],
                "private_record_hash": observation["private_record_hash"],
                "attempt_id": observation["attempt_id"],
                "attempt_number": observation["attempt_number"],
                "evidence_hash": observation["evidence_hash"],
            }
            try:
                calculated_proof_hash = encode_document(proof_payload)[1]
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "negative reconciliation proof is invalid"
                ) from error
            if (
                observation["failure_code"] != "DISPATCH_NOT_STARTED"
                or observation["evidence_kind"]
                != "managed_pre_running_failure"
                or observation["attempt_number"] != 1
                or observation["attempt_id"]
                != normalized_evidence["attempt_id"]
                or normalized_evidence["attempt_number"] != 1
                or normalized_evidence["ticket"]["state"]
                not in {"staged", "leased", "spawned", "failed"}
                or normalized_evidence["ready"] is not None
                or normalized_evidence["heartbeat"] is not None
                or (
                    observation["adapter_version"],
                    observation["private_schema_version"],
                )
                not in {
                    ("1.4.0", "1.1.0"),
                    ("1.5.0", "1.2.0"),
                    ("1.6.0", "1.2.0"),
                }
                or observation["adapter_version"] != row["adapter_version"]
                or not _is_sha256(observation["private_record_hash"])
                or calculated_proof_hash != observation["private_proof_hash"]
                or observation_document
                != {
                    "schema_version": "1.0.0",
                    "intent_id": row["intent_id"],
                    "task_id": row["task_id"],
                    "source_outcome_hash": row["outcome_hash"],
                    "classification": "exact_negative",
                    "failure_code": "DISPATCH_NOT_STARTED",
                    "evidence": proof_payload,
                    "observed_at": observed_at,
                    "extensions": {},
                }
            ):
                raise TaskStoreCorruption(
                    "negative reconciliation observation is inconsistent"
                )
            exact_observation = (observation, normalized_evidence, proof_payload)

        if exact_observation is not None and negative_resolution is None:
            raise TaskStoreCorruption(
                "negative reconciliation observation lacks its resolution"
            )
        if negative_resolution is not None and exact_observation is None:
            raise TaskStoreCorruption(
                "negative reconciliation resolution lacks exact evidence"
            )
        if (
            negative_resolution is None
            and row["reconciliation_result"] is None
            and latest_action is not None
            and reconciliation is not None
        ):
            reconciliation = DispatchReconciliationSnapshot(
                failure_code=reconciliation.failure_code,
                recorded_at=reconciliation.recorded_at,
                state="required",
                action_classification=latest_action["classification"],
                action_failure_code=latest_action["failure_code"],
                action_observed_at=latest_action["observed_at"],
            )
        if row["reconciliation_result"] is not None:
            if (
                state != "reconciliation_required"
                or reconciliation is None
                or row["reconciliation_result"] != "dispatched"
                or row["resolution_source_hash"] != row["outcome_hash"]
                or row["resolution_evidence_kind"]
                not in {"managed_worker_receipt", "private_receipt"}
            ):
                raise TaskStoreCorruption(
                    "dispatch reconciliation resolution identity is inconsistent"
                )
            resolution_handle = _decode_document(
                row["resolution_handle_json"],
                label="dispatch reconciliation handle",
            )
            try:
                _, resolution_handle_hash = encode_document(resolution_handle)
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "dispatch reconciliation handle is invalid"
                ) from error
            effective_outcome = _decode_hashed_document(
                {
                    "document_json": row["effective_outcome_json"],
                    "document_hash": row["effective_outcome_hash"],
                },
                label="effective dispatch outcome",
            )
            resolution_document = _decode_hashed_document(
                {
                    "document_json": row["resolution_json"],
                    "document_hash": row["resolution_hash"],
                },
                label="dispatch reconciliation resolution",
            )
            try:
                resolved_at, resolved_at_us = _runtime_timestamp(
                    row["resolved_at"]
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "dispatch reconciliation resolution time is invalid"
                ) from error
            authorization = connection.execute(
                """
                SELECT project_id, principal_id, source_outcome_hash,
                       authorized_at, authorized_at_us
                FROM supervised_dispatch_reconciliation_attempts
                WHERE intent_id = ? AND fencing_token = ?
                  AND evidence_kind = ?
                """,
                (
                    row["intent_id"],
                    row["resolution_fencing_token"],
                    row["resolution_evidence_kind"],
                ),
            ).fetchone()
            if authorization is None:
                raise TaskStoreCorruption(
                    "dispatch reconciliation resolution lacks authorization"
                )
            try:
                authorized_at, authorized_at_us = _runtime_timestamp(
                    authorization["authorized_at"]
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "dispatch reconciliation authorization time is invalid"
                ) from error
            if (
                resolution_handle_hash != row["resolution_handle_hash"]
                or resolved_at != row["resolved_at"]
                or resolved_at_us != row["resolved_at_us"]
                or type(row["resolution_fencing_token"]) is not int
                or row["resolution_fencing_token"] < 1
                or authorization["project_id"] != task_identity["project_id"]
                or authorization["principal_id"]
                != task_identity["principal_id"]
                or authorization["source_outcome_hash"] != row["outcome_hash"]
                or authorized_at != authorization["authorized_at"]
                or authorized_at_us != authorization["authorized_at_us"]
                or authorized_at_us > resolved_at_us
                or effective_outcome
                != {
                    "status": "dispatched",
                    "handle": resolution_handle,
                    "recorded_at": resolved_at,
                }
            ):
                raise TaskStoreCorruption(
                    "dispatch reconciliation resolution differs from its index"
                )

            evidence_kind = row["resolution_evidence_kind"]
            if evidence_kind == "managed_worker_receipt":
                attempt_id = row["resolution_attempt_id"]
                observation_sequence = row[
                    "resolution_observation_sequence"
                ]
                attempt = connection.execute(
                    "SELECT * FROM worker_launch_attempts WHERE attempt_id = ?",
                    (attempt_id,),
                ).fetchone()
                observation = connection.execute(
                    """
                    SELECT * FROM worker_attempt_observations
                    WHERE attempt_id = ? AND observation_sequence = ?
                    """,
                    (attempt_id, observation_sequence),
                ).fetchone()
                adoption = connection.execute(
                    """
                    SELECT * FROM supervised_dispatch_adoptions
                    WHERE intent_id = ?
                    """,
                    (row["intent_id"],),
                ).fetchone()
                if attempt is None or observation is None or adoption is None:
                    raise TaskStoreCorruption(
                        "managed reconciliation proof is incomplete"
                    )
                evidence = _decode_worker_observation_row(observation)
                _require_worker_attempt_observation_binding(
                    attempt,
                    evidence,
                    intent_id=row["intent_id"],
                    task_id=row["task_id"],
                    project_id=task_identity["project_id"],
                    principal_id=task_identity["principal_id"],
                )
                checkpoint_waiting_evidence = (
                    evidence["heartbeat"] is not None
                    and evidence["heartbeat"]["state"] == "waiting"
                    and row["adapter_version"] == "1.6.0"
                    and request.get("algorithm")
                    == {
                        "id": "deepwave.acoustic_fwi",
                        "version": "1.6.0",
                    }
                    and resolution_handle.get("adapter_version") == "1.6.0"
                    and resolution_handle.get("algorithm")
                    == {
                        "id": "deepwave.acoustic_fwi",
                        "version": "1.6.0",
                    }
                )
                if (
                    type(observation_sequence) is not int
                    or observation_sequence < 1
                    or observation["document_hash"]
                    != row["evidence_record_hash"]
                    or evidence["ticket"]["state"] != "spawned"
                    or evidence["ready"] is None
                    or evidence["heartbeat"] is None
                    or (
                        evidence["heartbeat"]["state"]
                        not in {"running", "succeeded", "failed"}
                        and not checkpoint_waiting_evidence
                    )
                    or resolution_handle.get("submission_id")
                    != evidence["submission_id"]
                    or resolution_handle.get("job_id") != evidence["job_id"]
                    or resolution_handle.get("request_hash")
                    != evidence["request_hash"]
                    or adoption["attempt_id"] != attempt_id
                    or adoption["project_id"] != task_identity["project_id"]
                    or adoption["principal_id"]
                    != task_identity["principal_id"]
                    or adoption["fencing_token"]
                    != row["resolution_fencing_token"]
                    or adoption["recorded_at"] != resolved_at
                    or adoption["recorded_at_us"] != resolved_at_us
                    or row["resolution_private_schema_version"] is not None
                    or row["resolution_receipt_record_hash"] is not None
                ):
                    raise TaskStoreCorruption(
                        "managed reconciliation proof is inconsistent"
                    )
                resolution_evidence = {
                    "kind": "managed_worker_receipt",
                    "attempt_id": attempt_id,
                    "observation_sequence": observation_sequence,
                    "observation_document_hash": observation[
                        "document_hash"
                    ],
                }
            else:
                adoption = connection.execute(
                    """
                    SELECT * FROM supervised_private_receipt_adoptions
                    WHERE intent_id = ?
                    """,
                    (row["intent_id"],),
                ).fetchone()
                if (
                    adoption is None
                    or row["resolution_attempt_id"] is not None
                    or row["resolution_observation_sequence"] is not None
                    or row["resolution_private_schema_version"] != "1.0.0"
                    or row["resolution_receipt_record_hash"]
                    != row["evidence_record_hash"]
                    or adoption["private_schema_version"] != "1.0.0"
                    or adoption["receipt_record_hash"]
                    != row["evidence_record_hash"]
                    or adoption["outcome_document_hash"]
                    != row["effective_outcome_hash"]
                    or adoption["project_id"] != task_identity["project_id"]
                    or adoption["principal_id"]
                    != task_identity["principal_id"]
                    or adoption["fencing_token"]
                    != row["resolution_fencing_token"]
                    or adoption["recorded_at"] != resolved_at
                    or adoption["recorded_at_us"] != resolved_at_us
                ):
                    raise TaskStoreCorruption(
                        "private reconciliation proof is inconsistent"
                    )
                resolution_evidence = {
                    "kind": "private_receipt",
                    "private_schema_version": "1.0.0",
                    "receipt_record_hash": row["evidence_record_hash"],
                }
            expected_resolution_document = {
                "schema_version": "1.0.0",
                "intent_id": row["intent_id"],
                "task_id": row["task_id"],
                "source_outcome_hash": row["outcome_hash"],
                "result": "dispatched",
                "effective_outcome": effective_outcome,
                "evidence": resolution_evidence,
                "resolved_at": resolved_at,
                "extensions": {},
            }
            if resolution_document != expected_resolution_document:
                raise TaskStoreCorruption(
                    "dispatch reconciliation resolution fields are inconsistent"
                )
            state = "dispatched"
            handle = resolution_handle
            failure_code = None
            outcome_recorded_at = resolved_at
            reconciliation = DispatchReconciliationSnapshot(
                failure_code=reconciliation.failure_code,
                recorded_at=reconciliation.recorded_at,
                state="resolved",
                result="dispatched",
                evidence_kind=evidence_kind,
                resolved_at=resolved_at,
            )
        elif negative_resolution is not None:
            assert reconciliation is not None
            assert exact_observation is not None
            observation, negative_evidence, proof_payload = exact_observation
            try:
                resolved_at, resolved_at_us = _runtime_timestamp(
                    negative_resolution["resolved_at"]
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "negative reconciliation resolution time is invalid"
                ) from error
            resolution_document = _decode_hashed_document(
                negative_resolution,
                label="negative dispatch reconciliation resolution",
            )
            terminal = connection.execute(
                "SELECT * FROM run_events WHERE task_id = ? AND sequence = ?",
                (
                    row["task_id"],
                    negative_resolution["terminal_event_sequence"],
                ),
            ).fetchone()
            commit_record = connection.execute(
                """
                SELECT * FROM supervised_run_event_commits
                WHERE task_id = ? AND sequence = ?
                """,
                (
                    row["task_id"],
                    negative_resolution["terminal_event_sequence"],
                ),
            ).fetchone()
            budget = connection.execute(
                """
                SELECT max_tasks, tasks_used FROM approval_budgets
                WHERE task_id = ? AND approval_id = ?
                """,
                (row["task_id"], row["approval_id"]),
            ).fetchone()
            term = connection.execute(
                """
                SELECT owner_id, acquired_at, acquired_at_us
                FROM runtime_supervisor_terms
                WHERE project_id = ? AND principal_id = ?
                  AND fencing_token = ?
                """,
                (
                    task_identity["project_id"],
                    task_identity["principal_id"],
                    negative_resolution["fencing_token"],
                ),
            ).fetchone()
            if terminal is None or commit_record is None:
                raise TaskStoreCorruption(
                    "negative reconciliation lacks its supervised terminal event"
                )
            terminal_document = _decode_hashed_document(
                terminal, label="negative reconciliation terminal event"
            )
            try:
                _, fingerprint_hash = encode_document(fingerprint)
                _, occurred_at_us = _runtime_timestamp(
                    terminal["occurred_at"]
                )
                committed_at, committed_at_us = _runtime_timestamp(
                    commit_record["recorded_at"]
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "negative reconciliation terminal identity is invalid"
                ) from error
            _, identity_hash = encode_document(
                {
                    "intent_id": row["intent_id"],
                    "attempt_id": observation["attempt_id"],
                    "evidence_hash": observation["evidence_hash"],
                    "private_proof_hash": observation["private_proof_hash"],
                    "event_type": "node_failed",
                    "sequence": negative_resolution[
                        "terminal_event_sequence"
                    ],
                }
            )
            dag_negative = dag_admission is not None
            projected_task_status = "Failed"
            dag_terminal = None
            dag_transition = None
            dag_state = None
            if dag_negative:
                dag_terminal = connection.execute(
                    "SELECT * FROM dag_node_terminal_facts WHERE intent_id = ?",
                    (row["intent_id"],),
                ).fetchone()
                dag_transition = connection.execute(
                    """
                    SELECT * FROM dag_node_execution_transition_facts
                    WHERE intent_id = ? AND state = 'Failed'
                    """,
                    (row["intent_id"],),
                ).fetchone()
                dag_state = connection.execute(
                    """
                    SELECT * FROM dag_node_state_events
                    WHERE task_id = ? AND plan_id = ? AND node_id = ?
                    ORDER BY revision DESC LIMIT 1
                    """,
                    (row["task_id"], row["plan_id"], row["node_id"]),
                ).fetchone()
                final_state_map = self._load_dag_node_state_map(
                    connection,
                    task_id=row["task_id"],
                    plan=plan,
                )
                if final_state_map is None:
                    raise TaskStoreCorruption(
                        "DAG negative reconciliation lacks its final state map"
                    )
                try:
                    final_readiness = evaluate_dag_readiness(
                        plan,
                        node_states={
                            node.node_id: node.state
                            for node in final_state_map.nodes
                        },
                    )
                except DagScheduleError as error:
                    raise TaskStoreCorruption(
                        "DAG negative reconciliation aggregate is invalid"
                    ) from error
                projected_task_status = self._dag_aggregate_status(
                    final_readiness
                )
            event_extension = {
                "intent_id": row["intent_id"],
                "attempt_id": observation["attempt_id"],
                "attempt_number": 1,
                "evidence_hash": observation["evidence_hash"],
                "adapter_version": observation["adapter_version"],
                "private_schema_version": observation[
                    "private_schema_version"
                ],
                "private_record_hash": observation["private_record_hash"],
                "private_proof_hash": observation["private_proof_hash"],
                "result": "not_dispatched",
            }
            expected_terminal_document = {
                "schema_version": "1.0.0",
                "event_id": "event-"
                + identity_hash.removeprefix("sha256:")[:32],
                "sequence": negative_resolution["terminal_event_sequence"],
                "task_id": row["task_id"],
                "node_id": row["node_id"],
                "event_type": "node_failed",
                "task_status": projected_task_status,
                "error": {
                    "code": "dispatch_not_started",
                    "message": "FWI Worker did not reach its running boundary",
                    "retryable": False,
                },
                "occurred_at": negative_evidence["ticket"]["updated_at"],
                "fingerprint": fingerprint,
                "extensions": {
                    "org.agent_rpc.dispatch_reconciliation": event_extension
                },
            }
            expected_resolution_document = {
                "schema_version": "1.0.0",
                "intent_id": row["intent_id"],
                "task_id": row["task_id"],
                "source_outcome_hash": row["outcome_hash"],
                "result": "not_dispatched",
                "terminal_status": projected_task_status,
                "failure_code": "DISPATCH_NOT_STARTED",
                "evidence": {
                    **proof_payload,
                    "observation_sequence": observation[
                        "observation_sequence"
                    ],
                },
                "terminal_event_sequence": negative_resolution[
                    "terminal_event_sequence"
                ],
                "approval_budget": {
                    "tasks_used": negative_resolution[
                        "approval_tasks_used"
                    ],
                    "refunded": False,
                },
                "resolved_at": resolved_at,
                "extensions": {},
            }
            conflicting_rows = connection.execute(
                """
                SELECT
                    EXISTS(
                        SELECT 1 FROM worker_retry_reservations
                        WHERE intent_id = ?
                    ) AS has_retry,
                    EXISTS(
                        SELECT 1 FROM worker_exit_retry_reservations
                        WHERE intent_id = ?
                    ) AS has_worker_exit_retry,
                    EXISTS(
                        SELECT 1 FROM task_cancel_requests
                        WHERE task_id = ?
                    ) AS has_cancel
                """,
                (row["intent_id"], row["intent_id"], row["task_id"]),
            ).fetchone()
            if (
                negative_resolution["task_id"] != row["task_id"]
                or negative_resolution["project_id"]
                != task_identity["project_id"]
                or negative_resolution["principal_id"]
                != task_identity["principal_id"]
                or negative_resolution["approval_id"] != row["approval_id"]
                or negative_resolution["source_outcome_hash"]
                != row["outcome_hash"]
                or negative_resolution["result"] != "not_dispatched"
                or negative_resolution["evidence_kind"]
                != "managed_pre_running_failure"
                or negative_resolution["observation_sequence"]
                != observation["observation_sequence"]
                or observation["observation_sequence"]
                != len(observation_rows)
                or negative_resolution["approval_budget_refunded"] != 0
                or budget is None
                or negative_resolution["approval_tasks_used"]
                != budget["tasks_used"]
                or budget["tasks_used"] < 1
                or budget["tasks_used"] > budget["max_tasks"]
                or type(negative_resolution["fencing_token"]) is not int
                or negative_resolution["fencing_token"] < 1
                or negative_resolution["fencing_token"]
                != observation["fencing_token"]
                or resolved_at != negative_resolution["resolved_at"]
                or resolved_at_us != negative_resolution["resolved_at_us"]
                or observation["observed_at_us"] > resolved_at_us
                or term is None
                or term["acquired_at_us"] > resolved_at_us
                or terminal["document_hash"]
                != negative_resolution["terminal_event_hash"]
                or terminal["event_type"] != "node_failed"
                or terminal["task_status"] != projected_task_status
                or terminal["node_id"] != row["node_id"]
                or terminal["fingerprint_hash"] != fingerprint_hash
                or terminal["recorded_at"] != resolved_at
                or occurred_at_us > resolved_at_us
                or terminal_document != expected_terminal_document
                or commit_record["project_id"]
                != task_identity["project_id"]
                or commit_record["principal_id"]
                != task_identity["principal_id"]
                or commit_record["fencing_token"]
                != negative_resolution["fencing_token"]
                or committed_at != resolved_at
                or committed_at_us != resolved_at_us
                or commit_record["recorded_at_us"] != resolved_at_us
                or resolution_document != expected_resolution_document
                or task_identity["status"] != projected_task_status
                or conflicting_rows["has_retry"]
                or conflicting_rows["has_worker_exit_retry"]
                or conflicting_rows["has_cancel"]
            ):
                raise TaskStoreCorruption(
                    "negative reconciliation resolution is inconsistent"
                )
            if dag_negative:
                assert dag_admission is not None
                expected_node_revision = int(dag_admission["queued_revision"]) + 1
                proof_json, proof_hash = encode_document(proof_payload)
                if (
                    dag_terminal is None
                    or dag_transition is None
                    or dag_state is None
                    or dag_terminal["task_id"] != row["task_id"]
                    or dag_terminal["plan_id"] != row["plan_id"]
                    or dag_terminal["plan_hash"] != row["plan_hash"]
                    or dag_terminal["approval_id"] != row["approval_id"]
                    or dag_terminal["node_id"] != row["node_id"]
                    or dag_terminal["input_binding_node_revision"]
                    != dag_admission["pending_revision"]
                    or dag_terminal["input_binding_document_hash"]
                    != dag_admission["input_binding_document_hash"]
                    or dag_terminal["input_fencing_token"]
                    != dag_admission["input_fencing_token"]
                    or dag_terminal["input_owner_id"]
                    != dag_admission["input_owner_id"]
                    or dag_terminal["input_term_acquired_at"]
                    != dag_admission["input_term_acquired_at"]
                    or dag_terminal["node_revision"] != expected_node_revision
                    or dag_terminal["node_state"] != "Failed"
                    or dag_terminal["event_sequence"] != terminal["sequence"]
                    or dag_terminal["event_hash"] != terminal["document_hash"]
                    or dag_terminal["attempt_id"] is not None
                    or dag_terminal["attempt_number"] is not None
                    or dag_terminal["worker_observation_sequence"] is not None
                    or dag_terminal["worker_observation_hash"] is not None
                    or dag_terminal["dispatch_handle_json"] is not None
                    or dag_terminal["dispatch_handle_hash"] is not None
                    or dag_terminal["dispatch_outcome_document_hash"]
                    != row["outcome_hash"]
                    or dag_terminal["adapter_status_json"] != proof_json
                    or dag_terminal["adapter_status_hash"] != proof_hash
                    or dag_terminal["project_id"]
                    != task_identity["project_id"]
                    or dag_terminal["principal_id"]
                    != task_identity["principal_id"]
                    or dag_terminal["completion_fencing_token"]
                    != negative_resolution["fencing_token"]
                    or term is None
                    or dag_terminal["completion_owner_id"] != term["owner_id"]
                    or dag_terminal["completion_term_acquired_at"]
                    != term["acquired_at"]
                    or dag_terminal["receipt_document_json"] is not None
                    or dag_terminal["receipt_document_hash"] is not None
                    or dag_terminal["recorded_at"] != resolved_at
                    or dag_terminal["recorded_at_us"] != resolved_at_us
                    or dag_transition["node_revision"] != expected_node_revision
                    or dag_transition["previous_state"] != "Queued"
                    or dag_transition["state"] != "Failed"
                    or dag_transition["event_sequence"] != terminal["sequence"]
                    or dag_transition["event_hash"] != terminal["document_hash"]
                    or dag_transition["reason"] != "dispatch_not_started"
                    or dag_transition["fencing_token"]
                    != negative_resolution["fencing_token"]
                    or dag_transition["recorded_at"] != resolved_at
                    or dag_transition["recorded_at_us"] != resolved_at_us
                    or dag_state["revision"] != expected_node_revision
                    or dag_state["previous_state"] != "Queued"
                    or dag_state["state"] != "Failed"
                    or dag_state["recorded_at"] != resolved_at
                    or dag_state["recorded_at_us"] != resolved_at_us
                ):
                    raise TaskStoreCorruption(
                        "DAG negative reconciliation projection is inconsistent"
                    )
            state = "not_dispatched"
            handle = None
            failure_code = "DISPATCH_NOT_STARTED"
            outcome_recorded_at = resolved_at
            reconciliation = DispatchReconciliationSnapshot(
                failure_code=reconciliation.failure_code,
                recorded_at=reconciliation.recorded_at,
                state="resolved",
                result="not_dispatched",
                evidence_kind="managed_pre_running_failure",
                resolved_at=resolved_at,
            )
        elif state == "reconciliation_required":
            dangling_adoption = connection.execute(
                """
                SELECT EXISTS(
                           SELECT 1 FROM supervised_dispatch_adoptions
                           WHERE intent_id = ?
                       )
                       OR EXISTS(
                           SELECT 1 FROM supervised_private_receipt_adoptions
                           WHERE intent_id = ?
                       )
                """,
                (row["intent_id"], row["intent_id"]),
            ).fetchone()[0]
            if dangling_adoption:
                raise TaskStoreCorruption(
                    "unresolved reconciliation has a dangling adoption"
                )

        source_handle = None if handle is None else copy.deepcopy(handle)
        source_outcome_hash = (
            row["effective_outcome_hash"]
            if row["reconciliation_result"] is not None
            else row["outcome_hash"]
        )
        worker_exit_retry = connection.execute(
            """
            SELECT retry.*,
                   replacement.attempt_id AS replacement_attempt_id,
                   replacement.observation_sequence
                       AS replacement_observation_sequence,
                   replacement.evidence_hash AS replacement_evidence_hash,
                   replacement.handle_json AS replacement_handle_json,
                   replacement.handle_hash AS replacement_handle_hash,
                   replacement.effective_outcome_json
                       AS replacement_outcome_json,
                   replacement.effective_outcome_hash
                       AS replacement_outcome_hash,
                   replacement.fencing_token AS replacement_fencing_token,
                   replacement.replaced_at,
                   replacement.replaced_at_us,
                   exhausted.attempt_id AS exhausted_attempt_id,
                   exhausted.observation_sequence
                       AS exhausted_observation_sequence,
                   exhausted.evidence_hash AS exhausted_evidence_hash,
                   exhausted.private_schema_version
                       AS exhausted_private_schema_version,
                   exhausted.private_proof_hash
                       AS exhausted_private_proof_hash,
                   exhausted.failure_kind AS exhausted_failure_kind,
                   exhausted.terminal_event_sequence,
                   exhausted.terminal_event_hash,
                   exhausted.fencing_token AS exhausted_fencing_token,
                   exhausted.exhausted_at,
                   exhausted.exhausted_at_us
            FROM worker_exit_retry_reservations AS retry
            LEFT JOIN worker_exit_retry_dispatch_replacements AS replacement
              ON replacement.intent_id = retry.intent_id
             AND replacement.attempt_number = retry.attempt_number
            LEFT JOIN worker_exit_retry_exhaustions AS exhausted
              ON exhausted.intent_id = retry.intent_id
             AND exhausted.attempt_number = retry.attempt_number
            WHERE retry.intent_id = ? AND retry.attempt_number = 2
            """,
            (row["intent_id"],),
        ).fetchone()
        if worker_exit_retry is not None:
            retry_event_row = connection.execute(
                """
                SELECT document_json, document_hash, event_type, task_status,
                       node_id, fingerprint_hash, recorded_at
                FROM run_events WHERE task_id = ? AND sequence = ?
                """,
                (
                    row["task_id"],
                    worker_exit_retry["retry_event_sequence"],
                ),
            ).fetchone()
            if retry_event_row is None:
                raise TaskStoreCorruption(
                    "worker-exit retry reservation lacks its transition event"
                )
            retry_event = _decode_hashed_document(
                retry_event_row, label="worker-exit retry event"
            )
            retry_extension = retry_event.get("extensions", {}).get(
                "org.agent_rpc.worker_exit_retry"
            )
            try:
                _, source_handle_hash = encode_document(source_handle)
                reserved_at, reserved_at_us = _runtime_timestamp(
                    worker_exit_retry["reserved_at"]
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "worker-exit retry source identity is invalid"
                ) from error
            expected_retry_extension = {
                "intent_id": row["intent_id"],
                "attempt_number": 2,
                "previous_attempt_id": worker_exit_retry[
                    "previous_attempt_id"
                ],
                "previous_observation_sequence": worker_exit_retry[
                    "previous_observation_sequence"
                ],
                "evidence_hash": worker_exit_retry["evidence_hash"],
                "private_schema_version": worker_exit_retry[
                    "private_schema_version"
                ],
                "private_proof_hash": worker_exit_retry[
                    "private_proof_hash"
                ],
                "failure_kind": "worker_exit",
                "max_attempts": 2,
                "source_outcome_document_hash": worker_exit_retry[
                    "source_outcome_document_hash"
                ],
                "source_handle_hash": worker_exit_retry["source_handle_hash"],
            }
            if (
                state != "dispatched"
                or source_handle is None
                or worker_exit_retry["task_id"] != row["task_id"]
                or worker_exit_retry["approval_id"] != row["approval_id"]
                or worker_exit_retry["project_id"]
                != task_identity["project_id"]
                or worker_exit_retry["principal_id"]
                != task_identity["principal_id"]
                or worker_exit_retry["failure_kind"] != "worker_exit"
                or worker_exit_retry["private_schema_version"]
                not in {"1.1.0", "1.2.0"}
                or source_outcome_hash
                != worker_exit_retry["source_outcome_document_hash"]
                or source_handle_hash != worker_exit_retry["source_handle_hash"]
                or retry_event_row["document_hash"]
                != worker_exit_retry["retry_event_hash"]
                or retry_event_row["event_type"] != "node_retrying"
                or retry_event_row["task_status"] != "Retrying"
                or retry_event_row["node_id"] != row["node_id"]
                or retry_event_row["fingerprint_hash"] != row["fingerprint_hash"]
                or retry_event_row["recorded_at"] != reserved_at
                or reserved_at != worker_exit_retry["reserved_at"]
                or reserved_at_us != worker_exit_retry["reserved_at_us"]
                or retry_extension != expected_retry_extension
            ):
                raise TaskStoreCorruption(
                    "worker-exit retry reservation differs from its source"
                )

            state = "retrying"
            handle = None
            failure_code = None
            outcome_recorded_at = reserved_at
            if worker_exit_retry["replacement_attempt_id"] is not None:
                replacement_handle = _decode_document(
                    worker_exit_retry["replacement_handle_json"],
                    label="worker-exit retry replacement handle",
                )
                try:
                    _, replacement_handle_hash = encode_document(
                        replacement_handle
                    )
                    replaced_at, replaced_at_us = _runtime_timestamp(
                        worker_exit_retry["replaced_at"]
                    )
                except TaskStoreConflict as error:
                    raise TaskStoreCorruption(
                        "worker-exit retry replacement is invalid"
                    ) from error
                replacement_outcome = _decode_hashed_document(
                    {
                        "document_json": worker_exit_retry[
                            "replacement_outcome_json"
                        ],
                        "document_hash": worker_exit_retry[
                            "replacement_outcome_hash"
                        ],
                    },
                    label="worker-exit retry replacement outcome",
                )
                replacement_attempt = connection.execute(
                    "SELECT * FROM worker_launch_attempts WHERE attempt_id = ?",
                    (worker_exit_retry["replacement_attempt_id"],),
                ).fetchone()
                replacement_observation = connection.execute(
                    """
                    SELECT document_hash FROM worker_attempt_observations
                    WHERE attempt_id = ? AND observation_sequence = ?
                    """,
                    (
                        worker_exit_retry["replacement_attempt_id"],
                        worker_exit_retry["replacement_observation_sequence"],
                    ),
                ).fetchone()
                if (
                    not isinstance(replacement_handle, dict)
                    or replacement_handle_hash
                    != worker_exit_retry["replacement_handle_hash"]
                    or replacement_outcome
                    != {
                        "status": "dispatched",
                        "handle": replacement_handle,
                        "recorded_at": replaced_at,
                    }
                    or replaced_at != worker_exit_retry["replaced_at"]
                    or replaced_at_us != worker_exit_retry["replaced_at_us"]
                    or replacement_attempt is None
                    or int(replacement_attempt["attempt_number"]) != 2
                    or replacement_attempt["intent_id"] != row["intent_id"]
                    or replacement_attempt["submission_id"]
                    != replacement_handle.get("submission_id")
                    or replacement_attempt["job_id"]
                    != replacement_handle.get("job_id")
                    or replacement_attempt["adapter_request_hash"]
                    != replacement_handle.get("request_hash")
                    or replacement_observation is None
                    or replacement_observation["document_hash"]
                    != worker_exit_retry["replacement_evidence_hash"]
                ):
                    raise TaskStoreCorruption(
                        "worker-exit retry replacement differs from attempt 2"
                    )
                state = "dispatched"
                handle = replacement_handle
                outcome_recorded_at = replaced_at

            if worker_exit_retry["exhausted_attempt_id"] is not None:
                exhaustion_event = connection.execute(
                    """
                    SELECT event_type, task_status, document_hash
                    FROM run_events WHERE task_id = ? AND sequence = ?
                    """,
                    (
                        row["task_id"],
                        worker_exit_retry["terminal_event_sequence"],
                    ),
                ).fetchone()
                if (
                    worker_exit_retry["exhausted_private_schema_version"]
                    != "1.3.0"
                    or worker_exit_retry["exhausted_failure_kind"]
                    not in {"pre_running_launch_failure", "worker_exit"}
                    or exhaustion_event is None
                    or exhaustion_event["event_type"] != "node_failed"
                    or exhaustion_event["task_status"] != "Failed"
                    or exhaustion_event["document_hash"]
                    != worker_exit_retry["terminal_event_hash"]
                    or task_identity["status"] != "Failed"
                ):
                    raise TaskStoreCorruption(
                        "worker-exit retry exhaustion is inconsistent"
                    )
                if worker_exit_retry["exhausted_failure_kind"] == "pre_running_launch_failure":
                    if handle is not None:
                        raise TaskStoreCorruption(
                            "pre-running B2 exhaustion retained a handle"
                        )
                    state = "retry_exhausted"
                    failure_code = "WORKER_RETRY_EXHAUSTED"
                    outcome_recorded_at = worker_exit_retry["exhausted_at"]
                elif handle is None:
                    raise TaskStoreCorruption(
                        "post-ready B2 exhaustion lacks its replacement handle"
                    )
        if row["exhaustion_attempt_number"] is not None:
            exhaustion_event = connection.execute(
                """
                SELECT event_type, task_status, document_hash
                FROM run_events WHERE task_id = ? AND sequence = ?
                """,
                (
                    row["task_id"],
                    row["exhaustion_terminal_event_sequence"],
                ),
            ).fetchone()
            if (
                state != "dispatching"
                or handle is not None
                or reconciliation is not None
                or int(row["exhaustion_attempt_number"]) != 2
                or not _is_sha256(row["exhaustion_terminal_event_hash"])
                or exhaustion_event is None
                or exhaustion_event["event_type"] != "node_failed"
                or exhaustion_event["task_status"] != "Failed"
                or exhaustion_event["document_hash"]
                != row["exhaustion_terminal_event_hash"]
                or task_identity["status"] != "Failed"
                or not isinstance(row["exhaustion_recorded_at"], str)
            ):
                raise TaskStoreCorruption(
                    "retry exhaustion dispatch projection is inconsistent"
                )
            state = "retry_exhausted"
            failure_code = "WORKER_RETRY_EXHAUSTED"
            outcome_recorded_at = row["exhaustion_recorded_at"]
        result = DispatchIntentSnapshot(
            intent_id=row["intent_id"],
            task_id=row["task_id"],
            plan_id=row["plan_id"],
            plan_hash=row["plan_hash"],
            approval_id=row["approval_id"],
            node_id=row["node_id"],
            node_idempotency_key=row["node_idempotency_key"],
            adapter_id=row["adapter_id"],
            adapter_version=row["adapter_version"],
            request=request,
            request_hash=row["document_hash"],
            queue_fingerprint=fingerprint,
            state=state,
            handle=handle,
            failure_code=failure_code,
            created_at=row["created_at"],
            dispatch_claimed_at=dispatch_claimed_at,
            outcome_recorded_at=outcome_recorded_at,
            reconciliation=reconciliation,
        )
        if handle is not None:
            try:
                self._validate_dispatch_handle(result, handle)
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "dispatch handle differs from its immutable intent"
                ) from error
        return result

    def get_dispatch_intent(self, task_id: str) -> DispatchIntentSnapshot | None:
        connection = self._connect()
        try:
            return self._load_dispatch_intent(connection, task_id=task_id)
        finally:
            connection.close()

    def get_dispatch_intent_by_id(
        self, intent_id: str
    ) -> DispatchIntentSnapshot | None:
        connection = self._connect()
        try:
            return self._load_dispatch_intent(connection, intent_id=intent_id)
        finally:
            connection.close()

    def list_dispatch_intents(
        self, task_id: str
    ) -> tuple[DispatchIntentSnapshot, ...]:
        connection = self._connect()
        try:
            rows = connection.execute(
                """
                SELECT intent.intent_id
                FROM dispatch_intents AS intent
                LEFT JOIN dag_node_execution_admissions AS admission
                  ON admission.intent_id = intent.intent_id
                WHERE intent.task_id = ?
                ORDER BY COALESCE(admission.admitted_at_us, 0), intent.intent_id
                """,
                (task_id,),
            ).fetchall()
            values: list[DispatchIntentSnapshot] = []
            for row in rows:
                intent = self._load_dispatch_intent(
                    connection, intent_id=row["intent_id"]
                )
                if intent is None:
                    raise TaskStoreCorruption("dispatch intent cannot be read")
                values.append(intent)
            return tuple(values)
        finally:
            connection.close()

    @staticmethod
    def _task_id_for_intent(
        connection: sqlite3.Connection, intent_id: str
    ) -> str | None:
        row = connection.execute(
            "SELECT task_id FROM dispatch_intents WHERE intent_id = ?",
            (intent_id,),
        ).fetchone()
        return None if row is None else str(row["task_id"])

    def claim_dispatch(
        self, *, intent_id: str, now: str
    ) -> tuple[DispatchIntentSnapshot, bool]:
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(
                connection, intent_id=intent_id
            )
            if intent is None:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            if intent.state != "pending":
                connection.commit()
                return intent, False
            connection.execute(
                "INSERT INTO dispatch_attempts(intent_id, claimed_at) VALUES (?, ?)",
                (intent_id, now),
            )
            claimed = self._load_dispatch_intent(connection, intent_id=intent_id)
            if claimed is None or claimed.state != "dispatching":
                raise TaskStoreCorruption("claimed dispatch intent cannot be read")
            connection.commit()
            return claimed, True
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict("dispatch claim conflicts with durable state") from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    @staticmethod
    def _validate_dispatch_handle(
        intent: DispatchIntentSnapshot, handle: Mapping[str, Any]
    ) -> None:
        required = {
            "submission_id",
            "task_id",
            "node_id",
            "job_id",
            "idempotency_key",
            "plan_hash",
            "request_hash",
            "algorithm",
            "adapter_version",
            "fingerprint",
        }
        algorithm = handle.get("algorithm")
        fingerprint = handle.get("fingerprint")
        request = intent.request
        if (
            set(handle) != required
            or handle.get("task_id") != intent.task_id
            or handle.get("node_id") != intent.node_id
            or handle.get("idempotency_key") != intent.node_idempotency_key
            or handle.get("plan_hash") != intent.plan_hash
            or handle.get("adapter_version") != intent.adapter_version
            or algorithm != request.get("algorithm")
            or not isinstance(fingerprint, Mapping)
            or fingerprint.get("algorithm") != request.get("algorithm")
            or fingerprint.get("adapter_version") != intent.adapter_version
            or fingerprint.get("seed") != request.get("parameters", {}).get("seed")
            or fingerprint.get("hardware", {}).get("device")
            != request.get("resources", {}).get("device")
            or fingerprint.get("normalized_config_hash")
            != request.get("normalized_config_hash")
            or fingerprint.get("input_hashes")
            != [request.get("dataset", {}).get("content_hash")]
            or not isinstance(handle.get("submission_id"), str)
            or not isinstance(handle.get("job_id"), str)
            or not isinstance(handle.get("request_hash"), str)
        ):
            raise TaskStoreConflict(
                "dispatch handle differs from its immutable intent"
            )

    def record_dispatch_success(
        self,
        *,
        intent_id: str,
        handle: Mapping[str, Any],
        now: str,
    ) -> DispatchIntentSnapshot:
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            if intent is None:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            self._validate_dispatch_handle(intent, handle)
            if intent.state == "dispatched":
                if intent.handle != dict(handle):
                    raise TaskStoreConflict(
                        "dispatch success differs from the recorded handle"
                    )
                connection.commit()
                return intent
            if intent.state != "dispatching":
                raise TaskStoreConflict("dispatch intent is not claimed")
            document = {
                "status": "dispatched",
                "handle": dict(handle),
                "recorded_at": now,
            }
            document_json, document_hash = encode_document(document)
            connection.execute(
                """
                INSERT INTO dispatch_outcomes(
                    intent_id, outcome, document_json, document_hash, recorded_at
                ) VALUES (?, 'dispatched', ?, ?, ?)
                """,
                (intent_id, document_json, document_hash, now),
            )
            stored = self._load_dispatch_intent(connection, intent_id=intent_id)
            if stored is None or stored.state != "dispatched":
                raise TaskStoreCorruption("dispatch success cannot be read")
            connection.commit()
            return stored
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict("dispatch outcome conflicts with durable state") from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def record_dispatch_reconciliation(
        self,
        *,
        intent_id: str,
        failure_code: str,
        now: str,
    ) -> DispatchIntentSnapshot:
        if (
            not isinstance(failure_code, str)
            or not failure_code
            or len(failure_code) > 128
            or not failure_code.replace("_", "").isalnum()
            or failure_code.upper() != failure_code
        ):
            raise TaskStoreConflict("dispatch failure code is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            if intent is None:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            if intent.reconciliation is not None:
                if intent.reconciliation.failure_code != failure_code:
                    raise TaskStoreConflict(
                        "dispatch reconciliation outcome already differs"
                    )
                connection.commit()
                return intent
            if intent.state != "dispatching":
                raise TaskStoreConflict("dispatch intent is not claimed")
            document = {
                "status": "reconciliation_required",
                "failure_code": failure_code,
                "recorded_at": now,
            }
            document_json, document_hash = encode_document(document)
            connection.execute(
                """
                INSERT INTO dispatch_outcomes(
                    intent_id, outcome, document_json, document_hash, recorded_at
                ) VALUES (?, 'reconciliation_required', ?, ?, ?)
                """,
                (intent_id, document_json, document_hash, now),
            )
            stored = self._load_dispatch_intent(connection, intent_id=intent_id)
            if stored is None or stored.state != "reconciliation_required":
                raise TaskStoreCorruption(
                    "dispatch reconciliation outcome cannot be read"
                )
            connection.commit()
            return stored
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict("dispatch outcome conflicts with durable state") from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def append_draft_revision(
        self,
        *,
        task_id: str,
        expected_revision: int,
        draft: Mapping[str, Any],
        now: str,
        project_id: str | None = None,
        principal_id: str | None = None,
        idempotency_key: str | None = None,
        request_hash: str | None = None,
    ) -> TaskSnapshot:
        document_json, document_hash = encode_document(draft)
        mutation = self._mutation_arguments(
            project_id=project_id,
            principal_id=principal_id,
            idempotency_key=idempotency_key,
            request_hash=request_hash,
        )
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            row = connection.execute(
                """
                SELECT status, project_id, principal_id,
                       current_draft_id, current_draft_revision
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if row is None:
                raise TaskStoreConflict("task does not exist")
            if mutation is not None:
                mutation_project, mutation_principal, key, mutation_hash = mutation
                if (
                    row["project_id"] != mutation_project
                    or row["principal_id"] != mutation_principal
                ):
                    raise TaskStoreConflict("workbench mutation crosses task scope")
                replay = self._load_workbench_mutation(
                    connection,
                    project_id=mutation_project,
                    principal_id=mutation_principal,
                    operation="revise_draft",
                    idempotency_key=key,
                    request_hash=mutation_hash,
                )
                if replay is not None:
                    if replay.task_id != task_id:
                        raise IdempotencyConflict(
                            "idempotency key identifies another task"
                        )
                    snapshot = self._load_snapshot(connection, task_id)
                    if snapshot is None:
                        raise TaskStoreCorruption(
                            "workbench replay references a missing task"
                        )
                    connection.commit()
                    return snapshot
            if row["current_draft_revision"] != expected_revision:
                raise TaskStoreConflict("draft revision precondition failed")
            if draft["draft_id"] != row["current_draft_id"]:
                raise TaskStoreConflict("draft_id is immutable within a task")
            if draft["revision"] != expected_revision + 1:
                raise TaskStoreConflict("draft revision must increase by exactly one")
            if row["status"] not in {"Draft", "NeedsInput", "AwaitingApproval"}:
                raise TaskStoreConflict("draft cannot be revised after runtime entry")
            if draft["status"] not in ALLOWED_TRANSITIONS[row["status"]]:
                raise TaskStoreConflict("invalid draft status transition")
            connection.execute(
                """
                INSERT INTO draft_revisions(
                    task_id, draft_id, revision, document_json,
                    document_hash, recorded_at
                ) VALUES (?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    draft["draft_id"],
                    draft["revision"],
                    document_json,
                    document_hash,
                    now,
                ),
            )
            connection.execute(
                """
                UPDATE tasks
                SET status = ?, current_draft_revision = ?,
                    current_plan_id = NULL, current_approval_id = NULL,
                    updated_at = ?
                WHERE task_id = ?
                """,
                (draft["status"], draft["revision"], now, task_id),
            )
            if mutation is not None:
                self._record_workbench_mutation(
                    connection,
                    project_id=mutation[0],
                    principal_id=mutation[1],
                    operation="revise_draft",
                    idempotency_key=mutation[2],
                    request_hash=mutation[3],
                    task_id=task_id,
                    outcome={
                        "task_id": task_id,
                        "draft_id": draft["draft_id"],
                        "draft_revision": draft["revision"],
                    },
                    now=now,
                )
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None:
                raise TaskStoreCorruption("updated task cannot be read")
            connection.commit()
            return snapshot
        except (IdempotencyConflict, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict("draft revision conflicts with durable state") from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def store_plan(
        self,
        *,
        task_id: str,
        plan: Mapping[str, Any],
        now: str,
        project_id: str | None = None,
        principal_id: str | None = None,
        idempotency_key: str | None = None,
        request_hash: str | None = None,
    ) -> TaskSnapshot:
        document_json, document_hash = encode_document(plan)
        mutation = self._mutation_arguments(
            project_id=project_id,
            principal_id=principal_id,
            idempotency_key=idempotency_key,
            request_hash=request_hash,
        )
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            task = connection.execute(
                """
                SELECT status, project_id, principal_id,
                       current_draft_id, current_draft_revision
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreConflict("task does not exist")
            if mutation is not None:
                mutation_project, mutation_principal, key, mutation_hash = mutation
                if (
                    task["project_id"] != mutation_project
                    or task["principal_id"] != mutation_principal
                ):
                    raise TaskStoreConflict("workbench mutation crosses task scope")
                replay = self._load_workbench_mutation(
                    connection,
                    project_id=mutation_project,
                    principal_id=mutation_principal,
                    operation="persist_plan",
                    idempotency_key=key,
                    request_hash=mutation_hash,
                )
                if replay is not None:
                    if replay.task_id != task_id:
                        raise IdempotencyConflict(
                            "idempotency key identifies another task"
                        )
                    snapshot = self._load_snapshot(connection, task_id)
                    if snapshot is None:
                        raise TaskStoreCorruption(
                            "workbench replay references a missing task"
                        )
                    connection.commit()
                    return snapshot
            if task["status"] != "AwaitingApproval":
                raise TaskStoreConflict(
                    "plans can only target an AwaitingApproval draft"
                )
            expected_draft = {
                "draft_id": task["current_draft_id"],
                "revision": task["current_draft_revision"],
            }
            if plan["draft"] != expected_draft:
                raise TaskStoreConflict("plan does not target the current draft revision")
            existing = connection.execute(
                "SELECT task_id, document_hash FROM plans WHERE plan_id = ?",
                (plan["plan_id"],),
            ).fetchone()
            if existing is not None:
                if (
                    existing["task_id"] != task_id
                    or existing["document_hash"] != document_hash
                ):
                    raise TaskStoreConflict("plan_id already identifies another plan")
                if mutation is not None:
                    self._record_workbench_mutation(
                        connection,
                        project_id=mutation[0],
                        principal_id=mutation[1],
                        operation="persist_plan",
                        idempotency_key=mutation[2],
                        request_hash=mutation[3],
                        task_id=task_id,
                        outcome={
                            "task_id": task_id,
                            "plan_id": plan["plan_id"],
                            "plan_hash": plan["plan_hash"],
                        },
                        now=now,
                    )
                snapshot = self._load_snapshot(connection, task_id)
                if snapshot is None:
                    raise TaskStoreCorruption("task with stored plan cannot be read")
                connection.commit()
                return snapshot
            else:
                connection.execute(
                    """
                    INSERT INTO plans(
                        task_id, plan_id, draft_id, draft_revision, plan_hash,
                        document_json, document_hash, recorded_at
                    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        task_id,
                        plan["plan_id"],
                        plan["draft"]["draft_id"],
                        plan["draft"]["revision"],
                        plan["plan_hash"],
                        document_json,
                        document_hash,
                        now,
                    ),
                )
                for node in plan["nodes"]:
                    connection.execute(
                        """
                        INSERT INTO plan_node_idempotency(
                            task_id, plan_id, node_id, idempotency_key
                        ) VALUES (?, ?, ?, ?)
                        """,
                        (
                            task_id,
                            plan["plan_id"],
                            node["node_id"],
                            node["idempotency_key"],
                        ),
                    )
            connection.execute(
                """
                UPDATE tasks
                SET current_plan_id = ?, current_approval_id = NULL, updated_at = ?
                WHERE task_id = ?
                """,
                (plan["plan_id"], now, task_id),
            )
            if mutation is not None:
                self._record_workbench_mutation(
                    connection,
                    project_id=mutation[0],
                    principal_id=mutation[1],
                    operation="persist_plan",
                    idempotency_key=mutation[2],
                    request_hash=mutation[3],
                    task_id=task_id,
                    outcome={
                        "task_id": task_id,
                        "plan_id": plan["plan_id"],
                        "plan_hash": plan["plan_hash"],
                    },
                    now=now,
                )
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None:
                raise TaskStoreCorruption("task with stored plan cannot be read")
            connection.commit()
            return snapshot
        except (IdempotencyConflict, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict("plan conflicts with durable state") from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def store_approval(
        self,
        *,
        task_id: str,
        approval: Mapping[str, Any],
        now: str,
        project_id: str | None = None,
        principal_id: str | None = None,
        idempotency_key: str | None = None,
        request_hash: str | None = None,
    ) -> TaskSnapshot:
        document_json, document_hash = encode_document(approval)
        mutation = self._mutation_arguments(
            project_id=project_id,
            principal_id=principal_id,
            idempotency_key=idempotency_key,
            request_hash=request_hash,
        )
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            task = connection.execute(
                """
                SELECT status, project_id, principal_id, current_plan_id
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreConflict("task does not exist")
            approval_plan = connection.execute(
                """
                SELECT plan_hash, document_json, document_hash
                FROM plans WHERE task_id = ? AND plan_id = ?
                """,
                (task_id, approval.get("plan_id")),
            ).fetchone()
            if approval_plan is None:
                raise TaskStoreConflict("approval plan does not exist")
            plan_document = _decode_hashed_document(
                approval_plan, label="approval plan"
            )
            if approval.get("plan_hash") != approval_plan["plan_hash"]:
                raise TaskStoreConflict(
                    "approval does not bind its durable plan hash"
                )
            _validate_store_approval_retry_policy(approval, plan_document)
            if mutation is not None:
                mutation_project, mutation_principal, key, mutation_hash = mutation
                if (
                    task["project_id"] != mutation_project
                    or task["principal_id"] != mutation_principal
                ):
                    raise TaskStoreConflict("workbench mutation crosses task scope")
                replay = self._load_workbench_mutation(
                    connection,
                    project_id=mutation_project,
                    principal_id=mutation_principal,
                    operation="persist_approval",
                    idempotency_key=key,
                    request_hash=mutation_hash,
                )
                if replay is not None:
                    if replay.task_id != task_id:
                        raise IdempotencyConflict(
                            "idempotency key identifies another task"
                        )
                    snapshot = self._load_snapshot(connection, task_id)
                    if snapshot is None:
                        raise TaskStoreCorruption(
                            "workbench replay references a missing task"
                        )
                    connection.commit()
                    return snapshot
            if task["status"] != "AwaitingApproval":
                raise TaskStoreConflict(
                    "decisions can only target an AwaitingApproval task"
                )
            if task["current_plan_id"] is None:
                raise TaskStoreConflict("task has no current plan")
            if (
                approval.get("plan_id") != task["current_plan_id"]
                or approval.get("plan_hash") != approval_plan["plan_hash"]
            ):
                raise TaskStoreConflict("approval does not bind the current plan hash")
            existing = connection.execute(
                "SELECT task_id, document_hash FROM approvals WHERE approval_id = ?",
                (approval["approval_id"],),
            ).fetchone()
            if existing is not None:
                if (
                    existing["task_id"] != task_id
                    or existing["document_hash"] != document_hash
                ):
                    raise TaskStoreConflict(
                        "approval_id already identifies another decision"
                    )
                if mutation is not None:
                    self._record_workbench_mutation(
                        connection,
                        project_id=mutation[0],
                        principal_id=mutation[1],
                        operation="persist_approval",
                        idempotency_key=mutation[2],
                        request_hash=mutation[3],
                        task_id=task_id,
                        outcome={
                            "task_id": task_id,
                            "approval_id": approval["approval_id"],
                            "decision": approval["decision"],
                        },
                        now=now,
                    )
                snapshot = self._load_snapshot(connection, task_id)
                if snapshot is None:
                    raise TaskStoreCorruption(
                        "task with stored approval cannot be read"
                    )
                connection.commit()
                return snapshot
            else:
                connection.execute(
                    """
                    INSERT INTO approvals(
                        task_id, approval_id, plan_id, plan_hash, decision,
                        document_json, document_hash, recorded_at
                    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        task_id,
                        approval["approval_id"],
                        approval["plan_id"],
                        approval["plan_hash"],
                        approval["decision"],
                        document_json,
                        document_hash,
                        now,
                    ),
                )
            connection.execute(
                """
                UPDATE tasks
                SET current_approval_id = ?, updated_at = ?
                WHERE task_id = ?
                """,
                (approval["approval_id"], now, task_id),
            )
            if mutation is not None:
                self._record_workbench_mutation(
                    connection,
                    project_id=mutation[0],
                    principal_id=mutation[1],
                    operation="persist_approval",
                    idempotency_key=mutation[2],
                    request_hash=mutation[3],
                    task_id=task_id,
                    outcome={
                        "task_id": task_id,
                        "approval_id": approval["approval_id"],
                        "decision": approval["decision"],
                    },
                    now=now,
                )
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None:
                raise TaskStoreCorruption("task with stored approval cannot be read")
            connection.commit()
            return snapshot
        except (IdempotencyConflict, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict("approval conflicts with durable state") from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def abandon_task(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        abandonment: Mapping[str, Any],
        idempotency_key: str,
        request_hash: str,
        now: str,
    ) -> tuple[TaskSnapshot, bool]:
        """Atomically audit a user discard and terminate only pre-runtime work."""

        document_json, document_hash = encode_document(abandonment)
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            replay = self._load_workbench_mutation(
                connection,
                project_id=project_id,
                principal_id=principal_id,
                operation="abandon_task",
                idempotency_key=idempotency_key,
                request_hash=request_hash,
            )
            if replay is not None:
                if replay.task_id != task_id:
                    raise IdempotencyConflict(
                        "idempotency key identifies another task"
                    )
                snapshot = self._load_snapshot(connection, task_id)
                if snapshot is None:
                    raise TaskStoreCorruption(
                        "workbench replay references a missing task"
                    )
                connection.commit()
                return snapshot, True

            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreConflict("task does not exist")
            if (
                task["project_id"] != project_id
                or task["principal_id"] != principal_id
            ):
                raise TaskStoreConflict("workbench mutation crosses task scope")
            if task["status"] not in {"Draft", "NeedsInput", "AwaitingApproval"}:
                raise TaskStoreConflict(
                    "only a pre-runtime task can be abandoned"
                )
            if connection.execute(
                """
                SELECT 1 FROM approvals
                WHERE task_id = ? AND decision = 'approved'
                LIMIT 1
                """,
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "an approved task cannot be abandoned before submit reconciliation"
                )
            if "Cancelled" not in ALLOWED_TRANSITIONS[task["status"]]:
                raise TaskStoreConflict("pre-runtime abandonment transition is invalid")
            if connection.execute(
                "SELECT 1 FROM task_abandonments WHERE task_id = ?", (task_id,)
            ).fetchone() is not None:
                raise TaskStoreCorruption(
                    "pre-runtime task already has an abandonment record"
                )
            connection.execute(
                """
                INSERT INTO task_abandonments(
                    task_id, project_id, principal_id, document_json,
                    document_hash, abandoned_at
                ) VALUES (?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    project_id,
                    principal_id,
                    document_json,
                    document_hash,
                    now,
                ),
            )
            connection.execute(
                """
                UPDATE tasks SET status = 'Cancelled', updated_at = ?
                WHERE task_id = ?
                """,
                (now, task_id),
            )
            self._record_workbench_mutation(
                connection,
                project_id=project_id,
                principal_id=principal_id,
                operation="abandon_task",
                idempotency_key=idempotency_key,
                request_hash=request_hash,
                task_id=task_id,
                outcome={"task_id": task_id, "status": "Cancelled"},
                now=now,
            )
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None or snapshot.status != "Cancelled":
                raise TaskStoreCorruption("abandoned task cannot be read")
            connection.commit()
            return snapshot, False
        except (IdempotencyConflict, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict(
                "task abandonment conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def change_task_visibility(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        operation: str,
        expected_visibility_revision: int,
        idempotency_key: str,
        request_hash: str,
        now: str,
    ) -> TaskVisibilityMutationRecord:
        """Atomically append one scope-bound trash/restore visibility event."""

        if operation not in TASK_VISIBILITY_OPERATIONS:
            raise TaskStoreConflict("task visibility operation is invalid")
        if (
            type(expected_visibility_revision) is not int
            or expected_visibility_revision < 0
        ):
            raise TaskStoreConflict("task visibility revision is invalid")
        target_action = "trashed" if operation == "trash_task" else "restored"
        expected_state = "active" if operation == "trash_task" else "trashed"
        target_state = "trashed" if operation == "trash_task" else "active"

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            if operation == "restore_task":
                purge_request = connection.execute(
                    """
                    SELECT 1 FROM task_purge_requests
                    WHERE task_id = ? AND project_id = ? AND principal_id = ?
                    """,
                    (task_id, project_id, principal_id),
                ).fetchone()
                if purge_request is not None:
                    raise TaskStoreConflict(
                        "a task with a purge request cannot be restored"
                    )
            mutation = connection.execute(
                """
                SELECT task_id, request_hash, visibility_revision,
                       outcome_json AS document_json,
                       outcome_hash AS document_hash
                FROM task_visibility_mutations
                WHERE project_id = ? AND principal_id = ?
                  AND operation = ? AND idempotency_key = ?
                """,
                (project_id, principal_id, operation, idempotency_key),
            ).fetchone()
            if mutation is not None:
                if mutation["request_hash"] != request_hash:
                    raise IdempotencyConflict(
                        "idempotency key was already used for another visibility request"
                    )
                outcome = _decode_hashed_document(
                    mutation, label="task visibility mutation outcome"
                )
                if (
                    mutation["task_id"] != task_id
                    or set(outcome)
                    != {"task_id", "visibility_revision", "trashed_at"}
                    or outcome.get("task_id") != task_id
                    or type(outcome.get("visibility_revision")) is not int
                    or outcome["visibility_revision"] < 1
                    or outcome["visibility_revision"]
                    != mutation["visibility_revision"]
                    or (
                        operation == "trash_task"
                        and not isinstance(outcome.get("trashed_at"), str)
                    )
                    or (
                        operation == "restore_task"
                        and outcome.get("trashed_at") is not None
                    )
                ):
                    raise TaskStoreCorruption(
                        "task visibility mutation outcome is invalid"
                    )
                snapshot = self._load_snapshot(connection, task_id)
                if (
                    snapshot is None
                    or snapshot.project_id != project_id
                    or snapshot.principal_id != principal_id
                ):
                    raise TaskStoreCorruption(
                        "task visibility replay references a missing task"
                    )
                replay_snapshot = replace(
                    snapshot,
                    visibility_revision=outcome["visibility_revision"],
                    trashed_at=outcome["trashed_at"],
                )
                connection.commit()
                return TaskVisibilityMutationRecord(replay_snapshot, True)

            task = connection.execute(
                """
                SELECT task_id, project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreConflict("task does not exist")
            if (
                task["project_id"] != project_id
                or task["principal_id"] != principal_id
            ):
                raise TaskStoreConflict("task visibility mutation crosses task scope")

            visibility = connection.execute(
                """
                SELECT state, revision, trashed_at
                FROM task_visibility WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            current_state = "active" if visibility is None else visibility["state"]
            current_revision = 0 if visibility is None else visibility["revision"]
            if (
                current_revision != expected_visibility_revision
                or current_state != expected_state
            ):
                raise TaskStoreConflict(
                    "task visibility revision or state precondition failed"
                )

            if operation == "trash_task":
                if task["status"] not in {"Succeeded", "Failed", "Cancelled"}:
                    raise TaskStoreConflict(
                        "only a terminal task can be moved to trash"
                    )
                evidence = connection.execute(
                    """
                    SELECT
                        EXISTS(
                            SELECT 1 FROM task_abandonments
                            WHERE task_id = ?
                        ) AS abandoned,
                        EXISTS(
                            SELECT 1 FROM approvals
                            WHERE task_id = ? AND decision = 'approved'
                        ) AS approved,
                        (
                            SELECT intent.intent_id
                            FROM dispatch_intents AS intent
                            WHERE intent.task_id = ?
                        ) AS intent_id,
                        EXISTS(
                            SELECT 1
                            FROM dispatch_intents AS intent
                            JOIN effective_dispatched_intents AS outcome
                              ON outcome.intent_id = intent.intent_id
                            WHERE intent.task_id = ?
                        ) AS has_effective_dispatch,
                        EXISTS(
                            SELECT 1
                            FROM dispatch_intents AS intent
                            JOIN worker_retry_exhaustions AS exhaustion
                              ON exhaustion.intent_id = intent.intent_id
                             AND exhaustion.attempt_number = 2
                            JOIN run_events AS terminal
                              ON terminal.task_id = exhaustion.task_id
                             AND terminal.sequence =
                                 exhaustion.terminal_event_sequence
                            WHERE intent.task_id = ?
                              AND exhaustion.task_id = intent.task_id
                              AND terminal.event_type = 'node_failed'
                              AND terminal.task_status = 'Failed'
                              AND terminal.document_hash =
                                  exhaustion.terminal_event_hash
                        ) AS has_retry_exhaustion,
                        EXISTS(
                            SELECT 1
                            FROM dispatch_intents AS intent
                            JOIN worker_exit_retry_exhaustions AS exhaustion
                              ON exhaustion.intent_id = intent.intent_id
                             AND exhaustion.attempt_number = 2
                            JOIN run_events AS terminal
                              ON terminal.task_id = exhaustion.task_id
                             AND terminal.sequence =
                                 exhaustion.terminal_event_sequence
                            WHERE intent.task_id = ?
                              AND exhaustion.task_id = intent.task_id
                              AND exhaustion.failure_kind =
                                  'pre_running_launch_failure'
                              AND terminal.event_type = 'node_failed'
                              AND terminal.task_status = 'Failed'
                              AND terminal.document_hash =
                                  exhaustion.terminal_event_hash
                        ) AS has_worker_exit_retry_exhaustion,
                        EXISTS(
                            SELECT 1
                            FROM dispatch_intents AS intent
                            JOIN dispatch_reconciliation_negative_resolutions
                                 AS negative
                              ON negative.intent_id = intent.intent_id
                            JOIN run_events AS terminal
                              ON terminal.task_id = negative.task_id
                             AND terminal.sequence =
                                 negative.terminal_event_sequence
                            WHERE intent.task_id = ?
                              AND negative.task_id = intent.task_id
                              AND terminal.event_type = 'node_failed'
                              AND terminal.task_status = 'Failed'
                              AND terminal.document_hash =
                                  negative.terminal_event_hash
                        ) AS has_negative_reconciliation
                    """,
                    (
                        task_id,
                        task_id,
                        task_id,
                        task_id,
                        task_id,
                        task_id,
                        task_id,
                    ),
                ).fetchone()
                pre_runtime_abandonment = (
                    task["status"] == "Cancelled"
                    and evidence["abandoned"] == 1
                    and evidence["approved"] == 0
                    and evidence["intent_id"] is None
                )
                resolved_runtime = (
                    evidence["intent_id"] is not None
                    and (
                        evidence["has_effective_dispatch"] == 1
                        or (
                            task["status"] == "Failed"
                            and (
                                evidence["has_retry_exhaustion"] == 1
                                or evidence[
                                    "has_worker_exit_retry_exhaustion"
                                ]
                                == 1
                                or evidence["has_negative_reconciliation"]
                                == 1
                            )
                        )
                    )
                )
                if not (pre_runtime_abandonment or resolved_runtime):
                    raise TaskStoreConflict(
                        "an unresolved task cannot be moved to trash"
                    )

            next_revision = current_revision + 1
            trashed_at = now if target_state == "trashed" else None
            identity_text = "\x1f".join(
                (
                    project_id,
                    principal_id,
                    operation,
                    idempotency_key,
                    request_hash,
                    task_id,
                    str(next_revision),
                )
            )
            event_id = "visibility-" + hashlib.sha256(
                identity_text.encode("utf-8")
            ).hexdigest()[:32]
            event = {
                "schema_version": "1.0.0",
                "event_id": event_id,
                "task_id": task_id,
                "revision": next_revision,
                "action": target_action,
                "previous_state": current_state,
                "state": target_state,
                "trashed_at": trashed_at,
                "actor": {"type": "user", "id": principal_id},
                "occurred_at": now,
                "extensions": {},
            }
            event_json, event_hash = encode_document(event)
            connection.execute(
                """
                INSERT INTO task_visibility_events(
                    task_id, project_id, principal_id, revision, event_id,
                    action, previous_state, state, trashed_at,
                    document_json, document_hash, occurred_at, recorded_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    project_id,
                    principal_id,
                    next_revision,
                    event_id,
                    target_action,
                    current_state,
                    target_state,
                    trashed_at,
                    event_json,
                    event_hash,
                    now,
                    now,
                ),
            )
            if visibility is None:
                connection.execute(
                    """
                    INSERT INTO task_visibility(
                        task_id, project_id, principal_id, state,
                        revision, trashed_at, updated_at
                    ) VALUES (?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        task_id,
                        project_id,
                        principal_id,
                        target_state,
                        next_revision,
                        trashed_at,
                        now,
                    ),
                )
            else:
                updated = connection.execute(
                    """
                    UPDATE task_visibility
                    SET state = ?, revision = ?, trashed_at = ?, updated_at = ?
                    WHERE task_id = ? AND revision = ? AND state = ?
                    """,
                    (
                        target_state,
                        next_revision,
                        trashed_at,
                        now,
                        task_id,
                        current_revision,
                        current_state,
                    ),
                )
                if updated.rowcount != 1:
                    raise TaskStoreConflict(
                        "task visibility changed concurrently"
                    )

            outcome = {
                "task_id": task_id,
                "visibility_revision": next_revision,
                "trashed_at": trashed_at,
            }
            outcome_json, outcome_hash = encode_document(outcome)
            connection.execute(
                """
                INSERT INTO task_visibility_mutations(
                    project_id, principal_id, operation, idempotency_key,
                    request_hash, task_id, visibility_revision,
                    outcome_json, outcome_hash, created_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    project_id,
                    principal_id,
                    operation,
                    idempotency_key,
                    request_hash,
                    task_id,
                    next_revision,
                    outcome_json,
                    outcome_hash,
                    now,
                ),
            )
            snapshot = self._load_snapshot(connection, task_id)
            if (
                snapshot is None
                or snapshot.visibility_revision != next_revision
                or snapshot.trashed_at != trashed_at
            ):
                raise TaskStoreCorruption(
                    "task visibility mutation cannot be read"
                )
            connection.commit()
            return TaskVisibilityMutationRecord(snapshot, False)
        except (IdempotencyConflict, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict(
                "task visibility mutation conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def reserve_task_purge(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        expected_visibility_revision: int,
        idempotency_key: str,
        request_hash: str,
        now: str,
    ) -> TaskPurgeRecord:
        """Reserve an irreversible purge before any filesystem side effect."""

        strings = (
            task_id,
            project_id,
            principal_id,
            idempotency_key,
            request_hash,
            now,
        )
        if (
            any(not isinstance(value, str) or not value for value in strings)
            or type(expected_visibility_revision) is not int
            or expected_visibility_revision < 1
        ):
            raise TaskStoreConflict("task purge request is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            alias = connection.execute(
                """
                SELECT request_hash, purge_id, task_id, visibility_revision
                FROM task_purge_idempotency
                WHERE project_id = ? AND principal_id = ?
                  AND operation = 'purge_task' AND idempotency_key = ?
                """,
                (project_id, principal_id, idempotency_key),
            ).fetchone()
            if alias is not None:
                if (
                    alias["request_hash"] != request_hash
                    or alias["task_id"] != task_id
                    or alias["visibility_revision"]
                    != expected_visibility_revision
                ):
                    raise IdempotencyConflict(
                        "idempotency key was already used for another purge request"
                    )
                record = self._load_task_purge_record(
                    connection, task_id=task_id, replayed=True
                )
                if (
                    record.purge_id != alias["purge_id"]
                    or record.snapshot.project_id != project_id
                    or record.snapshot.principal_id != principal_id
                ):
                    raise TaskStoreCorruption(
                        "task purge replay crosses its durable scope"
                    )
                connection.commit()
                return record

            task = connection.execute(
                "SELECT project_id, principal_id FROM tasks WHERE task_id = ?",
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreConflict("task does not exist")
            if (
                task["project_id"] != project_id
                or task["principal_id"] != principal_id
            ):
                raise TaskStoreConflict("task purge crosses task scope")

            existing = connection.execute(
                """
                SELECT request.purge_id, request.request_hash,
                       request.visibility_revision,
                       outcome.purge_id AS completed_purge_id
                FROM task_purge_requests AS request
                LEFT JOIN task_purge_outcomes AS outcome
                  ON outcome.purge_id = request.purge_id
                WHERE request.task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if existing is not None:
                if existing["completed_purge_id"] is not None:
                    raise TaskStoreConflict("task is already purged")
                if (
                    existing["request_hash"] != request_hash
                    or existing["visibility_revision"]
                    != expected_visibility_revision
                ):
                    raise TaskStoreConflict(
                        "task already has another purge reservation"
                    )
                connection.execute(
                    """
                    INSERT INTO task_purge_idempotency(
                        project_id, principal_id, operation, idempotency_key,
                        request_hash, purge_id, task_id,
                        visibility_revision, created_at
                    ) VALUES (?, ?, 'purge_task', ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        project_id,
                        principal_id,
                        idempotency_key,
                        request_hash,
                        existing["purge_id"],
                        task_id,
                        expected_visibility_revision,
                        now,
                    ),
                )
                record = self._load_task_purge_record(
                    connection, task_id=task_id, replayed=True
                )
                connection.commit()
                return record

            visibility = connection.execute(
                """
                SELECT state, revision FROM task_visibility
                WHERE task_id = ? AND project_id = ? AND principal_id = ?
                """,
                (task_id, project_id, principal_id),
            ).fetchone()
            if (
                visibility is None
                or visibility["state"] != "trashed"
                or visibility["revision"] != expected_visibility_revision
            ):
                raise TaskStoreConflict(
                    "task purge revision or trash precondition failed"
                )
            intent = self._load_dispatch_intent(connection, task_id=task_id)
            if intent is not None and intent.state == "not_dispatched":
                raise TaskStoreConflict(
                    "negative reconciliation has no authorized purge cleanup"
                )
            identity_text = "\x1f".join(
                (
                    project_id,
                    principal_id,
                    task_id,
                    str(expected_visibility_revision),
                )
            )
            purge_id = "purge-" + hashlib.sha256(
                identity_text.encode("utf-8")
            ).hexdigest()[:32]
            request = {
                "schema_version": "1.0.0",
                "purge_id": purge_id,
                "task_id": task_id,
                "visibility_revision": expected_visibility_revision,
                "purge_state": "pending",
                "actor": {"type": "user", "id": principal_id},
                "requested_at": now,
                "extensions": {},
            }
            document_json, document_hash = encode_document(request)
            connection.execute(
                """
                INSERT INTO task_purge_requests(
                    purge_id, task_id, project_id, principal_id,
                    visibility_revision, request_hash,
                    document_json, document_hash, requested_at, recorded_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    purge_id,
                    task_id,
                    project_id,
                    principal_id,
                    expected_visibility_revision,
                    request_hash,
                    document_json,
                    document_hash,
                    now,
                    now,
                ),
            )
            connection.execute(
                """
                INSERT INTO task_purge_idempotency(
                    project_id, principal_id, operation, idempotency_key,
                    request_hash, purge_id, task_id,
                    visibility_revision, created_at
                ) VALUES (?, ?, 'purge_task', ?, ?, ?, ?, ?, ?)
                """,
                (
                    project_id,
                    principal_id,
                    idempotency_key,
                    request_hash,
                    purge_id,
                    task_id,
                    expected_visibility_revision,
                    now,
                ),
            )
            record = self._load_task_purge_record(
                connection, task_id=task_id, replayed=False
            )
            connection.commit()
            return record
        except (IdempotencyConflict, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict(
                "task purge reservation conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def _load_worker_exit_retry_exhaustion_cleanup_proof(
        self,
        connection: sqlite3.Connection,
        *,
        purge_id: str,
        task_id: str,
        project_id: str,
        principal_id: str,
        purge_requested_at: str,
        intent: DispatchIntentSnapshot,
    ) -> RetryExhaustionCleanupProof:
        """Authenticate the no-handle B2 mixed-lineage purge proof."""

        exhaustion = connection.execute(
            """
            SELECT * FROM worker_exit_retry_exhaustions
            WHERE intent_id = ? AND attempt_number = 2
            """,
            (intent.intent_id,),
        ).fetchone()
        reservation = connection.execute(
            """
            SELECT * FROM worker_exit_retry_reservations
            WHERE intent_id = ? AND attempt_number = 2
            """,
            (intent.intent_id,),
        ).fetchone()
        conflicting_lineage = connection.execute(
            """
            SELECT EXISTS(
                       SELECT 1 FROM worker_retry_exhaustions
                       WHERE intent_id = ? AND attempt_number = 2
                   )
                   OR EXISTS(
                       SELECT 1 FROM worker_retry_reservations
                       WHERE intent_id = ? AND attempt_number = 2
                   )
            """,
            (intent.intent_id, intent.intent_id),
        ).fetchone()[0]
        replacement = connection.execute(
            """
            SELECT 1 FROM worker_exit_retry_dispatch_replacements
            WHERE intent_id = ? AND attempt_number = 2
            """,
            (intent.intent_id,),
        ).fetchone()
        attempts = connection.execute(
            """
            SELECT * FROM worker_launch_attempts
            WHERE intent_id = ? ORDER BY attempt_number
            """,
            (intent.intent_id,),
        ).fetchall()
        if (
            exhaustion is None
            or reservation is None
            or bool(conflicting_lineage)
            or replacement is not None
            or len(attempts) != 2
        ):
            raise TaskStoreCorruption(
                "worker-exit retry cleanup lineage is incomplete"
            )
        previous_attempt, current_attempt = attempts
        observation = connection.execute(
            """
            SELECT * FROM worker_attempt_observations
            WHERE attempt_id = ? AND observation_sequence = ?
            """,
            (
                exhaustion["attempt_id"],
                exhaustion["observation_sequence"],
            ),
        ).fetchone()
        previous_observation = connection.execute(
            """
            SELECT * FROM worker_attempt_observations
            WHERE attempt_id = ? AND observation_sequence = ?
            """,
            (
                reservation["previous_attempt_id"],
                reservation["previous_observation_sequence"],
            ),
        ).fetchone()
        if observation is None or previous_observation is None:
            raise TaskStoreCorruption(
                "worker-exit retry cleanup observations are incomplete"
            )
        evidence = _decode_worker_observation_row(observation)
        previous_evidence = _decode_worker_observation_row(
            previous_observation
        )
        _require_worker_attempt_observation_binding(
            current_attempt,
            evidence,
            intent_id=intent.intent_id,
            task_id=task_id,
            project_id=project_id,
            principal_id=principal_id,
        )
        _require_worker_attempt_observation_binding(
            previous_attempt,
            previous_evidence,
            intent_id=intent.intent_id,
            task_id=task_id,
            project_id=project_id,
            principal_id=principal_id,
        )
        latest_current = connection.execute(
            """
            SELECT MAX(observation_sequence)
            FROM worker_attempt_observations WHERE attempt_id = ?
            """,
            (exhaustion["attempt_id"],),
        ).fetchone()[0]
        latest_previous = connection.execute(
            """
            SELECT MAX(observation_sequence)
            FROM worker_attempt_observations WHERE attempt_id = ?
            """,
            (reservation["previous_attempt_id"],),
        ).fetchone()[0]

        retry_event_row = connection.execute(
            """
            SELECT * FROM run_events
            WHERE task_id = ? AND sequence = ?
            """,
            (task_id, reservation["retry_event_sequence"]),
        ).fetchone()
        retry_commit = connection.execute(
            """
            SELECT * FROM supervised_run_event_commits
            WHERE task_id = ? AND sequence = ?
            """,
            (task_id, reservation["retry_event_sequence"]),
        ).fetchone()
        terminal_event_row = connection.execute(
            """
            SELECT * FROM run_events
            WHERE task_id = ? AND sequence = ?
            """,
            (task_id, exhaustion["terminal_event_sequence"]),
        ).fetchone()
        terminal_commit = connection.execute(
            """
            SELECT * FROM supervised_run_event_commits
            WHERE task_id = ? AND sequence = ?
            """,
            (task_id, exhaustion["terminal_event_sequence"]),
        ).fetchone()
        if any(
            row is None
            for row in (
                retry_event_row,
                retry_commit,
                terminal_event_row,
                terminal_commit,
            )
        ):
            raise TaskStoreCorruption(
                "worker-exit retry cleanup event chain is incomplete"
            )
        assert retry_event_row is not None
        assert retry_commit is not None
        assert terminal_event_row is not None
        assert terminal_commit is not None
        retry_event = _decode_run_event_row(
            retry_event_row, label="worker-exit retry transition event"
        )
        terminal_event = _decode_run_event_row(
            terminal_event_row,
            label="worker-exit retry exhaustion terminal event",
        )
        retry_extension = retry_event.get("extensions", {}).get(
            "org.agent_rpc.worker_exit_retry"
        )
        terminal_extension = terminal_event.get("extensions", {}).get(
            "org.agent_rpc.retry_exhaustion"
        )
        expected_retry_extension = {
            "intent_id": intent.intent_id,
            "attempt_number": 2,
            "previous_attempt_id": reservation["previous_attempt_id"],
            "previous_observation_sequence": reservation[
                "previous_observation_sequence"
            ],
            "evidence_hash": reservation["evidence_hash"],
            "private_schema_version": reservation["private_schema_version"],
            "private_proof_hash": reservation["private_proof_hash"],
            "failure_kind": "worker_exit",
            "max_attempts": 2,
            "source_outcome_document_hash": reservation[
                "source_outcome_document_hash"
            ],
            "source_handle_hash": reservation["source_handle_hash"],
        }
        expected_terminal_extension = {
            "intent_id": intent.intent_id,
            "attempt_id": exhaustion["attempt_id"],
            "attempt_number": 2,
            "observation_sequence": exhaustion["observation_sequence"],
            "evidence_hash": exhaustion["evidence_hash"],
            "private_schema_version": "1.3.0",
            "private_proof_hash": exhaustion["private_proof_hash"],
            "failure_kind": "pre_running_launch_failure",
            "max_attempts": 2,
        }
        current_failure_payload = {
            "schema_version": "1.0.0",
            "failure_kind": "pre_running_launch_failure",
            "submission_id": evidence["submission_id"],
            "attempt_id": evidence["attempt_id"],
            "attempt_number": evidence["attempt_number"],
            "job_id": evidence["job_id"],
            "request_hash": evidence["request_hash"],
            "binding_hash": evidence["binding_hash"],
            "ticket_record_hash": evidence["ticket"]["record_hash"],
        }
        source_handle = self._load_worker_exit_retry_source_handle(
            connection, reservation
        )
        control_conflict = connection.execute(
            """
            SELECT EXISTS(
                       SELECT 1 FROM task_cancel_requests
                       WHERE task_id = ?
                   )
                   OR EXISTS(
                       SELECT 1 FROM task_timeout_outcomes
                       WHERE task_id = ?
                   )
                   OR EXISTS(
                       SELECT 1
                       FROM worker_attempt_timeout_windows AS timeout
                       JOIN supervised_timeout_attempts AS delivery
                         ON delivery.timeout_id = timeout.timeout_id
                       WHERE timeout.attempt_id = ?
                   )
            """,
            (task_id, task_id, exhaustion["attempt_id"]),
        ).fetchone()[0]
        if (
            exhaustion["task_id"] != task_id
            or exhaustion["project_id"] != project_id
            or exhaustion["principal_id"] != principal_id
            or exhaustion["approval_id"] != intent.approval_id
            or exhaustion["attempt_number"] != 2
            or exhaustion["max_attempts"] != 2
            or exhaustion["private_schema_version"] != "1.3.0"
            or exhaustion["failure_kind"] != "pre_running_launch_failure"
            or observation["document_hash"] != exhaustion["evidence_hash"]
            or observation["observation_sequence"] != latest_current
            or evidence["attempt_number"] != 2
            or evidence["ticket"]["state"] != "failed"
            or evidence["ticket"]["worker_pid"] is not None
            or evidence["ready"] is not None
            or evidence["heartbeat"] is not None
            or encode_document(current_failure_payload)[1]
            != exhaustion["private_proof_hash"]
            or reservation["task_id"] != task_id
            or reservation["project_id"] != project_id
            or reservation["principal_id"] != principal_id
            or reservation["approval_id"] != intent.approval_id
            or reservation["failure_kind"] != "worker_exit"
            or reservation["private_schema_version"] not in {"1.1.0", "1.2.0"}
            or not _is_sha256(reservation["private_proof_hash"])
            or previous_observation["document_hash"]
            != reservation["evidence_hash"]
            or previous_observation["observation_sequence"] != latest_previous
            or previous_evidence["attempt_number"] != 1
            or previous_evidence["ticket"]["state"] != "spawned"
            or previous_evidence["ticket"]["worker_pid"] is None
            or previous_evidence["ready"] is None
            or previous_evidence["heartbeat"] is None
            or previous_evidence["heartbeat"]["state"] != "running"
            or previous_attempt["attempt_number"] != 1
            or current_attempt["attempt_number"] != 2
            or previous_attempt["attempt_id"]
            != reservation["previous_attempt_id"]
            or current_attempt["attempt_id"] != exhaustion["attempt_id"]
            or current_attempt["submission_id"]
            != previous_attempt["submission_id"]
            or current_attempt["adapter_request_hash"]
            != previous_attempt["adapter_request_hash"]
            or current_attempt["created_at"] != reservation["reserved_at"]
            or current_attempt["job_id"] == previous_attempt["job_id"]
            or source_handle.get("submission_id")
            != previous_attempt["submission_id"]
            or source_handle.get("job_id") != previous_attempt["job_id"]
            or source_handle.get("request_hash")
            != previous_attempt["adapter_request_hash"]
            or retry_event_row["document_hash"] != reservation["retry_event_hash"]
            or retry_event.get("event_type") != "node_retrying"
            or retry_event.get("task_status") != "Retrying"
            or retry_event.get("node_id") != intent.node_id
            or retry_event.get("fingerprint") != intent.queue_fingerprint
            or retry_extension != expected_retry_extension
            or retry_event_row["recorded_at"] != reservation["reserved_at"]
            or retry_commit["project_id"] != project_id
            or retry_commit["principal_id"] != principal_id
            or retry_commit["fencing_token"] != reservation["first_fencing_token"]
            or retry_commit["recorded_at"] != reservation["reserved_at"]
            or retry_commit["recorded_at_us"] != reservation["reserved_at_us"]
            or terminal_event_row["document_hash"]
            != exhaustion["terminal_event_hash"]
            or terminal_event.get("event_type") != "node_failed"
            or terminal_event.get("task_status") != "Failed"
            or terminal_event.get("error")
            != {
                "code": "retry_exhausted",
                "message": "FWI Worker exhausted its approved attempts",
                "retryable": False,
            }
            or terminal_extension != expected_terminal_extension
            or terminal_event_row["recorded_at"] != exhaustion["exhausted_at"]
            or terminal_commit["project_id"] != project_id
            or terminal_commit["principal_id"] != principal_id
            or terminal_commit["fencing_token"] != exhaustion["fencing_token"]
            or terminal_commit["recorded_at"] != exhaustion["exhausted_at"]
            or terminal_commit["recorded_at_us"] != exhaustion["exhausted_at_us"]
            or bool(control_conflict)
        ):
            raise TaskStoreCorruption(
                "worker-exit retry cleanup proof is inconsistent"
            )
        try:
            _, requested_at_us = _runtime_timestamp(purge_requested_at)
            _, exhausted_at_us = _runtime_timestamp(exhaustion["exhausted_at"])
            _, reserved_at_us = _runtime_timestamp(reservation["reserved_at"])
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "worker-exit retry cleanup time is invalid"
            ) from error
        if (
            exhausted_at_us != exhaustion["exhausted_at_us"]
            or reserved_at_us != reservation["reserved_at_us"]
            or exhausted_at_us < reserved_at_us
            or requested_at_us < exhausted_at_us
        ):
            raise TaskStoreCorruption(
                "worker-exit retry cleanup violates causal order"
            )
        return RetryExhaustionCleanupProof(
            purge_id=purge_id,
            intent_id=intent.intent_id,
            task_id=task_id,
            project_id=project_id,
            principal_id=principal_id,
            approval_id=intent.approval_id,
            attempt_id=exhaustion["attempt_id"],
            observation_sequence=int(exhaustion["observation_sequence"]),
            evidence=copy.deepcopy(evidence),
            evidence_hash=exhaustion["evidence_hash"],
            private_schema_version=exhaustion["private_schema_version"],
            private_proof_hash=exhaustion["private_proof_hash"],
            failure_kind=exhaustion["failure_kind"],
            previous_attempt_id=reservation["previous_attempt_id"],
            previous_observation_sequence=int(
                reservation["previous_observation_sequence"]
            ),
            previous_private_proof_hash=reservation["private_proof_hash"],
            retry_reserved_at=reservation["reserved_at"],
            terminal_event_sequence=int(exhaustion["terminal_event_sequence"]),
            terminal_event_hash=exhaustion["terminal_event_hash"],
            exhausted_at=exhaustion["exhausted_at"],
            previous_failure_kind="worker_exit",
            previous_private_schema_version=reservation[
                "private_schema_version"
            ],
        )

    def get_retry_exhaustion_cleanup_proof(
        self,
        *,
        purge_id: str,
        task_id: str,
        project_id: str,
        principal_id: str,
    ) -> RetryExhaustionCleanupProof | None:
        """Project the exact immutable proof for an exhausted no-handle purge.

        A normal dispatched or pre-runtime purge has no such proof and returns
        ``None``.  Once an exhaustion row is present, every relationship is
        revalidated against its hashed observation, retry reservation,
        terminal RunEvent, and the pending purge reservation before anything
        may cross the Adapter boundary.
        """

        values = (purge_id, task_id, project_id, principal_id)
        if any(not isinstance(value, str) or not value for value in values):
            raise TaskStoreConflict("retry exhaustion cleanup request is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN")
            purge = connection.execute(
                """
                SELECT request.task_id, request.project_id,
                       request.principal_id, request.visibility_revision,
                       request.requested_at,
                       task.status,
                       visibility.state AS visibility_state,
                       visibility.revision AS current_visibility_revision,
                       outcome.purge_id AS completed_purge_id
                FROM task_purge_requests AS request
                JOIN tasks AS task ON task.task_id = request.task_id
                JOIN task_visibility AS visibility
                  ON visibility.task_id = request.task_id
                 AND visibility.project_id = request.project_id
                 AND visibility.principal_id = request.principal_id
                LEFT JOIN task_purge_outcomes AS outcome
                  ON outcome.purge_id = request.purge_id
                WHERE request.purge_id = ?
                """,
                (purge_id,),
            ).fetchone()
            if purge is None:
                raise TaskStoreConflict("task purge reservation does not exist")
            if (
                purge["task_id"] != task_id
                or purge["project_id"] != project_id
                or purge["principal_id"] != principal_id
                or purge["status"] != "Failed"
                or purge["visibility_state"] != "trashed"
                or purge["current_visibility_revision"]
                != purge["visibility_revision"]
                or purge["completed_purge_id"] is not None
            ):
                raise TaskStoreConflict(
                    "retry exhaustion cleanup crosses its pending purge reservation"
                )

            intent = self._load_dispatch_intent(connection, task_id=task_id)
            if intent is None or intent.state != "retry_exhausted":
                connection.commit()
                return None
            if (
                intent.task_id != task_id
                or intent.handle is not None
                or intent.failure_code != "WORKER_RETRY_EXHAUSTED"
                or intent.adapter_id != "fwi.deepwave_adapter"
                or intent.adapter_version not in {"1.5.0", "1.6.0"}
                or intent.request.get("algorithm")
                != {
                    "id": "deepwave.acoustic_fwi",
                    "version": intent.adapter_version,
                }
            ):
                raise TaskStoreCorruption(
                    "retry exhaustion cleanup intent is inconsistent"
                )

            worker_exit_exhaustion = connection.execute(
                """
                SELECT 1 FROM worker_exit_retry_exhaustions
                WHERE intent_id = ? AND attempt_number = 2
                  AND failure_kind = 'pre_running_launch_failure'
                """,
                (intent.intent_id,),
            ).fetchone()
            if worker_exit_exhaustion is not None:
                result = (
                    self._load_worker_exit_retry_exhaustion_cleanup_proof(
                        connection,
                        purge_id=purge_id,
                        task_id=task_id,
                        project_id=project_id,
                        principal_id=principal_id,
                        purge_requested_at=purge["requested_at"],
                        intent=intent,
                    )
                )
                connection.commit()
                return result

            exhaustion = connection.execute(
                """
                SELECT * FROM worker_retry_exhaustions
                WHERE intent_id = ? AND attempt_number = 2
                """,
                (intent.intent_id,),
            ).fetchone()
            if exhaustion is None:
                raise TaskStoreCorruption(
                    "retry exhaustion cleanup has no immutable exhaustion"
                )
            reservation = connection.execute(
                """
                SELECT * FROM worker_retry_reservations
                WHERE intent_id = ? AND attempt_number = 2
                """,
                (intent.intent_id,),
            ).fetchone()
            attempts = connection.execute(
                """
                SELECT * FROM worker_launch_attempts
                WHERE intent_id = ? ORDER BY attempt_number
                """,
                (intent.intent_id,),
            ).fetchall()
            if reservation is None or len(attempts) != 2:
                raise TaskStoreCorruption(
                    "retry exhaustion cleanup lineage is incomplete"
                )
            previous_attempt, current_attempt = attempts
            observation = connection.execute(
                """
                SELECT * FROM worker_attempt_observations
                WHERE attempt_id = ? AND observation_sequence = ?
                """,
                (
                    exhaustion["attempt_id"],
                    exhaustion["observation_sequence"],
                ),
            ).fetchone()
            previous_observation = connection.execute(
                """
                SELECT * FROM worker_attempt_observations
                WHERE attempt_id = ? AND observation_sequence = ?
                """,
                (
                    reservation["previous_attempt_id"],
                    reservation["previous_observation_sequence"],
                ),
            ).fetchone()
            if observation is None or previous_observation is None:
                raise TaskStoreCorruption(
                    "retry exhaustion cleanup observations are incomplete"
                )
            evidence = _decode_worker_observation_row(observation)
            previous_evidence = _decode_worker_observation_row(
                previous_observation
            )
            _require_worker_attempt_observation_binding(
                current_attempt,
                evidence,
                intent_id=intent.intent_id,
                task_id=task_id,
                project_id=project_id,
                principal_id=principal_id,
            )
            _require_worker_attempt_observation_binding(
                previous_attempt,
                previous_evidence,
                intent_id=intent.intent_id,
                task_id=task_id,
                project_id=project_id,
                principal_id=principal_id,
            )
            latest_current = connection.execute(
                """
                SELECT MAX(observation_sequence)
                FROM worker_attempt_observations WHERE attempt_id = ?
                """,
                (exhaustion["attempt_id"],),
            ).fetchone()[0]
            latest_previous = connection.execute(
                """
                SELECT MAX(observation_sequence)
                FROM worker_attempt_observations WHERE attempt_id = ?
                """,
                (reservation["previous_attempt_id"],),
            ).fetchone()[0]

            event_row = connection.execute(
                """
                SELECT * FROM run_events
                WHERE task_id = ? AND sequence = ?
                """,
                (task_id, exhaustion["terminal_event_sequence"]),
            ).fetchone()
            commit_record = connection.execute(
                """
                SELECT * FROM supervised_run_event_commits
                WHERE task_id = ? AND sequence = ?
                """,
                (task_id, exhaustion["terminal_event_sequence"]),
            ).fetchone()
            if event_row is None or commit_record is None:
                raise TaskStoreCorruption(
                    "retry exhaustion cleanup terminal proof is incomplete"
                )
            event = _decode_run_event_row(
                event_row, label="retry exhaustion terminal event"
            )
            extension = event.get("extensions", {}).get(
                "org.agent_rpc.retry_exhaustion"
            )
            current_failure_payload = {
                "schema_version": "1.0.0",
                "failure_kind": "pre_running_launch_failure",
                "submission_id": evidence["submission_id"],
                "attempt_id": evidence["attempt_id"],
                "attempt_number": evidence["attempt_number"],
                "job_id": evidence["job_id"],
                "request_hash": evidence["request_hash"],
                "binding_hash": evidence["binding_hash"],
                "ticket_record_hash": evidence["ticket"]["record_hash"],
            }
            previous_failure_payload = {
                "schema_version": "1.0.0",
                "failure_kind": "pre_running_launch_failure",
                "submission_id": previous_evidence["submission_id"],
                "attempt_id": previous_evidence["attempt_id"],
                "attempt_number": previous_evidence["attempt_number"],
                "job_id": previous_evidence["job_id"],
                "request_hash": previous_evidence["request_hash"],
                "binding_hash": previous_evidence["binding_hash"],
                "ticket_record_hash": previous_evidence["ticket"]["record_hash"],
            }
            expected_extension = {
                "intent_id": intent.intent_id,
                "attempt_id": exhaustion["attempt_id"],
                "attempt_number": 2,
                "observation_sequence": exhaustion["observation_sequence"],
                "evidence_hash": exhaustion["evidence_hash"],
                "private_schema_version": "1.2.0",
                "private_proof_hash": exhaustion["private_proof_hash"],
                "failure_kind": "pre_running_launch_failure",
                "max_attempts": 2,
            }
            if (
                exhaustion["task_id"] != task_id
                or exhaustion["project_id"] != project_id
                or exhaustion["principal_id"] != principal_id
                or exhaustion["approval_id"] != intent.approval_id
                or exhaustion["attempt_number"] != 2
                or exhaustion["max_attempts"] != 2
                or exhaustion["private_schema_version"] != "1.2.0"
                or exhaustion["failure_kind"]
                != "pre_running_launch_failure"
                or observation["document_hash"] != exhaustion["evidence_hash"]
                or observation["observation_sequence"] != latest_current
                or evidence["attempt_number"] != 2
                or evidence["ticket"]["state"] != "failed"
                or evidence["ticket"]["worker_pid"] is not None
                or evidence["ready"] is not None
                or evidence["heartbeat"] is not None
                or encode_document(current_failure_payload)[1]
                != exhaustion["private_proof_hash"]
                or reservation["task_id"] != task_id
                or reservation["project_id"] != project_id
                or reservation["principal_id"] != principal_id
                or reservation["approval_id"] != intent.approval_id
                or reservation["failure_kind"]
                != "pre_running_launch_failure"
                or previous_observation["document_hash"]
                != reservation["evidence_hash"]
                or previous_observation["observation_sequence"]
                != latest_previous
                or previous_evidence["attempt_number"] != 1
                or previous_evidence["ticket"]["state"] != "failed"
                or previous_evidence["ticket"]["worker_pid"] is not None
                or previous_evidence["ready"] is not None
                or previous_evidence["heartbeat"] is not None
                or encode_document(previous_failure_payload)[1]
                != reservation["private_proof_hash"]
                or previous_attempt["attempt_number"] != 1
                or current_attempt["attempt_number"] != 2
                or previous_attempt["attempt_id"]
                != reservation["previous_attempt_id"]
                or current_attempt["attempt_id"] != exhaustion["attempt_id"]
                or current_attempt["submission_id"]
                != previous_attempt["submission_id"]
                or current_attempt["adapter_request_hash"]
                != previous_attempt["adapter_request_hash"]
                or current_attempt["created_at"] != reservation["reserved_at"]
                or current_attempt["job_id"] == previous_attempt["job_id"]
                or event_row["document_hash"]
                != exhaustion["terminal_event_hash"]
                or event.get("event_type") != "node_failed"
                or event.get("task_status") != "Failed"
                or event.get("error")
                != {
                    "code": "retry_exhausted",
                    "message": "FWI Worker exhausted its approved launch attempts",
                    "retryable": False,
                }
                or extension != expected_extension
                or commit_record["project_id"] != project_id
                or commit_record["principal_id"] != principal_id
                or commit_record["fencing_token"]
                != exhaustion["fencing_token"]
                or commit_record["recorded_at"] != exhaustion["exhausted_at"]
                or commit_record["recorded_at_us"]
                != exhaustion["exhausted_at_us"]
            ):
                raise TaskStoreCorruption(
                    "retry exhaustion cleanup proof is inconsistent"
                )
            try:
                _, requested_at_us = _runtime_timestamp(purge["requested_at"])
                _, exhausted_at_us = _runtime_timestamp(
                    exhaustion["exhausted_at"]
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "retry exhaustion cleanup time is invalid"
                ) from error
            if (
                exhausted_at_us != exhaustion["exhausted_at_us"]
                or requested_at_us < exhausted_at_us
            ):
                raise TaskStoreCorruption(
                    "retry exhaustion cleanup violates causal order"
                )

            result = RetryExhaustionCleanupProof(
                purge_id=purge_id,
                intent_id=intent.intent_id,
                task_id=task_id,
                project_id=project_id,
                principal_id=principal_id,
                approval_id=intent.approval_id,
                attempt_id=exhaustion["attempt_id"],
                observation_sequence=int(exhaustion["observation_sequence"]),
                evidence=copy.deepcopy(evidence),
                evidence_hash=exhaustion["evidence_hash"],
                private_schema_version=exhaustion["private_schema_version"],
                private_proof_hash=exhaustion["private_proof_hash"],
                failure_kind=exhaustion["failure_kind"],
                previous_attempt_id=reservation["previous_attempt_id"],
                previous_observation_sequence=int(
                    reservation["previous_observation_sequence"]
                ),
                previous_private_proof_hash=reservation[
                    "private_proof_hash"
                ],
                retry_reserved_at=reservation["reserved_at"],
                terminal_event_sequence=int(
                    exhaustion["terminal_event_sequence"]
                ),
                terminal_event_hash=exhaustion["terminal_event_hash"],
                exhausted_at=exhaustion["exhausted_at"],
            )
            connection.commit()
            return result
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def complete_task_purge(
        self,
        *,
        purge_id: str,
        task_id: str,
        project_id: str,
        principal_id: str,
        local_run_state: str,
        now: str,
    ) -> TaskPurgeRecord:
        """Finalize a reserved purge after local run cleanup succeeds."""

        strings = (purge_id, task_id, project_id, principal_id, now)
        if (
            any(not isinstance(value, str) or not value for value in strings)
            or local_run_state not in TASK_PURGE_LOCAL_RUN_STATES
        ):
            raise TaskStoreConflict("task purge completion is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            request = connection.execute(
                """
                SELECT task_id, project_id, principal_id, visibility_revision
                FROM task_purge_requests WHERE purge_id = ?
                """,
                (purge_id,),
            ).fetchone()
            if request is None:
                raise TaskStoreConflict("task purge reservation does not exist")
            if (
                request["task_id"] != task_id
                or request["project_id"] != project_id
                or request["principal_id"] != principal_id
            ):
                raise TaskStoreConflict(
                    "task purge completion crosses its reservation scope"
                )
            existing = connection.execute(
                """
                SELECT local_run_state FROM task_purge_outcomes
                WHERE purge_id = ?
                """,
                (purge_id,),
            ).fetchone()
            if existing is not None:
                if existing["local_run_state"] != local_run_state:
                    raise TaskStoreConflict(
                        "task purge was completed with another local run state"
                    )
                record = self._load_task_purge_record(
                    connection, task_id=task_id, replayed=True
                )
                connection.commit()
                return record
            outcome = {
                "task_id": task_id,
                "purge_id": purge_id,
                "purge_state": "purged",
                "purged_at": now,
                "local_run_state": local_run_state,
                "audit_retained": True,
            }
            document_json, document_hash = encode_document(outcome)
            connection.execute(
                """
                INSERT INTO task_purge_outcomes(
                    purge_id, task_id, project_id, principal_id,
                    visibility_revision, local_run_state,
                    document_json, document_hash, purged_at, recorded_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    purge_id,
                    task_id,
                    project_id,
                    principal_id,
                    request["visibility_revision"],
                    local_run_state,
                    document_json,
                    document_hash,
                    now,
                    now,
                ),
            )
            record = self._load_task_purge_record(
                connection, task_id=task_id, replayed=False
            )
            connection.commit()
            return record
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict(
                "task purge completion conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    @staticmethod
    def _load_runtime_supervisor_lease(
        connection: sqlite3.Connection,
        *,
        project_id: str,
        principal_id: str,
    ) -> RuntimeSupervisorLease | None:
        row = connection.execute(
            """
            SELECT lease.*,
                   term.initial_expires_at,
                   term.initial_expires_at_us,
                   closure.reason AS closure_reason,
                   closure.final_heartbeat_at,
                   closure.final_heartbeat_at_us,
                   closure.final_expires_at,
                   closure.final_expires_at_us,
                   closure.closed_at,
                   closure.closed_at_us
            FROM runtime_supervisor_leases AS lease
            JOIN runtime_supervisor_terms AS term
              ON term.project_id = lease.project_id
             AND term.principal_id = lease.principal_id
             AND term.fencing_token = lease.fencing_token
             AND term.owner_id = lease.owner_id
             AND term.acquired_at = lease.acquired_at
             AND term.acquired_at_us = lease.acquired_at_us
            LEFT JOIN runtime_supervisor_term_closures AS closure
              ON closure.project_id = lease.project_id
             AND closure.principal_id = lease.principal_id
             AND closure.fencing_token = lease.fencing_token
            WHERE lease.project_id = ? AND lease.principal_id = ?
            """,
            (project_id, principal_id),
        ).fetchone()
        if row is None:
            return None
        try:
            fencing_token = row["fencing_token"]
            if type(fencing_token) is not int or fencing_token < 1:
                raise ValueError("invalid fencing token")
            for name in ("project_id", "principal_id", "owner_id"):
                if not isinstance(row[name], str) or not row[name]:
                    raise ValueError(f"invalid {name}")
            normalized: dict[str, tuple[str, int]] = {}
            for name in (
                "acquired_at",
                "heartbeat_at",
                "expires_at",
                "initial_expires_at",
            ):
                canonical, microseconds = _runtime_timestamp(row[name])
                if canonical != row[name] or microseconds != row[f"{name}_us"]:
                    raise ValueError(f"inconsistent {name}")
                normalized[name] = canonical, microseconds
            acquired_us = normalized["acquired_at"][1]
            heartbeat_us = normalized["heartbeat_at"][1]
            expires_us = normalized["expires_at"][1]
            initial_expires_us = normalized["initial_expires_at"][1]
            if (
                heartbeat_us < acquired_us
                or initial_expires_us <= acquired_us
                or expires_us <= heartbeat_us
                or expires_us < initial_expires_us
            ):
                raise ValueError("lease time order is invalid")
            state = "active"
            if row["closure_reason"] is not None:
                if row["closure_reason"] != "released":
                    raise ValueError("current term has an invalid closure")
                for name, expected in (
                    ("final_heartbeat_at", normalized["heartbeat_at"]),
                    ("final_expires_at", normalized["expires_at"]),
                    ("closed_at", None),
                ):
                    canonical, microseconds = _runtime_timestamp(row[name])
                    if canonical != row[name] or microseconds != row[f"{name}_us"]:
                        raise ValueError(f"inconsistent {name}")
                    if expected is not None and (canonical, microseconds) != expected:
                        raise ValueError(f"closure differs at {name}")
                if row["closed_at_us"] < heartbeat_us:
                    raise ValueError("closure precedes the heartbeat")
                state = "released"
        except (TaskStoreConflict, TypeError, ValueError) as error:
            raise TaskStoreCorruption(
                "runtime supervisor lease is inconsistent"
            ) from error
        return RuntimeSupervisorLease(
            project_id=row["project_id"],
            principal_id=row["principal_id"],
            fencing_token=fencing_token,
            owner_id=row["owner_id"],
            state=state,
            acquired_at=normalized["acquired_at"][0],
            heartbeat_at=normalized["heartbeat_at"][0],
            expires_at=normalized["expires_at"][0],
        )

    @staticmethod
    def _runtime_supervisor_identity_matches(
        current: RuntimeSupervisorLease, supplied: RuntimeSupervisorLease
    ) -> bool:
        return (
            isinstance(supplied, RuntimeSupervisorLease)
            and supplied.project_id == current.project_id
            and supplied.principal_id == current.principal_id
            and supplied.fencing_token == current.fencing_token
            and supplied.owner_id == current.owner_id
            and supplied.acquired_at == current.acquired_at
        )

    def _require_active_runtime_supervisor_lease(
        self,
        connection: sqlite3.Connection,
        *,
        project_id: str,
        principal_id: str,
        supplied: RuntimeSupervisorLease,
        now_us: int,
        action: str,
    ) -> RuntimeSupervisorLease:
        """Return the current exact term or fail before a supervised write."""

        current = self._load_runtime_supervisor_lease(
            connection,
            project_id=project_id,
            principal_id=principal_id,
        )
        if (
            current is None
            or current.state != "active"
            or not self._runtime_supervisor_identity_matches(current, supplied)
            or _runtime_timestamp(current.heartbeat_at)[1] > now_us
            or _runtime_timestamp(current.expires_at)[1] <= now_us
        ):
            raise RuntimeSupervisorLeaseLost(
                f"{action} lost its runtime supervisor lease"
            )
        return current

    @staticmethod
    def _insert_runtime_supervisor_term(
        connection: sqlite3.Connection,
        *,
        project_id: str,
        principal_id: str,
        fencing_token: int,
        owner_id: str,
        now: str,
        now_us: int,
        expires_at: str,
        expires_at_us: int,
    ) -> None:
        connection.execute(
            """
            INSERT INTO runtime_supervisor_terms(
                project_id, principal_id, fencing_token, owner_id,
                acquired_at, acquired_at_us,
                initial_expires_at, initial_expires_at_us
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?)
            """,
            (
                project_id, principal_id, fencing_token, owner_id,
                now, now_us, expires_at, expires_at_us,
            ),
        )

    @staticmethod
    def _close_runtime_supervisor_term(
        connection: sqlite3.Connection,
        *,
        lease: RuntimeSupervisorLease,
        reason: str,
        closed_at: str,
        closed_at_us: int,
    ) -> None:
        _, heartbeat_at_us = _runtime_timestamp(lease.heartbeat_at)
        _, expires_at_us = _runtime_timestamp(lease.expires_at)
        connection.execute(
            """
            INSERT INTO runtime_supervisor_term_closures(
                project_id, principal_id, fencing_token, reason,
                final_heartbeat_at, final_heartbeat_at_us,
                final_expires_at, final_expires_at_us,
                closed_at, closed_at_us
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
            """,
            (
                lease.project_id, lease.principal_id, lease.fencing_token,
                reason, lease.heartbeat_at, heartbeat_at_us,
                lease.expires_at, expires_at_us, closed_at, closed_at_us,
            ),
        )

    def get_runtime_supervisor_lease(
        self, *, project_id: str, principal_id: str
    ) -> RuntimeSupervisorLease | None:
        connection = self._connect()
        try:
            return self._load_runtime_supervisor_lease(
                connection,
                project_id=project_id,
                principal_id=principal_id,
            )
        finally:
            connection.close()

    @staticmethod
    def _load_dag_node_state_map(
        connection: sqlite3.Connection,
        *,
        task_id: str,
        plan: Mapping[str, Any],
    ) -> DagNodeStateMapSnapshot | None:
        """Load and validate the complete append-only state history."""

        plan_id = plan.get("plan_id")
        plan_hash = plan.get("plan_hash")
        plan_nodes = plan.get("nodes")
        if (
            not isinstance(plan_id, str)
            or not isinstance(plan_hash, str)
            or not isinstance(plan_nodes, list)
            or len(plan_nodes) <= 1
        ):
            raise TaskStoreConflict("task current plan is not a multi-node DAG")
        expected_node_ids = {
            node.get("node_id")
            for node in plan_nodes
            if isinstance(node, Mapping) and isinstance(node.get("node_id"), str)
        }
        if len(expected_node_ids) != len(plan_nodes):
            raise TaskStoreCorruption("persisted DAG node identity is inconsistent")

        rows = connection.execute(
            """
            SELECT task_id, plan_id, plan_hash, node_id, revision,
                   previous_state, state, recorded_at, recorded_at_us
            FROM dag_node_state_events
            WHERE task_id = ? AND plan_id = ?
            ORDER BY node_id ASC, revision ASC
            """,
            (task_id, plan_id),
        ).fetchall()
        if not rows:
            return None

        grouped: dict[str, list[sqlite3.Row]] = {}
        for row in rows:
            if (
                row["task_id"] != task_id
                or row["plan_id"] != plan_id
                or row["plan_hash"] != plan_hash
                or row["node_id"] not in expected_node_ids
            ):
                raise TaskStoreCorruption(
                    "persisted DAG node state differs from its plan identity"
                )
            grouped.setdefault(row["node_id"], []).append(row)
        if set(grouped) != expected_node_ids:
            raise TaskStoreCorruption("persisted DAG node state map is incomplete")

        current: list[DagNodeStateSnapshot] = []
        known_states = {
            "Pending",
            "Queued",
            "Running",
            "Waiting",
            "Retrying",
            "Succeeded",
            "Failed",
            "Cancelled",
            "Blocked",
        }
        for node_id in sorted(expected_node_ids):
            history = grouped[node_id]
            prior_state: str | None = None
            for expected_revision, row in enumerate(history, start=1):
                try:
                    recorded_at, recorded_at_us = _runtime_timestamp(
                        row["recorded_at"]
                    )
                except TaskStoreConflict as error:
                    raise TaskStoreCorruption(
                        "persisted DAG node state time is inconsistent"
                    ) from error
                if (
                    type(row["revision"]) is not int
                    or row["revision"] != expected_revision
                    or row["previous_state"] != prior_state
                    or row["state"] not in known_states
                    or recorded_at != row["recorded_at"]
                    or recorded_at_us != row["recorded_at_us"]
                ):
                    raise TaskStoreCorruption(
                        "persisted DAG node state history is inconsistent"
                    )
                prior_state = row["state"]
            latest = history[-1]
            current.append(
                DagNodeStateSnapshot(
                    task_id=task_id,
                    plan_id=plan_id,
                    plan_hash=plan_hash,
                    node_id=node_id,
                    revision=int(latest["revision"]),
                    state=latest["state"],
                    recorded_at=_runtime_timestamp(latest["recorded_at"])[0],
                )
            )
        try:
            readiness = evaluate_dag_readiness(
                plan,
                node_states={node.node_id: node.state for node in current},
            )
        except DagScheduleError as error:
            raise TaskStoreCorruption(
                "persisted DAG node state map is not schedulable"
            ) from error
        blockers_by_node = {
            blocked.node_id: blocked.blocked_by_node_ids
            for blocked in readiness.blocked_nodes
        }
        for node in current:
            if node.state != "Blocked":
                continue
            fact = connection.execute(
                """
                SELECT * FROM dag_node_scheduler_transition_facts
                WHERE task_id = ? AND plan_id = ? AND node_id = ?
                  AND node_revision = ?
                """,
                (task_id, plan_id, node.node_id, node.revision),
            ).fetchone()
            if fact is None:
                raise TaskStoreCorruption(
                    "persisted Blocked DAG node lacks its scheduler fact"
                )
            document = _decode_document(
                fact["blocker_document_json"], label="DAG blocker proof"
            )
            document_json, document_hash = encode_document(document)
            expected_blockers = blockers_by_node.get(node.node_id)
            if (
                expected_blockers is None
                or document_json != fact["blocker_document_json"]
                or document_hash != fact["blocker_document_hash"]
                or document
                != {
                    "schema_version": "1.0.0",
                    "task_id": task_id,
                    "plan_id": plan_id,
                    "plan_hash": plan_hash,
                    "approval_id": fact["approval_id"],
                    "node_id": node.node_id,
                    "previous_revision": fact["previous_revision"],
                    "blocked_revision": fact["node_revision"],
                    "blocked_by_node_ids": list(expected_blockers),
                    "supervisor_term": {
                        "fencing_token": fact["fencing_token"],
                        "owner_id": fact["owner_id"],
                        "acquired_at": fact["term_acquired_at"],
                    },
                    "recorded_at": fact["recorded_at"],
                }
            ):
                raise TaskStoreCorruption(
                    "persisted DAG Blocked proof differs from pure readiness"
                )
        return DagNodeStateMapSnapshot(
            task_id=task_id,
            plan_id=plan_id,
            plan_hash=plan_hash,
            nodes=tuple(current),
        )

    @staticmethod
    def _derive_dag_lineage_components(
        *,
        binding_document: Mapping[str, Any],
        outputs_by_port: Mapping[str, Mapping[str, Any]],
    ) -> tuple[list[dict[str, Any]], list[str], list[dict[str, Any]]]:
        """Return canonical Dataset roots, direct hashes, and output identities.

        Dataset roots are the public ArtifactManifest lineage projection.  A
        node-output input additionally retains its exact producer receipt and
        lineage hash in the immutable input-binding document, so the complete
        chain remains reconstructible without trusting a mutable file path.
        """

        inputs = binding_document.get("inputs")
        if not isinstance(inputs, list) or not inputs:
            raise TaskStoreCorruption("DAG trusted lineage has no exact inputs")
        roots_by_json: dict[str, dict[str, Any]] = {}
        direct_hashes: list[str] = []
        for expected_index, value in enumerate(inputs):
            if (
                not isinstance(value, Mapping)
                or value.get("input_index") != expected_index
                or not isinstance(value.get("target_input_port"), str)
            ):
                raise TaskStoreCorruption("DAG trusted input lineage is invalid")
            kind = value.get("kind")
            roots: Sequence[Mapping[str, Any]]
            if kind == "dataset":
                dataset = value.get("dataset")
                catalog_hash = value.get("dataset_document_hash")
                if not isinstance(dataset, Mapping):
                    raise TaskStoreCorruption("DAG Dataset lineage is invalid")
                roots = (
                    {
                        **dict(dataset),
                        "catalog_document_hash": catalog_hash,
                    },
                )
                direct_hash = dataset.get("content_hash")
            elif kind == "node_output":
                binding = value.get("binding")
                producer = value.get("producer")
                artifact = (
                    binding.get("artifact")
                    if isinstance(binding, Mapping)
                    else None
                )
                roots = (
                    producer.get("transitive_dataset_roots")
                    if isinstance(producer, Mapping)
                    else None
                )
                direct_hash = (
                    artifact.get("content_hash")
                    if isinstance(artifact, Mapping)
                    else None
                )
                if (
                    not isinstance(producer, Mapping)
                    or not _is_sha256(producer.get("semantic_cache_key_hash"))
                    or type(producer.get("cacheable")) is not bool
                    or not _is_sha256(
                        producer.get("trusted_lineage_document_hash")
                    )
                ):
                    raise TaskStoreCorruption(
                        "DAG producer lineage identity is invalid"
                    )
            else:
                raise TaskStoreCorruption("DAG trusted input kind is invalid")
            if (
                not _is_sha256(direct_hash)
                or not isinstance(roots, Sequence)
                or isinstance(roots, (str, bytes, bytearray))
                or not roots
            ):
                raise TaskStoreCorruption("DAG trusted input hashes are invalid")
            direct_hashes.append(str(direct_hash))
            for root in roots:
                if not isinstance(root, Mapping):
                    raise TaskStoreCorruption("DAG Dataset-root lineage is invalid")
                normalized = {
                    key: root.get(key)
                    for key in (
                        "id",
                        "version",
                        "content_hash",
                        "data_type",
                        "catalog_document_hash",
                    )
                }
                if (
                    any(
                        not isinstance(normalized[key], str)
                        or not normalized[key]
                        for key in ("id", "version", "data_type")
                    )
                    or not _is_sha256(normalized["content_hash"])
                    or not _is_sha256(normalized["catalog_document_hash"])
                ):
                    raise TaskStoreCorruption("DAG Dataset-root lineage is invalid")
                root_json, _ = encode_document(normalized)
                roots_by_json[root_json] = normalized
        dataset_roots = [roots_by_json[key] for key in sorted(roots_by_json)]
        public_roots = [
            {
                key: root[key]
                for key in ("id", "version", "content_hash", "data_type")
            }
            for root in dataset_roots
        ]
        outputs: list[dict[str, Any]] = []
        for output_port in sorted(outputs_by_port):
            output = outputs_by_port[output_port]
            manifest = output.get("artifact_manifest")
            if not isinstance(manifest, Mapping):
                raise TaskStoreCorruption("DAG trusted output manifest is invalid")
            fingerprint = manifest.get("fingerprint")
            lineage = manifest.get("lineage")
            if (
                schema_errors("artifact-manifest.schema.json", manifest)
                or lineage.get("inputs") != public_roots
                if isinstance(lineage, Mapping)
                else True
            ):
                raise TaskStoreCorruption("DAG output Dataset lineage diverged")
            if (
                not isinstance(fingerprint, Mapping)
                or fingerprint.get("input_hashes") != direct_hashes
            ):
                raise TaskStoreCorruption("DAG output input hashes diverged")
            outputs.append(
                {
                    "output_port": output_port,
                    "data_type": output.get("data_type"),
                    "artifact_manifest_hash": output.get(
                        "artifact_manifest_hash"
                    ),
                    "artifact": {
                        key: manifest.get(key)
                        for key in (
                            "artifact_id",
                            "schema_version",
                            "media_type",
                            "content_hash",
                            "size_bytes",
                        )
                    },
                }
            )
        if not outputs:
            raise TaskStoreCorruption("DAG trusted lineage has no outputs")
        return dataset_roots, direct_hashes, outputs

    def _build_executed_node_cache_documents(
        self,
        connection: sqlite3.Connection,
        *,
        task_id: str,
        plan: Mapping[str, Any],
        approval_id: str,
        source_state: DagNodeStateSnapshot,
        admission: sqlite3.Row,
        succeeded: Mapping[str, Any],
    ) -> tuple[NodeCacheIdentity, dict[str, Any]]:
        intent = self._load_dispatch_intent(
            connection, intent_id=admission["intent_id"]
        )
        task_row = connection.execute(
            """
            SELECT project_id, principal_id, current_plan_id,
                   current_approval_id
            FROM tasks WHERE task_id = ?
            """,
            (task_id,),
        ).fetchone()
        approval_row = connection.execute(
            """
            SELECT document_json, document_hash
            FROM approvals WHERE task_id = ? AND approval_id = ?
            """,
            (task_id, approval_id),
        ).fetchone()
        approval_document = (
            _decode_hashed_document(
                approval_row, label="DAG cache source Approval"
            )
            if approval_row is not None
            else None
        )
        if (
            task_row is None
            or task_row["current_plan_id"] != plan.get("plan_id")
            or task_row["current_approval_id"] != approval_id
            or not isinstance(approval_document, Mapping)
            or approval_document.get("approval_id") != approval_id
            or approval_document.get("plan_id") != plan.get("plan_id")
            or approval_document.get("plan_hash") != plan.get("plan_hash")
            or intent is None
            or intent.task_id != task_id
            or intent.node_id != source_state.node_id
        ):
            raise TaskStoreCorruption("DAG cache source documents are inconsistent")
        binding_row = connection.execute(
            """
            SELECT * FROM dag_node_input_binding_facts
            WHERE task_id = ? AND plan_id = ? AND approval_id = ?
              AND target_node_id = ? AND target_node_revision = ?
              AND fencing_token = ? AND binding_document_hash = ?
            """,
            (
                task_id,
                plan["plan_id"],
                approval_id,
                source_state.node_id,
                admission["pending_revision"],
                admission["input_fencing_token"],
                admission["input_binding_document_hash"],
            ),
        ).fetchone()
        if binding_row is None:
            raise TaskStoreCorruption("DAG cache source lost its input binding")
        binding_document = _decode_hashed_document(
            {
                "document_json": binding_row["binding_document_json"],
                "document_hash": binding_row["binding_document_hash"],
            },
            label="DAG cache source input binding",
        )
        outputs_by_port = succeeded.get("outputs_by_port")
        if not isinstance(outputs_by_port, Mapping):
            raise TaskStoreCorruption("DAG cache source outputs are invalid")
        dataset_roots, _direct_hashes, output_identities = (
            self._derive_dag_lineage_components(
                binding_document=binding_document,
                outputs_by_port=outputs_by_port,
            )
        )
        source_node = next(
            (
                value
                for value in plan.get("nodes", [])
                if isinstance(value, Mapping)
                and value.get("node_id") == source_state.node_id
            ),
            None,
        )
        algorithm = (
            source_node.get("algorithm")
            if isinstance(source_node, Mapping)
            else None
        )
        if not isinstance(algorithm, Mapping):
            raise TaskStoreCorruption("DAG cache source Algorithm is invalid")
        algorithm_row = connection.execute(
            """
            SELECT algorithm_id, version, allowlisted,
                   document_json, document_hash
            FROM algorithm_registry
            WHERE algorithm_id = ? AND version = ?
            """,
            (algorithm.get("id"), algorithm.get("version")),
        ).fetchone()
        if algorithm_row is None:
            raise TaskStoreCorruption("DAG cache source Algorithm is unavailable")
        algorithm_manifest = self._load_algorithm_registration(algorithm_row)
        execution_fingerprint = (
            intent.handle.get("fingerprint")
            if isinstance(intent.handle, Mapping)
            else None
        )
        if not isinstance(execution_fingerprint, Mapping):
            raise TaskStoreCorruption(
                "DAG cache source lacks its adopted execution fingerprint"
            )
        if any(
            output["artifact_manifest"].get("fingerprint")
            != execution_fingerprint
            for output in outputs_by_port.values()
        ):
            raise TaskStoreCorruption(
                "DAG cache source artifacts differ from adopted provenance"
            )
        identity = build_node_cache_identity(
            plan,
            node_id=source_state.node_id,
            input_binding_document=binding_document,
            adapter_id=intent.adapter_id,
            adapter_version=intent.adapter_version,
            queue_fingerprint=execution_fingerprint,
            algorithm_manifest=algorithm_manifest,
            algorithm_manifest_document_hash=algorithm_row["document_hash"],
            approval_scope=approval_document.get("scope"),
            project_id=task_row["project_id"],
            principal_id=task_row["principal_id"],
        )
        lineage_document = {
            "schema_version": "1.0.0",
            "permission_scope": {
                "project_id": task_row["project_id"],
                "principal_id": task_row["principal_id"],
            },
            "source": {
                "task_id": task_id,
                "plan_id": plan["plan_id"],
                "plan_hash": plan["plan_hash"],
                "approval_id": approval_id,
                "node_id": source_state.node_id,
                "succeeded_revision": source_state.revision,
                "input_binding_document_hash": admission[
                    "input_binding_document_hash"
                ],
                "receipt_document_hash": succeeded["receipt_document_hash"],
                "semantic_cache_key_hash": identity.document_hash,
            },
            "inputs": copy.deepcopy(binding_document["inputs"]),
            "dataset_roots": dataset_roots,
            "outputs": output_identities,
        }
        return identity, lineage_document

    def _insert_executed_node_cache_entry(
        self,
        connection: sqlite3.Connection,
        *,
        task_id: str,
        plan: Mapping[str, Any],
        approval_id: str,
        source_state: DagNodeStateSnapshot,
        admission: sqlite3.Row,
        succeeded: Mapping[str, Any],
        recorded_at: str,
        recorded_at_us: int,
    ) -> None:
        """Index one exact Succeeded execution, or deliberately leave it uncacheable."""

        try:
            identity, lineage = self._build_executed_node_cache_documents(
                connection,
                task_id=task_id,
                plan=plan,
                approval_id=approval_id,
                source_state=source_state,
                admission=admission,
                succeeded=succeeded,
            )
        except NodeCacheIdentityError as error:
            if error.code == "NODE_CACHE_UNCACHEABLE":
                return
            raise TaskStoreCorruption("DAG cache identity is invalid") from error
        key_json, key_hash = encode_document(identity.document)
        lineage_json, lineage_hash = encode_document(lineage)
        if key_hash != identity.document_hash:
            raise TaskStoreCorruption("DAG cache identity hash drifted")
        cache_entry_id = (
            "cache-entry-"
            + hashlib.sha256(
                (admission["intent_id"] + "\x1f" + key_hash).encode("utf-8")
            ).hexdigest()[:32]
        )
        connection.execute(
            """
            INSERT INTO dag_node_cache_entries(
                cache_entry_id, project_id, principal_id,
                cache_key_document_json, cache_key_hash,
                source_intent_id, source_task_id, source_plan_id,
                source_plan_hash, source_approval_id, source_node_id,
                source_pending_revision, source_queued_revision,
                source_succeeded_revision, source_admission_document_hash,
                source_input_binding_document_hash,
                source_terminal_event_sequence, source_terminal_event_hash,
                source_receipt_document_hash,
                trusted_lineage_document_json,
                trusted_lineage_document_hash, recorded_at, recorded_at_us
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?,
                      ?, ?, ?, ?, ?)
            """,
            (
                cache_entry_id,
                identity.document["permission_scope"]["project_id"],
                identity.document["permission_scope"]["principal_id"],
                key_json,
                key_hash,
                admission["intent_id"],
                task_id,
                plan["plan_id"],
                plan["plan_hash"],
                approval_id,
                source_state.node_id,
                admission["pending_revision"],
                admission["queued_revision"],
                source_state.revision,
                admission["document_hash"],
                admission["input_binding_document_hash"],
                succeeded["event_sequence"],
                succeeded["event_hash"],
                succeeded["receipt_document_hash"],
                lineage_json,
                lineage_hash,
                recorded_at,
                recorded_at_us,
            ),
        )

    def _load_dag_node_succeeded_outputs(
        self,
        connection: sqlite3.Connection,
        *,
        task_id: str,
        plan: Mapping[str, Any],
        approval_id: str,
        source_state: DagNodeStateSnapshot,
    ) -> dict[str, Any]:
        """Validate one exact producer-success/output attestation.

        Executable v20 nodes use their terminal receipt and exact P2 evidence.
        The legacy v19 fallback remains read-only and reserved for the dormant
        binding contract; it still has no public writer.
        """

        admission = self._load_dag_node_execution_admission(
            connection, task_id=task_id, node_id=source_state.node_id
        )
        cache_hit = connection.execute(
            """
            SELECT * FROM dag_node_cache_hit_facts
            WHERE target_task_id = ? AND target_plan_id = ?
              AND target_approval_id = ? AND target_node_id = ?
              AND target_succeeded_revision = ?
            """,
            (
                task_id,
                plan["plan_id"],
                approval_id,
                source_state.node_id,
                source_state.revision,
            ),
        ).fetchone()
        if admission is not None and cache_hit is not None:
            raise TaskStoreCorruption(
                "DAG Succeeded node has both Worker and cache-hit facts"
            )
        if admission is not None:
            succeeded = self._load_v20_dag_node_succeeded_outputs(
                connection,
                task_id=task_id,
                plan=plan,
                approval_id=approval_id,
                source_state=source_state,
                admission=admission,
            )
            try:
                identity, expected_lineage = (
                    self._build_executed_node_cache_documents(
                        connection,
                        task_id=task_id,
                        plan=plan,
                        approval_id=approval_id,
                        source_state=source_state,
                        admission=admission,
                        succeeded=succeeded,
                    )
                )
            except NodeCacheIdentityError as error:
                if error.code != "NODE_CACHE_UNCACHEABLE":
                    raise TaskStoreCorruption(
                        "DAG cache identity is invalid"
                    ) from error
                binding_row = connection.execute(
                    """
                    SELECT binding_document_json, binding_document_hash
                    FROM dag_node_input_binding_facts
                    WHERE task_id = ? AND plan_id = ? AND approval_id = ?
                      AND target_node_id = ? AND target_node_revision = ?
                      AND fencing_token = ? AND binding_document_hash = ?
                    """,
                    (
                        task_id,
                        plan["plan_id"],
                        approval_id,
                        source_state.node_id,
                        admission["pending_revision"],
                        admission["input_fencing_token"],
                        admission["input_binding_document_hash"],
                    ),
                ).fetchone()
                if binding_row is None:
                    raise TaskStoreCorruption(
                        "uncacheable DAG source lost its input binding"
                    )
                binding_document = _decode_hashed_document(
                    {
                        "document_json": binding_row["binding_document_json"],
                        "document_hash": binding_row["binding_document_hash"],
                    },
                    label="uncacheable DAG source input binding",
                )
                dataset_roots, direct_hashes, output_identities = (
                    self._derive_dag_lineage_components(
                        binding_document=binding_document,
                        outputs_by_port=succeeded["outputs_by_port"],
                    )
                )
                fallback_key = succeeded["receipt_document_hash"]
                expected_lineage = {
                    "schema_version": "1.0.0",
                    "permission_scope": {
                        "project_id": admission["project_id"],
                        "principal_id": admission["principal_id"],
                    },
                    "source": {
                        "task_id": task_id,
                        "plan_id": plan["plan_id"],
                        "plan_hash": plan["plan_hash"],
                        "approval_id": approval_id,
                        "node_id": source_state.node_id,
                        "succeeded_revision": source_state.revision,
                        "input_binding_document_hash": admission[
                            "input_binding_document_hash"
                        ],
                        "receipt_document_hash": fallback_key,
                        "semantic_cache_key_hash": fallback_key,
                    },
                    "inputs": copy.deepcopy(binding_document["inputs"]),
                    "dataset_roots": dataset_roots,
                    "outputs": output_identities,
                }
                _, lineage_hash = encode_document(expected_lineage)
                return {
                    **succeeded,
                    "semantic_cache_key_hash": fallback_key,
                    "cacheable": False,
                    "trusted_lineage_document": expected_lineage,
                    "trusted_lineage_document_hash": lineage_hash,
                    "transitive_dataset_roots": dataset_roots,
                    "direct_input_hashes": direct_hashes,
                }

            key_json, key_hash = encode_document(identity.document)
            lineage_json, lineage_hash = encode_document(expected_lineage)
            entry = connection.execute(
                """
                SELECT * FROM dag_node_cache_entries
                WHERE source_intent_id = ?
                """,
                (admission["intent_id"],),
            ).fetchone()
            if entry is None:
                raise TaskStoreCorruption(
                    "cacheable DAG Succeeded source lacks its durable cache entry"
                )
            stored_key = _decode_hashed_document(
                {
                    "document_json": entry["cache_key_document_json"],
                    "document_hash": entry["cache_key_hash"],
                },
                label="DAG node cache key",
            )
            stored_lineage = _decode_hashed_document(
                {
                    "document_json": entry["trusted_lineage_document_json"],
                    "document_hash": entry["trusted_lineage_document_hash"],
                },
                label="DAG node trusted lineage",
            )
            if (
                stored_key != identity.document
                or entry["cache_key_document_json"] != key_json
                or entry["cache_key_hash"] != key_hash
                or key_hash != identity.document_hash
                or stored_lineage != expected_lineage
                or entry["trusted_lineage_document_json"] != lineage_json
                or entry["trusted_lineage_document_hash"] != lineage_hash
                or entry["source_task_id"] != task_id
                or entry["source_plan_id"] != plan["plan_id"]
                or entry["source_plan_hash"] != plan["plan_hash"]
                or entry["source_approval_id"] != approval_id
                or entry["source_node_id"] != source_state.node_id
                or entry["source_pending_revision"]
                != admission["pending_revision"]
                or entry["source_queued_revision"]
                != admission["queued_revision"]
                or entry["source_succeeded_revision"] != source_state.revision
                or entry["source_admission_document_hash"]
                != admission["document_hash"]
                or entry["source_input_binding_document_hash"]
                != admission["input_binding_document_hash"]
                or entry["source_terminal_event_sequence"]
                != succeeded["event_sequence"]
                or entry["source_terminal_event_hash"] != succeeded["event_hash"]
                or entry["source_receipt_document_hash"]
                != succeeded["receipt_document_hash"]
            ):
                raise TaskStoreCorruption("DAG node cache entry is inconsistent")
            dataset_roots = expected_lineage["dataset_roots"]
            binding_inputs = expected_lineage["inputs"]
            direct_hashes = [
                (
                    value["dataset"]["content_hash"]
                    if value["kind"] == "dataset"
                    else value["binding"]["artifact"]["content_hash"]
                )
                for value in binding_inputs
            ]
            return {
                **succeeded,
                "cache_entry_id": entry["cache_entry_id"],
                "cache_key_document": identity.document,
                "semantic_cache_key_hash": key_hash,
                "cacheable": True,
                "trusted_lineage_document": expected_lineage,
                "trusted_lineage_document_hash": lineage_hash,
                "transitive_dataset_roots": dataset_roots,
                "direct_input_hashes": direct_hashes,
            }

        if cache_hit is not None:
            legacy_overlap = connection.execute(
                """
                SELECT 1 FROM dag_node_succeeded_outputs
                WHERE task_id = ? AND plan_id = ? AND approval_id = ?
                  AND node_id = ? AND node_revision = ?
                """,
                (
                    task_id,
                    plan["plan_id"],
                    approval_id,
                    source_state.node_id,
                    source_state.revision,
                ),
            ).fetchone()
            if legacy_overlap is not None:
                raise TaskStoreCorruption(
                    "cached DAG node has a conflicting legacy output fact"
                )
            return self._load_cached_dag_node_succeeded_outputs(
                connection,
                task_id=task_id,
                plan=plan,
                approval_id=approval_id,
                source_state=source_state,
                hit=cache_hit,
            )

        row = connection.execute(
            """
            SELECT *
            FROM dag_node_succeeded_outputs
            WHERE task_id = ? AND plan_id = ? AND approval_id = ?
              AND node_id = ? AND node_revision = ?
            """,
            (
                task_id,
                plan["plan_id"],
                approval_id,
                source_state.node_id,
                source_state.revision,
            ),
        ).fetchone()
        if row is None:
            raise TaskStoreConflict(
                "DAG source input requires an exact durable Succeeded output receipt"
            )
        if source_state.state != "Succeeded":
            raise TaskStoreConflict(
                "DAG source input requires the latest Succeeded node revision"
            )

        try:
            recorded_at, recorded_at_us = _runtime_timestamp(row["recorded_at"])
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "persisted DAG Succeeded output time is inconsistent"
            ) from error
        if recorded_at_us != row["recorded_at_us"]:
            raise TaskStoreCorruption(
                "persisted DAG Succeeded output time is inconsistent"
            )

        receipt = _decode_document(
            row["receipt_document_json"],
            label="DAG Succeeded output receipt",
        )
        try:
            receipt_json, receipt_hash = encode_document(receipt)
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "persisted DAG Succeeded output receipt is not canonical JSON"
            ) from error
        if (
            receipt_json != row["receipt_document_json"]
            or receipt_hash != row["receipt_document_hash"]
            or row["task_id"] != task_id
            or row["plan_id"] != plan["plan_id"]
            or row["plan_hash"] != plan["plan_hash"]
            or row["approval_id"] != approval_id
            or row["node_id"] != source_state.node_id
            or row["node_revision"] != source_state.revision
            or row["node_state"] != "Succeeded"
            or not _is_sha256(row["input_binding_document_hash"])
        ):
            raise TaskStoreCorruption(
                "persisted DAG Succeeded output identity is inconsistent"
            )

        prior_binding = connection.execute(
            """
            SELECT *
            FROM dag_node_input_binding_facts
            WHERE task_id = ? AND plan_id = ? AND approval_id = ?
              AND target_node_id = ? AND target_node_revision = ?
              AND fencing_token = ? AND binding_document_hash = ?
            """,
            (
                task_id,
                plan["plan_id"],
                approval_id,
                source_state.node_id,
                row["input_binding_node_revision"],
                row["fencing_token"],
                row["input_binding_document_hash"],
            ),
        ).fetchone()
        if prior_binding is None:
            raise TaskStoreCorruption(
                "DAG Succeeded output lost its producer input binding"
            )
        prior_document = _decode_document(
            prior_binding["binding_document_json"],
            label="producer DAG input binding",
        )
        try:
            prior_json, prior_hash = encode_document(prior_document)
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "producer DAG input binding is not canonical JSON"
            ) from error

        nodes_by_id = {
            node["node_id"]: node
            for node in plan["nodes"]
            if isinstance(node, Mapping) and isinstance(node.get("node_id"), str)
        }
        source_node = nodes_by_id.get(source_state.node_id)
        if not isinstance(source_node, Mapping):
            raise TaskStoreCorruption(
                "DAG Succeeded output source is absent from its plan"
            )
        expected_outputs = source_node.get("outputs")
        expected_inputs = source_node.get("inputs")
        if not isinstance(expected_outputs, list) or not isinstance(expected_inputs, list):
            raise TaskStoreCorruption(
                "DAG Succeeded output plan contract is inconsistent"
            )

        expected_prior_projection = {
            "task_id": task_id,
            "plan": {
                "plan_id": plan["plan_id"],
                "plan_hash": plan["plan_hash"],
            },
            "approval_id": approval_id,
            "target": {
                "node_id": source_state.node_id,
                "revision": row["input_binding_node_revision"],
                "state": "Pending",
            },
        }
        if (
            prior_json != prior_binding["binding_document_json"]
            or prior_hash != prior_binding["binding_document_hash"]
            or prior_hash != row["input_binding_document_hash"]
            or prior_document.get("task_id") != expected_prior_projection["task_id"]
            or prior_document.get("plan") != expected_prior_projection["plan"]
            or prior_document.get("approval_id") != approval_id
            or prior_document.get("target") != expected_prior_projection["target"]
            or not isinstance(prior_document.get("inputs"), list)
            or len(prior_document["inputs"]) != len(expected_inputs)
        ):
            raise TaskStoreCorruption(
                "producer DAG input binding is inconsistent"
            )

        required_receipt_keys = {
            "schema_version",
            "task_id",
            "plan",
            "approval_id",
            "node",
            "scope",
            "supervisor_term",
            "input_binding_document_hash",
            "receipt_record_hash",
            "outputs",
            "succeeded_at",
        }
        node_document = receipt.get("node")
        scope_document = receipt.get("scope")
        term_document = receipt.get("supervisor_term")
        outputs_document = receipt.get("outputs")
        if (
            set(receipt) != required_receipt_keys
            or receipt.get("schema_version") != "1.0.0"
            or receipt.get("task_id") != task_id
            or receipt.get("plan")
            != {"plan_id": plan["plan_id"], "plan_hash": plan["plan_hash"]}
            or receipt.get("approval_id") != approval_id
            or node_document
            != {
                "node_id": source_state.node_id,
                "input_binding_revision": row["input_binding_node_revision"],
                "succeeded_revision": source_state.revision,
                "state": "Succeeded",
            }
            or scope_document
            != {
                "project_id": row["project_id"],
                "principal_id": row["principal_id"],
            }
            or term_document
            != {
                "fencing_token": row["fencing_token"],
                "owner_id": row["owner_id"],
                "acquired_at": row["term_acquired_at"],
            }
            or receipt.get("input_binding_document_hash")
            != row["input_binding_document_hash"]
            or not _is_sha256(receipt.get("receipt_record_hash"))
            or receipt.get("succeeded_at") != recorded_at
            or not isinstance(outputs_document, list)
            or len(outputs_document) != len(expected_outputs)
        ):
            raise TaskStoreCorruption(
                "persisted DAG Succeeded output receipt is inconsistent"
            )

        try:
            source_recorded_us = _runtime_timestamp(source_state.recorded_at)[1]
            prior_recorded_us = _runtime_timestamp(prior_binding["recorded_at"])[1]
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "DAG Succeeded output chronology is inconsistent"
            ) from error
        if recorded_at_us < max(source_recorded_us, prior_recorded_us):
            raise TaskStoreCorruption(
                "DAG Succeeded output predates its causal facts"
            )

        outputs_by_port: dict[str, dict[str, Any]] = {}
        for expected_output, output in zip(expected_outputs, outputs_document):
            if (
                not isinstance(expected_output, Mapping)
                or not isinstance(output, Mapping)
                or set(output)
                != {
                    "output_port",
                    "data_type",
                    "artifact_manifest",
                    "artifact_manifest_hash",
                }
                or output.get("output_port") != expected_output.get("port")
                or output.get("data_type") != expected_output.get("data_type")
                or output.get("output_port") in outputs_by_port
            ):
                raise TaskStoreCorruption(
                    "DAG Succeeded output inventory differs from its plan"
                )
            manifest = output.get("artifact_manifest")
            if not isinstance(manifest, Mapping):
                raise TaskStoreCorruption(
                    "DAG Succeeded output manifest is invalid"
                )
            manifest_errors = schema_errors("artifact-manifest.schema.json", manifest)
            try:
                manifest_json, manifest_hash = encode_document(manifest)
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "DAG Succeeded output manifest is not canonical JSON"
                ) from error
            extensions = manifest.get("extensions")
            adapter_extension = (
                extensions.get("org.agent_rpc.adapter")
                if isinstance(extensions, Mapping)
                else None
            )
            lineage = manifest.get("lineage")
            fingerprint = manifest.get("fingerprint")
            if (
                manifest_errors
                or output.get("artifact_manifest_hash") != manifest_hash
                or manifest.get("task_id") != task_id
                or manifest.get("node_id") != source_state.node_id
                or manifest.get("artifact_type") != expected_output.get("data_type")
                or not _is_sha256(manifest.get("content_hash"))
                or type(manifest.get("size_bytes")) is not int
                or manifest["size_bytes"] < 0
                or not isinstance(adapter_extension, Mapping)
                or adapter_extension.get("output_port") != expected_output.get("port")
                or not isinstance(lineage, Mapping)
                or lineage.get("plan_hash") != plan["plan_hash"]
                or lineage.get("algorithm") != source_node.get("algorithm")
                or not isinstance(fingerprint, Mapping)
                or fingerprint.get("algorithm") != source_node.get("algorithm")
            ):
                raise TaskStoreCorruption(
                    "DAG Succeeded output manifest differs from its source"
                )
            outputs_by_port[str(output["output_port"])] = {
                "data_type": output["data_type"],
                "artifact_manifest": copy.deepcopy(dict(manifest)),
                "artifact_manifest_json": manifest_json,
                "artifact_manifest_hash": manifest_hash,
            }

        dataset_roots, direct_hashes, output_identities = (
            self._derive_dag_lineage_components(
                binding_document=prior_document,
                outputs_by_port=outputs_by_port,
            )
        )
        fallback_key = receipt_hash
        trusted_lineage = {
            "schema_version": "1.0.0",
            "permission_scope": {
                "project_id": row["project_id"],
                "principal_id": row["principal_id"],
            },
            "source": {
                "task_id": task_id,
                "plan_id": plan["plan_id"],
                "plan_hash": plan["plan_hash"],
                "approval_id": approval_id,
                "node_id": source_state.node_id,
                "succeeded_revision": source_state.revision,
                "input_binding_document_hash": row[
                    "input_binding_document_hash"
                ],
                "receipt_document_hash": receipt_hash,
                "semantic_cache_key_hash": fallback_key,
            },
            "inputs": copy.deepcopy(prior_document["inputs"]),
            "dataset_roots": dataset_roots,
            "outputs": output_identities,
        }
        _, trusted_lineage_hash = encode_document(trusted_lineage)
        return {
            "approval_id": approval_id,
            "input_binding_document_hash": row["input_binding_document_hash"],
            "receipt_document_hash": receipt_hash,
            "receipt_record_hash": receipt["receipt_record_hash"],
            "fencing_token": row["fencing_token"],
            "owner_id": row["owner_id"],
            "term_acquired_at": row["term_acquired_at"],
            "recorded_at": recorded_at,
            "semantic_cache_key_hash": fallback_key,
            "cacheable": False,
            "trusted_lineage_document": trusted_lineage,
            "trusted_lineage_document_hash": trusted_lineage_hash,
            "transitive_dataset_roots": dataset_roots,
            "direct_input_hashes": direct_hashes,
            "outputs_by_port": outputs_by_port,
        }

    def _load_cached_dag_node_succeeded_outputs(
        self,
        connection: sqlite3.Connection,
        *,
        task_id: str,
        plan: Mapping[str, Any],
        approval_id: str,
        source_state: DagNodeStateSnapshot,
        hit: sqlite3.Row,
    ) -> dict[str, Any]:
        """Validate a no-Worker Succeeded receipt and its physical source chain."""

        if (
            source_state.state != "Succeeded"
            or hit["target_task_id"] != task_id
            or hit["target_plan_id"] != plan.get("plan_id")
            or hit["target_plan_hash"] != plan.get("plan_hash")
            or hit["target_approval_id"] != approval_id
            or hit["target_node_id"] != source_state.node_id
            or hit["target_succeeded_revision"] != source_state.revision
            or hit["target_pending_revision"] + 1 != source_state.revision
        ):
            raise TaskStoreCorruption("cached DAG Succeeded identity is inconsistent")
        key = _decode_hashed_document(
            {
                "document_json": hit["cache_key_document_json"],
                "document_hash": hit["cache_key_hash"],
            },
            label="cached DAG node key",
        )
        verification = _decode_hashed_document(
            {
                "document_json": hit["artifact_verification_document_json"],
                "document_hash": hit["artifact_verification_document_hash"],
            },
            label="cached DAG artifact verification",
        )
        receipt = _decode_hashed_document(
            {
                "document_json": hit["output_receipt_document_json"],
                "document_hash": hit["output_receipt_document_hash"],
            },
            label="cached DAG output receipt",
        )
        entry = connection.execute(
            "SELECT * FROM dag_node_cache_entries WHERE cache_entry_id = ?",
            (hit["source_cache_entry_id"],),
        ).fetchone()
        if entry is None:
            raise TaskStoreCorruption("cached DAG source entry is missing")
        entry_key = _decode_hashed_document(
            {
                "document_json": entry["cache_key_document_json"],
                "document_hash": entry["cache_key_hash"],
            },
            label="cached DAG source key",
        )
        lineage = _decode_hashed_document(
            {
                "document_json": entry["trusted_lineage_document_json"],
                "document_hash": entry["trusted_lineage_document_hash"],
            },
            label="cached DAG source lineage",
        )
        event_row = connection.execute(
            "SELECT * FROM run_events WHERE task_id = ? AND sequence = ?",
            (task_id, hit["event_sequence"]),
        ).fetchone()
        event_commit = connection.execute(
            "SELECT * FROM supervised_run_event_commits "
            "WHERE task_id = ? AND sequence = ?",
            (task_id, hit["event_sequence"]),
        ).fetchone()
        if event_row is None or event_commit is None:
            raise TaskStoreCorruption("cached DAG event proof is missing")
        event = _decode_hashed_document(
            event_row, label="cached DAG node Succeeded event"
        )
        cache_extension = event.get("extensions", {}).get(
            "org.agent_rpc.node_cache", {}
        )
        expected_cache = {
            "state": "hit",
            "cache_hit_id": hit["cache_hit_id"],
            "cache_entry_id": hit["source_cache_entry_id"],
            "cache_key_hash": hit["cache_key_hash"],
            "source_intent_id": hit["source_intent_id"],
            "source_receipt_document_hash": hit[
                "source_receipt_document_hash"
            ],
            "worker_runtime_started": False,
        }
        if (
            key != entry_key
            or hit["cache_key_document_json"] != entry["cache_key_document_json"]
            or hit["cache_key_hash"] != entry["cache_key_hash"]
            or hit["source_intent_id"] != entry["source_intent_id"]
            or hit["source_task_id"] != entry["source_task_id"]
            or hit["source_plan_id"] != entry["source_plan_id"]
            or hit["source_plan_hash"] != entry["source_plan_hash"]
            or hit["source_approval_id"] != entry["source_approval_id"]
            or hit["source_node_id"] != entry["source_node_id"]
            or hit["source_succeeded_revision"]
            != entry["source_succeeded_revision"]
            or hit["source_admission_document_hash"]
            != entry["source_admission_document_hash"]
            or hit["source_receipt_document_hash"]
            != entry["source_receipt_document_hash"]
            or hit["source_trusted_lineage_document_hash"]
            != entry["trusted_lineage_document_hash"]
            or verification.get("cache_entry_id") != entry["cache_entry_id"]
            or verification.get("cache_key_hash") != entry["cache_key_hash"]
            or receipt.get("task_id") != task_id
            or receipt.get("plan")
            != {"plan_id": plan["plan_id"], "plan_hash": plan["plan_hash"]}
            or receipt.get("approval_id") != approval_id
            or receipt.get("node")
            != {
                "node_id": source_state.node_id,
                "input_binding_revision": hit["target_pending_revision"],
                "succeeded_revision": source_state.revision,
                "state": "Succeeded",
            }
            or receipt.get("input_binding_document_hash")
            != hit["target_input_binding_document_hash"]
            or receipt.get("scope")
            != {
                "project_id": hit["project_id"],
                "principal_id": hit["principal_id"],
            }
            or receipt.get("cache")
            != {
                "cache_hit_id": hit["cache_hit_id"],
                "cache_entry_id": hit["source_cache_entry_id"],
                "cache_key_hash": hit["cache_key_hash"],
                "source_receipt_document_hash": hit[
                    "source_receipt_document_hash"
                ],
                "trusted_lineage_document_hash": hit[
                    "source_trusted_lineage_document_hash"
                ],
                "artifact_verification_document_hash": hit[
                    "artifact_verification_document_hash"
                ],
            }
            or receipt.get("succeeded_at") != event.get("occurred_at")
            or event.get("event_type") != "node_succeeded"
            or event.get("node_id") != source_state.node_id
            or event.get("task_id") != task_id
            or event.get("fingerprint") != key.get("execution_fingerprint")
            or cache_extension != expected_cache
            or event_row["document_hash"] != hit["event_hash"]
            or event_commit["project_id"] != hit["project_id"]
            or event_commit["principal_id"] != hit["principal_id"]
            or event_commit["fencing_token"]
            != hit["completion_fencing_token"]
            or event_commit["recorded_at"] != hit["recorded_at"]
            or event_commit["recorded_at_us"] != hit["recorded_at_us"]
            or hit["target_input_fencing_token"]
            != hit["completion_fencing_token"]
            or hit["target_input_owner_id"] != hit["completion_owner_id"]
            or hit["target_input_term_acquired_at"]
            != hit["completion_term_acquired_at"]
        ):
            raise TaskStoreCorruption("cached DAG proof chain is inconsistent")
        source_snapshot = self._load_snapshot(connection, hit["source_task_id"])
        source_states = (
            self._load_dag_node_state_map(
                connection,
                task_id=hit["source_task_id"],
                plan=source_snapshot.plan,
            )
            if source_snapshot is not None
            and isinstance(source_snapshot.plan, Mapping)
            else None
        )
        physical_state = next(
            (
                value
                for value in (source_states.nodes if source_states is not None else ())
                if value.node_id == hit["source_node_id"]
            ),
            None,
        )
        if source_snapshot is None or physical_state is None:
            raise TaskStoreCorruption("cached DAG physical source is unavailable")
        physical = self._load_dag_node_succeeded_outputs(
            connection,
            task_id=hit["source_task_id"],
            plan=source_snapshot.plan,
            approval_id=hit["source_approval_id"],
            source_state=physical_state,
        )
        outputs_document = receipt.get("outputs")
        if not isinstance(outputs_document, list) or not outputs_document:
            raise TaskStoreCorruption("cached DAG output inventory is invalid")
        outputs_by_port: dict[str, dict[str, Any]] = {}
        for output in outputs_document:
            if not isinstance(output, Mapping):
                raise TaskStoreCorruption("cached DAG output is invalid")
            port = output.get("output_port")
            manifest = output.get("artifact_manifest")
            physical_source = output.get("physical_source")
            physical_output = physical["outputs_by_port"].get(port)
            if (
                not isinstance(port, str)
                or port in outputs_by_port
                or not isinstance(manifest, Mapping)
                or not isinstance(physical_source, Mapping)
                or physical_output is None
            ):
                raise TaskStoreCorruption("cached DAG output inventory diverged")
            manifest_json, manifest_hash = encode_document(manifest)
            physical_manifest = physical_output["artifact_manifest"]
            if (
                schema_errors("artifact-manifest.schema.json", manifest)
                or output.get("data_type") != physical_output["data_type"]
                or output.get("artifact_manifest_hash") != manifest_hash
                or manifest.get("task_id") != task_id
                or manifest.get("node_id") != source_state.node_id
                or manifest.get("content_hash")
                != physical_manifest.get("content_hash")
                or manifest.get("size_bytes") != physical_manifest.get("size_bytes")
                or manifest.get("media_type") != physical_manifest.get("media_type")
                or physical_source
                != {
                    "task_id": hit["source_task_id"],
                    "intent_id": hit["source_intent_id"],
                    "artifact_id": physical_manifest["artifact_id"],
                    "artifact_manifest_hash": physical_output[
                        "artifact_manifest_hash"
                    ],
                }
            ):
                raise TaskStoreCorruption("cached DAG artifact identity diverged")
            outputs_by_port[port] = {
                "data_type": output["data_type"],
                "artifact_manifest": copy.deepcopy(dict(manifest)),
                "artifact_manifest_json": manifest_json,
                "artifact_manifest_hash": manifest_hash,
                "physical_task_id": hit["source_task_id"],
                "physical_intent_id": hit["source_intent_id"],
                "physical_artifact_id": physical_manifest["artifact_id"],
                "physical_artifact_manifest": copy.deepcopy(physical_manifest),
                "physical_artifact_manifest_hash": physical_output[
                    "artifact_manifest_hash"
                ],
            }
        binding_row = connection.execute(
            """
            SELECT binding_document_json, binding_document_hash
            FROM dag_node_input_binding_facts
            WHERE task_id = ? AND plan_id = ? AND approval_id = ?
              AND target_node_id = ? AND target_node_revision = ?
              AND fencing_token = ? AND binding_document_hash = ?
            """,
            (
                task_id,
                plan["plan_id"],
                approval_id,
                source_state.node_id,
                hit["target_pending_revision"],
                hit["target_input_fencing_token"],
                hit["target_input_binding_document_hash"],
            ),
        ).fetchone()
        if binding_row is None:
            raise TaskStoreCorruption("cached DAG target binding is missing")
        binding = _decode_hashed_document(
            binding_row, label="cached DAG target input binding"
        )
        dataset_roots, direct_hashes, _outputs = self._derive_dag_lineage_components(
            binding_document=binding,
            outputs_by_port=outputs_by_port,
        )
        return {
            "approval_id": approval_id,
            "input_binding_document_hash": hit[
                "target_input_binding_document_hash"
            ],
            "receipt_document_hash": hit["output_receipt_document_hash"],
            "receipt_record_hash": hit["artifact_verification_document_hash"],
            "fencing_token": hit["completion_fencing_token"],
            "owner_id": hit["completion_owner_id"],
            "term_acquired_at": hit["completion_term_acquired_at"],
            "recorded_at": hit["recorded_at"],
            "event_sequence": int(hit["event_sequence"]),
            "event_hash": hit["event_hash"],
            "semantic_cache_key_hash": hit["cache_key_hash"],
            "cacheable": True,
            "trusted_lineage_document": lineage,
            "trusted_lineage_document_hash": hit[
                "source_trusted_lineage_document_hash"
            ],
            "transitive_dataset_roots": dataset_roots,
            "direct_input_hashes": direct_hashes,
            "outputs_by_port": outputs_by_port,
        }

    @staticmethod
    def _load_v20_dag_node_succeeded_outputs(
        connection: sqlite3.Connection,
        *,
        task_id: str,
        plan: Mapping[str, Any],
        approval_id: str,
        source_state: DagNodeStateSnapshot,
        admission: sqlite3.Row,
    ) -> dict[str, Any]:
        """Validate a v20 producer receipt with separate input/completion terms."""

        if source_state.state != "Succeeded":
            raise TaskStoreConflict(
                "DAG source input requires the latest Succeeded node revision"
            )
        terminal = connection.execute(
            """
            SELECT * FROM dag_node_terminal_facts
            WHERE task_id = ? AND plan_id = ? AND approval_id = ?
              AND node_id = ? AND node_revision = ?
            """,
            (
                task_id,
                plan["plan_id"],
                approval_id,
                source_state.node_id,
                source_state.revision,
            ),
        ).fetchone()
        legacy = connection.execute(
            """
            SELECT 1 FROM dag_node_succeeded_outputs
            WHERE task_id = ? AND plan_id = ? AND approval_id = ?
              AND node_id = ? AND node_revision = ?
            """,
            (
                task_id,
                plan["plan_id"],
                approval_id,
                source_state.node_id,
                source_state.revision,
            ),
        ).fetchone()
        if terminal is None:
            raise TaskStoreConflict(
                "DAG source input requires an exact durable Succeeded output receipt"
            )
        if legacy is not None:
            raise TaskStoreCorruption(
                "v20 DAG execution has conflicting legacy producer output facts"
            )
        if (
            terminal["intent_id"] != admission["intent_id"]
            or terminal["task_id"] != admission["task_id"]
            or terminal["plan_id"] != admission["plan_id"]
            or terminal["plan_hash"] != admission["plan_hash"]
            or terminal["approval_id"] != admission["approval_id"]
            or terminal["node_id"] != admission["node_id"]
            or terminal["input_binding_node_revision"]
            != admission["pending_revision"]
            or terminal["input_binding_document_hash"]
            != admission["input_binding_document_hash"]
            or terminal["input_fencing_token"] != admission["input_fencing_token"]
            or terminal["input_owner_id"] != admission["input_owner_id"]
            or terminal["input_term_acquired_at"]
            != admission["input_term_acquired_at"]
            or terminal["node_revision"] != admission["queued_revision"] + 2
            or terminal["node_state"] != "Succeeded"
            or terminal["attempt_number"] != 1
            or not isinstance(terminal["attempt_id"], str)
            or not _is_sha256(terminal["worker_observation_hash"])
            or not _is_sha256(terminal["dispatch_handle_hash"])
            or not _is_sha256(terminal["dispatch_outcome_document_hash"])
            or not _is_sha256(terminal["receipt_document_hash"])
        ):
            raise TaskStoreCorruption(
                "v20 DAG Succeeded output identity is inconsistent"
            )

        try:
            recorded_at, recorded_at_us = _runtime_timestamp(
                terminal["recorded_at"]
            )
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "v20 DAG Succeeded output time is invalid"
            ) from error
        if recorded_at_us != terminal["recorded_at_us"]:
            raise TaskStoreCorruption(
                "v20 DAG Succeeded output time is inconsistent"
            )
        receipt = _decode_document(
            terminal["receipt_document_json"], label="v20 DAG Succeeded receipt"
        )
        adapter_status = _decode_document(
            terminal["adapter_status_json"], label="v20 DAG Adapter status"
        )
        handle = _decode_document(
            terminal["dispatch_handle_json"], label="v20 DAG dispatch handle"
        )
        try:
            receipt_json, receipt_hash = encode_document(receipt)
            adapter_json, adapter_hash = encode_document(adapter_status)
            handle_json, handle_hash = encode_document(handle)
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "v20 DAG Succeeded proof is not canonical JSON"
            ) from error
        if (
            receipt_json != terminal["receipt_document_json"]
            or receipt_hash != terminal["receipt_document_hash"]
            or adapter_json != terminal["adapter_status_json"]
            or adapter_hash != terminal["adapter_status_hash"]
            or handle_json != terminal["dispatch_handle_json"]
            or handle_hash != terminal["dispatch_handle_hash"]
        ):
            raise TaskStoreCorruption(
                "v20 DAG Succeeded proof hash is inconsistent"
            )

        event = connection.execute(
            "SELECT * FROM run_events WHERE task_id = ? AND sequence = ?",
            (task_id, terminal["event_sequence"]),
        ).fetchone()
        event_commit = connection.execute(
            """
            SELECT * FROM supervised_run_event_commits
            WHERE task_id = ? AND sequence = ?
            """,
            (task_id, terminal["event_sequence"]),
        ).fetchone()
        transition = connection.execute(
            """
            SELECT * FROM dag_node_execution_transition_facts
            WHERE intent_id = ? AND node_revision = ?
            """,
            (terminal["intent_id"], terminal["node_revision"]),
        ).fetchone()
        effective = connection.execute(
            """
            SELECT * FROM effective_dispatched_intents WHERE intent_id = ?
            """,
            (terminal["intent_id"],),
        ).fetchone()
        attempt = connection.execute(
            "SELECT * FROM worker_launch_attempts WHERE attempt_id = ?",
            (terminal["attempt_id"],),
        ).fetchone()
        observation = connection.execute(
            """
            SELECT * FROM worker_attempt_observations
            WHERE attempt_id = ? AND observation_sequence = ?
            """,
            (
                terminal["attempt_id"],
                terminal["worker_observation_sequence"],
            ),
        ).fetchone()
        adoption = connection.execute(
            """
            SELECT * FROM supervised_dispatch_adoptions WHERE intent_id = ?
            """,
            (terminal["intent_id"],),
        ).fetchone()
        if any(
            value is None
            for value in (
                event,
                event_commit,
                transition,
                effective,
                attempt,
                observation,
                adoption,
            )
        ):
            raise TaskStoreCorruption(
                "v20 DAG Succeeded output lacks its exact execution proof"
            )
        assert event is not None
        assert event_commit is not None
        assert transition is not None
        assert effective is not None
        assert attempt is not None
        assert observation is not None
        assert adoption is not None
        event_document = _decode_hashed_document(
            event, label="v20 DAG Succeeded event"
        )
        effective_document = _decode_document(
            effective["outcome_document_json"],
            label="v20 DAG effective dispatch outcome",
        )
        try:
            effective_json, effective_hash = encode_document(effective_document)
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "v20 DAG effective dispatch outcome is not canonical JSON"
            ) from error
        latest_observation = connection.execute(
            """
            SELECT MAX(observation_sequence) FROM worker_attempt_observations
            WHERE attempt_id = ?
            """,
            (terminal["attempt_id"],),
        ).fetchone()[0]
        try:
            adoption_recorded_at, adoption_recorded_at_us = _runtime_timestamp(
                adoption["recorded_at"]
            )
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "v20 DAG dispatch adoption time is invalid"
            ) from error
        if (
            event["document_hash"] != terminal["event_hash"]
            or event["event_type"] != "node_succeeded"
            or event["task_status"] not in {"Running", "Succeeded", "Failed"}
            or event["node_id"] != source_state.node_id
            or event_document.get("fingerprint") != handle.get("fingerprint")
            or event_commit["project_id"] != terminal["project_id"]
            or event_commit["principal_id"] != terminal["principal_id"]
            or event_commit["fencing_token"]
            != terminal["completion_fencing_token"]
            or event_commit["recorded_at"] != recorded_at
            or event_commit["recorded_at_us"] != recorded_at_us
            or transition["previous_state"] != "Running"
            or transition["state"] != "Succeeded"
            or transition["event_sequence"] != terminal["event_sequence"]
            or transition["event_hash"] != terminal["event_hash"]
            or transition["reason"] != "adapter_succeeded"
            or transition["fencing_token"]
            != terminal["completion_fencing_token"]
            or transition["recorded_at"] != recorded_at
            or transition["recorded_at_us"] != recorded_at_us
            or effective_json != effective["outcome_document_json"]
            or effective_hash != effective["outcome_document_hash"]
            or effective_hash != terminal["dispatch_outcome_document_hash"]
            or effective_document.get("status") != "dispatched"
            or effective_document.get("handle") != handle
            or attempt["intent_id"] != terminal["intent_id"]
            or attempt["attempt_number"] != 1
            or observation["document_hash"]
            != terminal["worker_observation_hash"]
            or observation["heartbeat_state"] != "succeeded"
            or observation["observation_sequence"] != latest_observation
            or adoption["attempt_id"] != terminal["attempt_id"]
            or adoption["project_id"] != terminal["project_id"]
            or adoption["principal_id"] != terminal["principal_id"]
            or type(adoption["fencing_token"]) is not int
            or adoption["fencing_token"] < 1
            or adoption_recorded_at != adoption["recorded_at"]
            or adoption_recorded_at_us != adoption["recorded_at_us"]
            or adoption_recorded_at_us > recorded_at_us
        ):
            raise TaskStoreCorruption(
                "v20 DAG Succeeded execution proof is inconsistent"
            )

        prior_binding = connection.execute(
            """
            SELECT * FROM dag_node_input_binding_facts
            WHERE task_id = ? AND plan_id = ? AND approval_id = ?
              AND target_node_id = ? AND target_node_revision = ?
              AND fencing_token = ? AND binding_document_hash = ?
            """,
            (
                task_id,
                plan["plan_id"],
                approval_id,
                source_state.node_id,
                admission["pending_revision"],
                admission["input_fencing_token"],
                admission["input_binding_document_hash"],
            ),
        ).fetchone()
        if prior_binding is None:
            raise TaskStoreCorruption(
                "v20 DAG Succeeded output lost its producer input binding"
            )
        prior_document = _decode_hashed_document(
            {
                "document_json": prior_binding["binding_document_json"],
                "document_hash": prior_binding["binding_document_hash"],
            },
            label="v20 producer DAG input binding",
        )
        expected_target = {
            "node_id": source_state.node_id,
            "revision": admission["pending_revision"],
            "state": "Pending",
        }
        if (
            prior_document.get("task_id") != task_id
            or prior_document.get("plan")
            != {"plan_id": plan["plan_id"], "plan_hash": plan["plan_hash"]}
            or prior_document.get("approval_id") != approval_id
            or prior_document.get("target") != expected_target
            or prior_binding["owner_id"] != admission["input_owner_id"]
            or prior_binding["term_acquired_at"]
            != admission["input_term_acquired_at"]
        ):
            raise TaskStoreCorruption(
                "v20 producer DAG input binding is inconsistent"
            )

        source_node = next(
            (
                node
                for node in plan.get("nodes", [])
                if isinstance(node, Mapping)
                and node.get("node_id") == source_state.node_id
            ),
            None,
        )
        expected_outputs = (
            source_node.get("outputs") if isinstance(source_node, Mapping) else None
        )
        outputs_document = receipt.get("outputs")
        dispatch_document = receipt.get("dispatch")
        succeeded_at = receipt.get("succeeded_at")
        if (
            set(receipt)
            != {
                "schema_version",
                "task_id",
                "plan",
                "approval_id",
                "node",
                "input_binding_document_hash",
                "scope",
                "input_supervisor_term",
                "completion_supervisor_term",
                "dispatch",
                "receipt_record_hash",
                "outputs",
                "succeeded_at",
            }
            or receipt.get("schema_version") != "2.0.0"
            or receipt.get("task_id") != task_id
            or receipt.get("plan")
            != {"plan_id": plan["plan_id"], "plan_hash": plan["plan_hash"]}
            or receipt.get("approval_id") != approval_id
            or receipt.get("node")
            != {
                "node_id": source_state.node_id,
                "input_binding_revision": admission["pending_revision"],
                "succeeded_revision": source_state.revision,
                "state": "Succeeded",
            }
            or receipt.get("input_binding_document_hash")
            != admission["input_binding_document_hash"]
            or receipt.get("scope")
            != {
                "project_id": terminal["project_id"],
                "principal_id": terminal["principal_id"],
            }
            or receipt.get("input_supervisor_term")
            != {
                "fencing_token": admission["input_fencing_token"],
                "owner_id": admission["input_owner_id"],
                "acquired_at": admission["input_term_acquired_at"],
            }
            or receipt.get("completion_supervisor_term")
            != {
                "fencing_token": terminal["completion_fencing_token"],
                "owner_id": terminal["completion_owner_id"],
                "acquired_at": terminal["completion_term_acquired_at"],
            }
            or dispatch_document
            != {
                "intent_id": terminal["intent_id"],
                "node_idempotency_key": admission["node_idempotency_key"],
                "handle_hash": handle_hash,
                "attempt_id": terminal["attempt_id"],
                "attempt_number": 1,
                "worker_observation_sequence": terminal[
                    "worker_observation_sequence"
                ],
                "worker_observation_hash": terminal["worker_observation_hash"],
                "outcome_document_hash": terminal[
                    "dispatch_outcome_document_hash"
                ],
            }
            or not _is_sha256(receipt.get("receipt_record_hash"))
            or adapter_status.get("status") != "Succeeded"
            or adapter_status.get("updated_at") != succeeded_at
            or event["occurred_at"] != succeeded_at
            or not isinstance(expected_outputs, list)
            or not isinstance(outputs_document, list)
            or len(outputs_document) != len(expected_outputs)
        ):
            raise TaskStoreCorruption(
                "v20 DAG Succeeded receipt is inconsistent"
            )
        try:
            succeeded_at, succeeded_at_us = _runtime_timestamp(str(succeeded_at))
            source_recorded_us = _runtime_timestamp(source_state.recorded_at)[1]
            binding_recorded_us = _runtime_timestamp(prior_binding["recorded_at"])[1]
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "v20 DAG Succeeded chronology is invalid"
            ) from error
        if (
            succeeded_at_us > recorded_at_us
            or recorded_at_us < source_recorded_us
            or recorded_at_us < binding_recorded_us
        ):
            raise TaskStoreCorruption(
                "v20 DAG Succeeded chronology is inconsistent"
            )

        outputs_by_port: dict[str, dict[str, Any]] = {}
        for expected_output, output in zip(expected_outputs, outputs_document):
            if (
                not isinstance(expected_output, Mapping)
                or not isinstance(output, Mapping)
                or set(output)
                != {
                    "output_port",
                    "data_type",
                    "artifact_manifest",
                    "artifact_manifest_hash",
                }
                or output.get("output_port") != expected_output.get("port")
                or output.get("data_type") != expected_output.get("data_type")
                or output.get("output_port") in outputs_by_port
            ):
                raise TaskStoreCorruption(
                    "v20 DAG output inventory differs from its Plan"
                )
            manifest = output.get("artifact_manifest")
            if not isinstance(manifest, Mapping):
                raise TaskStoreCorruption("v20 DAG output manifest is invalid")
            try:
                manifest_json, manifest_hash = encode_document(manifest)
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "v20 DAG output manifest is not canonical JSON"
                ) from error
            extensions = manifest.get("extensions")
            adapter_extension = (
                extensions.get("org.agent_rpc.adapter")
                if isinstance(extensions, Mapping)
                else None
            )
            lineage = manifest.get("lineage")
            fingerprint = manifest.get("fingerprint")
            if (
                schema_errors("artifact-manifest.schema.json", manifest)
                or output.get("artifact_manifest_hash") != manifest_hash
                or manifest.get("task_id") != task_id
                or manifest.get("node_id") != source_state.node_id
                or manifest.get("artifact_type") != expected_output.get("data_type")
                or not _is_sha256(manifest.get("content_hash"))
                or type(manifest.get("size_bytes")) is not int
                or manifest["size_bytes"] < 0
                or not isinstance(adapter_extension, Mapping)
                or adapter_extension.get("output_port")
                != expected_output.get("port")
                or not isinstance(lineage, Mapping)
                or lineage.get("plan_hash") != plan["plan_hash"]
                or lineage.get("algorithm") != source_node.get("algorithm")
                or not isinstance(fingerprint, Mapping)
                or fingerprint.get("algorithm") != source_node.get("algorithm")
            ):
                raise TaskStoreCorruption(
                    "v20 DAG output manifest differs from its source"
                )
            outputs_by_port[str(output["output_port"])] = {
                "data_type": output["data_type"],
                "artifact_manifest": copy.deepcopy(dict(manifest)),
                "artifact_manifest_json": manifest_json,
                "artifact_manifest_hash": manifest_hash,
            }

        return {
            "approval_id": approval_id,
            "input_binding_document_hash": admission[
                "input_binding_document_hash"
            ],
            "receipt_document_hash": receipt_hash,
            "receipt_record_hash": receipt["receipt_record_hash"],
            "fencing_token": terminal["completion_fencing_token"],
            "owner_id": terminal["completion_owner_id"],
            "term_acquired_at": terminal["completion_term_acquired_at"],
            "recorded_at": succeeded_at,
            "event_sequence": int(terminal["event_sequence"]),
            "event_hash": terminal["event_hash"],
            "attempt_id": terminal["attempt_id"],
            "attempt_number": int(terminal["attempt_number"]),
            "admission_document_hash": admission["document_hash"],
            "physical_task_id": task_id,
            "physical_intent_id": terminal["intent_id"],
            "outputs_by_port": outputs_by_port,
        }

    def get_dag_node_state_snapshot(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
    ) -> DagNodeStateMapSnapshot | None:
        """Read the durable P3 node-state map without creating facts."""

        connection = self._connect()
        try:
            connection.execute("BEGIN")
            snapshot = self._load_snapshot(connection, task_id)
            if (
                snapshot is None
                or snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
            ):
                raise TaskStoreConflict("task does not exist in the requested scope")
            if snapshot.plan is None:
                connection.commit()
                return None
            if len(snapshot.plan.get("nodes", [])) <= 1:
                plan_id = snapshot.plan.get("plan_id")
                impossible = connection.execute(
                    """
                    SELECT 1 FROM dag_node_state_events
                    WHERE task_id = ? AND plan_id = ?
                    UNION ALL
                    SELECT 1 FROM dag_node_claim_candidates
                    WHERE task_id = ? AND plan_id = ?
                    LIMIT 1
                    """,
                    (task_id, plan_id, task_id, plan_id),
                ).fetchone()
                if impossible is not None:
                    raise TaskStoreCorruption(
                        "single-node plan has impossible durable DAG facts"
                    )
                connection.commit()
                return None
            state_map = self._load_dag_node_state_map(
                connection,
                task_id=task_id,
                plan=snapshot.plan,
            )
            connection.commit()
            return state_map
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def claim_ready_dag_node_candidate(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        expected_plan_hash: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagNodeClaimCandidate:
        """Persist one deterministic readiness candidate under an active term.

        This dormant P3 primitive intentionally leaves every node ``Pending``
        and never writes Task status, RunEvent, dispatch intent, or Approval
        budget state.  A candidate is audit evidence only, not execution
        authorization.
        """

        if (
            not isinstance(task_id, str)
            or not task_id
            or not isinstance(project_id, str)
            or not project_id
            or not isinstance(principal_id, str)
            or not principal_id
            or not _is_sha256(expected_plan_hash)
        ):
            raise TaskStoreConflict("DAG node claim identity is invalid")
        if not isinstance(supervisor_lease, RuntimeSupervisorLease):
            raise TaskStoreConflict("runtime supervisor lease is invalid")
        if not callable(supervisor_clock):
            raise TaskStoreConflict("runtime supervisor clock is invalid")

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            recorded_at, recorded_at_us = _runtime_timestamp(supervisor_clock())
            snapshot = self._load_snapshot(connection, task_id)
            if (
                snapshot is None
                or snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
            ):
                raise TaskStoreConflict("task does not exist in the requested scope")
            plan = snapshot.plan
            approval = snapshot.approval
            if (
                snapshot.status not in {"AwaitingApproval", "Running"}
                or not isinstance(plan, Mapping)
                or len(plan.get("nodes", [])) <= 1
                or plan.get("plan_hash") != expected_plan_hash
                or not isinstance(approval, Mapping)
                or approval.get("decision") != "approved"
                or approval.get("plan_id") != plan.get("plan_id")
                or approval.get("plan_hash") != expected_plan_hash
            ):
                raise TaskStoreConflict(
                    "DAG node claim requires the exact current approved multi-node plan"
                )
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=project_id,
                principal_id=principal_id,
                supplied=supervisor_lease,
                now_us=recorded_at_us,
                action="DAG node claim candidate",
            )

            state_map = self._load_dag_node_state_map(
                connection,
                task_id=task_id,
                plan=plan,
            )
            if state_map is None:
                for node in sorted(plan["nodes"], key=lambda item: item["node_id"]):
                    connection.execute(
                        """
                        INSERT INTO dag_node_state_events(
                            task_id, plan_id, plan_hash, node_id, revision,
                            previous_state, state, recorded_at, recorded_at_us
                        ) VALUES (?, ?, ?, ?, 1, NULL, 'Pending', ?, ?)
                        """,
                        (
                            task_id,
                            plan["plan_id"],
                            expected_plan_hash,
                            node["node_id"],
                            recorded_at,
                            recorded_at_us,
                        ),
                    )
                state_map = self._load_dag_node_state_map(
                    connection,
                    task_id=task_id,
                    plan=plan,
                )
            if state_map is None:
                raise TaskStoreCorruption("DAG node state initialization was lost")

            node_states = {node.node_id: node.state for node in state_map.nodes}
            try:
                readiness = evaluate_dag_readiness(plan, node_states=node_states)
            except DagScheduleError as error:
                raise TaskStoreCorruption(
                    "persisted DAG state cannot be evaluated"
                ) from error
            if readiness.active_node_ids:
                raise TaskStoreConflict(
                    "DAG task already has an active admitted node"
                )
            if not readiness.runnable_node_ids:
                raise TaskStoreConflict("DAG plan has no runnable Pending node")
            selected_node_id = readiness.runnable_node_ids[0]
            selected = next(
                node for node in state_map.nodes if node.node_id == selected_node_id
            )
            if selected.state != "Pending":
                raise TaskStoreCorruption("DAG readiness selected a non-Pending node")

            readiness_document = {
                "schema_version": "1.0.0",
                "task_id": task_id,
                "plan_id": state_map.plan_id,
                "plan_hash": state_map.plan_hash,
                "approval_id": approval["approval_id"],
                "node_states": [
                    {
                        "node_id": node.node_id,
                        "revision": node.revision,
                        "state": node.state,
                    }
                    for node in state_map.nodes
                ],
                "topological_layers": [
                    list(layer) for layer in readiness.topological_layers
                ],
                "runnable_node_ids": list(readiness.runnable_node_ids),
                "waiting_node_ids": list(readiness.waiting_node_ids),
                "active_node_ids": list(readiness.active_node_ids),
                "succeeded_node_ids": list(readiness.succeeded_node_ids),
                "failed_node_ids": list(readiness.failed_node_ids),
                "cancelled_node_ids": list(readiness.cancelled_node_ids),
                "blocked_nodes": [
                    {
                        "node_id": blocked.node_id,
                        "blocked_by_node_ids": list(blocked.blocked_by_node_ids),
                    }
                    for blocked in readiness.blocked_nodes
                ],
                "selected_node_id": selected_node_id,
            }
            readiness_json, readiness_hash = encode_document(readiness_document)

            existing = connection.execute(
                """
                SELECT * FROM dag_node_claim_candidates
                WHERE task_id = ? AND plan_id = ? AND approval_id = ?
                  AND node_id = ? AND node_revision = ?
                  AND fencing_token = ?
                """,
                (
                    task_id,
                    state_map.plan_id,
                    approval["approval_id"],
                    selected.node_id,
                    selected.revision,
                    supervisor_lease.fencing_token,
                ),
            ).fetchone()
            if existing is not None:
                stored_document = _decode_document(
                    existing["readiness_document_json"],
                    label="DAG readiness claim",
                )
                stored_json, stored_hash = encode_document(stored_document)
                try:
                    existing_at, existing_at_us = _runtime_timestamp(
                        existing["recorded_at"]
                    )
                except TaskStoreConflict as error:
                    raise TaskStoreCorruption(
                        "persisted DAG node claim time is inconsistent"
                    ) from error
                if (
                    existing["plan_hash"] != expected_plan_hash
                    or existing["approval_id"] != approval["approval_id"]
                    or existing["node_id"] != selected.node_id
                    or existing["node_revision"] != selected.revision
                    or existing["node_state"] != selected.state
                    or existing["project_id"] != project_id
                    or existing["principal_id"] != principal_id
                    or existing["owner_id"] != supervisor_lease.owner_id
                    or existing["term_acquired_at"] != supervisor_lease.acquired_at
                    or stored_json != readiness_json
                    or stored_hash != readiness_hash
                    or existing["readiness_document_hash"] != readiness_hash
                    or existing_at != existing["recorded_at"]
                    or existing_at_us != existing["recorded_at_us"]
                ):
                    raise TaskStoreCorruption(
                        "persisted DAG node claim candidate is inconsistent"
                    )
                connection.commit()
                return DagNodeClaimCandidate(
                    node=selected,
                    approval_id=approval["approval_id"],
                    project_id=project_id,
                    principal_id=principal_id,
                    fencing_token=supervisor_lease.fencing_token,
                    owner_id=supervisor_lease.owner_id,
                    term_acquired_at=supervisor_lease.acquired_at,
                    readiness_document_hash=readiness_hash,
                    recorded_at=existing_at,
                    replayed=True,
                )

            connection.execute(
                """
                INSERT INTO dag_node_claim_candidates(
                    task_id, plan_id, plan_hash, approval_id,
                    node_id, node_revision, node_state,
                    project_id, principal_id, fencing_token,
                    owner_id, term_acquired_at,
                    readiness_document_json, readiness_document_hash,
                    recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    state_map.plan_id,
                    state_map.plan_hash,
                    approval["approval_id"],
                    selected.node_id,
                    selected.revision,
                    selected.state,
                    project_id,
                    principal_id,
                    supervisor_lease.fencing_token,
                    supervisor_lease.owner_id,
                    supervisor_lease.acquired_at,
                    readiness_json,
                    readiness_hash,
                    recorded_at,
                    recorded_at_us,
                ),
            )
            connection.commit()
            return DagNodeClaimCandidate(
                node=selected,
                approval_id=approval["approval_id"],
                project_id=project_id,
                principal_id=principal_id,
                fencing_token=supervisor_lease.fencing_token,
                owner_id=supervisor_lease.owner_id,
                term_acquired_at=supervisor_lease.acquired_at,
                readiness_document_hash=readiness_hash,
                recorded_at=recorded_at,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "DAG node claim candidate lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "DAG node claim candidate conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def bind_ready_dag_node_inputs(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        expected_plan_hash: str,
        claim_candidate: DagNodeClaimCandidate,
        artifact_inputs: Sequence[DagNodeArtifactInput],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagNodeInputBindingFact:
        """Persist one canonical, non-executable all-input binding fact.

        Exact artifact bytes are re-hashed from the immutable ``bytes`` values
        supplied to this call while the current Plan/Approval/node snapshot is
        pinned by ``BEGIN IMMEDIATE``.  This does not attest that an external
        artifact file remains immutable: the reserved producer-success rows
        have no public writer yet, and a later execution admission must verify
        the fixed Adapter evidence and bytes again under its kernel fences.
        """

        if (
            not isinstance(task_id, str)
            or not task_id
            or not isinstance(project_id, str)
            or not project_id
            or not isinstance(principal_id, str)
            or not principal_id
            or not _is_sha256(expected_plan_hash)
            or not isinstance(claim_candidate, DagNodeClaimCandidate)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
            or not isinstance(artifact_inputs, Sequence)
            or isinstance(artifact_inputs, (str, bytes, bytearray))
            or len(artifact_inputs) > 32
            or any(not isinstance(value, DagNodeArtifactInput) for value in artifact_inputs)
        ):
            raise TaskStoreConflict("DAG input binding identity is invalid")

        material_by_port: dict[str, DagNodeArtifactInput] = {}
        for material in artifact_inputs:
            if (
                not isinstance(material.target_input_port, str)
                or not material.target_input_port
                or not isinstance(material.artifact_manifest, Mapping)
                or not isinstance(material.artifact_data, bytes)
                or material.target_input_port in material_by_port
            ):
                raise TaskStoreConflict("DAG artifact input material is invalid")
            material_by_port[material.target_input_port] = material

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            recorded_at, recorded_at_us = _runtime_timestamp(supervisor_clock())
            snapshot = self._load_snapshot(connection, task_id)
            if (
                snapshot is None
                or snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
            ):
                raise TaskStoreConflict("task does not exist in the requested scope")
            plan = snapshot.plan
            approval = snapshot.approval
            if (
                snapshot.status not in {"AwaitingApproval", "Running"}
                or not isinstance(plan, Mapping)
                or len(plan.get("nodes", [])) <= 1
                or plan.get("plan_hash") != expected_plan_hash
                or not isinstance(approval, Mapping)
                or approval.get("decision") != "approved"
                or approval.get("plan_id") != plan.get("plan_id")
                or approval.get("plan_hash") != expected_plan_hash
            ):
                raise TaskStoreConflict(
                    "DAG input binding requires the exact current approved multi-node plan"
                )
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=project_id,
                principal_id=principal_id,
                supplied=supervisor_lease,
                now_us=recorded_at_us,
                action="DAG input binding",
            )

            state_map = self._load_dag_node_state_map(
                connection,
                task_id=task_id,
                plan=plan,
            )
            if state_map is None:
                raise TaskStoreConflict(
                    "DAG input binding requires an initialized node-state map"
                )
            node_states = {node.node_id: node.state for node in state_map.nodes}
            states_by_id = {node.node_id: node for node in state_map.nodes}
            try:
                readiness = evaluate_dag_readiness(plan, node_states=node_states)
            except DagScheduleError as error:
                raise TaskStoreCorruption(
                    "persisted DAG state cannot be evaluated for input binding"
                ) from error
            if readiness.active_node_ids:
                raise TaskStoreConflict(
                    "DAG task already has an active admitted node"
                )
            if not readiness.runnable_node_ids:
                raise TaskStoreConflict("DAG plan has no runnable Pending node")
            selected_node_id = readiness.runnable_node_ids[0]
            selected = states_by_id.get(selected_node_id)
            if selected is None or selected.state != "Pending":
                raise TaskStoreCorruption(
                    "DAG readiness selected an invalid input-binding target"
                )

            if (
                claim_candidate.node != selected
                or claim_candidate.approval_id != approval.get("approval_id")
                or claim_candidate.project_id != project_id
                or claim_candidate.principal_id != principal_id
                or claim_candidate.fencing_token != supervisor_lease.fencing_token
                or claim_candidate.owner_id != supervisor_lease.owner_id
                or claim_candidate.term_acquired_at != supervisor_lease.acquired_at
            ):
                raise TaskStoreConflict(
                    "DAG input binding requires the exact current claim candidate"
                )

            readiness_document = {
                "schema_version": "1.0.0",
                "task_id": task_id,
                "plan_id": state_map.plan_id,
                "plan_hash": state_map.plan_hash,
                "approval_id": approval["approval_id"],
                "node_states": [
                    {
                        "node_id": node.node_id,
                        "revision": node.revision,
                        "state": node.state,
                    }
                    for node in state_map.nodes
                ],
                "topological_layers": [
                    list(layer) for layer in readiness.topological_layers
                ],
                "runnable_node_ids": list(readiness.runnable_node_ids),
                "waiting_node_ids": list(readiness.waiting_node_ids),
                "active_node_ids": list(readiness.active_node_ids),
                "succeeded_node_ids": list(readiness.succeeded_node_ids),
                "failed_node_ids": list(readiness.failed_node_ids),
                "cancelled_node_ids": list(readiness.cancelled_node_ids),
                "blocked_nodes": [
                    {
                        "node_id": blocked.node_id,
                        "blocked_by_node_ids": list(blocked.blocked_by_node_ids),
                    }
                    for blocked in readiness.blocked_nodes
                ],
                "selected_node_id": selected_node_id,
            }
            readiness_json, readiness_hash = encode_document(readiness_document)
            claim_row = connection.execute(
                """
                SELECT *
                FROM dag_node_claim_candidates
                WHERE task_id = ? AND plan_id = ? AND approval_id = ?
                  AND node_id = ? AND node_revision = ?
                  AND fencing_token = ?
                """,
                (
                    task_id,
                    plan["plan_id"],
                    approval["approval_id"],
                    selected.node_id,
                    selected.revision,
                    supervisor_lease.fencing_token,
                ),
            ).fetchone()
            if claim_row is None:
                raise TaskStoreConflict(
                    "DAG input binding requires a durable claim candidate"
                )
            stored_readiness = _decode_document(
                claim_row["readiness_document_json"],
                label="DAG input-binding claim",
            )
            stored_readiness_json, stored_readiness_hash = encode_document(
                stored_readiness
            )
            try:
                claim_recorded_at, claim_recorded_at_us = _runtime_timestamp(
                    claim_row["recorded_at"]
                )
            except TaskStoreConflict as error:
                raise TaskStoreCorruption(
                    "persisted DAG input-binding claim time is inconsistent"
                ) from error
            if (
                stored_readiness_json != readiness_json
                or stored_readiness_hash != readiness_hash
                or claim_row["readiness_document_hash"] != readiness_hash
                or claim_candidate.readiness_document_hash != readiness_hash
                or claim_row["plan_hash"] != expected_plan_hash
                or claim_row["project_id"] != project_id
                or claim_row["principal_id"] != principal_id
                or claim_row["owner_id"] != supervisor_lease.owner_id
                or claim_row["term_acquired_at"] != supervisor_lease.acquired_at
                or claim_recorded_at != claim_row["recorded_at"]
                or claim_recorded_at_us != claim_row["recorded_at_us"]
                or claim_candidate.recorded_at != claim_recorded_at
                or claim_recorded_at_us > recorded_at_us
            ):
                raise TaskStoreCorruption(
                    "persisted DAG input-binding claim is inconsistent"
                )

            nodes_by_id = {
                node["node_id"]: node
                for node in plan["nodes"]
                if isinstance(node, Mapping)
                and isinstance(node.get("node_id"), str)
            }
            target_node = nodes_by_id.get(selected.node_id)
            if not isinstance(target_node, Mapping) or not isinstance(
                target_node.get("inputs"), list
            ):
                raise TaskStoreCorruption(
                    "DAG input-binding target differs from its plan"
                )

            input_documents: list[dict[str, Any]] = []
            consumed_material_ports: set[str] = set()
            for input_index, input_binding in enumerate(target_node["inputs"]):
                if (
                    not isinstance(input_binding, Mapping)
                    or not isinstance(input_binding.get("port"), str)
                ):
                    raise TaskStoreCorruption(
                        "persisted DAG target input contract is invalid"
                    )
                target_port = input_binding["port"]
                dataset = input_binding.get("dataset")
                source = input_binding.get("source")
                if isinstance(dataset, Mapping) and source is None:
                    dataset_row = connection.execute(
                        """
                        SELECT project_id, dataset_id, version, content_hash,
                               data_type, document_json, document_hash
                        FROM dataset_catalog
                        WHERE project_id = ? AND dataset_id = ? AND version = ?
                        """,
                        (
                            project_id,
                            dataset.get("id"),
                            dataset.get("version"),
                        ),
                    ).fetchone()
                    if dataset_row is None:
                        raise TaskStoreConflict(
                            "DAG dataset input is absent from the current project catalog"
                        )
                    catalog_dataset = self._load_dataset_registration(
                        connection,
                        dataset_row,
                    )
                    dataset_identity = {
                        key: catalog_dataset.get(key)
                        for key in ("id", "version", "content_hash", "data_type")
                    }
                    if dict(dataset) != dataset_identity:
                        raise TaskStoreConflict(
                            "DAG dataset input differs from the immutable catalog"
                        )
                    input_documents.append(
                        {
                            "input_index": input_index,
                            "target_input_port": target_port,
                            "kind": "dataset",
                            "dataset": dict(dataset),
                            "dataset_document_hash": dataset_row["document_hash"],
                        }
                    )
                    continue

                if not isinstance(source, Mapping) or dataset is not None:
                    raise TaskStoreCorruption(
                        "persisted DAG target input kind is invalid"
                    )
                material = material_by_port.get(target_port)
                if material is None:
                    raise TaskStoreConflict(
                        "DAG node-output inputs must be supplied exactly once"
                    )
                consumed_material_ports.add(target_port)
                source_node_id = source.get("node_id")
                source_state = (
                    states_by_id.get(source_node_id)
                    if isinstance(source_node_id, str)
                    else None
                )
                if source_state is None or source_state.state != "Succeeded":
                    raise TaskStoreConflict(
                        "DAG node-output input requires the latest Succeeded producer"
                    )
                succeeded = self._load_dag_node_succeeded_outputs(
                    connection,
                    task_id=task_id,
                    plan=plan,
                    approval_id=approval["approval_id"],
                    source_state=source_state,
                )
                try:
                    edge_binding = bind_dag_artifact_input(
                        plan,
                        task_id=task_id,
                        target_node_id=selected.node_id,
                        target_input_port=target_port,
                        artifact_manifest=material.artifact_manifest,
                        artifact_data=material.artifact_data,
                        expected_lineage_inputs=[
                            {
                                key: root[key]
                                for key in (
                                    "id",
                                    "version",
                                    "content_hash",
                                    "data_type",
                                )
                            }
                            for root in succeeded[
                                "transitive_dataset_roots"
                            ]
                        ],
                        expected_fingerprint_input_hashes=succeeded[
                            "direct_input_hashes"
                        ],
                    )
                except DagDataBindingError as error:
                    raise TaskStoreConflict(
                        f"{error.code}: {'; '.join(error.errors)}"
                    ) from error
                if edge_binding.source_node_id != source_state.node_id:
                    raise TaskStoreCorruption(
                        "DAG data edge differs from its producer receipt"
                    )
                if _runtime_timestamp(succeeded["recorded_at"])[1] > recorded_at_us:
                    raise TaskStoreCorruption(
                        "DAG producer receipt is newer than its input binding"
                    )
                stored_output = succeeded["outputs_by_port"].get(
                    edge_binding.source_output_port
                )
                if stored_output is None:
                    raise TaskStoreCorruption(
                        "DAG producer receipt lacks the bound output"
                    )
                try:
                    supplied_manifest_json, supplied_manifest_hash = encode_document(
                        material.artifact_manifest
                    )
                except TaskStoreConflict as error:
                    raise TaskStoreConflict(
                        "DAG artifact manifest is not finite canonical JSON"
                    ) from error
                if (
                    supplied_manifest_json != stored_output["artifact_manifest_json"]
                    or supplied_manifest_hash
                    != stored_output["artifact_manifest_hash"]
                    or stored_output["data_type"] != edge_binding.data_type
                    or stored_output["artifact_manifest"]
                    != dict(material.artifact_manifest)
                ):
                    raise TaskStoreConflict(
                        "DAG artifact bytes/manifest differ from the producer receipt"
                    )
                input_documents.append(
                    {
                        "input_index": input_index,
                        "target_input_port": target_port,
                        "kind": "node_output",
                        "binding": edge_binding.document(),
                        "binding_document_hash": (
                            edge_binding.binding_document_hash
                        ),
                        "artifact_manifest_hash": supplied_manifest_hash,
                        "producer": {
                            "node_id": source_state.node_id,
                            "succeeded_revision": source_state.revision,
                            "approval_id": succeeded["approval_id"],
                            "input_binding_document_hash": succeeded[
                                "input_binding_document_hash"
                            ],
                            "receipt_document_hash": succeeded[
                                "receipt_document_hash"
                            ],
                            "receipt_record_hash": succeeded[
                                "receipt_record_hash"
                            ],
                            "succeeded_at": succeeded["recorded_at"],
                            "fencing_token": succeeded["fencing_token"],
                            "owner_id": succeeded["owner_id"],
                            "term_acquired_at": succeeded["term_acquired_at"],
                            "semantic_cache_key_hash": succeeded[
                                "semantic_cache_key_hash"
                            ],
                            "cacheable": succeeded["cacheable"],
                            "trusted_lineage_document_hash": succeeded[
                                "trusted_lineage_document_hash"
                            ],
                            "transitive_dataset_roots": copy.deepcopy(
                                succeeded["transitive_dataset_roots"]
                            ),
                        },
                    }
                )

            if set(material_by_port) != consumed_material_ports:
                raise TaskStoreConflict(
                    "DAG artifact input set contains missing or extra target ports"
                )

            binding_document = {
                "schema_version": "1.0.0",
                "task_id": task_id,
                "plan": {
                    "plan_id": plan["plan_id"],
                    "plan_hash": plan["plan_hash"],
                },
                "approval_id": approval["approval_id"],
                "target": {
                    "node_id": selected.node_id,
                    "revision": selected.revision,
                    "state": "Pending",
                },
                "scope": {
                    "project_id": project_id,
                    "principal_id": principal_id,
                },
                "supervisor_term": {
                    "fencing_token": supervisor_lease.fencing_token,
                    "owner_id": supervisor_lease.owner_id,
                    "acquired_at": supervisor_lease.acquired_at,
                },
                "claim_readiness_document_hash": readiness_hash,
                "inputs": input_documents,
            }
            binding_json, binding_hash = encode_document(binding_document)

            existing = connection.execute(
                """
                SELECT *
                FROM dag_node_input_binding_facts
                WHERE task_id = ? AND plan_id = ? AND approval_id = ?
                  AND target_node_id = ? AND target_node_revision = ?
                  AND fencing_token = ?
                """,
                (
                    task_id,
                    plan["plan_id"],
                    approval["approval_id"],
                    selected.node_id,
                    selected.revision,
                    supervisor_lease.fencing_token,
                ),
            ).fetchone()
            if existing is not None:
                existing_document = _decode_document(
                    existing["binding_document_json"],
                    label="DAG all-input binding",
                )
                existing_json, existing_hash = encode_document(existing_document)
                try:
                    existing_at, existing_at_us = _runtime_timestamp(
                        existing["recorded_at"]
                    )
                except TaskStoreConflict as error:
                    raise TaskStoreCorruption(
                        "persisted DAG all-input binding time is inconsistent"
                    ) from error
                if (
                    existing["plan_hash"] != plan["plan_hash"]
                    or existing["target_node_state"] != "Pending"
                    or existing["project_id"] != project_id
                    or existing["principal_id"] != principal_id
                    or existing["owner_id"] != supervisor_lease.owner_id
                    or existing["term_acquired_at"] != supervisor_lease.acquired_at
                    or existing["claim_readiness_document_hash"] != readiness_hash
                    or existing_json != binding_json
                    or existing_hash != binding_hash
                    or existing["binding_document_hash"] != binding_hash
                    or existing_at != existing["recorded_at"]
                    or existing_at_us != existing["recorded_at_us"]
                ):
                    raise TaskStoreCorruption(
                        "persisted DAG all-input binding is inconsistent"
                    )
                connection.commit()
                return DagNodeInputBindingFact(
                    task_id=task_id,
                    plan_id=plan["plan_id"],
                    plan_hash=plan["plan_hash"],
                    approval_id=approval["approval_id"],
                    target_node_id=selected.node_id,
                    target_node_revision=selected.revision,
                    project_id=project_id,
                    principal_id=principal_id,
                    fencing_token=supervisor_lease.fencing_token,
                    owner_id=supervisor_lease.owner_id,
                    term_acquired_at=supervisor_lease.acquired_at,
                    claim_readiness_document_hash=readiness_hash,
                    binding_document=copy.deepcopy(existing_document),
                    binding_document_hash=binding_hash,
                    recorded_at=existing_at,
                    replayed=True,
                )

            connection.execute(
                """
                INSERT INTO dag_node_input_binding_facts(
                    task_id, plan_id, plan_hash, approval_id,
                    target_node_id, target_node_revision, target_node_state,
                    project_id, principal_id, fencing_token,
                    owner_id, term_acquired_at,
                    claim_readiness_document_hash,
                    binding_document_json, binding_document_hash,
                    recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, 'Pending', ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    plan["plan_id"],
                    plan["plan_hash"],
                    approval["approval_id"],
                    selected.node_id,
                    selected.revision,
                    project_id,
                    principal_id,
                    supervisor_lease.fencing_token,
                    supervisor_lease.owner_id,
                    supervisor_lease.acquired_at,
                    readiness_hash,
                    binding_json,
                    binding_hash,
                    recorded_at,
                    recorded_at_us,
                ),
            )
            connection.commit()
            return DagNodeInputBindingFact(
                task_id=task_id,
                plan_id=plan["plan_id"],
                plan_hash=plan["plan_hash"],
                approval_id=approval["approval_id"],
                target_node_id=selected.node_id,
                target_node_revision=selected.revision,
                project_id=project_id,
                principal_id=principal_id,
                fencing_token=supervisor_lease.fencing_token,
                owner_id=supervisor_lease.owner_id,
                term_acquired_at=supervisor_lease.acquired_at,
                claim_readiness_document_hash=readiness_hash,
                binding_document=copy.deepcopy(binding_document),
                binding_document_hash=binding_hash,
                recorded_at=recorded_at,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "DAG input binding lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "DAG input binding conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def _load_dag_node_execution_admission(
        self,
        connection: sqlite3.Connection,
        *,
        task_id: str | None = None,
        intent_id: str | None = None,
        node_id: str | None = None,
    ) -> sqlite3.Row | None:
        """Read and verify the immutable index for one admitted DAG node."""

        if intent_id is not None:
            rows = connection.execute(
                "SELECT * FROM dag_node_execution_admissions WHERE intent_id = ?",
                (intent_id,),
            ).fetchall()
        elif task_id is not None and node_id is not None:
            rows = connection.execute(
                """
                SELECT * FROM dag_node_execution_admissions
                WHERE task_id = ? AND node_id = ?
                """,
                (task_id, node_id),
            ).fetchall()
        elif task_id is not None:
            selected_intent_id = self._select_task_dispatch_intent_id(
                connection, task_id=task_id
            )
            if selected_intent_id is None:
                return None
            rows = connection.execute(
                "SELECT * FROM dag_node_execution_admissions WHERE intent_id = ?",
                (selected_intent_id,),
            ).fetchall()
        else:
            raise TaskStoreCorruption(
                "DAG admission loader requires an exact selector"
            )
        if len(rows) > 1:
            raise TaskStoreCorruption("DAG node has multiple execution admissions")
        row = rows[0] if rows else None
        if row is None:
            return None
        document = _decode_document(
            row["document_json"], label="DAG node execution admission"
        )
        document_json, document_hash = encode_document(document)
        expected = {
            "schema_version": "1.0.0",
            "intent_id": row["intent_id"],
            "task_id": row["task_id"],
            "plan": {
                "plan_id": row["plan_id"],
                "plan_hash": row["plan_hash"],
            },
            "approval_id": row["approval_id"],
            "node": {
                "node_id": row["node_id"],
                "idempotency_key": row["node_idempotency_key"],
                "pending_revision": row["pending_revision"],
                "queued_revision": row["queued_revision"],
            },
            "input_binding": {
                "document_hash": row["input_binding_document_hash"],
                "supervisor_term": {
                    "fencing_token": row["input_fencing_token"],
                    "owner_id": row["input_owner_id"],
                    "acquired_at": row["input_term_acquired_at"],
                },
            },
            "scope": {
                "project_id": row["project_id"],
                "principal_id": row["principal_id"],
            },
            "admission_supervisor_term": {
                "fencing_token": row["admission_fencing_token"],
                "owner_id": row["admission_owner_id"],
                "acquired_at": row["admission_term_acquired_at"],
            },
            "max_node_attempts": row["max_node_attempts"],
            "admitted_at": row["admitted_at"],
        }
        admitted_at, admitted_at_us = _runtime_timestamp(row["admitted_at"])
        if (
            document != expected
            or document_json != row["document_json"]
            or document_hash != row["document_hash"]
            or admitted_at != row["admitted_at"]
            or admitted_at_us != row["admitted_at_us"]
            or row["queued_revision"] != row["pending_revision"] + 1
            or row["max_node_attempts"] != 1
        ):
            raise TaskStoreCorruption(
                "DAG node execution admission differs from its immutable index"
            )
        return row

    def is_dag_node_execution_intent(self, intent_id: str) -> bool:
        connection = self._connect()
        try:
            return connection.execute(
                """
                SELECT 1 FROM dag_node_execution_admissions WHERE intent_id = ?
                """,
                (intent_id,),
            ).fetchone() is not None
        finally:
            connection.close()

    def get_active_dag_node_execution(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
    ) -> DagNodeExecutionAdmission | None:
        connection = self._connect()
        try:
            connection.execute("BEGIN")
            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if (
                task is None
                or task["project_id"] != project_id
                or task["principal_id"] != principal_id
            ):
                raise TaskStoreConflict("task does not exist in the requested scope")
            rows = connection.execute(
                """
                SELECT admission.intent_id
                FROM dag_node_execution_admissions AS admission
                JOIN dag_node_state_events AS state
                  ON state.task_id = admission.task_id
                 AND state.plan_id = admission.plan_id
                 AND state.node_id = admission.node_id
                WHERE admission.task_id = ?
                  AND state.state IN ('Queued', 'Running', 'Waiting', 'Retrying')
                  AND NOT EXISTS (
                      SELECT 1 FROM dag_node_state_events AS later
                      WHERE later.task_id = state.task_id
                        AND later.plan_id = state.plan_id
                        AND later.node_id = state.node_id
                        AND later.revision > state.revision
                  )
                ORDER BY admission.admitted_at_us, admission.intent_id
                """,
                (task_id,),
            ).fetchall()
            if len(rows) > 1:
                raise TaskStoreCorruption(
                    "DAG task has multiple active execution admissions"
                )
            if task["status"] == "Cancelled":
                connection.commit()
                return None
            if task["status"] in {"Succeeded", "Failed"} and rows:
                raise TaskStoreCorruption(
                    "terminal DAG Task retains an active execution admission"
                )
            if not rows:
                connection.commit()
                return None
            admission = self._load_dag_node_execution_admission(
                connection, intent_id=rows[0]["intent_id"]
            )
            intent = self._load_dispatch_intent(
                connection, intent_id=rows[0]["intent_id"]
            )
            snapshot = self._load_snapshot(connection, task_id)
            if admission is None or intent is None or snapshot is None:
                raise TaskStoreCorruption("active DAG execution cannot be read")
            result = DagNodeExecutionAdmission(
                snapshot=snapshot,
                intent=intent,
                input_binding_document_hash=admission[
                    "input_binding_document_hash"
                ],
                pending_revision=int(admission["pending_revision"]),
                queued_revision=int(admission["queued_revision"]),
                admission_fencing_token=int(
                    admission["admission_fencing_token"]
                ),
                admitted_at=admission["admitted_at"],
                replayed=True,
            )
            connection.commit()
            return result
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def get_dag_node_input_binding_for_intent(
        self, intent_id: str
    ) -> DagNodeInputBindingFact | None:
        connection = self._connect()
        try:
            connection.execute("BEGIN")
            admission = self._load_dag_node_execution_admission(
                connection, intent_id=intent_id
            )
            if admission is None:
                connection.commit()
                return None
            row = connection.execute(
                """
                SELECT * FROM dag_node_input_binding_facts
                WHERE task_id = ? AND plan_id = ? AND approval_id = ?
                  AND target_node_id = ? AND target_node_revision = ?
                  AND fencing_token = ? AND binding_document_hash = ?
                """,
                (
                    admission["task_id"],
                    admission["plan_id"],
                    admission["approval_id"],
                    admission["node_id"],
                    admission["pending_revision"],
                    admission["input_fencing_token"],
                    admission["input_binding_document_hash"],
                ),
            ).fetchone()
            if row is None:
                raise TaskStoreCorruption("DAG intent lost its input binding")
            document = _decode_hashed_document(
                {
                    "document_json": row["binding_document_json"],
                    "document_hash": row["binding_document_hash"],
                },
                label="DAG intent input binding",
            )
            result = DagNodeInputBindingFact(
                task_id=row["task_id"],
                plan_id=row["plan_id"],
                plan_hash=row["plan_hash"],
                approval_id=row["approval_id"],
                target_node_id=row["target_node_id"],
                target_node_revision=int(row["target_node_revision"]),
                project_id=row["project_id"],
                principal_id=row["principal_id"],
                fencing_token=int(row["fencing_token"]),
                owner_id=row["owner_id"],
                term_acquired_at=row["term_acquired_at"],
                claim_readiness_document_hash=row[
                    "claim_readiness_document_hash"
                ],
                binding_document=document,
                binding_document_hash=row["binding_document_hash"],
                recorded_at=row["recorded_at"],
                replayed=True,
            )
            connection.commit()
            return result
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def get_dag_node_output_materializations(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        source_node_id: str,
    ) -> tuple[DagNodeOutputMaterialization, ...]:
        connection = self._connect()
        try:
            connection.execute("BEGIN")
            snapshot = self._load_snapshot(connection, task_id)
            if (
                snapshot is None
                or snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
                or not isinstance(snapshot.plan, Mapping)
                or not isinstance(snapshot.approval, Mapping)
            ):
                raise TaskStoreConflict("task does not exist in the requested scope")
            state_map = self._load_dag_node_state_map(
                connection, task_id=task_id, plan=snapshot.plan
            )
            source_state = next(
                (
                    value
                    for value in (state_map.nodes if state_map is not None else ())
                    if value.node_id == source_node_id
                ),
                None,
            )
            if source_state is None or source_state.state != "Succeeded":
                raise TaskStoreConflict("DAG source node is not Succeeded")
            succeeded = self._load_dag_node_succeeded_outputs(
                connection,
                task_id=task_id,
                plan=snapshot.plan,
                approval_id=snapshot.approval["approval_id"],
                source_state=source_state,
            )
            result: list[DagNodeOutputMaterialization] = []
            for port, output in sorted(succeeded["outputs_by_port"].items()):
                logical = output["artifact_manifest"]
                physical_manifest = output.get(
                    "physical_artifact_manifest", logical
                )
                physical_task_id = output.get("physical_task_id", task_id)
                physical_intent_id = output.get(
                    "physical_intent_id", succeeded.get("physical_intent_id")
                )
                if (
                    not isinstance(physical_manifest, Mapping)
                    or not isinstance(physical_task_id, str)
                    or not isinstance(physical_intent_id, str)
                ):
                    raise TaskStoreCorruption(
                        "DAG output physical source is invalid"
                    )
                physical_snapshot = self._load_snapshot(
                    connection, physical_task_id
                )
                if (
                    physical_snapshot is None
                    or physical_snapshot.project_id != project_id
                    or physical_snapshot.principal_id != principal_id
                ):
                    raise TaskStoreCorruption(
                        "DAG output physical source crossed its scope"
                    )
                result.append(
                    DagNodeOutputMaterialization(
                        source_node_id=source_node_id,
                        source_output_port=port,
                        data_type=output["data_type"],
                        logical_artifact_manifest=copy.deepcopy(logical),
                        logical_artifact_manifest_hash=output[
                            "artifact_manifest_hash"
                        ],
                        physical_task_id=physical_task_id,
                        physical_intent_id=physical_intent_id,
                        physical_artifact_id=output.get(
                            "physical_artifact_id",
                            physical_manifest["artifact_id"],
                        ),
                        physical_artifact_manifest=copy.deepcopy(
                            dict(physical_manifest)
                        ),
                        physical_artifact_manifest_hash=output.get(
                            "physical_artifact_manifest_hash",
                            output["artifact_manifest_hash"],
                        ),
                        trusted_lineage_document=copy.deepcopy(
                            succeeded["trusted_lineage_document"]
                        ),
                        trusted_lineage_document_hash=succeeded[
                            "trusted_lineage_document_hash"
                        ],
                    )
                )
            connection.commit()
            return tuple(result)
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def record_supervised_dag_dispatch_reconciliation(
        self,
        *,
        intent_id: str,
        failure_code: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DispatchIntentSnapshot:
        """Fence one ambiguous DAG dispatch outcome under the active term."""

        if (
            not isinstance(intent_id, str)
            or not intent_id
            or not isinstance(failure_code, str)
            or not 1 <= len(failure_code) <= 128
            or not failure_code.replace("_", "").isalnum()
            or failure_code.upper() != failure_code
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("supervised DAG reconciliation is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            recorded_at, recorded_at_us = _runtime_timestamp(supervisor_clock())
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            if intent is None or intent.intent_id != intent_id:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            task = connection.execute(
                "SELECT project_id, principal_id, status FROM tasks WHERE task_id = ?",
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("DAG dispatch task cannot be read")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=recorded_at_us,
                action="DAG dispatch reconciliation",
            )
            admission = self._require_current_dag_node_execution(
                connection,
                intent=intent,
                allowed_node_states=frozenset({"Queued"}),
            )
            if admission is None or task["status"] not in {"Queued", "Running"}:
                raise TaskStoreConflict(
                    "DAG reconciliation requires the exact admitted queued node"
                )
            existing = connection.execute(
                "SELECT outcome, document_json FROM dispatch_outcomes "
                "WHERE intent_id = ?",
                (intent_id,),
            ).fetchone()
            expected_document = {
                "status": "reconciliation_required",
                "failure_code": failure_code,
                "recorded_at": recorded_at,
            }
            if existing is not None:
                stored = self._load_dispatch_intent(
                    connection, intent_id=intent_id
                )
                if (
                    stored is None
                    or existing["outcome"] != "reconciliation_required"
                    or stored.state != "reconciliation_required"
                    or stored.failure_code != failure_code
                ):
                    raise TaskStoreConflict(
                        "DAG dispatch reconciliation outcome already differs"
                    )
                connection.commit()
                return stored
            if intent.state != "dispatching":
                raise TaskStoreConflict("DAG dispatch intent is not dispatching")
            document_json, document_hash = encode_document(expected_document)
            connection.execute(
                """
                INSERT INTO dispatch_outcomes(
                    intent_id, outcome, document_json, document_hash, recorded_at
                ) VALUES (?, 'reconciliation_required', ?, ?, ?)
                """,
                (intent_id, document_json, document_hash, recorded_at),
            )
            stored = self._load_dispatch_intent(
                connection, intent_id=intent_id
            )
            if stored is None or stored.state != "reconciliation_required":
                raise TaskStoreCorruption(
                    "supervised DAG reconciliation cannot be read"
                )
            connection.commit()
            return stored
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "DAG reconciliation lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "supervised DAG reconciliation conflicts with durable state"
            ) from error
        finally:
            connection.close()

    def find_dag_node_cache_candidate(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        input_binding: DagNodeInputBindingFact,
        adapter_id: str,
        adapter_version: str,
        queue_fingerprint: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagNodeCacheCandidate | None:
        """Resolve one exact scope-local semantic hit without authorizing it."""

        if (
            not isinstance(task_id, str)
            or not task_id
            or not isinstance(project_id, str)
            or not project_id
            or not isinstance(principal_id, str)
            or not principal_id
            or not isinstance(input_binding, DagNodeInputBindingFact)
            or not isinstance(adapter_id, str)
            or not adapter_id
            or not isinstance(adapter_version, str)
            or not adapter_version
            or not isinstance(queue_fingerprint, Mapping)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("DAG cache lookup identity is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN")
            observed_at, observed_at_us = _runtime_timestamp(supervisor_clock())
            del observed_at
            snapshot = self._load_snapshot(connection, task_id)
            if (
                snapshot is None
                or snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
            ):
                raise TaskStoreConflict("task does not exist in the requested scope")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=project_id,
                principal_id=principal_id,
                supplied=supervisor_lease,
                now_us=observed_at_us,
                action="DAG node cache lookup",
            )
            plan = snapshot.plan
            approval = snapshot.approval
            if (
                snapshot.status not in {"AwaitingApproval", "Running"}
                or not isinstance(plan, Mapping)
                or not isinstance(approval, Mapping)
                or approval.get("decision") != "approved"
                or approval.get("plan_id") != plan.get("plan_id")
                or approval.get("plan_hash") != plan.get("plan_hash")
                or input_binding.task_id != task_id
                or input_binding.plan_id != plan.get("plan_id")
                or input_binding.plan_hash != plan.get("plan_hash")
                or input_binding.approval_id != approval.get("approval_id")
                or input_binding.project_id != project_id
                or input_binding.principal_id != principal_id
                or input_binding.fencing_token != supervisor_lease.fencing_token
                or input_binding.owner_id != supervisor_lease.owner_id
                or input_binding.term_acquired_at != supervisor_lease.acquired_at
            ):
                raise TaskStoreConflict(
                    "DAG cache lookup requires the exact current approved binding"
                )
            binding_row = connection.execute(
                """
                SELECT * FROM dag_node_input_binding_facts
                WHERE task_id = ? AND plan_id = ? AND approval_id = ?
                  AND target_node_id = ? AND target_node_revision = ?
                  AND fencing_token = ? AND binding_document_hash = ?
                """,
                (
                    task_id,
                    input_binding.plan_id,
                    input_binding.approval_id,
                    input_binding.target_node_id,
                    input_binding.target_node_revision,
                    input_binding.fencing_token,
                    input_binding.binding_document_hash,
                ),
            ).fetchone()
            if binding_row is None:
                raise TaskStoreConflict("DAG cache lookup lost its input binding")
            binding_document = _decode_hashed_document(
                {
                    "document_json": binding_row["binding_document_json"],
                    "document_hash": binding_row["binding_document_hash"],
                },
                label="DAG cache target input binding",
            )
            if (
                binding_document != input_binding.binding_document
                or binding_row["target_node_state"] != "Pending"
                or binding_row["project_id"] != project_id
                or binding_row["principal_id"] != principal_id
            ):
                raise TaskStoreCorruption("DAG cache target binding is inconsistent")
            state_map = self._load_dag_node_state_map(
                connection, task_id=task_id, plan=plan
            )
            target_state = next(
                (
                    value
                    for value in (state_map.nodes if state_map is not None else ())
                    if value.node_id == input_binding.target_node_id
                ),
                None,
            )
            if (
                target_state is None
                or target_state.state != "Pending"
                or target_state.revision != input_binding.target_node_revision
                or self._load_dag_node_execution_admission(
                    connection,
                    task_id=task_id,
                    node_id=input_binding.target_node_id,
                )
                is not None
            ):
                raise TaskStoreConflict("DAG cache target is no longer Pending")
            target_node = next(
                (
                    value
                    for value in plan.get("nodes", [])
                    if isinstance(value, Mapping)
                    and value.get("node_id") == target_state.node_id
                ),
                None,
            )
            algorithm = (
                target_node.get("algorithm")
                if isinstance(target_node, Mapping)
                else None
            )
            if not isinstance(algorithm, Mapping):
                raise TaskStoreCorruption("DAG cache target Algorithm is invalid")
            algorithm_row = connection.execute(
                """
                SELECT algorithm_id, version, allowlisted,
                       document_json, document_hash
                FROM algorithm_registry
                WHERE algorithm_id = ? AND version = ?
                """,
                (algorithm.get("id"), algorithm.get("version")),
            ).fetchone()
            if algorithm_row is None:
                raise TaskStoreConflict("DAG cache target Algorithm is unavailable")
            manifest = self._load_algorithm_registration(algorithm_row)
            try:
                identity = build_node_cache_identity(
                    plan,
                    node_id=target_state.node_id,
                    input_binding_document=binding_document,
                    adapter_id=adapter_id,
                    adapter_version=adapter_version,
                    queue_fingerprint=queue_fingerprint,
                    algorithm_manifest=manifest,
                    algorithm_manifest_document_hash=algorithm_row["document_hash"],
                    approval_scope=approval.get("scope"),
                    project_id=project_id,
                    principal_id=principal_id,
                )
            except NodeCacheIdentityError as error:
                if error.code == "NODE_CACHE_UNCACHEABLE":
                    connection.commit()
                    return None
                raise TaskStoreConflict("DAG cache target identity is invalid") from error
            rows = connection.execute(
                """
                SELECT * FROM dag_node_cache_entries
                WHERE project_id = ? AND principal_id = ? AND cache_key_hash = ?
                ORDER BY recorded_at_us ASC, cache_entry_id ASC
                """,
                (project_id, principal_id, identity.document_hash),
            ).fetchall()
            if not rows:
                connection.commit()
                return None
            selected: tuple[sqlite3.Row, TaskSnapshot, DispatchIntentSnapshot,
                            DagNodeStateSnapshot, dict[str, Any]] | None = None
            expected_key_json, expected_key_hash = encode_document(identity.document)
            for entry in rows:
                stored_key = _decode_hashed_document(
                    {
                        "document_json": entry["cache_key_document_json"],
                        "document_hash": entry["cache_key_hash"],
                    },
                    label="DAG node cache candidate key",
                )
                stored_lineage = _decode_hashed_document(
                    {
                        "document_json": entry["trusted_lineage_document_json"],
                        "document_hash": entry["trusted_lineage_document_hash"],
                    },
                    label="DAG node cache candidate lineage",
                )
                if (
                    stored_key != identity.document
                    or entry["cache_key_document_json"] != expected_key_json
                    or entry["cache_key_hash"] != expected_key_hash
                    or stored_lineage.get("permission_scope")
                    != {"project_id": project_id, "principal_id": principal_id}
                ):
                    raise TaskStoreCorruption("DAG node cache candidate diverged")
                source_snapshot = self._load_snapshot(
                    connection, entry["source_task_id"]
                )
                if (
                    source_snapshot is None
                    or source_snapshot.project_id != project_id
                    or source_snapshot.principal_id != principal_id
                    or not isinstance(source_snapshot.plan, Mapping)
                ):
                    raise TaskStoreCorruption("DAG cache source scope is inconsistent")
                source_states = self._load_dag_node_state_map(
                    connection,
                    task_id=entry["source_task_id"],
                    plan=source_snapshot.plan,
                )
                source_state = next(
                    (
                        value
                        for value in (
                            source_states.nodes if source_states is not None else ()
                        )
                        if value.node_id == entry["source_node_id"]
                    ),
                    None,
                )
                source_admission = self._load_dag_node_execution_admission(
                    connection, intent_id=entry["source_intent_id"]
                )
                source_intent = self._load_dispatch_intent(
                    connection, intent_id=entry["source_intent_id"]
                )
                if (
                    source_state is None
                    or source_state.state != "Succeeded"
                    or source_state.revision != entry["source_succeeded_revision"]
                    or source_admission is None
                    or source_intent is None
                ):
                    raise TaskStoreCorruption("DAG cache source is not Succeeded")
                succeeded = self._load_dag_node_succeeded_outputs(
                    connection,
                    task_id=entry["source_task_id"],
                    plan=source_snapshot.plan,
                    approval_id=entry["source_approval_id"],
                    source_state=source_state,
                )
                if (
                    succeeded.get("cache_entry_id") != entry["cache_entry_id"]
                    or succeeded.get("semantic_cache_key_hash")
                    != identity.document_hash
                    or succeeded.get("trusted_lineage_document") != stored_lineage
                    or succeeded.get("receipt_document_hash")
                    != entry["source_receipt_document_hash"]
                    or source_admission["document_hash"]
                    != entry["source_admission_document_hash"]
                ):
                    raise TaskStoreCorruption("DAG cache source proof diverged")
                if selected is None:
                    selected = (
                        entry,
                        source_snapshot,
                        source_intent,
                        source_state,
                        succeeded,
                    )
            if selected is None:
                raise TaskStoreCorruption("DAG cache candidate selection failed")
            entry, source_snapshot, source_intent, source_state, succeeded = selected
            outputs = tuple(
                {
                    "output_port": port,
                    "data_type": output["data_type"],
                    "artifact_manifest": copy.deepcopy(
                        output["artifact_manifest"]
                    ),
                    "artifact_manifest_hash": output[
                        "artifact_manifest_hash"
                    ],
                }
                for port, output in sorted(succeeded["outputs_by_port"].items())
            )
            candidate = DagNodeCacheCandidate(
                cache_entry_id=entry["cache_entry_id"],
                cache_key_document=copy.deepcopy(identity.document),
                cache_key_hash=identity.document_hash,
                source_snapshot=source_snapshot,
                source_intent=source_intent,
                source_node_revision=source_state.revision,
                source_admission_document_hash=entry[
                    "source_admission_document_hash"
                ],
                source_receipt_document_hash=succeeded[
                    "receipt_document_hash"
                ],
                source_receipt_record_hash=succeeded["receipt_record_hash"],
                source_attempt_id=succeeded["attempt_id"],
                source_attempt_number=succeeded["attempt_number"],
                trusted_lineage_document=copy.deepcopy(
                    succeeded["trusted_lineage_document"]
                ),
                trusted_lineage_document_hash=succeeded[
                    "trusted_lineage_document_hash"
                ],
                outputs=outputs,
            )
            connection.commit()
            return candidate
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def commit_dag_node_cache_hit(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        input_binding: DagNodeInputBindingFact,
        candidate: DagNodeCacheCandidate,
        artifact_verification: Mapping[str, Any],
        cache_hit_id: str,
        validate: Callable[
            [SubmitGateContext, str],
            tuple[Mapping[str, Any], Mapping[str, Any]],
        ],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagNodeCacheHitFact:
        """Atomically publish a reverified, no-Worker Pending-to-Succeeded hit."""

        if (
            not isinstance(task_id, str)
            or not task_id
            or not isinstance(project_id, str)
            or not project_id
            or not isinstance(principal_id, str)
            or not principal_id
            or not isinstance(input_binding, DagNodeInputBindingFact)
            or not isinstance(candidate, DagNodeCacheCandidate)
            or not isinstance(artifact_verification, Mapping)
            or not isinstance(cache_hit_id, str)
            or re.fullmatch(r"cache-hit-[0-9a-f]{32}", cache_hit_id) is None
            or not callable(validate)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("DAG node cache hit is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            recorded_at, recorded_at_us = _runtime_timestamp(supervisor_clock())
            snapshot = self._load_snapshot(connection, task_id)
            if (
                snapshot is None
                or snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
            ):
                raise TaskStoreConflict("task does not exist in the requested scope")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=project_id,
                principal_id=principal_id,
                supplied=supervisor_lease,
                now_us=recorded_at_us,
                action="DAG node cache hit",
            )
            plan = snapshot.plan
            approval = snapshot.approval
            if (
                snapshot.status not in {"AwaitingApproval", "Running"}
                or not isinstance(plan, Mapping)
                or len(plan.get("nodes", [])) <= 1
                or not isinstance(approval, Mapping)
                or approval.get("decision") != "approved"
                or approval.get("plan_id") != plan.get("plan_id")
                or approval.get("plan_hash") != plan.get("plan_hash")
                or input_binding.task_id != task_id
                or input_binding.plan_id != plan.get("plan_id")
                or input_binding.plan_hash != plan.get("plan_hash")
                or input_binding.approval_id != approval.get("approval_id")
                or input_binding.project_id != project_id
                or input_binding.principal_id != principal_id
                or input_binding.fencing_token != supervisor_lease.fencing_token
                or input_binding.owner_id != supervisor_lease.owner_id
                or input_binding.term_acquired_at != supervisor_lease.acquired_at
            ):
                raise TaskStoreConflict(
                    "DAG cache hit requires the exact current approved binding"
                )
            binding_row = connection.execute(
                """
                SELECT * FROM dag_node_input_binding_facts
                WHERE task_id = ? AND plan_id = ? AND approval_id = ?
                  AND target_node_id = ? AND target_node_revision = ?
                  AND fencing_token = ? AND binding_document_hash = ?
                """,
                (
                    task_id,
                    input_binding.plan_id,
                    input_binding.approval_id,
                    input_binding.target_node_id,
                    input_binding.target_node_revision,
                    input_binding.fencing_token,
                    input_binding.binding_document_hash,
                ),
            ).fetchone()
            if binding_row is None:
                raise TaskStoreConflict("DAG cache hit lost its input binding")
            binding_document = _decode_hashed_document(
                {
                    "document_json": binding_row["binding_document_json"],
                    "document_hash": binding_row["binding_document_hash"],
                },
                label="DAG cache-hit input binding",
            )
            if (
                binding_document != input_binding.binding_document
                or binding_row["target_node_state"] != "Pending"
                or binding_row["project_id"] != project_id
                or binding_row["principal_id"] != principal_id
                or binding_row["owner_id"] != supervisor_lease.owner_id
                or binding_row["term_acquired_at"]
                != supervisor_lease.acquired_at
            ):
                raise TaskStoreCorruption("DAG cache-hit binding is inconsistent")
            state_map = self._load_dag_node_state_map(
                connection, task_id=task_id, plan=plan
            )
            selected_state = next(
                (
                    value
                    for value in (state_map.nodes if state_map is not None else ())
                    if value.node_id == input_binding.target_node_id
                ),
                None,
            )
            if (
                selected_state is None
                or selected_state.state != "Pending"
                or selected_state.revision != input_binding.target_node_revision
                or self._load_dag_node_execution_admission(
                    connection, task_id=task_id, node_id=selected_state.node_id
                )
                is not None
                or connection.execute(
                    "SELECT 1 FROM dispatch_intents "
                    "WHERE task_id = ? AND plan_id = ? AND node_id = ?",
                    (task_id, plan["plan_id"], selected_state.node_id),
                ).fetchone()
                is not None
            ):
                raise TaskStoreConflict("DAG cache-hit target is no longer Pending")

            entry = connection.execute(
                "SELECT * FROM dag_node_cache_entries WHERE cache_entry_id = ?",
                (candidate.cache_entry_id,),
            ).fetchone()
            if entry is None:
                raise TaskStoreConflict("DAG cache source entry no longer exists")
            stored_key = _decode_hashed_document(
                {
                    "document_json": entry["cache_key_document_json"],
                    "document_hash": entry["cache_key_hash"],
                },
                label="DAG cache-hit key",
            )
            stored_lineage = _decode_hashed_document(
                {
                    "document_json": entry["trusted_lineage_document_json"],
                    "document_hash": entry["trusted_lineage_document_hash"],
                },
                label="DAG cache-hit source lineage",
            )
            if (
                stored_key != candidate.cache_key_document
                or entry["cache_key_hash"] != candidate.cache_key_hash
                or stored_lineage != candidate.trusted_lineage_document
                or entry["trusted_lineage_document_hash"]
                != candidate.trusted_lineage_document_hash
                or entry["source_task_id"] != candidate.source_snapshot.task_id
                or entry["source_intent_id"] != candidate.source_intent.intent_id
                or entry["source_succeeded_revision"]
                != candidate.source_node_revision
                or entry["source_admission_document_hash"]
                != candidate.source_admission_document_hash
                or entry["source_receipt_document_hash"]
                != candidate.source_receipt_document_hash
                or entry["project_id"] != project_id
                or entry["principal_id"] != principal_id
            ):
                raise TaskStoreCorruption("DAG cache-hit source identity diverged")
            source_snapshot = self._load_snapshot(
                connection, entry["source_task_id"]
            )
            source_states = (
                self._load_dag_node_state_map(
                    connection,
                    task_id=entry["source_task_id"],
                    plan=source_snapshot.plan,
                )
                if source_snapshot is not None
                and isinstance(source_snapshot.plan, Mapping)
                else None
            )
            source_state = next(
                (
                    value
                    for value in (
                        source_states.nodes if source_states is not None else ()
                    )
                    if value.node_id == entry["source_node_id"]
                ),
                None,
            )
            if source_snapshot is None or source_state is None:
                raise TaskStoreCorruption("DAG cache source cannot be reconstructed")
            succeeded = self._load_dag_node_succeeded_outputs(
                connection,
                task_id=entry["source_task_id"],
                plan=source_snapshot.plan,
                approval_id=entry["source_approval_id"],
                source_state=source_state,
            )
            source_outputs = tuple(
                {
                    "output_port": port,
                    "data_type": output["data_type"],
                    "artifact_manifest": copy.deepcopy(
                        output["artifact_manifest"]
                    ),
                    "artifact_manifest_hash": output[
                        "artifact_manifest_hash"
                    ],
                }
                for port, output in sorted(succeeded["outputs_by_port"].items())
            )
            if (
                source_outputs != candidate.outputs
                or succeeded["attempt_id"] != candidate.source_attempt_id
                or succeeded["attempt_number"] != candidate.source_attempt_number
                or succeeded["receipt_record_hash"]
                != candidate.source_receipt_record_hash
            ):
                raise TaskStoreCorruption("DAG cache source receipt diverged")
            expected_verification = {
                "schema_version": "1.0.0",
                "permission_scope": {
                    "project_id": project_id,
                    "principal_id": principal_id,
                },
                "cache_entry_id": candidate.cache_entry_id,
                "cache_key_hash": candidate.cache_key_hash,
                "source": {
                    "intent_id": candidate.source_intent.intent_id,
                    "receipt_document_hash": (
                        candidate.source_receipt_document_hash
                    ),
                    "trusted_lineage_document_hash": (
                        candidate.trusted_lineage_document_hash
                    ),
                },
                "artifacts": [
                    {
                        "output_port": output["output_port"],
                        "data_type": output["data_type"],
                        "schema_version": output["artifact_manifest"][
                            "schema_version"
                        ],
                        "media_type": output["artifact_manifest"]["media_type"],
                        "content_hash": output["artifact_manifest"][
                            "content_hash"
                        ],
                        "size_bytes": output["artifact_manifest"]["size_bytes"],
                        "artifact_manifest_hash": output[
                            "artifact_manifest_hash"
                        ],
                        "symlink": False,
                    }
                    for output in source_outputs
                ],
            }
            verification_json, verification_hash = encode_document(
                artifact_verification
            )
            if dict(artifact_verification) != expected_verification:
                raise TaskStoreConflict("DAG cache artifact verification diverged")

            run_row = connection.execute(
                "SELECT * FROM dag_task_execution_runs WHERE task_id = ?",
                (task_id,),
            ).fetchone()
            prior_hit = connection.execute(
                "SELECT 1 FROM dag_node_cache_hit_facts "
                "WHERE target_task_id = ? LIMIT 1",
                (task_id,),
            ).fetchone()
            first_execution = run_row is None and prior_hit is None
            if first_execution and snapshot.status != "AwaitingApproval":
                raise TaskStoreConflict("first DAG cache hit is not awaiting execution")
            if not first_execution and snapshot.status != "Running":
                raise TaskStoreCorruption("successor DAG cache hit lost its run anchor")
            budget = self._load_approval_budget(
                connection,
                task_id=task_id,
                approval_id=input_binding.approval_id,
            )
            if budget is None:
                raise TaskStoreCorruption("DAG cache hit has no approval budget")
            if first_execution:
                if budget.tasks_used != 0:
                    raise TaskStoreConflict("DAG cache-hit budget was already consumed")
                gate_budget = budget
            else:
                if budget.tasks_used != 1:
                    raise TaskStoreCorruption("DAG cache-hit budget is inconsistent")
                gate_budget = replace(budget, tasks_used=0)
            try:
                dataset_keys = [
                    (value["id"], value["version"])
                    for value in snapshot.draft.get("datasets", [])
                ]
                algorithm_keys = [
                    (value["algorithm"]["id"], value["algorithm"]["version"])
                    for value in plan["nodes"]
                ]
            except (KeyError, TypeError) as error:
                raise TaskStoreCorruption("DAG cache Gate inputs are invalid") from error
            registry = self._load_registry_snapshots(
                connection,
                project_id=project_id,
                dataset_keys=dataset_keys,
                algorithm_keys=algorithm_keys,
            )
            intent_document, _queued_event = validate(
                SubmitGateContext(
                    snapshot=snapshot,
                    registry=registry,
                    budget=gate_budget,
                    task_budget_already_consumed=not first_execution,
                ),
                recorded_at,
            )
            intent_document = dict(intent_document)
            adapter = intent_document.get("adapter")
            fingerprint = intent_document.get("queue_fingerprint")
            target_node = next(
                value
                for value in plan["nodes"]
                if value["node_id"] == selected_state.node_id
            )
            algorithm_row = connection.execute(
                """
                SELECT algorithm_id, version, allowlisted,
                       document_json, document_hash
                FROM algorithm_registry
                WHERE algorithm_id = ? AND version = ?
                """,
                (
                    target_node["algorithm"]["id"],
                    target_node["algorithm"]["version"],
                ),
            ).fetchone()
            if (
                algorithm_row is None
                or not isinstance(adapter, Mapping)
                or not isinstance(fingerprint, Mapping)
            ):
                raise TaskStoreConflict("DAG cache Gate result is invalid")
            manifest = self._load_algorithm_registration(algorithm_row)
            try:
                target_identity = build_node_cache_identity(
                    plan,
                    node_id=selected_state.node_id,
                    input_binding_document=binding_document,
                    adapter_id=adapter.get("id"),
                    adapter_version=adapter.get("version"),
                    queue_fingerprint=fingerprint,
                    algorithm_manifest=manifest,
                    algorithm_manifest_document_hash=algorithm_row["document_hash"],
                    approval_scope=approval.get("scope"),
                    project_id=project_id,
                    principal_id=principal_id,
                )
            except NodeCacheIdentityError as error:
                raise TaskStoreConflict("DAG cache identity changed at commit") from error
            if (
                target_identity.document != candidate.cache_key_document
                or target_identity.document_hash != candidate.cache_key_hash
                or intent_document.get("task_id") != task_id
                or intent_document.get("plan_id") != plan["plan_id"]
                or intent_document.get("plan_hash") != plan["plan_hash"]
                or intent_document.get("approval_id") != approval["approval_id"]
                or intent_document.get("node_id") != selected_state.node_id
            ):
                raise TaskStoreConflict("DAG cache identity changed at commit")

            projected_states = {
                value.node_id: value.state for value in state_map.nodes
            }
            projected_states[selected_state.node_id] = "Succeeded"
            try:
                readiness = evaluate_dag_readiness(
                    plan, node_states=projected_states
                )
            except DagScheduleError as error:
                raise TaskStoreCorruption("DAG cache projection is invalid") from error
            aggregate_status = self._dag_aggregate_status(readiness)
            next_sequence = connection.execute(
                "SELECT COALESCE(MAX(sequence), 0) + 1 FROM run_events "
                "WHERE task_id = ?",
                (task_id,),
            ).fetchone()[0]
            event = {
                "schema_version": "1.0.0",
                "event_id": "event-" + cache_hit_id,
                "sequence": next_sequence,
                "task_id": task_id,
                "node_id": selected_state.node_id,
                "event_type": "node_succeeded",
                "task_status": aggregate_status,
                "occurred_at": recorded_at,
                "fingerprint": copy.deepcopy(
                    candidate.cache_key_document["execution_fingerprint"]
                ),
                "extensions": {
                    "org.agent_rpc.node_cache": {
                        "state": "hit",
                        "cache_hit_id": cache_hit_id,
                        "cache_entry_id": candidate.cache_entry_id,
                        "cache_key_hash": candidate.cache_key_hash,
                        "source_intent_id": candidate.source_intent.intent_id,
                        "source_receipt_document_hash": (
                            candidate.source_receipt_document_hash
                        ),
                        "worker_runtime_started": False,
                    }
                },
            }
            if schema_errors("run-event.schema.json", event):
                raise TaskStoreCorruption("DAG cache-hit RunEvent is invalid")
            event_json, event_hash = encode_document(event)
            _, fingerprint_hash = encode_document(event["fingerprint"])

            public_roots = [
                {
                    key: root[key]
                    for key in ("id", "version", "content_hash", "data_type")
                }
                for root in candidate.trusted_lineage_document["dataset_roots"]
            ]
            output_documents: list[dict[str, Any]] = []
            for output in source_outputs:
                source_manifest = output["artifact_manifest"]
                alias = copy.deepcopy(source_manifest)
                alias_identity = hashlib.sha256(
                    (cache_hit_id + "\x1f" + output["output_port"]).encode("utf-8")
                ).hexdigest()[:32]
                alias["artifact_id"] = "artifact-cache-" + alias_identity
                alias["task_id"] = task_id
                alias["node_id"] = selected_state.node_id
                alias["created_at"] = recorded_at
                alias["fingerprint"] = copy.deepcopy(event["fingerprint"])
                alias["lineage"] = {
                    "plan_hash": plan["plan_hash"],
                    "algorithm": copy.deepcopy(target_node["algorithm"]),
                    "inputs": public_roots,
                }
                if schema_errors("artifact-manifest.schema.json", alias):
                    raise TaskStoreCorruption("cached ArtifactManifest is invalid")
                alias_json, alias_hash = encode_document(alias)
                if alias_json != encode_document(alias)[0]:
                    raise TaskStoreCorruption("cached ArtifactManifest drifted")
                output_documents.append(
                    {
                        "output_port": output["output_port"],
                        "data_type": output["data_type"],
                        "artifact_manifest": alias,
                        "artifact_manifest_hash": alias_hash,
                        "physical_source": {
                            "task_id": candidate.source_snapshot.task_id,
                            "intent_id": candidate.source_intent.intent_id,
                            "artifact_id": source_manifest["artifact_id"],
                            "artifact_manifest_hash": output[
                                "artifact_manifest_hash"
                            ],
                        },
                    }
                )
            output_receipt = {
                "schema_version": "1.0.0",
                "task_id": task_id,
                "plan": {
                    "plan_id": plan["plan_id"],
                    "plan_hash": plan["plan_hash"],
                },
                "approval_id": approval["approval_id"],
                "node": {
                    "node_id": selected_state.node_id,
                    "input_binding_revision": selected_state.revision,
                    "succeeded_revision": selected_state.revision + 1,
                    "state": "Succeeded",
                },
                "input_binding_document_hash": input_binding.binding_document_hash,
                "scope": {
                    "project_id": project_id,
                    "principal_id": principal_id,
                },
                "cache": {
                    "cache_hit_id": cache_hit_id,
                    "cache_entry_id": candidate.cache_entry_id,
                    "cache_key_hash": candidate.cache_key_hash,
                    "source_receipt_document_hash": (
                        candidate.source_receipt_document_hash
                    ),
                    "trusted_lineage_document_hash": (
                        candidate.trusted_lineage_document_hash
                    ),
                    "artifact_verification_document_hash": verification_hash,
                },
                "outputs": output_documents,
                "succeeded_at": recorded_at,
            }
            output_json, output_hash = encode_document(output_receipt)
            if first_execution:
                consumed = connection.execute(
                    """
                    UPDATE approval_budgets
                    SET tasks_used = tasks_used + 1, updated_at = ?
                    WHERE task_id = ? AND approval_id = ?
                      AND tasks_used = 0 AND tasks_used < max_tasks
                    """,
                    (recorded_at, task_id, approval["approval_id"]),
                )
                if consumed.rowcount != 1:
                    raise TaskStoreConflict("approval task budget is exhausted")
            connection.execute(
                """
                INSERT INTO run_events(
                    task_id, sequence, event_id, event_type, task_status,
                    node_id, fingerprint_hash, document_json, document_hash,
                    occurred_at, recorded_at
                ) VALUES (?, ?, ?, 'node_succeeded', ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    next_sequence,
                    event["event_id"],
                    aggregate_status,
                    selected_state.node_id,
                    fingerprint_hash,
                    event_json,
                    event_hash,
                    recorded_at,
                    recorded_at,
                ),
            )
            connection.execute(
                """
                INSERT INTO supervised_run_event_commits(
                    task_id, sequence, project_id, principal_id,
                    fencing_token, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    next_sequence,
                    project_id,
                    principal_id,
                    supervisor_lease.fencing_token,
                    recorded_at,
                    recorded_at_us,
                ),
            )
            key_json, key_hash = encode_document(candidate.cache_key_document)
            if key_hash != candidate.cache_key_hash:
                raise TaskStoreCorruption("DAG cache key hash drifted")
            connection.execute(
                """
                INSERT INTO dag_node_cache_hit_facts(
                    cache_hit_id, target_task_id, target_plan_id,
                    target_plan_hash, target_approval_id, target_node_id,
                    target_pending_revision, target_succeeded_revision,
                    target_input_binding_document_hash,
                    target_input_fencing_token, target_input_owner_id,
                    target_input_term_acquired_at, source_cache_entry_id,
                    source_intent_id, source_task_id, source_plan_id,
                    source_plan_hash, source_approval_id, source_node_id,
                    source_succeeded_revision,
                    source_admission_document_hash,
                    source_receipt_document_hash,
                    source_trusted_lineage_document_hash,
                    cache_key_document_json, cache_key_hash,
                    artifact_verification_document_json,
                    artifact_verification_document_hash,
                    output_receipt_document_json,
                    output_receipt_document_hash, event_sequence, event_hash,
                    project_id, principal_id, completion_fencing_token,
                    completion_owner_id, completion_term_acquired_at,
                    recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?,
                          ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?,
                          ?, ?, ?)
                """,
                (
                    cache_hit_id,
                    task_id,
                    plan["plan_id"],
                    plan["plan_hash"],
                    approval["approval_id"],
                    selected_state.node_id,
                    selected_state.revision,
                    selected_state.revision + 1,
                    input_binding.binding_document_hash,
                    input_binding.fencing_token,
                    input_binding.owner_id,
                    input_binding.term_acquired_at,
                    candidate.cache_entry_id,
                    candidate.source_intent.intent_id,
                    candidate.source_snapshot.task_id,
                    entry["source_plan_id"],
                    entry["source_plan_hash"],
                    entry["source_approval_id"],
                    entry["source_node_id"],
                    candidate.source_node_revision,
                    candidate.source_admission_document_hash,
                    candidate.source_receipt_document_hash,
                    candidate.trusted_lineage_document_hash,
                    key_json,
                    candidate.cache_key_hash,
                    verification_json,
                    verification_hash,
                    output_json,
                    output_hash,
                    next_sequence,
                    event_hash,
                    project_id,
                    principal_id,
                    supervisor_lease.fencing_token,
                    supervisor_lease.owner_id,
                    supervisor_lease.acquired_at,
                    recorded_at,
                    recorded_at_us,
                ),
            )
            connection.execute(
                """
                INSERT INTO dag_node_state_events(
                    task_id, plan_id, plan_hash, node_id, revision,
                    previous_state, state, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, 'Pending', 'Succeeded', ?, ?)
                """,
                (
                    task_id,
                    plan["plan_id"],
                    plan["plan_hash"],
                    selected_state.node_id,
                    selected_state.revision + 1,
                    recorded_at,
                    recorded_at_us,
                ),
            )
            self._materialize_dag_blocked_nodes(
                connection,
                task_id=task_id,
                plan=plan,
                approval_id=approval["approval_id"],
                project_id=project_id,
                principal_id=principal_id,
                supervisor_lease=supervisor_lease,
                recorded_at=recorded_at,
                recorded_at_us=recorded_at_us,
            )
            reconciled_states = self._load_dag_node_state_map(
                connection, task_id=task_id, plan=plan
            )
            if reconciled_states is None:
                raise TaskStoreCorruption("DAG cache projection cannot be read")
            reconciled_readiness = evaluate_dag_readiness(
                plan,
                node_states={value.node_id: value.state for value in reconciled_states.nodes},
            )
            if self._dag_aggregate_status(reconciled_readiness) != aggregate_status:
                raise TaskStoreCorruption("DAG cache blockers changed aggregate status")
            connection.execute(
                "UPDATE tasks SET status = ?, updated_at = ? WHERE task_id = ?",
                (aggregate_status, recorded_at, task_id),
            )
            stored_snapshot = self._load_snapshot(connection, task_id)
            if stored_snapshot is None or stored_snapshot.status != aggregate_status:
                raise TaskStoreCorruption("DAG cache-hit Task cannot be read")
            connection.commit()
            return DagNodeCacheHitFact(
                snapshot=stored_snapshot,
                cache_hit_id=cache_hit_id,
                node_id=selected_state.node_id,
                node_revision=selected_state.revision + 1,
                cache_key_hash=candidate.cache_key_hash,
                source_cache_entry_id=candidate.cache_entry_id,
                source_receipt_document_hash=(
                    candidate.source_receipt_document_hash
                ),
                output_receipt_document_hash=output_hash,
                event_sequence=next_sequence,
                completion_fencing_token=supervisor_lease.fencing_token,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "DAG cache hit lost its supervisor lease"
                ) from error
            raise TaskStoreConflict("DAG cache hit conflicts with durable state") from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def admit_ready_dag_node_execution(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        expected_plan_hash: str,
        input_binding: DagNodeInputBindingFact,
        admit: Callable[
            [SubmitGateContext, str],
            tuple[Mapping[str, Any], Mapping[str, Any]],
        ],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagNodeExecutionAdmission:
        """Atomically admit one exact ready node into the unchanged P2 kernel."""

        if (
            not isinstance(task_id, str)
            or not task_id
            or not isinstance(project_id, str)
            or not project_id
            or not isinstance(principal_id, str)
            or not principal_id
            or not _is_sha256(expected_plan_hash)
            or not isinstance(input_binding, DagNodeInputBindingFact)
            or not callable(admit)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("DAG node execution admission is invalid")

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            admitted_at, admitted_at_us = _runtime_timestamp(supervisor_clock())
            snapshot = self._load_snapshot(connection, task_id)
            if (
                snapshot is None
                or snapshot.project_id != project_id
                or snapshot.principal_id != principal_id
            ):
                raise TaskStoreConflict("task does not exist in the requested scope")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=project_id,
                principal_id=principal_id,
                supplied=supervisor_lease,
                now_us=admitted_at_us,
                action="DAG node execution admission",
            )
            plan = snapshot.plan
            approval = snapshot.approval
            if (
                not isinstance(plan, Mapping)
                or len(plan.get("nodes", [])) <= 1
                or plan.get("plan_hash") != expected_plan_hash
                or not isinstance(approval, Mapping)
                or approval.get("decision") != "approved"
                or approval.get("plan_id") != plan.get("plan_id")
                or approval.get("plan_hash") != expected_plan_hash
                or input_binding.task_id != task_id
                or input_binding.plan_id != plan.get("plan_id")
                or input_binding.plan_hash != expected_plan_hash
                or input_binding.approval_id != approval.get("approval_id")
                or input_binding.project_id != project_id
                or input_binding.principal_id != principal_id
                or not _is_sha256(input_binding.binding_document_hash)
            ):
                raise TaskStoreConflict(
                    "DAG admission requires the exact current approved plan and binding"
                )

            binding_row = connection.execute(
                """
                SELECT * FROM dag_node_input_binding_facts
                WHERE task_id = ? AND plan_id = ? AND approval_id = ?
                  AND target_node_id = ? AND target_node_revision = ?
                  AND fencing_token = ? AND binding_document_hash = ?
                """,
                (
                    task_id,
                    input_binding.plan_id,
                    input_binding.approval_id,
                    input_binding.target_node_id,
                    input_binding.target_node_revision,
                    input_binding.fencing_token,
                    input_binding.binding_document_hash,
                ),
            ).fetchone()
            if binding_row is None:
                raise TaskStoreConflict(
                    "DAG admission requires the durable all-input binding"
                )
            binding_document = _decode_document(
                binding_row["binding_document_json"],
                label="DAG node execution input binding",
            )
            binding_json, binding_hash = encode_document(binding_document)
            if (
                binding_document != input_binding.binding_document
                or binding_json != binding_row["binding_document_json"]
                or binding_hash != input_binding.binding_document_hash
                or binding_hash != binding_row["binding_document_hash"]
                or binding_row["plan_hash"] != expected_plan_hash
                or binding_row["target_node_state"] != "Pending"
                or binding_row["project_id"] != project_id
                or binding_row["principal_id"] != principal_id
                or binding_row["owner_id"] != input_binding.owner_id
                or binding_row["term_acquired_at"]
                != input_binding.term_acquired_at
            ):
                raise TaskStoreCorruption(
                    "DAG admission input binding differs from its immutable index"
                )

            existing = self._load_dag_node_execution_admission(
                connection,
                task_id=task_id,
                node_id=input_binding.target_node_id,
            )
            if existing is not None:
                intent = self._load_dispatch_intent(
                    connection, intent_id=existing["intent_id"]
                )
                if (
                    intent is None
                    or intent.intent_id != existing["intent_id"]
                    or existing["plan_id"] != input_binding.plan_id
                    or existing["plan_hash"] != input_binding.plan_hash
                    or existing["approval_id"] != input_binding.approval_id
                    or existing["node_id"] != input_binding.target_node_id
                    or existing["pending_revision"]
                    != input_binding.target_node_revision
                    or existing["input_binding_document_hash"]
                    != input_binding.binding_document_hash
                    or existing["input_fencing_token"]
                    != input_binding.fencing_token
                    or existing["input_owner_id"] != input_binding.owner_id
                    or existing["input_term_acquired_at"]
                    != input_binding.term_acquired_at
                ):
                    raise TaskStoreConflict(
                        "DAG node admission replay differs from durable state"
                    )
                connection.commit()
                return DagNodeExecutionAdmission(
                    snapshot=snapshot,
                    intent=intent,
                    input_binding_document_hash=input_binding.binding_document_hash,
                    pending_revision=int(existing["pending_revision"]),
                    queued_revision=int(existing["queued_revision"]),
                    admission_fencing_token=int(existing["admission_fencing_token"]),
                    admitted_at=existing["admitted_at"],
                    replayed=True,
                )

            run_row = connection.execute(
                """
                SELECT * FROM dag_task_execution_runs WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            has_cache_hit_table = connection.execute(
                "SELECT 1 FROM sqlite_master WHERE type = 'table' "
                "AND name = 'dag_node_cache_hit_facts'"
            ).fetchone() is not None
            cache_started = (
                connection.execute(
                    "SELECT 1 FROM dag_node_cache_hit_facts "
                    "WHERE target_task_id = ? LIMIT 1",
                    (task_id,),
                ).fetchone()
                is not None
                if has_cache_hit_table
                else False
            )
            first_worker_admission = run_row is None
            first_execution = first_worker_admission and not cache_started
            if first_execution and snapshot.status != "AwaitingApproval":
                raise TaskStoreConflict("task is not awaiting DAG execution admission")
            if not first_execution:
                if (
                    snapshot.status != "Running"
                    or (
                        run_row is not None
                        and (
                            run_row["plan_id"] != input_binding.plan_id
                            or run_row["plan_hash"] != input_binding.plan_hash
                            or run_row["approval_id"] != input_binding.approval_id
                            or run_row["project_id"] != project_id
                            or run_row["principal_id"] != principal_id
                        )
                    )
                    or (run_row is None and not cache_started)
                ):
                    raise TaskStoreCorruption(
                        "successor admission differs from its DAG Task run"
                    )
            if connection.execute(
                "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "DAG admission is blocked by durable cancellation"
                )
            if (
                input_binding.fencing_token != supervisor_lease.fencing_token
                or input_binding.owner_id != supervisor_lease.owner_id
                or input_binding.term_acquired_at != supervisor_lease.acquired_at
            ):
                raise RuntimeSupervisorLeaseLost(
                    "DAG admission requires the binding's exact active term"
                )
            state_map = self._load_dag_node_state_map(
                connection, task_id=task_id, plan=plan
            )
            selected_state = next(
                (
                    value
                    for value in (state_map.nodes if state_map is not None else ())
                    if value.node_id == input_binding.target_node_id
                ),
                None,
            )
            if (
                selected_state is None
                or selected_state.state != "Pending"
                or selected_state.revision != input_binding.target_node_revision
            ):
                raise TaskStoreConflict(
                    "DAG admission requires the latest Pending node revision"
                )

            budget = self._load_approval_budget(
                connection,
                task_id=task_id,
                approval_id=input_binding.approval_id,
            )
            if budget is None:
                raise TaskStoreCorruption("current approval has no durable budget")
            if first_execution:
                if budget.tasks_used != 0:
                    raise TaskStoreConflict(
                        "DAG Task approval budget was already consumed"
                    )
                gate_budget = budget
            else:
                if budget.tasks_used != 1:
                    raise TaskStoreCorruption(
                        "successor admission requires one consumed Task budget"
                    )
                gate_budget = replace(budget, tasks_used=0)
            try:
                dataset_keys = [
                    (value["id"], value["version"])
                    for value in snapshot.draft.get("datasets", [])
                ]
                algorithm_keys = [
                    (value["algorithm"]["id"], value["algorithm"]["version"])
                    for value in plan["nodes"]
                ]
            except (KeyError, TypeError) as error:
                raise TaskStoreCorruption(
                    "current DAG documents cannot identify registry records"
                ) from error
            registry = self._load_registry_snapshots(
                connection,
                project_id=project_id,
                dataset_keys=dataset_keys,
                algorithm_keys=algorithm_keys,
            )
            intent_document, queued_event = admit(
                SubmitGateContext(
                    snapshot=snapshot,
                    registry=registry,
                    budget=gate_budget,
                    task_budget_already_consumed=not first_execution,
                ),
                admitted_at,
            )
            intent_document = dict(intent_document)
            queued_event = dict(queued_event)
            node = next(
                value
                for value in plan["nodes"]
                if value["node_id"] == selected_state.node_id
            )
            adapter = intent_document.get("adapter")
            request = intent_document.get("request")
            fingerprint = intent_document.get("queue_fingerprint")
            if (
                intent_document.get("schema_version") != "1.0.0"
                or intent_document.get("task_id") != task_id
                or intent_document.get("plan_id") != plan["plan_id"]
                or intent_document.get("plan_hash") != plan["plan_hash"]
                or intent_document.get("approval_id") != approval["approval_id"]
                or intent_document.get("node_id") != node["node_id"]
                or intent_document.get("node_idempotency_key")
                != node["idempotency_key"]
                or intent_document.get("created_at") != admitted_at
                or not isinstance(intent_document.get("intent_id"), str)
                or not isinstance(adapter, Mapping)
                or set(adapter) != {"id", "version"}
                or not isinstance(request, Mapping)
                or not isinstance(fingerprint, Mapping)
                or queued_event.get("schema_version") != "1.0.0"
                or queued_event.get("sequence") != 1
                or queued_event.get("task_id") != task_id
                or queued_event.get("event_type") != "task_queued"
                or queued_event.get("task_status") != "Queued"
                or queued_event.get("node_id") is not None
                or queued_event.get("occurred_at") != admitted_at
                or queued_event.get("fingerprint") != fingerprint
            ):
                raise TaskStoreConflict(
                    "DAG dispatch intent or queued event differs from admission"
                )

            intent_json, intent_hash = encode_document(intent_document)
            _, fingerprint_hash = encode_document(fingerprint)
            event_json, event_hash = encode_document(queued_event)
            _, event_fingerprint_hash = encode_document(
                queued_event.get("fingerprint", {})
            )
            if event_fingerprint_hash != fingerprint_hash:
                raise TaskStoreConflict("DAG queued fingerprint is inconsistent")

            if first_execution:
                consumed = connection.execute(
                    """
                    UPDATE approval_budgets
                    SET tasks_used = tasks_used + 1, updated_at = ?
                    WHERE task_id = ? AND approval_id = ?
                      AND tasks_used = 0 AND tasks_used < max_tasks
                    """,
                    (admitted_at, task_id, approval["approval_id"]),
                )
                if consumed.rowcount != 1:
                    raise TaskStoreConflict("approval task budget is exhausted")
            connection.execute(
                """
                INSERT INTO dispatch_intents(
                    intent_id, task_id, plan_id, plan_hash, approval_id,
                    node_id, node_idempotency_key, adapter_id,
                    adapter_version, request_json, request_hash,
                    fingerprint_hash, created_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    intent_document["intent_id"],
                    task_id,
                    plan["plan_id"],
                    plan["plan_hash"],
                    approval["approval_id"],
                    node["node_id"],
                    node["idempotency_key"],
                    adapter["id"],
                    adapter["version"],
                    intent_json,
                    intent_hash,
                    fingerprint_hash,
                    admitted_at,
                ),
            )
            admission_document = {
                "schema_version": "1.0.0",
                "intent_id": intent_document["intent_id"],
                "task_id": task_id,
                "plan": {
                    "plan_id": plan["plan_id"],
                    "plan_hash": plan["plan_hash"],
                },
                "approval_id": approval["approval_id"],
                "node": {
                    "node_id": node["node_id"],
                    "idempotency_key": node["idempotency_key"],
                    "pending_revision": selected_state.revision,
                    "queued_revision": selected_state.revision + 1,
                },
                "input_binding": {
                    "document_hash": input_binding.binding_document_hash,
                    "supervisor_term": {
                        "fencing_token": input_binding.fencing_token,
                        "owner_id": input_binding.owner_id,
                        "acquired_at": input_binding.term_acquired_at,
                    },
                },
                "scope": {
                    "project_id": project_id,
                    "principal_id": principal_id,
                },
                "admission_supervisor_term": {
                    "fencing_token": supervisor_lease.fencing_token,
                    "owner_id": supervisor_lease.owner_id,
                    "acquired_at": supervisor_lease.acquired_at,
                },
                "max_node_attempts": 1,
                "admitted_at": admitted_at,
            }
            admission_json, admission_hash = encode_document(admission_document)
            connection.execute(
                """
                INSERT INTO dag_node_execution_admissions(
                    intent_id, task_id, plan_id, plan_hash, approval_id,
                    node_id, pending_revision, queued_revision,
                    node_idempotency_key, input_binding_document_hash,
                    input_fencing_token, input_owner_id,
                    input_term_acquired_at, project_id, principal_id,
                    admission_fencing_token, admission_owner_id,
                    admission_term_acquired_at, max_node_attempts,
                    document_json, document_hash, admitted_at, admitted_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?,
                          ?, ?, 1, ?, ?, ?, ?)
                """,
                (
                    intent_document["intent_id"],
                    task_id,
                    plan["plan_id"],
                    plan["plan_hash"],
                    approval["approval_id"],
                    node["node_id"],
                    selected_state.revision,
                    selected_state.revision + 1,
                    node["idempotency_key"],
                    input_binding.binding_document_hash,
                    input_binding.fencing_token,
                    input_binding.owner_id,
                    input_binding.term_acquired_at,
                    project_id,
                    principal_id,
                    supervisor_lease.fencing_token,
                    supervisor_lease.owner_id,
                    supervisor_lease.acquired_at,
                    admission_json,
                    admission_hash,
                    admitted_at,
                    admitted_at_us,
                ),
            )
            if first_worker_admission:
                connection.execute(
                    """
                    INSERT INTO dag_task_execution_runs(
                        task_id, plan_id, plan_hash, approval_id,
                        first_intent_id, project_id, principal_id,
                        admitted_at, admitted_at_us
                    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        task_id,
                        plan["plan_id"],
                        plan["plan_hash"],
                        approval["approval_id"],
                        intent_document["intent_id"],
                        project_id,
                        principal_id,
                        admitted_at,
                        admitted_at_us,
                    ),
                )
            if first_execution:
                connection.execute(
                    """
                    INSERT INTO run_events(
                        task_id, sequence, event_id, event_type, task_status,
                        node_id, fingerprint_hash, document_json, document_hash,
                        occurred_at, recorded_at
                    ) VALUES (?, 1, ?, 'task_queued', 'Queued', NULL, ?, ?, ?, ?, ?)
                    """,
                    (
                        task_id,
                        queued_event["event_id"],
                        event_fingerprint_hash,
                        event_json,
                        event_hash,
                        queued_event["occurred_at"],
                        admitted_at,
                    ),
                )
                connection.execute(
                    """
                    INSERT INTO supervised_run_event_commits(
                        task_id, sequence, project_id, principal_id,
                        fencing_token, recorded_at, recorded_at_us
                    ) VALUES (?, 1, ?, ?, ?, ?, ?)
                    """,
                    (
                        task_id,
                        project_id,
                        principal_id,
                        supervisor_lease.fencing_token,
                        admitted_at,
                        admitted_at_us,
                    ),
                )
                connection.execute(
                    """
                    INSERT INTO dag_node_execution_transition_facts(
                        intent_id, node_revision, previous_state, state,
                        event_sequence, event_hash, reason,
                        project_id, principal_id, fencing_token,
                        owner_id, term_acquired_at, recorded_at, recorded_at_us
                    ) VALUES (?, ?, 'Pending', 'Queued', 1, ?,
                              'execution_admitted', ?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        intent_document["intent_id"],
                        selected_state.revision + 1,
                        event_hash,
                        project_id,
                        principal_id,
                        supervisor_lease.fencing_token,
                        supervisor_lease.owner_id,
                        supervisor_lease.acquired_at,
                        admitted_at,
                        admitted_at_us,
                    ),
                )
            connection.execute(
                """
                INSERT INTO dag_node_state_events(
                    task_id, plan_id, plan_hash, node_id, revision,
                    previous_state, state, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, 'Pending', 'Queued', ?, ?)
                """,
                (
                    task_id,
                    plan["plan_id"],
                    plan["plan_hash"],
                    node["node_id"],
                    selected_state.revision + 1,
                    admitted_at,
                    admitted_at_us,
                ),
            )
            if first_execution:
                connection.execute(
                    "UPDATE tasks SET status = 'Queued', updated_at = ? "
                    "WHERE task_id = ?",
                    (admitted_at, task_id),
                )
            queued_snapshot = self._load_snapshot(connection, task_id)
            stored_intent = self._load_dispatch_intent(
                connection, intent_id=intent_document["intent_id"]
            )
            stored_admission = self._load_dag_node_execution_admission(
                connection, intent_id=intent_document["intent_id"]
            )
            if (
                queued_snapshot is None
                or queued_snapshot.status
                != ("Queued" if first_execution else "Running")
                or stored_intent is None
                or stored_intent.intent_id != intent_document["intent_id"]
                or stored_admission is None
            ):
                raise TaskStoreCorruption("admitted DAG node cannot be read")
            connection.commit()
            return DagNodeExecutionAdmission(
                snapshot=queued_snapshot,
                intent=stored_intent,
                input_binding_document_hash=input_binding.binding_document_hash,
                pending_revision=selected_state.revision,
                queued_revision=selected_state.revision + 1,
                admission_fencing_token=supervisor_lease.fencing_token,
                admitted_at=admitted_at,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "DAG node execution admission lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "DAG node execution admission conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def authorize_supervised_dispatch(
        self,
        *,
        intent_id: str,
        reason: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedDispatchAuthorization:
        """Fence one first-dispatch or exact pre-Popen recovery authorization."""

        allowed_reasons = {
            "pending_first_dispatch",
            "dispatching_no_record_takeover",
            "staged_attempt_resume",
        }
        if reason not in allowed_reasons:
            raise TaskStoreConflict("supervised dispatch reason is invalid")
        if not isinstance(supervisor_lease, RuntimeSupervisorLease):
            raise TaskStoreConflict("runtime supervisor lease is invalid")
        if not callable(supervisor_clock):
            raise TaskStoreConflict("runtime supervisor clock is invalid")

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            authorized_at, authorized_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            if intent is None:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("supervised dispatch task cannot be read")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=authorized_at_us,
                action="supervised dispatch",
            )
            dag_admission = self._require_current_dag_node_execution(
                connection,
                intent=intent,
                allowed_node_states=frozenset({"Queued"}),
            )
            if task["status"] != "Queued" and not (
                dag_admission is not None and task["status"] == "Running"
            ):
                raise TaskStoreConflict(
                    "only a queued task can receive supervised dispatch"
                )
            if connection.execute(
                "SELECT 1 FROM task_purge_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("purged task cannot receive supervised dispatch")
            if connection.execute(
                "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "task with a cancel request cannot receive supervised dispatch"
                )
            if intent.state not in {"pending", "dispatching"}:
                raise TaskStoreConflict(
                    "dispatch intent is not eligible for supervised dispatch"
                )
            if reason == "dispatching_no_record_takeover":
                if connection.execute(
                    "SELECT 1 FROM worker_launch_attempts WHERE intent_id = ?",
                    (intent_id,),
                ).fetchone() is not None:
                    raise TaskStoreConflict(
                        "no-record takeover conflicts with Worker evidence"
                    )
            elif reason == "staged_attempt_resume":
                staged = connection.execute(
                    """
                    SELECT attempt.attempt_number, observation.*
                    FROM worker_launch_attempts AS attempt
                    JOIN worker_attempt_observations AS observation
                      ON observation.attempt_id = attempt.attempt_id
                    WHERE attempt.intent_id = ?
                    ORDER BY observation.observation_sequence DESC LIMIT 1
                    """,
                    (intent_id,),
                ).fetchone()
                if (
                    staged is None
                    or int(staged["attempt_number"]) != 1
                    or staged["ticket_state"] != "staged"
                    or staged["capacity_slot"] is not None
                    or staged["capacity_generation"] is not None
                    or staged["ticket_worker_pid"] is not None
                    or staged["ready_record_hash"] is not None
                    or staged["heartbeat_record_hash"] is not None
                ):
                    raise TaskStoreConflict(
                        "staged resume requires exact pre-Popen Worker evidence"
                    )

            existing = connection.execute(
                """
                SELECT * FROM supervised_dispatch_attempts
                WHERE intent_id = ? AND fencing_token = ?
                """,
                (intent_id, supervisor_lease.fencing_token),
            ).fetchone()
            if existing is not None:
                if (
                    existing["project_id"] != task["project_id"]
                    or existing["principal_id"] != task["principal_id"]
                    or existing["reason"] not in allowed_reasons
                ):
                    raise TaskStoreCorruption(
                        "supervised dispatch authorization is inconsistent"
                    )
                stored_at, stored_at_us = _runtime_timestamp(
                    existing["authorized_at"]
                )
                if stored_at_us != int(existing["authorized_at_us"]):
                    raise TaskStoreCorruption(
                        "supervised dispatch authorization time is inconsistent"
                    )
                connection.commit()
                return SupervisedDispatchAuthorization(
                    intent=intent,
                    reason=existing["reason"],
                    fencing_token=supervisor_lease.fencing_token,
                    authorized_at=stored_at,
                    replayed=True,
                )

            expected_state = {
                "pending_first_dispatch": "pending",
                "dispatching_no_record_takeover": "dispatching",
                "staged_attempt_resume": "dispatching",
            }[reason]
            if intent.state != expected_state:
                raise TaskStoreConflict(
                    "dispatch intent is not eligible for supervised dispatch"
                )
            if reason == "pending_first_dispatch":
                connection.execute(
                    "INSERT INTO dispatch_attempts(intent_id, claimed_at) VALUES (?, ?)",
                    (intent_id, authorized_at),
                )
                intent = self._load_dispatch_intent(
                    connection, intent_id=intent_id
                )
                if intent is None or intent.state != "dispatching":
                    raise TaskStoreCorruption(
                        "supervised dispatch claim cannot be read"
                    )
            connection.execute(
                """
                INSERT INTO supervised_dispatch_attempts(
                    intent_id, project_id, principal_id, fencing_token,
                    reason, authorized_at, authorized_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    intent_id,
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    reason,
                    authorized_at,
                    authorized_at_us,
                ),
            )
            connection.commit()
            return SupervisedDispatchAuthorization(
                intent=intent,
                reason=reason,
                fencing_token=supervisor_lease.fencing_token,
                authorized_at=authorized_at,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict(
                "supervised dispatch authorization conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def authorize_supervised_retry(
        self,
        *,
        intent_id: str,
        previous_attempt_id: str,
        previous_observation_sequence: int,
        failure_kind: str,
        private_proof_hash: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedRetryAuthorization:
        """Consume attempt 2 once, then audit delivery under the active term."""

        if (
            not isinstance(intent_id, str)
            or not intent_id
            or not isinstance(previous_attempt_id, str)
            or WORKER_ATTEMPT_ID.fullmatch(previous_attempt_id) is None
            or type(previous_observation_sequence) is not int
            or previous_observation_sequence < 1
            or failure_kind != "pre_running_launch_failure"
            or not _is_sha256(private_proof_hash)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("supervised retry authorization is invalid")

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            authorized_at, authorized_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(
                connection, intent_id=intent_id
            )
            if intent is None:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("retry target task cannot be read")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=authorized_at_us,
                action="supervised retry",
            )
            expected_algorithm = {
                "id": "deepwave.acoustic_fwi",
                "version": intent.adapter_version,
            }
            if (
                task["status"] != "Queued"
                or intent.state != "dispatching"
                or intent.adapter_id != "fwi.deepwave_adapter"
                or intent.adapter_version not in {"1.5.0", "1.6.0"}
                or intent.request.get("algorithm") != expected_algorithm
            ):
                raise TaskStoreConflict(
                    "retry requires a current stopped managed dispatch"
                )
            if connection.execute(
                "SELECT 1 FROM dispatch_outcomes WHERE intent_id = ?",
                (intent_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("completed dispatch cannot be retried")
            if connection.execute(
                "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("cancelled task cannot be retried")
            if connection.execute(
                "SELECT 1 FROM task_purge_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("purged task cannot be retried")

            budget = connection.execute(
                """
                SELECT * FROM approval_retry_budgets
                WHERE task_id = ? AND approval_id = ?
                """,
                (task_id, intent.approval_id),
            ).fetchone()
            if (
                budget is None
                or int(budget["max_attempts"]) != 2
                or int(budget["max_concurrent_attempts"]) != 1
                or budget["retryable_failure_classes_json"]
                != '["pre_running_launch_failure","worker_exit"]'
            ):
                raise TaskStoreConflict("approval does not authorize finite retry")

            attempt = connection.execute(
                """
                SELECT * FROM worker_launch_attempts
                WHERE intent_id = ?
                ORDER BY attempt_number DESC LIMIT 1
                """,
                (intent_id,),
            ).fetchone()
            observation = connection.execute(
                """
                SELECT * FROM worker_attempt_observations
                WHERE attempt_id = ? AND observation_sequence = ?
                """,
                (previous_attempt_id, previous_observation_sequence),
            ).fetchone()
            if (
                attempt is None
                or attempt["attempt_id"] != previous_attempt_id
                or int(attempt["attempt_number"]) != 1
                or observation is None
                or observation["ticket_state"] != "failed"
                or observation["ticket_worker_pid"] is not None
                or observation["ready_record_hash"] is not None
                or observation["heartbeat_record_hash"] is not None
            ):
                raise TaskStoreConflict(
                    "retry requires exact stopped attempt 1 evidence"
                )
            latest_sequence = connection.execute(
                """
                SELECT MAX(observation_sequence)
                FROM worker_attempt_observations WHERE attempt_id = ?
                """,
                (previous_attempt_id,),
            ).fetchone()[0]
            if int(latest_sequence) != previous_observation_sequence:
                raise TaskStoreConflict(
                    "retry evidence is not the latest attempt observation"
                )
            evidence_hash = observation["document_hash"]
            if not _is_sha256(evidence_hash):
                raise TaskStoreCorruption("retry evidence hash is invalid")

            existing_reservation = connection.execute(
                """
                SELECT * FROM worker_retry_reservations
                WHERE intent_id = ? AND attempt_number = 2
                """,
                (intent_id,),
            ).fetchone()
            if existing_reservation is None:
                connection.execute(
                    """
                    INSERT INTO worker_retry_reservations(
                        intent_id, attempt_number, task_id, project_id,
                        principal_id, approval_id, previous_attempt_id,
                        previous_observation_sequence, evidence_hash,
                        private_proof_hash, failure_kind, first_fencing_token,
                        reserved_at, reserved_at_us
                    ) VALUES (?, 2, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        intent_id,
                        task_id,
                        task["project_id"],
                        task["principal_id"],
                        intent.approval_id,
                        previous_attempt_id,
                        previous_observation_sequence,
                        evidence_hash,
                        private_proof_hash,
                        failure_kind,
                        supervisor_lease.fencing_token,
                        authorized_at,
                        authorized_at_us,
                    ),
                )
                reserved_at = authorized_at
                reservation_replayed = False
            else:
                expected_reservation = {
                    "task_id": task_id,
                    "project_id": task["project_id"],
                    "principal_id": task["principal_id"],
                    "approval_id": intent.approval_id,
                    "previous_attempt_id": previous_attempt_id,
                    "previous_observation_sequence": previous_observation_sequence,
                    "evidence_hash": evidence_hash,
                    "private_proof_hash": private_proof_hash,
                    "failure_kind": failure_kind,
                }
                if any(
                    existing_reservation[key] != expected
                    for key, expected in expected_reservation.items()
                ):
                    raise TaskStoreConflict(
                        "retry reservation differs from exact stopped evidence"
                    )
                reserved_at, reserved_at_us = _runtime_timestamp(
                    existing_reservation["reserved_at"]
                )
                if reserved_at_us != int(existing_reservation["reserved_at_us"]):
                    raise TaskStoreCorruption("retry reservation time is inconsistent")
                reservation_replayed = True

            existing_authorization = connection.execute(
                """
                SELECT * FROM supervised_retry_attempts
                WHERE intent_id = ? AND attempt_number = 2
                  AND fencing_token = ?
                """,
                (intent_id, supervisor_lease.fencing_token),
            ).fetchone()
            if existing_authorization is None:
                connection.execute(
                    """
                    INSERT INTO supervised_retry_attempts(
                        intent_id, attempt_number, project_id, principal_id,
                        fencing_token, authorized_at, authorized_at_us
                    ) VALUES (?, 2, ?, ?, ?, ?, ?)
                    """,
                    (
                        intent_id,
                        task["project_id"],
                        task["principal_id"],
                        supervisor_lease.fencing_token,
                        authorized_at,
                        authorized_at_us,
                    ),
                )
                authorization_replayed = False
            else:
                if (
                    existing_authorization["project_id"] != task["project_id"]
                    or existing_authorization["principal_id"]
                    != task["principal_id"]
                ):
                    raise TaskStoreCorruption(
                        "retry delivery authorization is inconsistent"
                    )
                stored_authorized_at, stored_authorized_at_us = _runtime_timestamp(
                    existing_authorization["authorized_at"]
                )
                if stored_authorized_at_us != int(
                    existing_authorization["authorized_at_us"]
                ):
                    raise TaskStoreCorruption(
                        "retry delivery authorization time is inconsistent"
                    )
                authorized_at = stored_authorized_at
                authorization_replayed = True

            connection.commit()
            return SupervisedRetryAuthorization(
                intent=intent,
                attempt_number=2,
                previous_attempt_id=previous_attempt_id,
                previous_observation_sequence=previous_observation_sequence,
                evidence_hash=evidence_hash,
                private_proof_hash=private_proof_hash,
                failure_kind=failure_kind,
                fencing_token=supervisor_lease.fencing_token,
                reserved_at=reserved_at,
                authorized_at=authorized_at,
                reservation_replayed=reservation_replayed,
                authorization_replayed=authorization_replayed,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "supervised retry lost its active term"
                ) from error
            raise TaskStoreConflict(
                "supervised retry authorization conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def resume_supervised_retry(
        self,
        *,
        intent_id: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedRetryAuthorization:
        """Re-deliver an existing attempt-2 token for an exact staged retry."""

        if (
            not isinstance(intent_id, str)
            or not intent_id
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("supervised retry resume is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            authorized_at, authorized_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(
                connection, intent_id=intent_id
            )
            if intent is None:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("retry resume task cannot be read")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=authorized_at_us,
                action="supervised retry resume",
            )
            if (
                task["status"] != "Queued"
                or intent.state != "dispatching"
                or intent.adapter_id != "fwi.deepwave_adapter"
                or intent.adapter_version not in {"1.5.0", "1.6.0"}
                or intent.request.get("algorithm")
                != {
                    "id": "deepwave.acoustic_fwi",
                    "version": intent.adapter_version,
                }
                or connection.execute(
                    "SELECT 1 FROM dispatch_outcomes WHERE intent_id = ?",
                    (intent_id,),
                ).fetchone()
                is not None
                or connection.execute(
                    "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                    (task_id,),
                ).fetchone()
                is not None
                or connection.execute(
                    "SELECT 1 FROM task_purge_requests WHERE task_id = ?",
                    (task_id,),
                ).fetchone()
                is not None
            ):
                raise TaskStoreConflict(
                    "retry resume requires a current stopped managed dispatch"
                )

            reservation = connection.execute(
                """
                SELECT * FROM worker_retry_reservations
                WHERE intent_id = ? AND attempt_number = 2
                """,
                (intent_id,),
            ).fetchone()
            if (
                reservation is None
                or reservation["task_id"] != task_id
                or reservation["project_id"] != task["project_id"]
                or reservation["principal_id"] != task["principal_id"]
                or reservation["approval_id"] != intent.approval_id
                or reservation["failure_kind"]
                != "pre_running_launch_failure"
                or not _is_sha256(reservation["evidence_hash"])
                or not _is_sha256(reservation["private_proof_hash"])
            ):
                raise TaskStoreConflict("retry resume lacks its exact reservation")
            reserved_at, reserved_at_us = _runtime_timestamp(
                reservation["reserved_at"]
            )
            if (
                reserved_at_us != int(reservation["reserved_at_us"])
                or authorized_at_us < reserved_at_us
            ):
                raise TaskStoreConflict("retry resume clock regressed")

            staged = connection.execute(
                """
                SELECT attempt.attempt_id, attempt.attempt_number,
                       observation.ticket_state, observation.capacity_slot,
                       observation.capacity_generation,
                       observation.ticket_worker_pid,
                       observation.ready_record_hash,
                       observation.heartbeat_record_hash
                FROM worker_launch_attempts AS attempt
                JOIN worker_attempt_observations AS observation
                  ON observation.attempt_id = attempt.attempt_id
                WHERE attempt.intent_id = ?
                  AND attempt.attempt_number = 2
                ORDER BY observation.observation_sequence DESC LIMIT 1
                """,
                (intent_id,),
            ).fetchone()
            if (
                staged is None
                or staged["ticket_state"] != "staged"
                or staged["capacity_slot"] is not None
                or staged["capacity_generation"] is not None
                or staged["ticket_worker_pid"] is not None
                or staged["ready_record_hash"] is not None
                or staged["heartbeat_record_hash"] is not None
            ):
                raise TaskStoreConflict("retry resume requires exact staged attempt 2")

            existing = connection.execute(
                """
                SELECT * FROM supervised_retry_attempts
                WHERE intent_id = ? AND attempt_number = 2
                  AND fencing_token = ?
                """,
                (intent_id, supervisor_lease.fencing_token),
            ).fetchone()
            if existing is None:
                connection.execute(
                    """
                    INSERT INTO supervised_retry_attempts(
                        intent_id, attempt_number, project_id, principal_id,
                        fencing_token, authorized_at, authorized_at_us
                    ) VALUES (?, 2, ?, ?, ?, ?, ?)
                    """,
                    (
                        intent_id,
                        task["project_id"],
                        task["principal_id"],
                        supervisor_lease.fencing_token,
                        authorized_at,
                        authorized_at_us,
                    ),
                )
                authorization_replayed = False
            else:
                if (
                    existing["project_id"] != task["project_id"]
                    or existing["principal_id"] != task["principal_id"]
                ):
                    raise TaskStoreCorruption(
                        "retry resume authorization is inconsistent"
                    )
                stored_at, stored_at_us = _runtime_timestamp(
                    existing["authorized_at"]
                )
                if stored_at_us != int(existing["authorized_at_us"]):
                    raise TaskStoreCorruption(
                        "retry resume authorization time is inconsistent"
                    )
                authorized_at = stored_at
                authorization_replayed = True

            connection.commit()
            return SupervisedRetryAuthorization(
                intent=intent,
                attempt_number=2,
                previous_attempt_id=reservation["previous_attempt_id"],
                previous_observation_sequence=int(
                    reservation["previous_observation_sequence"]
                ),
                evidence_hash=reservation["evidence_hash"],
                private_proof_hash=reservation["private_proof_hash"],
                failure_kind=reservation["failure_kind"],
                fencing_token=supervisor_lease.fencing_token,
                reserved_at=reserved_at,
                authorized_at=authorized_at,
                reservation_replayed=True,
                authorization_replayed=authorization_replayed,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "supervised retry resume lost its active term"
                ) from error
            raise TaskStoreConflict(
                "supervised retry resume conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def finalize_supervised_retry_exhaustion(
        self,
        *,
        intent_id: str,
        attempt_id: str,
        observation_sequence: int,
        evidence: Mapping[str, Any],
        private_schema_version: str,
        private_proof_hash: str,
        failure_kind: str,
        terminal_event: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedRetryExhaustion:
        """Atomically fail one Task after exact attempt 2 exhausts its budget."""

        if (
            not isinstance(intent_id, str)
            or not intent_id
            or not isinstance(attempt_id, str)
            or WORKER_ATTEMPT_ID.fullmatch(attempt_id) is None
            or type(observation_sequence) is not int
            or observation_sequence < 1
            or private_schema_version != "1.2.0"
            or not _is_sha256(private_proof_hash)
            or failure_kind != "pre_running_launch_failure"
            or not isinstance(terminal_event, Mapping)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("supervised retry exhaustion is invalid")
        normalized = _validated_worker_evidence(evidence)
        if (
            normalized["attempt_id"] != attempt_id
            or normalized["attempt_number"] != 2
            or normalized["ticket"]["state"] != "failed"
            or normalized["ticket"]["worker_pid"] is not None
            or normalized["ready"] is not None
            or normalized["heartbeat"] is not None
        ):
            raise TaskStoreConflict(
                "retry exhaustion requires exact stopped attempt 2 evidence"
            )
        evidence_json, evidence_hash = encode_document(normalized)
        failure_payload = {
            "schema_version": "1.0.0",
            "failure_kind": "pre_running_launch_failure",
            "submission_id": normalized["submission_id"],
            "attempt_id": normalized["attempt_id"],
            "attempt_number": normalized["attempt_number"],
            "job_id": normalized["job_id"],
            "request_hash": normalized["request_hash"],
            "binding_hash": normalized["binding_hash"],
            "ticket_record_hash": normalized["ticket"]["record_hash"],
        }
        if encode_document(failure_payload)[1] != private_proof_hash:
            raise TaskStoreConflict("retry exhaustion private proof is invalid")
        event = copy.deepcopy(dict(terminal_event))
        event_json, event_hash = encode_document(event)

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            exhausted_at, exhausted_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(
                connection, intent_id=intent_id
            )
            if intent is None:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("retry exhaustion task cannot be read")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=exhausted_at_us,
                action="supervised retry exhaustion",
            )

            extension = {
                "intent_id": intent_id,
                "attempt_id": attempt_id,
                "attempt_number": 2,
                "observation_sequence": observation_sequence,
                "evidence_hash": evidence_hash,
                "private_schema_version": "1.2.0",
                "private_proof_hash": private_proof_hash,
                "failure_kind": "pre_running_launch_failure",
                "max_attempts": 2,
            }
            expected_sequence = connection.execute(
                """
                SELECT COALESCE(MAX(sequence), 0) + 1
                FROM run_events WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()[0]
            _, identity_hash = encode_document(
                {
                    "intent_id": intent_id,
                    "attempt_id": attempt_id,
                    "observation_sequence": observation_sequence,
                    "evidence_hash": evidence_hash,
                    "private_proof_hash": private_proof_hash,
                    "event_type": "node_failed",
                    "sequence": event.get("sequence"),
                }
            )
            expected_event_id = (
                "event-" + identity_hash.removeprefix("sha256:")[:32]
            )
            expected_event_fields = {
                "schema_version",
                "event_id",
                "sequence",
                "task_id",
                "node_id",
                "event_type",
                "task_status",
                "error",
                "occurred_at",
                "fingerprint",
                "extensions",
            }
            if (
                set(event) != expected_event_fields
                or event.get("schema_version") != "1.0.0"
                or event.get("event_id") != expected_event_id
                or event.get("task_id") != task_id
                or event.get("node_id") != intent.node_id
                or event.get("event_type") != "node_failed"
                or event.get("task_status") != "Failed"
                or event.get("error")
                != {
                    "code": "retry_exhausted",
                    "message": "FWI Worker exhausted its approved launch attempts",
                    "retryable": False,
                }
                or event.get("occurred_at")
                != normalized["ticket"]["updated_at"]
                or event.get("fingerprint") != intent.queue_fingerprint
                or event.get("extensions")
                != {"org.agent_rpc.retry_exhaustion": extension}
            ):
                raise TaskStoreConflict(
                    "retry exhaustion terminal event is invalid"
                )
            try:
                _, occurred_at_us = _runtime_timestamp(event["occurred_at"])
            except TaskStoreConflict as error:
                raise TaskStoreConflict(
                    "retry exhaustion terminal event is invalid"
                ) from error
            if occurred_at_us > exhausted_at_us:
                raise TaskStoreConflict(
                    "retry exhaustion terminal event violates causal order"
                )

            existing = connection.execute(
                """
                SELECT * FROM worker_retry_exhaustions
                WHERE intent_id = ? AND attempt_number = 2
                """,
                (intent_id,),
            ).fetchone()
            if existing is not None:
                stored_event = connection.execute(
                    """
                    SELECT document_hash FROM run_events
                    WHERE task_id = ? AND sequence = ?
                    """,
                    (task_id, existing["terminal_event_sequence"]),
                ).fetchone()
                expected_existing = {
                    "task_id": task_id,
                    "project_id": task["project_id"],
                    "principal_id": task["principal_id"],
                    "approval_id": intent.approval_id,
                    "attempt_id": attempt_id,
                    "observation_sequence": observation_sequence,
                    "evidence_hash": evidence_hash,
                    "private_schema_version": private_schema_version,
                    "private_proof_hash": private_proof_hash,
                    "failure_kind": failure_kind,
                    "max_attempts": 2,
                    "terminal_event_sequence": event.get("sequence"),
                    "terminal_event_hash": event_hash,
                }
                if (
                    any(
                        existing[key] != expected
                        for key, expected in expected_existing.items()
                    )
                    or stored_event is None
                    or stored_event["document_hash"] != event_hash
                    or task["status"] != "Failed"
                ):
                    raise TaskStoreConflict(
                        "retry exhaustion differs from its terminal outcome"
                    )
                snapshot = self._load_snapshot(connection, task_id)
                if snapshot is None or snapshot.status != "Failed":
                    raise TaskStoreCorruption(
                        "retry exhaustion terminal task cannot be read"
                    )
                stored_exhausted_at, stored_exhausted_at_us = _runtime_timestamp(
                    existing["exhausted_at"]
                )
                if stored_exhausted_at_us != int(existing["exhausted_at_us"]):
                    raise TaskStoreCorruption(
                        "retry exhaustion time is inconsistent"
                    )
                connection.commit()
                return SupervisedRetryExhaustion(
                    snapshot=snapshot,
                    intent=intent,
                    attempt_id=attempt_id,
                    observation_sequence=observation_sequence,
                    evidence_hash=evidence_hash,
                    private_proof_hash=private_proof_hash,
                    terminal_event_sequence=int(
                        existing["terminal_event_sequence"]
                    ),
                    fencing_token=int(existing["fencing_token"]),
                    exhausted_at=stored_exhausted_at,
                    replayed=True,
                )

            if (
                task["status"] != "Queued"
                or intent.state != "dispatching"
                or intent.adapter_id != "fwi.deepwave_adapter"
                or intent.adapter_version not in {"1.5.0", "1.6.0"}
                or intent.request.get("algorithm")
                != {
                    "id": "deepwave.acoustic_fwi",
                    "version": intent.adapter_version,
                }
                or event.get("sequence") != expected_sequence
            ):
                raise TaskStoreConflict(
                    "retry exhaustion requires a current managed dispatch"
                )
            if connection.execute(
                "SELECT 1 FROM dispatch_outcomes WHERE intent_id = ?",
                (intent_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "completed dispatch cannot be closed by retry exhaustion"
                )
            if connection.execute(
                "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "task cancellation owns the terminal transition"
                )
            if connection.execute(
                "SELECT 1 FROM task_purge_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "purged task cannot be closed by retry exhaustion"
                )

            budget = connection.execute(
                """
                SELECT * FROM approval_retry_budgets
                WHERE task_id = ? AND approval_id = ?
                """,
                (task_id, intent.approval_id),
            ).fetchone()
            reservation = connection.execute(
                """
                SELECT * FROM worker_retry_reservations
                WHERE intent_id = ? AND attempt_number = 2
                """,
                (intent_id,),
            ).fetchone()
            attempts = connection.execute(
                """
                SELECT * FROM worker_launch_attempts
                WHERE intent_id = ? ORDER BY attempt_number
                """,
                (intent_id,),
            ).fetchall()
            if (
                budget is None
                or int(budget["max_attempts"]) != 2
                or int(budget["max_concurrent_attempts"]) != 1
                or budget["retryable_failure_classes_json"]
                != '["pre_running_launch_failure","worker_exit"]'
                or reservation is None
                or reservation["task_id"] != task_id
                or reservation["project_id"] != task["project_id"]
                or reservation["principal_id"] != task["principal_id"]
                or reservation["approval_id"] != intent.approval_id
                or len(attempts) != 2
                or int(attempts[0]["attempt_number"]) != 1
                or int(attempts[1]["attempt_number"]) != 2
                or attempts[0]["attempt_id"]
                != reservation["previous_attempt_id"]
                or attempts[1]["attempt_id"] != attempt_id
                or attempts[1]["submission_id"] != attempts[0]["submission_id"]
                or attempts[1]["adapter_request_hash"]
                != attempts[0]["adapter_request_hash"]
                or attempts[1]["created_at"] != reservation["reserved_at"]
                or attempts[1]["job_id"] == attempts[0]["job_id"]
            ):
                raise TaskStoreConflict(
                    "retry exhaustion lost its approved attempt lineage"
                )
            observation = connection.execute(
                """
                SELECT * FROM worker_attempt_observations
                WHERE attempt_id = ? AND observation_sequence = ?
                """,
                (attempt_id, observation_sequence),
            ).fetchone()
            latest_sequence = connection.execute(
                """
                SELECT MAX(observation_sequence)
                FROM worker_attempt_observations WHERE attempt_id = ?
                """,
                (attempt_id,),
            ).fetchone()[0]
            if (
                observation is None
                or latest_sequence is None
                or int(latest_sequence) != observation_sequence
                or observation["document_hash"] != evidence_hash
                or observation["document_json"] != evidence_json
                or _decode_worker_observation_row(observation) != normalized
                or observation["ticket_state"] != "failed"
                or observation["ticket_worker_pid"] is not None
                or observation["ready_record_hash"] is not None
                or observation["heartbeat_record_hash"] is not None
                or int(observation["observed_at_us"]) > exhausted_at_us
            ):
                raise TaskStoreConflict(
                    "retry exhaustion requires the latest stopped observation"
                )

            _, fingerprint_hash = encode_document(event["fingerprint"])
            connection.execute(
                """
                INSERT INTO run_events(
                    task_id, sequence, event_id, event_type, task_status,
                    node_id, fingerprint_hash, document_json, document_hash,
                    occurred_at, recorded_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    event["sequence"],
                    event["event_id"],
                    event["event_type"],
                    event["task_status"],
                    event["node_id"],
                    fingerprint_hash,
                    event_json,
                    event_hash,
                    event["occurred_at"],
                    exhausted_at,
                ),
            )
            connection.execute(
                """
                INSERT INTO supervised_run_event_commits(
                    task_id, sequence, project_id, principal_id,
                    fencing_token, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    event["sequence"],
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    exhausted_at,
                    exhausted_at_us,
                ),
            )
            connection.execute(
                """
                INSERT INTO worker_retry_exhaustions(
                    intent_id, attempt_number, task_id, project_id,
                    principal_id, approval_id, attempt_id,
                    observation_sequence, evidence_hash,
                    private_schema_version, private_proof_hash, failure_kind,
                    max_attempts, terminal_event_sequence,
                    terminal_event_hash, fencing_token,
                    exhausted_at, exhausted_at_us
                ) VALUES (?, 2, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 2, ?, ?, ?, ?, ?)
                """,
                (
                    intent_id,
                    task_id,
                    task["project_id"],
                    task["principal_id"],
                    intent.approval_id,
                    attempt_id,
                    observation_sequence,
                    evidence_hash,
                    private_schema_version,
                    private_proof_hash,
                    failure_kind,
                    event["sequence"],
                    event_hash,
                    supervisor_lease.fencing_token,
                    exhausted_at,
                    exhausted_at_us,
                ),
            )
            connection.execute(
                "UPDATE tasks SET status = 'Failed', updated_at = ? "
                "WHERE task_id = ?",
                (exhausted_at, task_id),
            )
            heartbeat = connection.execute(
                """
                UPDATE runtime_supervisor_leases
                SET heartbeat_at = ?, heartbeat_at_us = ?
                WHERE project_id = ? AND principal_id = ?
                  AND fencing_token = ? AND owner_id = ?
                  AND acquired_at = ?
                """,
                (
                    exhausted_at,
                    exhausted_at_us,
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    supervisor_lease.owner_id,
                    supervisor_lease.acquired_at,
                ),
            )
            if heartbeat.rowcount != 1:
                raise RuntimeSupervisorLeaseLost(
                    "retry exhaustion lost its runtime supervisor lease"
                )
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None or snapshot.status != "Failed":
                raise TaskStoreCorruption(
                    "retry exhaustion terminal task cannot be read"
                )
            closed_intent = self._load_dispatch_intent(
                connection, task_id=task_id
            )
            if closed_intent is None or closed_intent.state != "retry_exhausted":
                raise TaskStoreCorruption(
                    "retry exhaustion dispatch projection cannot be read"
                )
            connection.commit()
            return SupervisedRetryExhaustion(
                snapshot=snapshot,
                intent=closed_intent,
                attempt_id=attempt_id,
                observation_sequence=observation_sequence,
                evidence_hash=evidence_hash,
                private_proof_hash=private_proof_hash,
                terminal_event_sequence=event["sequence"],
                fencing_token=supervisor_lease.fencing_token,
                exhausted_at=exhausted_at,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "retry exhaustion lost its runtime supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "retry exhaustion conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    @staticmethod
    def _insert_dispatch_reconciliation_resolution(
        connection: sqlite3.Connection,
        *,
        intent: DispatchIntentSnapshot,
        task_id: str,
        project_id: str,
        principal_id: str,
        source_outcome_hash: str,
        handle: Mapping[str, Any],
        evidence_kind: str,
        evidence_record_hash: str,
        attempt_id: str | None,
        observation_sequence: int | None,
        private_schema_version: str | None,
        receipt_record_hash: str | None,
        supervisor_lease: RuntimeSupervisorLease,
        resolved_at: str,
        resolved_at_us: int,
    ) -> tuple[str, str]:
        """Append one effective dispatch receipt from exact positive proof."""

        authorization = connection.execute(
            """
            SELECT project_id, principal_id, source_outcome_hash,
                   authorized_at, authorized_at_us
            FROM supervised_dispatch_reconciliation_attempts
            WHERE intent_id = ? AND fencing_token = ?
              AND evidence_kind = ?
            """,
            (
                intent.intent_id,
                supervisor_lease.fencing_token,
                evidence_kind,
            ),
        ).fetchone()
        if authorization is None:
            raise TaskStoreConflict(
                "dispatch reconciliation lacks current-term authorization"
            )
        try:
            authorized_at, authorized_at_us = _runtime_timestamp(
                authorization["authorized_at"]
            )
        except TaskStoreConflict as error:
            raise TaskStoreCorruption(
                "dispatch reconciliation authorization time is invalid"
            ) from error
        if (
            authorization["project_id"] != project_id
            or authorization["principal_id"] != principal_id
            or authorization["source_outcome_hash"] != source_outcome_hash
            or authorized_at != authorization["authorized_at"]
            or authorized_at_us != authorization["authorized_at_us"]
            or authorized_at_us > resolved_at_us
        ):
            raise TaskStoreCorruption(
                "dispatch reconciliation authorization is inconsistent"
            )
        normalized_handle = dict(handle)
        handle_json, handle_hash = encode_document(normalized_handle)
        effective_outcome = {
            "status": "dispatched",
            "handle": normalized_handle,
            "recorded_at": resolved_at,
        }
        effective_outcome_json, effective_outcome_hash = encode_document(
            effective_outcome
        )
        if evidence_kind == "managed_worker_receipt":
            evidence = {
                "kind": evidence_kind,
                "attempt_id": attempt_id,
                "observation_sequence": observation_sequence,
                "observation_document_hash": evidence_record_hash,
            }
        elif evidence_kind == "private_receipt":
            evidence = {
                "kind": evidence_kind,
                "private_schema_version": private_schema_version,
                "receipt_record_hash": receipt_record_hash,
            }
        else:
            raise TaskStoreConflict(
                "dispatch reconciliation evidence kind is invalid"
            )
        resolution = {
            "schema_version": "1.0.0",
            "intent_id": intent.intent_id,
            "task_id": task_id,
            "source_outcome_hash": source_outcome_hash,
            "result": "dispatched",
            "effective_outcome": effective_outcome,
            "evidence": evidence,
            "resolved_at": resolved_at,
            "extensions": {},
        }
        resolution_json, resolution_hash = encode_document(resolution)
        connection.execute(
            """
            INSERT INTO dispatch_reconciliation_resolutions(
                intent_id, task_id, project_id, principal_id,
                source_outcome_hash, result, evidence_kind,
                handle_json, handle_hash, evidence_record_hash,
                attempt_id, observation_sequence, private_schema_version,
                receipt_record_hash, effective_outcome_json,
                effective_outcome_hash, document_json, document_hash,
                fencing_token, resolved_at, resolved_at_us
            ) VALUES (?, ?, ?, ?, ?, 'dispatched', ?, ?, ?, ?, ?, ?, ?, ?,
                      ?, ?, ?, ?, ?, ?, ?)
            """,
            (
                intent.intent_id,
                task_id,
                project_id,
                principal_id,
                source_outcome_hash,
                evidence_kind,
                handle_json,
                handle_hash,
                evidence_record_hash,
                attempt_id,
                observation_sequence,
                private_schema_version,
                receipt_record_hash,
                effective_outcome_json,
                effective_outcome_hash,
                resolution_json,
                resolution_hash,
                supervisor_lease.fencing_token,
                resolved_at,
                resolved_at_us,
            ),
        )
        return effective_outcome_hash, resolution_hash

    def record_supervised_private_receipt_adoption(
        self,
        *,
        intent_id: str,
        handle: Mapping[str, Any],
        private_schema_version: str,
        receipt_record_hash: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedPrivateReceiptAdoption:
        """Fence adoption of one current receipt without managed evidence."""

        if private_schema_version != "1.0.0":
            raise TaskStoreConflict("private receipt schema is not adoptable")
        if not _is_sha256(receipt_record_hash):
            raise TaskStoreConflict("private receipt hash is invalid")
        if not isinstance(handle, Mapping):
            raise TaskStoreConflict("private receipt handle is invalid")
        normalized_handle = dict(handle)
        if not isinstance(supervisor_lease, RuntimeSupervisorLease):
            raise TaskStoreConflict("runtime supervisor lease is invalid")
        if not callable(supervisor_clock):
            raise TaskStoreConflict("runtime supervisor clock is invalid")

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            recorded_at, recorded_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            if intent is None:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("private receipt task cannot be read")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=recorded_at_us,
                action="private receipt adoption",
            )
            dag_admission = self._require_current_dag_node_execution(
                connection,
                intent=intent,
                allowed_node_states=frozenset({"Queued"}),
            )
            if dag_admission is not None:
                raise TaskStoreConflict(
                    "DAG execution requires a managed Worker receipt"
                )
            if (
                task["status"] != "Queued"
                or intent.adapter_id != "fwi.deepwave_adapter"
                or intent.adapter_version != "1.4.0"
            ):
                raise TaskStoreConflict(
                    "dispatch intent cannot adopt a private receipt"
                )
            if connection.execute(
                "SELECT 1 FROM task_purge_requests WHERE task_id = ?",
                (task_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict("purged task cannot adopt a private receipt")
            if connection.execute(
                "SELECT 1 FROM worker_launch_attempts WHERE intent_id = ?",
                (intent_id,),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "managed Worker evidence cannot use private receipt adoption"
                )
            self._validate_dispatch_handle(intent, normalized_handle)

            existing = connection.execute(
                """
                SELECT * FROM supervised_private_receipt_adoptions
                WHERE intent_id = ?
                """,
                (intent_id,),
            ).fetchone()
            if intent.state == "dispatched":
                if intent.handle != normalized_handle:
                    raise TaskStoreConflict(
                        "private receipt differs from the dispatch outcome"
                    )
                if (
                    intent.reconciliation is not None
                    and intent.reconciliation.evidence_kind != "private_receipt"
                ):
                    raise TaskStoreConflict(
                        "dispatch reconciliation used managed Worker evidence"
                    )
                outcome = connection.execute(
                    """
                    SELECT outcome_document_hash
                    FROM effective_dispatched_intents WHERE intent_id = ?
                    """,
                    (intent_id,),
                ).fetchone()
                if outcome is None:
                    raise TaskStoreCorruption(
                        "dispatched intent has no durable outcome"
                    )
                if existing is None:
                    connection.commit()
                    return SupervisedPrivateReceiptAdoption(
                        intent=intent,
                        private_schema_version=private_schema_version,
                        receipt_record_hash=receipt_record_hash,
                        outcome_document_hash=outcome["outcome_document_hash"],
                        fencing_token=supervisor_lease.fencing_token,
                        recorded_at=recorded_at,
                        adopted=False,
                        replayed=True,
                    )
                if (
                    existing["private_schema_version"] != private_schema_version
                    or existing["receipt_record_hash"] != receipt_record_hash
                    or existing["outcome_document_hash"]
                    != outcome["outcome_document_hash"]
                ):
                    raise TaskStoreConflict(
                        "private receipt adoption replay differs"
                    )
                stored_at, stored_at_us = _runtime_timestamp(existing["recorded_at"])
                if stored_at_us != int(existing["recorded_at_us"]):
                    raise TaskStoreCorruption(
                        "private receipt adoption time is inconsistent"
                    )
                connection.commit()
                return SupervisedPrivateReceiptAdoption(
                    intent=intent,
                    private_schema_version=private_schema_version,
                    receipt_record_hash=receipt_record_hash,
                    outcome_document_hash=outcome["outcome_document_hash"],
                    fencing_token=int(existing["fencing_token"]),
                    recorded_at=stored_at,
                    adopted=False,
                    replayed=True,
                )
            reconciliation_pending = (
                intent.state == "reconciliation_required"
                and intent.reconciliation is not None
                and intent.reconciliation.state == "required"
            )
            if (
                intent.state != "dispatching" and not reconciliation_pending
            ) or existing is not None:
                raise TaskStoreConflict(
                    "dispatch intent cannot adopt a private receipt"
                )

            outcome_document = {
                "status": "dispatched",
                "handle": normalized_handle,
                "recorded_at": recorded_at,
            }
            outcome_json, outcome_hash = encode_document(outcome_document)
            source_outcome_hash: str | None = None
            if reconciliation_pending:
                source_outcome = connection.execute(
                    """
                    SELECT outcome, document_hash FROM dispatch_outcomes
                    WHERE intent_id = ?
                    """,
                    (intent_id,),
                ).fetchone()
                if (
                    source_outcome is None
                    or source_outcome["outcome"] != "reconciliation_required"
                ):
                    raise TaskStoreCorruption(
                        "dispatch reconciliation source outcome cannot be read"
                    )
                source_outcome_hash = str(source_outcome["document_hash"])
            else:
                connection.execute(
                    """
                    INSERT INTO dispatch_outcomes(
                        intent_id, outcome, document_json, document_hash,
                        recorded_at
                    ) VALUES (?, 'dispatched', ?, ?, ?)
                    """,
                    (intent_id, outcome_json, outcome_hash, recorded_at),
                )
            connection.execute(
                """
                INSERT INTO supervised_private_receipt_adoptions(
                    intent_id, project_id, principal_id, fencing_token,
                    private_schema_version, receipt_record_hash,
                    outcome_document_hash, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    intent_id,
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    private_schema_version,
                    receipt_record_hash,
                    outcome_hash,
                    recorded_at,
                    recorded_at_us,
                ),
            )
            if reconciliation_pending:
                if source_outcome_hash is None:
                    raise TaskStoreCorruption(
                        "dispatch reconciliation source hash is unavailable"
                    )
                effective_hash, _ = self._insert_dispatch_reconciliation_resolution(
                    connection,
                    intent=intent,
                    task_id=task_id,
                    project_id=task["project_id"],
                    principal_id=task["principal_id"],
                    source_outcome_hash=source_outcome_hash,
                    handle=normalized_handle,
                    evidence_kind="private_receipt",
                    evidence_record_hash=receipt_record_hash,
                    attempt_id=None,
                    observation_sequence=None,
                    private_schema_version=private_schema_version,
                    receipt_record_hash=receipt_record_hash,
                    supervisor_lease=supervisor_lease,
                    resolved_at=recorded_at,
                    resolved_at_us=recorded_at_us,
                )
                if effective_hash != outcome_hash:
                    raise TaskStoreCorruption(
                        "private reconciliation effective outcome hash changed"
                    )
            stored = self._load_dispatch_intent(
                connection, intent_id=intent_id
            )
            if stored is None or stored.state != "dispatched":
                raise TaskStoreCorruption(
                    "private receipt adoption cannot be read"
                )
            connection.commit()
            return SupervisedPrivateReceiptAdoption(
                intent=stored,
                private_schema_version=private_schema_version,
                receipt_record_hash=receipt_record_hash,
                outcome_document_hash=outcome_hash,
                fencing_token=supervisor_lease.fencing_token,
                recorded_at=recorded_at,
                adopted=True,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "private receipt adoption lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "private receipt adoption conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def acquire_runtime_supervisor_lease(
        self,
        *,
        project_id: str,
        principal_id: str,
        owner_id: str,
        lease_seconds: int,
        clock: Callable[[], str],
    ) -> RuntimeSupervisorLeaseAcquisition:
        if type(lease_seconds) is not int or not 1 <= lease_seconds <= 3600:
            raise TaskStoreConflict("runtime supervisor lease window is invalid")
        if not callable(clock):
            raise TaskStoreConflict("runtime supervisor clock is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            now, now_us, expires_at, expires_at_us = _runtime_lease_window(
                clock, lease_seconds
            )
            current = self._load_runtime_supervisor_lease(
                connection,
                project_id=project_id,
                principal_id=principal_id,
            )
            if current is None:
                fencing_token = 1
                self._insert_runtime_supervisor_term(
                    connection,
                    project_id=project_id,
                    principal_id=principal_id,
                    fencing_token=fencing_token,
                    owner_id=owner_id,
                    now=now,
                    now_us=now_us,
                    expires_at=expires_at,
                    expires_at_us=expires_at_us,
                )
                connection.execute(
                    """
                    INSERT INTO runtime_supervisor_leases(
                        project_id, principal_id, fencing_token, owner_id,
                        acquired_at, acquired_at_us,
                        heartbeat_at, heartbeat_at_us,
                        expires_at, expires_at_us
                    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        project_id, principal_id, fencing_token, owner_id,
                        now, now_us, now, now_us, expires_at, expires_at_us,
                    ),
                )
            else:
                _, heartbeat_at_us = _runtime_timestamp(current.heartbeat_at)
                _, current_expires_at_us = _runtime_timestamp(current.expires_at)
                if current.state == "active" and now_us < current_expires_at_us:
                    if current.owner_id == owner_id and now_us < heartbeat_at_us:
                        raise TaskStoreConflict(
                            "runtime supervisor acquisition clock regressed"
                        )
                    connection.commit()
                    return RuntimeSupervisorLeaseAcquisition(
                        lease=current,
                        acquired=current.owner_id == owner_id,
                    )
                temporal_floor_us = heartbeat_at_us
                if current.state == "released":
                    closure = connection.execute(
                        """
                        SELECT closed_at_us
                        FROM runtime_supervisor_term_closures
                        WHERE project_id = ? AND principal_id = ?
                          AND fencing_token = ? AND reason = 'released'
                        """,
                        (
                            current.project_id,
                            current.principal_id,
                            current.fencing_token,
                        ),
                    ).fetchone()
                    if closure is None:
                        raise TaskStoreCorruption(
                            "released runtime supervisor term has no closure"
                        )
                    temporal_floor_us = int(closure["closed_at_us"])
                if now_us < temporal_floor_us:
                    raise TaskStoreConflict(
                        "runtime supervisor acquisition clock regressed"
                    )
                if current.fencing_token >= 9_223_372_036_854_775_807:
                    raise TaskStoreCorruption(
                        "runtime supervisor fencing token is exhausted"
                    )
                if current.state == "active":
                    self._close_runtime_supervisor_term(
                        connection,
                        lease=current,
                        reason="expired_takeover",
                        closed_at=now,
                        closed_at_us=now_us,
                    )
                fencing_token = current.fencing_token + 1
                self._insert_runtime_supervisor_term(
                    connection,
                    project_id=project_id,
                    principal_id=principal_id,
                    fencing_token=fencing_token,
                    owner_id=owner_id,
                    now=now,
                    now_us=now_us,
                    expires_at=expires_at,
                    expires_at_us=expires_at_us,
                )
                connection.execute(
                    """
                    UPDATE runtime_supervisor_leases
                    SET fencing_token = ?, owner_id = ?,
                        acquired_at = ?, acquired_at_us = ?,
                        heartbeat_at = ?, heartbeat_at_us = ?,
                        expires_at = ?, expires_at_us = ?
                    WHERE project_id = ? AND principal_id = ?
                    """,
                    (
                        fencing_token, owner_id, now, now_us,
                        now, now_us, expires_at, expires_at_us,
                        project_id, principal_id,
                    ),
                )
            stored = self._load_runtime_supervisor_lease(
                connection,
                project_id=project_id,
                principal_id=principal_id,
            )
            if stored is None or stored.state != "active" or stored.owner_id != owner_id:
                raise TaskStoreCorruption(
                    "acquired runtime supervisor lease cannot be read"
                )
            connection.commit()
            return RuntimeSupervisorLeaseAcquisition(lease=stored, acquired=True)
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict(
                "runtime supervisor acquisition conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def heartbeat_runtime_supervisor_lease(
        self,
        *,
        lease: RuntimeSupervisorLease,
        lease_seconds: int,
        clock: Callable[[], str],
    ) -> RuntimeSupervisorLease:
        if not isinstance(lease, RuntimeSupervisorLease):
            raise TaskStoreConflict("runtime supervisor lease is invalid")
        if type(lease_seconds) is not int or not 1 <= lease_seconds <= 3600:
            raise TaskStoreConflict("runtime supervisor lease window is invalid")
        if not callable(clock):
            raise TaskStoreConflict("runtime supervisor clock is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            now, now_us, expires_at, expires_at_us = _runtime_lease_window(
                clock, lease_seconds
            )
            current = self._load_runtime_supervisor_lease(
                connection,
                project_id=lease.project_id,
                principal_id=lease.principal_id,
            )
            if (
                current is None
                or current.state != "active"
                or not self._runtime_supervisor_identity_matches(current, lease)
            ):
                raise RuntimeSupervisorLeaseLost(
                    "runtime supervisor lease is no longer current"
                )
            _, heartbeat_at_us = _runtime_timestamp(current.heartbeat_at)
            _, current_expires_at_us = _runtime_timestamp(current.expires_at)
            if now_us < heartbeat_at_us or now_us >= current_expires_at_us:
                raise RuntimeSupervisorLeaseLost(
                    "runtime supervisor lease expired or clock regressed"
                )
            if expires_at_us < current_expires_at_us:
                raise TaskStoreConflict(
                    "runtime supervisor expiry cannot regress"
                )
            connection.execute(
                """
                UPDATE runtime_supervisor_leases
                SET heartbeat_at = ?, heartbeat_at_us = ?,
                    expires_at = ?, expires_at_us = ?
                WHERE project_id = ? AND principal_id = ?
                  AND fencing_token = ? AND owner_id = ?
                """,
                (
                    now, now_us, expires_at, expires_at_us,
                    current.project_id, current.principal_id,
                    current.fencing_token, current.owner_id,
                ),
            )
            stored = self._load_runtime_supervisor_lease(
                connection,
                project_id=current.project_id,
                principal_id=current.principal_id,
            )
            if (
                stored is None
                or stored.state != "active"
                or not self._runtime_supervisor_identity_matches(stored, current)
            ):
                raise TaskStoreCorruption(
                    "runtime supervisor heartbeat cannot be read"
                )
            connection.commit()
            return stored
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise RuntimeSupervisorLeaseLost(
                "runtime supervisor heartbeat conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def release_runtime_supervisor_lease(
        self,
        *,
        lease: RuntimeSupervisorLease,
        clock: Callable[[], str],
    ) -> RuntimeSupervisorLease:
        if not isinstance(lease, RuntimeSupervisorLease):
            raise TaskStoreConflict("runtime supervisor lease is invalid")
        if not callable(clock):
            raise TaskStoreConflict("runtime supervisor clock is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            now, now_us = _runtime_timestamp(clock())
            current = self._load_runtime_supervisor_lease(
                connection,
                project_id=lease.project_id,
                principal_id=lease.principal_id,
            )
            if current is None or not self._runtime_supervisor_identity_matches(
                current, lease
            ):
                raise RuntimeSupervisorLeaseLost(
                    "runtime supervisor lease is no longer current"
                )
            if current.state == "released":
                connection.commit()
                return current
            _, heartbeat_at_us = _runtime_timestamp(current.heartbeat_at)
            if now_us < heartbeat_at_us:
                raise RuntimeSupervisorLeaseLost(
                    "runtime supervisor release clock regressed"
                )
            self._close_runtime_supervisor_term(
                connection,
                lease=current,
                reason="released",
                closed_at=now,
                closed_at_us=now_us,
            )
            stored = self._load_runtime_supervisor_lease(
                connection,
                project_id=current.project_id,
                principal_id=current.principal_id,
            )
            if stored is None or stored.state != "released":
                raise TaskStoreCorruption(
                    "released runtime supervisor lease cannot be read"
                )
            connection.commit()
            return stored
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise RuntimeSupervisorLeaseLost(
                "runtime supervisor release conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def record_supervised_worker_observation(
        self,
        *,
        intent_id: str,
        evidence: Mapping[str, Any],
        handle: Mapping[str, Any] | None,
        replacement_event: Mapping[str, Any] | None = None,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> WorkerAttemptProjection:
        """Atomically project exact Worker evidence and optionally adopt it."""

        if not isinstance(supervisor_lease, RuntimeSupervisorLease):
            raise TaskStoreConflict("runtime supervisor lease is invalid")
        if not callable(supervisor_clock):
            raise TaskStoreConflict("runtime supervisor clock is invalid")
        normalized = _validated_worker_evidence(evidence)
        document_json, document_hash = encode_document(normalized)
        normalized_handle = None if handle is None else dict(handle)
        normalized_replacement_event = (
            None
            if replacement_event is None
            else copy.deepcopy(dict(replacement_event))
        )
        ticket = normalized["ticket"]
        ready = normalized["ready"]
        heartbeat = normalized["heartbeat"]

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            observed_at, observed_at_us = _runtime_timestamp(supervisor_clock())
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            if intent is None:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            if (
                heartbeat is not None
                and heartbeat["state"] == "waiting"
                and not _is_checkpoint_capable_managed_dispatch(intent)
            ):
                raise TaskStoreConflict(
                    "checkpoint Waiting requires the exact 1.6 managed dispatch"
                )
            task = connection.execute(
                """
                SELECT project_id, principal_id, status
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("Worker attempt task cannot be read")
            current_lease = self._load_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
            )
            if (
                current_lease is None
                or current_lease.state != "active"
                or supervisor_lease.project_id != task["project_id"]
                or supervisor_lease.principal_id != task["principal_id"]
                or not self._runtime_supervisor_identity_matches(
                    current_lease, supervisor_lease
                )
                or _runtime_timestamp(current_lease.heartbeat_at)[1]
                > observed_at_us
                or _runtime_timestamp(current_lease.expires_at)[1]
                <= observed_at_us
            ):
                raise RuntimeSupervisorLeaseLost(
                    "Worker observation lost its supervisor lease"
                )
            self._require_current_dag_node_execution(
                connection,
                intent=intent,
                allowed_node_states=frozenset({"Queued", "Running"}),
            )
            reconciliation_pending = (
                intent.state == "reconciliation_required"
                and intent.reconciliation is not None
                and intent.reconciliation.state == "required"
            )
            if intent.state not in {"dispatching", "dispatched", "retrying"} and not (
                reconciliation_pending
            ):
                raise TaskStoreConflict("dispatch intent cannot accept Worker evidence")
            if (
                intent.state == "dispatched"
                and intent.reconciliation is not None
                and intent.reconciliation.evidence_kind != "managed_worker_receipt"
            ):
                raise TaskStoreConflict(
                    "dispatch reconciliation used a private receipt"
                )
            if intent.state != "retrying" and normalized_replacement_event is not None:
                raise TaskStoreConflict(
                    "dispatch replacement event has no retry reservation"
                )
            if reconciliation_pending and (
                normalized_handle is None
                or ticket["state"] != "spawned"
                or ready is None
                or heartbeat is None
                or (
                    heartbeat["state"]
                    not in {"running", "succeeded", "failed"}
                    and not (
                        heartbeat["state"] == "waiting"
                        and _is_checkpoint_capable_managed_dispatch(intent)
                    )
                )
            ):
                raise TaskStoreConflict(
                    "dispatch reconciliation requires exact positive Worker proof"
                )

            existing_attempt = connection.execute(
                """
                SELECT * FROM worker_launch_attempts
                WHERE intent_id = ? AND attempt_number = ?
                """,
                (intent_id, normalized["attempt_number"]),
            ).fetchone()
            latest_attempt_number = int(
                connection.execute(
                    """
                    SELECT COALESCE(MAX(attempt_number), 0)
                    FROM worker_launch_attempts WHERE intent_id = ?
                    """,
                    (intent_id,),
                ).fetchone()[0]
            )
            expected_attempt = {
                "attempt_id": normalized["attempt_id"],
                "intent_id": intent_id,
                "task_id": task_id,
                "project_id": task["project_id"],
                "principal_id": task["principal_id"],
                "attempt_number": normalized["attempt_number"],
                "submission_id": normalized["submission_id"],
                "job_id": normalized["job_id"],
                "adapter_request_hash": normalized["request_hash"],
                "binding_hash": normalized["binding_hash"],
                "created_at": normalized["created_at"],
            }
            if existing_attempt is None:
                if normalized["attempt_number"] != latest_attempt_number + 1:
                    raise TaskStoreConflict(
                        "Worker attempt number is not the next durable attempt"
                    )
                if normalized["attempt_number"] == 2:
                    pre_running_reservation = connection.execute(
                        """
                        SELECT * FROM worker_retry_reservations
                        WHERE intent_id = ? AND attempt_number = 2
                        """,
                        (intent_id,),
                    ).fetchone()
                    worker_exit_reservation = connection.execute(
                        """
                        SELECT * FROM worker_exit_retry_reservations
                        WHERE intent_id = ? AND attempt_number = 2
                        """,
                        (intent_id,),
                    ).fetchone()
                    reservation = (
                        pre_running_reservation
                        if pre_running_reservation is not None
                        else worker_exit_reservation
                    )
                    prior_attempt = connection.execute(
                        """
                        SELECT * FROM worker_launch_attempts
                        WHERE intent_id = ? AND attempt_number = 1
                        """,
                        (intent_id,),
                    ).fetchone()
                    if (
                        reservation is None
                        or (
                            pre_running_reservation is not None
                            and worker_exit_reservation is not None
                        )
                        or reservation["task_id"] != task_id
                        or reservation["project_id"] != task["project_id"]
                        or reservation["principal_id"] != task["principal_id"]
                        or prior_attempt is None
                        or prior_attempt["attempt_id"]
                        != reservation["previous_attempt_id"]
                        or normalized["submission_id"]
                        != prior_attempt["submission_id"]
                        or normalized["request_hash"]
                        != prior_attempt["adapter_request_hash"]
                        or normalized["created_at"]
                        != reservation["reserved_at"]
                        or normalized["job_id"] == prior_attempt["job_id"]
                    ):
                        raise TaskStoreConflict(
                            "Worker retry attempt lacks its exact reserved lineage"
                        )
                connection.execute(
                    """
                    INSERT INTO worker_launch_attempts(
                        attempt_id, intent_id, task_id, project_id, principal_id,
                        attempt_number, submission_id, job_id,
                        adapter_request_hash, binding_hash, created_at,
                        first_fencing_token, first_observed_at,
                        first_observed_at_us
                    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        normalized["attempt_id"],
                        intent_id,
                        task_id,
                        task["project_id"],
                        task["principal_id"],
                        normalized["attempt_number"],
                        normalized["submission_id"],
                        normalized["job_id"],
                        normalized["request_hash"],
                        normalized["binding_hash"],
                        normalized["created_at"],
                        supervisor_lease.fencing_token,
                        observed_at,
                        observed_at_us,
                    ),
                )
            else:
                if int(existing_attempt["attempt_number"]) != latest_attempt_number:
                    raise TaskStoreConflict(
                        "historical Worker attempt cannot become current"
                    )
                if any(
                    existing_attempt[key] != expected
                    for key, expected in expected_attempt.items()
                ):
                    raise TaskStoreConflict(
                        "Worker attempt differs from the durable intent projection"
                    )

            replay = connection.execute(
                """
                SELECT * FROM worker_attempt_observations
                WHERE attempt_id = ? AND document_hash = ?
                """,
                (normalized["attempt_id"], document_hash),
            ).fetchone()
            if replay is not None:
                latest_sequence = int(
                    connection.execute(
                        """
                        SELECT MAX(observation_sequence)
                        FROM worker_attempt_observations WHERE attempt_id = ?
                        """,
                        (normalized["attempt_id"],),
                    ).fetchone()[0]
                )
                if int(replay["observation_sequence"]) != latest_sequence:
                    raise TaskStoreConflict(
                        "historical Worker evidence cannot be replayed as current"
                    )
                stored_document = _decode_worker_observation_row(replay)
                if stored_document != normalized:
                    raise TaskStoreCorruption(
                        "Worker observation hash no longer identifies its evidence"
                    )
                observation_sequence = int(replay["observation_sequence"])
                replayed = True
            else:
                latest = connection.execute(
                    """
                    SELECT * FROM worker_attempt_observations
                    WHERE attempt_id = ?
                    ORDER BY observation_sequence DESC LIMIT 1
                    """,
                    (normalized["attempt_id"],),
                ).fetchone()
                if latest is not None:
                    prior = _decode_worker_observation_row(latest)
                    prior_ticket = prior["ticket"]
                    prior_ready = prior["ready"]
                    prior_heartbeat = prior["heartbeat"]
                    if (
                        _runtime_timestamp(ticket["updated_at"])[1]
                        < _runtime_timestamp(prior_ticket["updated_at"])[1]
                    ):
                        raise TaskStoreConflict(
                            "Worker launch ticket time cannot regress"
                        )
                    allowed_ticket_states = {
                        "staged": {"staged", "leased", "spawned", "failed"},
                        "leased": {"leased", "spawned", "failed"},
                        "spawned": {"spawned", "failed"},
                        "failed": {"failed"},
                    }
                    if ticket["state"] not in allowed_ticket_states[
                        prior_ticket["state"]
                    ]:
                        raise TaskStoreConflict(
                            "Worker launch ticket state cannot regress"
                        )
                    if (
                        ticket["state"] == prior_ticket["state"]
                        and ticket["record_hash"] != prior_ticket["record_hash"]
                    ):
                        raise TaskStoreConflict(
                            "Worker launch ticket changed within one state"
                        )
                    if prior_ticket["capacity_slot"] is not None and (
                        ticket["capacity_slot"]
                        != prior_ticket["capacity_slot"]
                        or ticket["capacity_generation"]
                        != prior_ticket["capacity_generation"]
                    ):
                        raise TaskStoreConflict(
                            "Worker capacity identity cannot change"
                        )
                    if (
                        prior_ticket["worker_pid"] is not None
                        and ticket["state"] != "failed"
                        and ticket["worker_pid"] != prior_ticket["worker_pid"]
                    ):
                        raise TaskStoreConflict(
                            "Worker process identity cannot change"
                        )
                    if prior_ready is not None and ready != prior_ready:
                        raise TaskStoreConflict(
                            "Worker ready evidence cannot disappear or change"
                        )
                    if prior_heartbeat is not None:
                        if heartbeat is None:
                            raise TaskStoreConflict(
                                "Worker heartbeat evidence cannot disappear"
                            )
                        if prior_heartbeat["state"] in {
                            "succeeded",
                            "failed",
                            "stopped",
                        }:
                            raise TaskStoreConflict(
                                "terminal Worker heartbeat cannot advance"
                            )
                        if heartbeat["sequence"] < prior_heartbeat["sequence"]:
                            raise TaskStoreConflict(
                                "Worker heartbeat sequence cannot regress"
                            )
                        if (
                            heartbeat["sequence"] == prior_heartbeat["sequence"]
                            and heartbeat["record_hash"]
                            != prior_heartbeat["record_hash"]
                        ):
                            raise TaskStoreConflict(
                                "Worker heartbeat cannot change at one sequence"
                            )
                        if (
                            heartbeat["sequence"]
                            > prior_heartbeat["sequence"]
                            and _runtime_timestamp(heartbeat["updated_at"])[1]
                            < _runtime_timestamp(
                                prior_heartbeat["updated_at"]
                            )[1]
                        ):
                            raise TaskStoreConflict(
                                "Worker heartbeat time cannot regress"
                            )
                observation_sequence = int(
                    connection.execute(
                        """
                        SELECT COALESCE(MAX(observation_sequence), 0) + 1
                        FROM worker_attempt_observations WHERE attempt_id = ?
                        """,
                        (normalized["attempt_id"],),
                    ).fetchone()[0]
                )
                columns = _worker_observation_columns(normalized)
                connection.execute(
                    """
                    INSERT INTO worker_attempt_observations(
                        attempt_id, observation_sequence, ticket_state,
                        capacity_slot, capacity_generation, ticket_worker_pid,
                        ticket_updated_at, ticket_record_hash,
                        ready_worker_pid, ready_started_at, ready_record_hash,
                        heartbeat_sequence, heartbeat_state,
                        heartbeat_updated_at, heartbeat_record_hash,
                        document_json, document_hash, project_id, principal_id,
                        fencing_token, observed_at, observed_at_us
                    ) VALUES (
                        ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?,
                        ?, ?, ?, ?
                    )
                    """,
                    (
                        columns["attempt_id"],
                        observation_sequence,
                        columns["ticket_state"],
                        columns["capacity_slot"],
                        columns["capacity_generation"],
                        columns["ticket_worker_pid"],
                        columns["ticket_updated_at"],
                        columns["ticket_record_hash"],
                        columns["ready_worker_pid"],
                        columns["ready_started_at"],
                        columns["ready_record_hash"],
                        columns["heartbeat_sequence"],
                        columns["heartbeat_state"],
                        columns["heartbeat_updated_at"],
                        columns["heartbeat_record_hash"],
                        document_json,
                        document_hash,
                        task["project_id"],
                        task["principal_id"],
                        supervisor_lease.fencing_token,
                        observed_at,
                        observed_at_us,
                    ),
                )
                replayed = False

            adopted = False
            if normalized_handle is not None:
                if ready is None or heartbeat is None:
                    raise TaskStoreConflict(
                        "dispatch adoption requires ready Worker heartbeat evidence"
                    )
                if any(
                    normalized_handle.get(key) != normalized[evidence_key]
                    for key, evidence_key in {
                        "submission_id": "submission_id",
                        "job_id": "job_id",
                        "request_hash": "request_hash",
                    }.items()
                ):
                    raise TaskStoreConflict(
                        "dispatch handle differs from exact Worker evidence"
                    )
                self._validate_dispatch_handle(intent, normalized_handle)
                if intent.state == "dispatched":
                    if intent.handle != normalized_handle:
                        raise TaskStoreConflict(
                            "Worker adoption differs from the dispatch outcome"
                        )
                elif intent.state == "retrying":
                    if (
                        normalized["attempt_number"] != 2
                        or task["status"] != "Retrying"
                        or normalized_replacement_event is None
                    ):
                        raise TaskStoreConflict(
                            "dispatch replacement requires exact retry state"
                        )
                    reservation = connection.execute(
                        """
                        SELECT * FROM worker_exit_retry_reservations
                        WHERE intent_id = ? AND attempt_number = 2
                        """,
                        (intent_id,),
                    ).fetchone()
                    if reservation is None:
                        raise TaskStoreCorruption(
                            "dispatch replacement lacks its retry reservation"
                        )
                    handle_json, handle_hash = encode_document(normalized_handle)
                    effective_outcome = {
                        "status": "dispatched",
                        "handle": normalized_handle,
                        "recorded_at": observed_at,
                    }
                    effective_json, effective_hash = encode_document(
                        effective_outcome
                    )
                    connection.execute(
                        """
                        INSERT INTO worker_exit_retry_dispatch_replacements(
                            intent_id, attempt_number, task_id, project_id,
                            principal_id, approval_id,
                            source_outcome_document_hash, source_handle_hash,
                            attempt_id, observation_sequence, evidence_hash,
                            handle_json, handle_hash, effective_outcome_json,
                            effective_outcome_hash, fencing_token,
                            replaced_at, replaced_at_us
                        ) VALUES (?, 2, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?,
                                  ?, ?, ?, ?)
                        """,
                        (
                            intent_id,
                            task_id,
                            task["project_id"],
                            task["principal_id"],
                            intent.approval_id,
                            reservation["source_outcome_document_hash"],
                            reservation["source_handle_hash"],
                            normalized["attempt_id"],
                            observation_sequence,
                            document_hash,
                            handle_json,
                            handle_hash,
                            effective_json,
                            effective_hash,
                            supervisor_lease.fencing_token,
                            observed_at,
                            observed_at_us,
                        ),
                    )
                    event_json, event_hash = encode_document(
                        normalized_replacement_event
                    )
                    next_event_sequence = int(
                        connection.execute(
                            "SELECT COALESCE(MAX(sequence), 0) + 1 "
                            "FROM run_events WHERE task_id = ?",
                            (task_id,),
                        ).fetchone()[0]
                    )
                    event_fingerprint = normalized_replacement_event.get(
                        "fingerprint"
                    )
                    _, event_fingerprint_hash = encode_document(
                        event_fingerprint
                    )
                    if (
                        normalized_replacement_event.get("task_id") != task_id
                        or normalized_replacement_event.get("sequence")
                        != next_event_sequence
                        or normalized_replacement_event.get("event_type")
                        != "node_started"
                        or normalized_replacement_event.get("task_status")
                        != "Running"
                        or normalized_replacement_event.get("node_id")
                        != intent.node_id
                        or event_fingerprint != intent.queue_fingerprint
                    ):
                        raise TaskStoreConflict(
                            "dispatch replacement transition is invalid"
                        )
                    connection.execute(
                        """
                        INSERT INTO run_events(
                            task_id, sequence, event_id, event_type,
                            task_status, node_id, fingerprint_hash,
                            document_json, document_hash, occurred_at,
                            recorded_at
                        ) VALUES (?, ?, ?, 'node_started', 'Running', ?, ?,
                                  ?, ?, ?, ?)
                        """,
                        (
                            task_id,
                            next_event_sequence,
                            normalized_replacement_event["event_id"],
                            intent.node_id,
                            event_fingerprint_hash,
                            event_json,
                            event_hash,
                            normalized_replacement_event["occurred_at"],
                            observed_at,
                        ),
                    )
                    connection.execute(
                        """
                        INSERT INTO supervised_run_event_commits(
                            task_id, sequence, project_id, principal_id,
                            fencing_token, recorded_at, recorded_at_us
                        ) VALUES (?, ?, ?, ?, ?, ?, ?)
                        """,
                        (
                            task_id,
                            next_event_sequence,
                            task["project_id"],
                            task["principal_id"],
                            supervisor_lease.fencing_token,
                            observed_at,
                            observed_at_us,
                        ),
                    )
                    connection.execute(
                        "UPDATE tasks SET status = 'Running', updated_at = ? "
                        "WHERE task_id = ?",
                        (observed_at, task_id),
                    )
                    adopted = True
                elif reconciliation_pending:
                    source_outcome = connection.execute(
                        """
                        SELECT outcome, document_hash FROM dispatch_outcomes
                        WHERE intent_id = ?
                        """,
                        (intent_id,),
                    ).fetchone()
                    if (
                        source_outcome is None
                        or source_outcome["outcome"]
                        != "reconciliation_required"
                    ):
                        raise TaskStoreCorruption(
                            "dispatch reconciliation source outcome cannot be read"
                        )
                    connection.execute(
                        """
                        INSERT INTO supervised_dispatch_adoptions(
                            intent_id, attempt_id, project_id, principal_id,
                            fencing_token, recorded_at, recorded_at_us
                        ) VALUES (?, ?, ?, ?, ?, ?, ?)
                        """,
                        (
                            intent_id,
                            normalized["attempt_id"],
                            task["project_id"],
                            task["principal_id"],
                            supervisor_lease.fencing_token,
                            observed_at,
                            observed_at_us,
                        ),
                    )
                    self._insert_dispatch_reconciliation_resolution(
                        connection,
                        intent=intent,
                        task_id=task_id,
                        project_id=task["project_id"],
                        principal_id=task["principal_id"],
                        source_outcome_hash=source_outcome["document_hash"],
                        handle=normalized_handle,
                        evidence_kind="managed_worker_receipt",
                        evidence_record_hash=document_hash,
                        attempt_id=normalized["attempt_id"],
                        observation_sequence=observation_sequence,
                        private_schema_version=None,
                        receipt_record_hash=None,
                        supervisor_lease=supervisor_lease,
                        resolved_at=observed_at,
                        resolved_at_us=observed_at_us,
                    )
                    adopted = True
                else:
                    outcome = {
                        "status": "dispatched",
                        "handle": normalized_handle,
                        "recorded_at": observed_at,
                    }
                    outcome_json, outcome_hash = encode_document(outcome)
                    connection.execute(
                        """
                        INSERT INTO dispatch_outcomes(
                            intent_id, outcome, document_json, document_hash,
                            recorded_at
                        ) VALUES (?, 'dispatched', ?, ?, ?)
                        """,
                        (intent_id, outcome_json, outcome_hash, observed_at),
                    )
                    connection.execute(
                        """
                        INSERT INTO supervised_dispatch_adoptions(
                            intent_id, attempt_id, project_id, principal_id,
                            fencing_token, recorded_at, recorded_at_us
                        ) VALUES (?, ?, ?, ?, ?, ?, ?)
                        """,
                        (
                            intent_id,
                            normalized["attempt_id"],
                            task["project_id"],
                            task["principal_id"],
                            supervisor_lease.fencing_token,
                            observed_at,
                            observed_at_us,
                        ),
                    )
                    adopted = True
            elif normalized_replacement_event is not None:
                raise TaskStoreConflict(
                    "dispatch replacement event requires a ready handle"
                )

            stored_intent = self._load_dispatch_intent(
                connection, intent_id=intent_id
            )
            if stored_intent is None:
                raise TaskStoreCorruption("projected dispatch intent cannot be read")
            connection.commit()
            return WorkerAttemptProjection(
                intent=stored_intent,
                attempt_id=normalized["attempt_id"],
                observation_sequence=observation_sequence,
                document_hash=document_hash,
                adopted=adopted,
                replayed=replayed,
            )
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "requires the active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "Worker observation lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "Worker observation conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    @staticmethod
    def _insert_checkpoint_run_event(
        connection: sqlite3.Connection,
        *,
        event: Mapping[str, Any],
        project_id: str,
        principal_id: str,
        fingerprint_hash: str,
        fencing_token: int,
        recorded_at: str,
        recorded_at_us: int,
    ) -> None:
        event_json, event_hash = encode_document(event)
        connection.execute(
            """
            INSERT INTO run_events(
                task_id, sequence, event_id, event_type, task_status,
                node_id, fingerprint_hash, document_json, document_hash,
                occurred_at, recorded_at
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
            """,
            (
                event["task_id"], event["sequence"], event["event_id"],
                event["event_type"], event["task_status"], event["node_id"],
                fingerprint_hash, event_json, event_hash,
                event["occurred_at"], recorded_at,
            ),
        )
        connection.execute(
            """
            INSERT INTO supervised_run_event_commits(
                task_id, sequence, project_id, principal_id,
                fencing_token, recorded_at, recorded_at_us
            ) VALUES (?, ?, ?, ?, ?, ?, ?)
            """,
            (
                event["task_id"], event["sequence"], project_id, principal_id,
                fencing_token, recorded_at, recorded_at_us,
            ),
        )

    def record_supervised_checkpoint_wait(
        self,
        *,
        intent_id: str,
        checkpoint_proof: Mapping[str, Any],
        checkpoint_event: Mapping[str, Any],
        waiting_event: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedCheckpointWait:
        """Atomically persist a safe checkpoint and enter Waiting."""

        if (
            not isinstance(intent_id, str)
            or not intent_id
            or not isinstance(checkpoint_event, Mapping)
            or not isinstance(waiting_event, Mapping)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("supervised checkpoint wait is invalid")
        proof = _validate_checkpoint_adapter_proof(
            checkpoint_proof, state="waiting", intent_id=intent_id
        )
        checkpoint_event = dict(checkpoint_event)
        waiting_event = dict(waiting_event)
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            recorded_at, recorded_at_us = _runtime_timestamp(supervisor_clock())
            task = connection.execute(
                """
                SELECT task.task_id, task.project_id, task.principal_id,
                       task.status
                FROM dispatch_intents AS intent
                JOIN tasks AS task ON task.task_id = intent.task_id
                WHERE intent.intent_id = ?
                """,
                (intent_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreConflict("checkpoint intent does not exist")
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=recorded_at_us,
                action="supervised checkpoint wait",
            )
            existing = connection.execute(
                "SELECT proof_json FROM worker_checkpoint_waits "
                "WHERE checkpoint_id = ?",
                (proof["checkpoint_id"],),
            ).fetchone()
            if existing is not None:
                stored = _decode_document(
                    existing["proof_json"], label="checkpoint Adapter proof"
                )
                if stored != proof:
                    raise TaskStoreConflict(
                        "checkpoint identity was already used by another proof"
                    )
                snapshot = self._load_snapshot(connection, task["task_id"])
                exact_checkpoint = self._load_task_checkpoint(
                    connection,
                    task["task_id"],
                    task_status=task["status"],
                    checkpoint_id=proof["checkpoint_id"],
                )
                if snapshot is None or exact_checkpoint is None:
                    raise TaskStoreCorruption("checkpoint replay cannot be read")
                connection.commit()
                return SupervisedCheckpointWait(
                    snapshot=snapshot,
                    checkpoint=exact_checkpoint,
                    checkpoint_proof=proof,
                    fencing_token=supervisor_lease.fencing_token,
                    recorded_at=recorded_at,
                    replayed=True,
                )
            intent = self._load_dispatch_intent(
                connection, task_id=task["task_id"]
            )
            if (
                intent is None
                or intent.intent_id != intent_id
                or intent.state != "dispatched"
                or intent.handle is None
                or task["status"] != "Running"
                or intent.adapter_version != "1.6.0"
                or not _is_supported_managed_dispatch(intent)
            ):
                raise TaskStoreConflict(
                    "checkpoint requires a current 1.6 managed dispatch"
                )
            if connection.execute(
                "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                (task["task_id"],),
            ).fetchone() is not None:
                raise TaskStoreConflict(
                    "task cancellation has priority over checkpoint Waiting"
                )
            timeout_owner = connection.execute(
                """
                SELECT 1
                FROM worker_attempt_timeout_windows AS timeout
                WHERE timeout.task_id = ?
                  AND NOT EXISTS (
                      SELECT 1
                      FROM worker_exit_retry_timeout_retirements AS retirement
                      WHERE retirement.timeout_id = timeout.timeout_id
                  )
                  AND NOT EXISTS (
                      SELECT 1 FROM task_timeout_outcomes AS outcome
                      WHERE outcome.timeout_id = timeout.timeout_id
                  )
                  AND (
                      timeout.deadline_at_us <= ?
                      OR EXISTS (
                          SELECT 1 FROM supervised_timeout_attempts AS delivery
                          WHERE delivery.timeout_id = timeout.timeout_id
                      )
                  )
                LIMIT 1
                """,
                (task["task_id"], recorded_at_us),
            ).fetchone()
            if timeout_owner is not None:
                raise TaskStoreConflict(
                    "due timeout has priority over checkpoint Waiting"
                )
            attempt = connection.execute(
                """
                SELECT attempt.*, observation.ready_record_hash,
                       observation.ticket_state,
                       observation.heartbeat_state,
                       observation.heartbeat_record_hash
                FROM worker_launch_attempts AS attempt
                JOIN worker_attempt_observations AS observation
                  ON observation.attempt_id = attempt.attempt_id
                WHERE attempt.intent_id = ?
                  AND attempt.attempt_number = (
                      SELECT MAX(latest.attempt_number)
                      FROM worker_launch_attempts AS latest
                      WHERE latest.intent_id = attempt.intent_id
                  )
                  AND observation.observation_sequence = (
                      SELECT MAX(latest.observation_sequence)
                      FROM worker_attempt_observations AS latest
                      WHERE latest.attempt_id = attempt.attempt_id
                  )
                """,
                (intent_id,),
            ).fetchone()
            effective = connection.execute(
                """
                SELECT outcome_document_json, outcome_document_hash
                FROM effective_dispatched_intents WHERE intent_id = ?
                """,
                (intent_id,),
            ).fetchone()
            if attempt is None or effective is None:
                raise TaskStoreConflict(
                    "checkpoint requires an observed live Worker attempt"
                )
            outcome = _decode_document(
                effective["outcome_document_json"],
                label="effective dispatch outcome",
            )
            source_handle = outcome.get("handle")
            if not isinstance(source_handle, Mapping):
                raise TaskStoreCorruption("effective dispatch handle is invalid")
            _, source_handle_hash = encode_document(source_handle)
            parameters = intent.request.get("parameters")
            iterations = (
                parameters.get("iterations")
                if isinstance(parameters, Mapping)
                else None
            )
            if (
                proof["task_id"] != task["task_id"]
                or proof["node_id"] != intent.node_id
                or proof["submission_id"] != attempt["submission_id"]
                or proof["submission_id"] != source_handle.get("submission_id")
                or proof["attempt_id"] != attempt["attempt_id"]
                or proof["attempt_number"] != attempt["attempt_number"]
                or proof["binding_hash"] != attempt["binding_hash"]
                or proof["ready_record_hash"] != attempt["ready_record_hash"]
                or attempt["ticket_state"] != "spawned"
                or attempt["heartbeat_state"] not in {"running", "waiting"}
                or attempt["heartbeat_record_hash"] is None
                or type(iterations) is not int
                or proof["completed_updates"] > iterations
            ):
                raise TaskStoreConflict(
                    "checkpoint proof differs from the exact live attempt"
                )
            checkpoint_index = int(
                connection.execute(
                    "SELECT COALESCE(MAX(checkpoint_index), 0) + 1 "
                    "FROM worker_checkpoint_waits "
                    "WHERE task_id = ? AND attempt_id = ?",
                    (task["task_id"], proof["attempt_id"]),
                ).fetchone()[0]
            )
            next_sequence = int(
                connection.execute(
                    "SELECT COALESCE(MAX(sequence), 0) + 1 "
                    "FROM run_events WHERE task_id = ?",
                    (task["task_id"],),
                ).fetchone()[0]
            )
            extension = {
                "org.agent_rpc.checkpoint_wait": {
                    "checkpoint_id": proof["checkpoint_id"],
                    "checkpoint_index": proof["checkpoint_index"],
                    "completed_updates": proof["completed_updates"],
                    "same_attempt": True,
                }
            }
            handle_fingerprint = intent.handle.get("fingerprint")
            if (
                not isinstance(handle_fingerprint, Mapping)
                or proof["checkpoint_index"] != checkpoint_index
                or checkpoint_event.get("task_id") != task["task_id"]
                or checkpoint_event.get("sequence") != next_sequence
                or checkpoint_event.get("event_type") != "checkpoint_created"
                or checkpoint_event.get("task_status") != "Running"
                or checkpoint_event.get("node_id") != intent.node_id
                or checkpoint_event.get("fingerprint") != handle_fingerprint
                or checkpoint_event.get("checkpoint")
                != {"relative_path": proof["checkpoint_manifest_relative_path"]}
                or checkpoint_event.get("extensions") != extension
                or checkpoint_event.get("occurred_at")
                != proof["checkpoint_created_at"]
                or waiting_event.get("task_id") != task["task_id"]
                or waiting_event.get("sequence") != next_sequence + 1
                or waiting_event.get("event_type") != "node_waiting"
                or waiting_event.get("task_status") != "Waiting"
                or waiting_event.get("node_id") != intent.node_id
                or waiting_event.get("fingerprint") != handle_fingerprint
                or waiting_event.get("extensions") != extension
            ):
                raise TaskStoreConflict(
                    "checkpoint Waiting events do not bind the exact proof"
                )
            created_at, created_at_us = _runtime_timestamp(
                proof["checkpoint_created_at"]
            )
            _, waiting_at_us = _runtime_timestamp(waiting_event.get("occurred_at"))
            if created_at_us > waiting_at_us or waiting_at_us > recorded_at_us:
                raise TaskStoreConflict("checkpoint event time order is invalid")
            _, fingerprint_hash = encode_document(handle_fingerprint)
            self._insert_checkpoint_run_event(
                connection,
                event=checkpoint_event,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                fingerprint_hash=fingerprint_hash,
                fencing_token=supervisor_lease.fencing_token,
                recorded_at=recorded_at,
                recorded_at_us=recorded_at_us,
            )
            self._insert_checkpoint_run_event(
                connection,
                event=waiting_event,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                fingerprint_hash=fingerprint_hash,
                fencing_token=supervisor_lease.fencing_token,
                recorded_at=recorded_at,
                recorded_at_us=recorded_at_us,
            )
            proof_json, _ = encode_document(proof)
            connection.execute(
                """
                INSERT INTO worker_checkpoint_waits(
                    checkpoint_id, task_id, project_id, principal_id,
                    intent_id, node_id, submission_id, attempt_id,
                    attempt_number, checkpoint_index, completed_updates,
                    checkpoint_manifest_relative_path,
                    checkpoint_manifest_size_bytes, binding_hash,
                    submission_receipt_record_hash, ready_record_hash,
                    checkpoint_manifest_hash, checkpoint_receipt_record_hash,
                    checkpoint_created_at, checkpoint_created_at_us,
                    source_outcome_document_hash, source_handle_hash,
                    proof_json, proof_hash, checkpoint_event_sequence,
                    waiting_event_sequence, fencing_token,
                    recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?,
                          ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    proof["checkpoint_id"], task["task_id"],
                    task["project_id"], task["principal_id"], intent_id,
                    intent.node_id, proof["submission_id"], proof["attempt_id"],
                    proof["attempt_number"], proof["checkpoint_index"],
                    proof["completed_updates"],
                    proof["checkpoint_manifest_relative_path"],
                    proof["checkpoint_manifest_size_bytes"],
                    proof["binding_hash"],
                    proof["submission_receipt_record_hash"],
                    proof["ready_record_hash"],
                    proof["checkpoint_manifest_hash"],
                    proof["checkpoint_receipt_record_hash"],
                    created_at, created_at_us,
                    effective["outcome_document_hash"], source_handle_hash,
                    proof_json, proof["proof_hash"], next_sequence,
                    next_sequence + 1, supervisor_lease.fencing_token,
                    recorded_at, recorded_at_us,
                ),
            )
            connection.execute(
                "UPDATE tasks SET status = 'Waiting', updated_at = ? "
                "WHERE task_id = ?",
                (recorded_at, task["task_id"]),
            )
            snapshot = self._load_snapshot(connection, task["task_id"])
            if snapshot is None or snapshot.checkpoint is None:
                raise TaskStoreCorruption("checkpoint wait cannot be read")
            connection.commit()
            return SupervisedCheckpointWait(
                snapshot=snapshot,
                checkpoint=snapshot.checkpoint,
                checkpoint_proof=proof,
                fencing_token=supervisor_lease.fencing_token,
                recorded_at=recorded_at,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "checkpoint wait lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "checkpoint wait conflicts with durable state"
            ) from error
        finally:
            connection.close()

    def request_checkpoint_resume(
        self,
        *,
        task_id: str,
        project_id: str,
        principal_id: str,
        idempotency_key: str,
        request_hash: str,
        clock: Callable[[], str],
    ) -> TaskCheckpointResumeRecord:
        """Persist one deterministic, server-owned resume admission."""

        if (
            any(
                not isinstance(value, str) or not value
                for value in (
                    task_id, project_id, principal_id,
                    idempotency_key, request_hash,
                )
            )
            or not _is_sha256(request_hash)
            or not callable(clock)
        ):
            raise TaskStoreConflict("checkpoint resume request is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            requested_at, requested_at_us = _runtime_timestamp(clock())
            task = connection.execute(
                "SELECT project_id, principal_id, status FROM tasks "
                "WHERE task_id = ?",
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreConflict("task does not exist")
            if (
                task["project_id"] != project_id
                or task["principal_id"] != principal_id
            ):
                raise TaskStoreConflict("checkpoint resume crosses task scope")
            snapshot = self._load_snapshot(connection, task_id)
            if (
                snapshot is None
                or snapshot.status != "Waiting"
                or snapshot.checkpoint is None
                or snapshot.checkpoint.state
                not in {"waiting", "resume_requested"}
            ):
                raise TaskStoreConflict(
                    "checkpoint resume requires the current Waiting checkpoint"
                )
            checkpoint = snapshot.checkpoint
            expected_idempotency_key = (
                f"checkpoint-resume:{checkpoint.checkpoint_id}"
            )
            admission = {
                "schema_version": "1.0.0",
                "task_id": task_id,
                "checkpoint_id": checkpoint.checkpoint_id,
                "action": "resume_exact_checkpoint",
                "extensions": {},
            }
            _, expected_request_hash = encode_document(admission)
            identity = "\x1f".join(
                (task_id, checkpoint.checkpoint_id, "resume_exact_checkpoint")
            )
            resume_id = "resume-" + hashlib.sha256(
                identity.encode("utf-8")
            ).hexdigest()[:32]
            if (
                idempotency_key != expected_idempotency_key
                or request_hash != expected_request_hash
            ):
                raise TaskStoreConflict(
                    "checkpoint resume request differs from its server identity"
                )
            existing = connection.execute(
                """
                SELECT resume_id, checkpoint_id, task_id, request_hash
                FROM task_checkpoint_resume_requests
                WHERE project_id = ? AND principal_id = ?
                  AND idempotency_key = ?
                """,
                (project_id, principal_id, idempotency_key),
            ).fetchone()
            if existing is not None:
                if (
                    existing["resume_id"] != resume_id
                    or existing["checkpoint_id"] != checkpoint.checkpoint_id
                    or existing["task_id"] != task_id
                    or existing["request_hash"] != request_hash
                ):
                    raise IdempotencyConflict(
                        "checkpoint resume idempotency key was already used"
                    )
                connection.commit()
                return TaskCheckpointResumeRecord(
                    snapshot=snapshot,
                    checkpoint=checkpoint,
                    resume_id=resume_id,
                    request_record_hash=request_hash,
                    replayed=True,
                )
            if snapshot.cancellation is not None:
                raise TaskStoreConflict(
                    "task cancellation already owns the Waiting attempt"
                )
            if snapshot.timeout is not None and snapshot.timeout.state in {
                "requested", "timed_out"
            }:
                raise TaskStoreConflict(
                    "authorized timeout already owns the Waiting attempt"
                )
            request_document = {
                **admission,
                "resume_id": resume_id,
                "requested_at": requested_at,
            }
            # Keep the immutable field order irrelevant while matching the
            # exact loader shape.
            request_document = {
                "schema_version": request_document["schema_version"],
                "resume_id": request_document["resume_id"],
                "task_id": request_document["task_id"],
                "checkpoint_id": request_document["checkpoint_id"],
                "action": request_document["action"],
                "requested_at": request_document["requested_at"],
                "extensions": request_document["extensions"],
            }
            document_json, document_hash = encode_document(request_document)
            connection.execute(
                """
                INSERT INTO task_checkpoint_resume_requests(
                    resume_id, checkpoint_id, task_id, project_id,
                    principal_id, intent_id, attempt_id, idempotency_key,
                    request_hash, document_json, document_hash,
                    requested_at, requested_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    resume_id, checkpoint.checkpoint_id, task_id, project_id,
                    principal_id, checkpoint.intent_id, checkpoint.attempt_id,
                    idempotency_key, request_hash, document_json, document_hash,
                    requested_at, requested_at_us,
                ),
            )
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None or snapshot.checkpoint is None:
                raise TaskStoreCorruption("checkpoint resume request cannot be read")
            connection.commit()
            return TaskCheckpointResumeRecord(
                snapshot=snapshot,
                checkpoint=snapshot.checkpoint,
                resume_id=resume_id,
                request_record_hash=request_hash,
                replayed=False,
            )
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            raise TaskStoreConflict(
                "checkpoint resume request conflicts with durable state"
            ) from error
        finally:
            connection.close()

    def authorize_supervised_checkpoint_resume(
        self,
        *,
        resume_id: str,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> SupervisedCheckpointResumeAuthorization:
        """Fence and authorize delivery to the same live Worker attempt."""

        if (
            not isinstance(resume_id, str)
            or RUNTIME_RESUME_ID.fullmatch(resume_id) is None
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict(
                "checkpoint resume authorization is invalid"
            )
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            authorized_at, authorized_at_us = _runtime_timestamp(
                supervisor_clock()
            )
            row = connection.execute(
                """
                SELECT request.task_id, request.project_id,
                       request.principal_id, request.checkpoint_id,
                       request.intent_id, request.attempt_id,
                       request.requested_at, request.requested_at_us,
                       checkpoint.node_id, checkpoint.submission_id,
                       checkpoint.attempt_number,
                       checkpoint.checkpoint_manifest_hash,
                       checkpoint.checkpoint_receipt_record_hash,
                       checkpoint.proof_hash AS checkpoint_proof_hash,
                       task.status
                FROM task_checkpoint_resume_requests AS request
                JOIN worker_checkpoint_waits AS checkpoint
                  ON checkpoint.checkpoint_id = request.checkpoint_id
                JOIN tasks AS task ON task.task_id = request.task_id
                WHERE request.resume_id = ?
                """,
                (resume_id,),
            ).fetchone()
            if row is None:
                raise TaskStoreConflict(
                    "checkpoint resume request does not exist"
                )
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=row["project_id"],
                principal_id=row["principal_id"],
                supplied=supervisor_lease,
                now_us=authorized_at_us,
                action="checkpoint resume authorization",
            )
            requested_at, requested_at_us = _runtime_timestamp(
                row["requested_at"]
            )
            if requested_at_us != int(row["requested_at_us"]):
                raise TaskStoreCorruption(
                    "checkpoint resume request time is inconsistent"
                )
            if authorized_at_us < requested_at_us:
                raise TaskStoreConflict(
                    "checkpoint resume authorization clock regressed"
                )
            snapshot = self._load_snapshot(connection, row["task_id"])
            if (
                snapshot is None
                or snapshot.status != "Waiting"
                or snapshot.checkpoint is None
                or snapshot.checkpoint.resume_id != resume_id
                or snapshot.checkpoint.state != "resume_requested"
                or snapshot.cancellation is not None
                or (
                    snapshot.timeout is not None
                    and snapshot.timeout.state in {"requested", "timed_out"}
                )
            ):
                raise TaskStoreConflict(
                    "checkpoint resume is no longer deliverable"
                )
            request_payload = {
                "schema_version": "1.0.0",
                "resume_id": resume_id,
                "submission_id": row["submission_id"],
                "attempt_id": row["attempt_id"],
                "attempt_number": row["attempt_number"],
                "checkpoint_id": row["checkpoint_id"],
                "checkpoint_manifest_hash": row[
                    "checkpoint_manifest_hash"
                ],
                "checkpoint_receipt_record_hash": row[
                    "checkpoint_receipt_record_hash"
                ],
                "checkpoint_proof_hash": row["checkpoint_proof_hash"],
                # This is the immutable Worker-request causal time.  The
                # per-term authorization time stays in SQLite below.
                "authorized_at": requested_at,
            }
            _, resume_request_record_hash = encode_document(request_payload)
            resume_request_document = {
                **request_payload,
                "record_hash": resume_request_record_hash,
            }
            existing = connection.execute(
                """
                SELECT resume_request_json, resume_request_record_hash,
                       authorization_json, authorization_hash,
                       fencing_token, authorized_at, authorized_at_us
                FROM supervised_checkpoint_resume_authorizations
                WHERE resume_id = ? AND fencing_token = ?
                """,
                (resume_id, supervisor_lease.fencing_token),
            ).fetchone()
            if existing is not None:
                stored_authorized_at, stored_authorized_at_us = (
                    _runtime_timestamp(existing["authorized_at"])
                )
                authorization = SupervisedCheckpointResumeAuthorization(
                    checkpoint=snapshot.checkpoint,
                    task_id=row["task_id"],
                    resume_id=resume_id,
                    resume_request_record_hash=resume_request_record_hash,
                    checkpoint_proof_hash=row["checkpoint_proof_hash"],
                    requested_at=requested_at,
                    authorized_at=stored_authorized_at,
                    fencing_token=supervisor_lease.fencing_token,
                    replayed=True,
                )
                expected_token = authorization.adapter_token()
                token = _decode_document(
                    existing["authorization_json"],
                    label="checkpoint resume authorization",
                )
                stored_request = _decode_document(
                    existing["resume_request_json"],
                    label="checkpoint Worker resume request",
                )
                if (
                    int(existing["fencing_token"])
                    != supervisor_lease.fencing_token
                    or stored_authorized_at_us
                    != int(existing["authorized_at_us"])
                    or stored_authorized_at_us < requested_at_us
                    or existing["resume_request_record_hash"]
                    != resume_request_record_hash
                    or stored_request != resume_request_document
                    or token != expected_token
                    or encode_document(token)[1]
                    != existing["authorization_hash"]
                ):
                    raise TaskStoreCorruption(
                        "checkpoint resume authorization is inconsistent"
                    )
                connection.commit()
                return authorization
            attempt = connection.execute(
                """
                SELECT attempt.submission_id, attempt.attempt_number,
                       observation.ticket_state,
                       observation.ready_record_hash,
                       observation.heartbeat_state,
                       observation.heartbeat_record_hash
                FROM worker_launch_attempts AS attempt
                JOIN worker_attempt_observations AS observation
                  ON observation.attempt_id = attempt.attempt_id
                WHERE attempt.intent_id = ? AND attempt.attempt_id = ?
                  AND attempt.attempt_number = (
                      SELECT MAX(latest.attempt_number)
                      FROM worker_launch_attempts AS latest
                      WHERE latest.intent_id = attempt.intent_id
                  )
                  AND observation.observation_sequence = (
                      SELECT MAX(latest.observation_sequence)
                      FROM worker_attempt_observations AS latest
                      WHERE latest.attempt_id = attempt.attempt_id
                  )
                """,
                (row["intent_id"], row["attempt_id"]),
            ).fetchone()
            if (
                attempt is None
                or attempt["submission_id"] != row["submission_id"]
                or attempt["attempt_number"] != row["attempt_number"]
                or attempt["ticket_state"] != "spawned"
                or attempt["ready_record_hash"] is None
                or attempt["heartbeat_state"] not in {"running", "waiting"}
                or attempt["heartbeat_record_hash"] is None
            ):
                raise TaskStoreConflict(
                    "checkpoint Worker attempt is no longer live"
                )
            resume_request_json, _ = encode_document(resume_request_document)
            authorization = SupervisedCheckpointResumeAuthorization(
                checkpoint=snapshot.checkpoint,
                task_id=row["task_id"],
                resume_id=resume_id,
                resume_request_record_hash=resume_request_record_hash,
                checkpoint_proof_hash=row["checkpoint_proof_hash"],
                requested_at=requested_at,
                authorized_at=authorized_at,
                fencing_token=supervisor_lease.fencing_token,
                replayed=False,
            )
            token = authorization.adapter_token()
            token_json, token_hash = encode_document(token)
            connection.execute(
                """
                INSERT INTO supervised_checkpoint_resume_authorizations(
                    resume_id, checkpoint_id, task_id, project_id,
                    principal_id, intent_id, attempt_id,
                    resume_request_json, resume_request_record_hash,
                    authorization_json, authorization_hash,
                    fencing_token, authorized_at, authorized_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    resume_id, row["checkpoint_id"], row["task_id"],
                    row["project_id"], row["principal_id"], row["intent_id"],
                    row["attempt_id"], resume_request_json,
                    resume_request_record_hash, token_json, token_hash,
                    supervisor_lease.fencing_token, authorized_at,
                    authorized_at_us,
                ),
            )
            connection.commit()
            return authorization
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "checkpoint resume authorization lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "checkpoint resume authorization conflicts with durable state"
            ) from error
        finally:
            connection.close()

    def complete_supervised_checkpoint_resume(
        self,
        *,
        resume_id: str,
        adapter_proof: Mapping[str, Any],
        running_event: Mapping[str, Any],
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> TaskCheckpointResumeRecord:
        """Commit exact resume acknowledgement and Waiting -> Running."""

        if (
            not isinstance(resume_id, str)
            or RUNTIME_RESUME_ID.fullmatch(resume_id) is None
            or not isinstance(adapter_proof, Mapping)
            or not isinstance(running_event, Mapping)
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("checkpoint resume completion is invalid")
        running_event = dict(running_event)
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            resumed_at, resumed_at_us = _runtime_timestamp(supervisor_clock())
            row = connection.execute(
                """
                SELECT request.task_id, request.project_id,
                       request.principal_id, request.request_hash,
                       request.checkpoint_id, request.intent_id,
                       request.attempt_id, request.requested_at,
                       request.requested_at_us, checkpoint.node_id,
                       checkpoint.submission_id, checkpoint.attempt_number,
                       checkpoint.proof_json AS waiting_proof_json,
                       checkpoint.proof_hash AS checkpoint_proof_hash,
                       task.status
                FROM task_checkpoint_resume_requests AS request
                JOIN worker_checkpoint_waits AS checkpoint
                  ON checkpoint.checkpoint_id = request.checkpoint_id
                JOIN tasks AS task ON task.task_id = request.task_id
                WHERE request.resume_id = ?
                """,
                (resume_id,),
            ).fetchone()
            if row is None:
                raise TaskStoreConflict(
                    "checkpoint resume request does not exist"
                )
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=row["project_id"],
                principal_id=row["principal_id"],
                supplied=supervisor_lease,
                now_us=resumed_at_us,
                action="checkpoint resume completion",
            )
            waiting_proof = _decode_document(
                row["waiting_proof_json"], label="checkpoint Adapter proof"
            )
            existing = connection.execute(
                """
                SELECT outcome.adapter_proof_json, outcome.adapter_proof_hash,
                       authorization.resume_request_record_hash,
                       authorization.authorization_hash,
                       authorization.authorized_at,
                       authorization.authorized_at_us
                FROM task_checkpoint_resume_outcomes AS outcome
                JOIN supervised_checkpoint_resume_authorizations AS authorization
                  ON authorization.resume_id = outcome.resume_id
                 AND authorization.fencing_token = outcome.fencing_token
                 AND authorization.authorization_hash = outcome.authorization_hash
                WHERE outcome.resume_id = ?
                """,
                (resume_id,),
            ).fetchone()
            if existing is None:
                authorization = connection.execute(
                    """
                    SELECT resume_request_record_hash, authorization_hash,
                           authorized_at, authorized_at_us
                    FROM supervised_checkpoint_resume_authorizations
                    WHERE resume_id = ? AND fencing_token = ?
                    """,
                    (resume_id, supervisor_lease.fencing_token),
                ).fetchone()
                if authorization is None:
                    raise TaskStoreConflict(
                        "checkpoint resume lacks this term's authorization"
                    )
            else:
                authorization = existing
            resume_request_record_hash = authorization[
                "resume_request_record_hash"
            ]
            authorization_hash = authorization["authorization_hash"]
            authorization_authorized_at = authorization["authorized_at"]
            authorization_authorized_at_us = int(
                authorization["authorized_at_us"]
            )
            proof = _validate_checkpoint_adapter_proof(
                adapter_proof,
                state="resumed",
                task_id=row["task_id"],
                node_id=row["node_id"],
                submission_id=row["submission_id"],
                attempt_id=row["attempt_id"],
                attempt_number=row["attempt_number"],
                checkpoint_id=row["checkpoint_id"],
                checkpoint_proof_hash=row["checkpoint_proof_hash"],
                resume_id=resume_id,
                resume_request_record_hash=resume_request_record_hash,
            )
            stable_fields = {
                "schema_version",
                "task_id",
                "node_id",
                "submission_id",
                "attempt_id",
                "attempt_number",
                "checkpoint_id",
                "checkpoint_index",
                "completed_updates",
                "binding_hash",
                "submission_receipt_record_hash",
                "ready_record_hash",
                "checkpoint_manifest_relative_path",
                "checkpoint_manifest_size_bytes",
                "checkpoint_manifest_hash",
                "checkpoint_receipt_record_hash",
                "checkpoint_created_at",
            }
            if any(
                proof.get(field) != waiting_proof.get(field)
                for field in stable_fields
            ):
                raise TaskStoreConflict(
                    "resume acknowledgement differs from its checkpoint proof"
                )
            proof_json, proof_document_hash = encode_document(proof)
            if existing is not None:
                stored_proof = _decode_document(
                    existing["adapter_proof_json"],
                    label="checkpoint resume Adapter proof",
                )
                if (
                    existing["adapter_proof_hash"]
                    != encode_document(stored_proof)[1]
                ):
                    raise TaskStoreCorruption(
                        "checkpoint resume outcome proof hash is inconsistent"
                    )
                if stored_proof != proof:
                    raise TaskStoreConflict(
                        "checkpoint resume was completed with another proof"
                    )
                snapshot = self._load_snapshot(connection, row["task_id"])
                exact_checkpoint = self._load_task_checkpoint(
                    connection,
                    row["task_id"],
                    task_status=row["status"],
                    checkpoint_id=row["checkpoint_id"],
                )
                if snapshot is None or exact_checkpoint is None:
                    raise TaskStoreCorruption(
                        "checkpoint resume replay cannot be read"
                    )
                connection.commit()
                return TaskCheckpointResumeRecord(
                    snapshot=snapshot,
                    checkpoint=exact_checkpoint,
                    resume_id=resume_id,
                    request_record_hash=resume_request_record_hash,
                    replayed=True,
                )
            snapshot = self._load_snapshot(connection, row["task_id"])
            if (
                row["status"] != "Waiting"
                or snapshot is None
                or snapshot.checkpoint is None
                or snapshot.checkpoint.state != "resume_requested"
                or snapshot.checkpoint.resume_id != resume_id
                or snapshot.cancellation is not None
                or (
                    snapshot.timeout is not None
                    and snapshot.timeout.state in {"requested", "timed_out"}
                )
            ):
                raise TaskStoreConflict(
                    "checkpoint resume is no longer completable"
                )
            attempt = connection.execute(
                """
                SELECT attempt.submission_id, attempt.attempt_number,
                       observation.ticket_state,
                       observation.heartbeat_state,
                       observation.heartbeat_record_hash
                FROM worker_launch_attempts AS attempt
                JOIN worker_attempt_observations AS observation
                  ON observation.attempt_id = attempt.attempt_id
                WHERE attempt.intent_id = ? AND attempt.attempt_id = ?
                  AND attempt.attempt_number = (
                      SELECT MAX(latest.attempt_number)
                      FROM worker_launch_attempts AS latest
                      WHERE latest.intent_id = attempt.intent_id
                  )
                  AND observation.observation_sequence = (
                      SELECT MAX(latest.observation_sequence)
                      FROM worker_attempt_observations AS latest
                      WHERE latest.attempt_id = attempt.attempt_id
                  )
                """,
                (row["intent_id"], row["attempt_id"]),
            ).fetchone()
            intent = self._load_dispatch_intent(
                connection, task_id=row["task_id"]
            )
            if (
                attempt is None
                or intent is None
                or intent.handle is None
                or intent.intent_id != row["intent_id"]
                or intent.adapter_version != "1.6.0"
                or attempt["submission_id"] != row["submission_id"]
                or attempt["attempt_number"] != row["attempt_number"]
                or attempt["ticket_state"] != "spawned"
                or attempt["heartbeat_state"] not in {"running", "waiting"}
                or attempt["heartbeat_record_hash"] is None
            ):
                raise TaskStoreConflict(
                    "checkpoint Worker attempt is no longer live"
                )
            next_sequence = int(
                connection.execute(
                    "SELECT COALESCE(MAX(sequence), 0) + 1 "
                    "FROM run_events WHERE task_id = ?",
                    (row["task_id"],),
                ).fetchone()[0]
            )
            extension = {
                "org.agent_rpc.checkpoint_resume": {
                    "checkpoint_id": row["checkpoint_id"],
                    "resume_id": resume_id,
                    "same_attempt": True,
                }
            }
            handle_fingerprint = intent.handle.get("fingerprint")
            acknowledged_at, acknowledged_at_us = _runtime_timestamp(
                proof["resume_acknowledged_at"]
            )
            requested_at, requested_at_us = _runtime_timestamp(
                row["requested_at"]
            )
            stored_authorized_at, stored_authorized_at_us = _runtime_timestamp(
                authorization_authorized_at
            )
            first_authorization = connection.execute(
                """
                SELECT MIN(authorized_at_us) AS first_authorized_at_us
                FROM supervised_checkpoint_resume_authorizations
                WHERE resume_id = ? AND resume_request_record_hash = ?
                """,
                (resume_id, resume_request_record_hash),
            ).fetchone()
            first_authorized_at_us = (
                None
                if first_authorization is None
                else first_authorization["first_authorized_at_us"]
            )
            if (
                not isinstance(handle_fingerprint, Mapping)
                or requested_at != row["requested_at"]
                or requested_at_us != int(row["requested_at_us"])
                or stored_authorized_at != authorization_authorized_at
                or stored_authorized_at_us != authorization_authorized_at_us
                or first_authorized_at_us is None
                or requested_at_us > int(first_authorized_at_us)
                or int(first_authorized_at_us) > acknowledged_at_us
                or authorization_authorized_at_us < requested_at_us
                or authorization_authorized_at_us > resumed_at_us
                or running_event.get("task_id") != row["task_id"]
                or running_event.get("sequence") != next_sequence
                or running_event.get("event_type") != "node_started"
                or running_event.get("task_status") != "Running"
                or running_event.get("node_id") != row["node_id"]
                or running_event.get("fingerprint") != handle_fingerprint
                or running_event.get("extensions") != extension
                or running_event.get("occurred_at") != acknowledged_at
                or acknowledged_at_us > resumed_at_us
            ):
                raise TaskStoreConflict(
                    "resume Running event does not bind the exact acknowledgement"
                )
            _, fingerprint_hash = encode_document(handle_fingerprint)
            self._insert_checkpoint_run_event(
                connection,
                event=running_event,
                project_id=row["project_id"],
                principal_id=row["principal_id"],
                fingerprint_hash=fingerprint_hash,
                fencing_token=supervisor_lease.fencing_token,
                recorded_at=resumed_at,
                recorded_at_us=resumed_at_us,
            )
            connection.execute(
                """
                INSERT INTO task_checkpoint_resume_outcomes(
                    resume_id, checkpoint_id, task_id, project_id,
                    principal_id, intent_id, attempt_id, authorization_hash,
                    adapter_proof_json, adapter_proof_hash,
                    resume_acknowledged_at, resume_acknowledged_at_us,
                    running_event_sequence, fencing_token,
                    resumed_at, resumed_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    resume_id, row["checkpoint_id"], row["task_id"],
                    row["project_id"], row["principal_id"], row["intent_id"],
                    row["attempt_id"], authorization_hash, proof_json,
                    proof_document_hash, acknowledged_at, acknowledged_at_us,
                    next_sequence, supervisor_lease.fencing_token,
                    resumed_at, resumed_at_us,
                ),
            )
            connection.execute(
                "UPDATE tasks SET status = 'Running', updated_at = ? "
                "WHERE task_id = ?",
                (resumed_at, row["task_id"]),
            )
            snapshot = self._load_snapshot(connection, row["task_id"])
            if snapshot is None or snapshot.checkpoint is None:
                raise TaskStoreCorruption("checkpoint resume cannot be read")
            connection.commit()
            return TaskCheckpointResumeRecord(
                snapshot=snapshot,
                checkpoint=snapshot.checkpoint,
                resume_id=resume_id,
                request_record_hash=resume_request_record_hash,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "checkpoint resume completion lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "checkpoint resume completion conflicts with durable state"
            ) from error
        finally:
            connection.close()

    def commit_dag_node_runtime_transition(
        self,
        *,
        intent_id: str,
        expected_node_state: str,
        event: Mapping[str, Any],
        adapter_status: Mapping[str, Any],
        output_receipt: Mapping[str, Any] | None,
        supervisor_lease: RuntimeSupervisorLease,
        supervisor_clock: Callable[[], str],
    ) -> DagNodeTerminalFact | TaskSnapshot:
        """Append one fenced DAG node/RunEvent projection without ending the DAG."""

        if (
            not isinstance(intent_id, str)
            or not intent_id
            or expected_node_state not in {"Queued", "Running"}
            or not isinstance(event, Mapping)
            or not isinstance(adapter_status, Mapping)
            or (output_receipt is not None and not isinstance(output_receipt, Mapping))
            or not isinstance(supervisor_lease, RuntimeSupervisorLease)
            or not callable(supervisor_clock)
        ):
            raise TaskStoreConflict("DAG node runtime transition is invalid")
        event = copy.deepcopy(dict(event))
        adapter_status = copy.deepcopy(dict(adapter_status))
        supplied_output = (
            None if output_receipt is None else copy.deepcopy(dict(output_receipt))
        )
        event_json, event_hash = encode_document(event)
        _, event_fingerprint_hash = encode_document(event.get("fingerprint", {}))
        adapter_status_json, adapter_status_hash = encode_document(adapter_status)
        event_type = event.get("event_type")
        if event_type not in {
            "node_started",
            "node_progress",
            "node_succeeded",
            "node_failed",
            "node_cancelled",
        }:
            raise TaskStoreConflict("DAG node event type is unsupported")
        terminal_state = {
            "node_succeeded": "Succeeded",
            "node_failed": "Failed",
            "node_cancelled": "Cancelled",
        }.get(event_type)
        if (
            event.get("task_status")
            not in ({"Running"} if terminal_state is None else {
                "Running", "Succeeded", "Failed"
            })
            or (terminal_state == "Succeeded") != (supplied_output is not None)
        ):
            raise TaskStoreConflict("DAG node event projection is inconsistent")

        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            recorded_at, recorded_at_us = _runtime_timestamp(supervisor_clock())
            try:
                _, event_occurred_at_us = _runtime_timestamp(
                    event.get("occurred_at")
                )
            except TaskStoreConflict as error:
                raise TaskStoreConflict(
                    "DAG node event time is invalid"
                ) from error
            if event_occurred_at_us > recorded_at_us:
                raise TaskStoreConflict(
                    "DAG node event cannot occur after its durable commit"
                )
            task_id = self._task_id_for_intent(connection, intent_id)
            if task_id is None:
                raise TaskStoreConflict("dispatch intent does not exist")
            intent = self._load_dispatch_intent(connection, intent_id=intent_id)
            if intent is None or intent.intent_id != intent_id:
                raise TaskStoreCorruption("dispatch intent cannot be read")
            task = connection.execute(
                "SELECT project_id, principal_id, status FROM tasks WHERE task_id = ?",
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreCorruption("DAG runtime task cannot be read")
            if event_type == "node_cancelled" and (
                expected_node_state != "Running"
                or adapter_status.get("status") != "Cancelled"
                or adapter_status.get("terminal") is not True
                or connection.execute(
                    "SELECT 1 FROM task_cancel_requests WHERE task_id = ?",
                    (task_id,),
                ).fetchone()
                is not None
            ):
                raise TaskStoreConflict(
                    "DAG node cancellation requires an exact node-local Adapter status"
                )
            self._require_active_runtime_supervisor_lease(
                connection,
                project_id=task["project_id"],
                principal_id=task["principal_id"],
                supplied=supervisor_lease,
                now_us=recorded_at_us,
                action="DAG node runtime transition",
            )
            admission = self._require_current_dag_node_execution(
                connection,
                intent=intent,
                allowed_node_states=frozenset({expected_node_state}),
            )
            if admission is None:
                raise TaskStoreConflict("dispatch intent is not a DAG node execution")
            latest_state = connection.execute(
                """
                SELECT revision, state FROM dag_node_state_events
                WHERE task_id = ? AND plan_id = ? AND node_id = ?
                ORDER BY revision DESC LIMIT 1
                """,
                (task_id, intent.plan_id, intent.node_id),
            ).fetchone()
            if (
                latest_state is None
                or latest_state["state"] != expected_node_state
                or event.get("task_id") != task_id
                or event.get("node_id") != intent.node_id
                or intent.handle is None
                or event.get("fingerprint") != intent.handle.get("fingerprint")
                or task["project_id"] != supervisor_lease.project_id
                or task["principal_id"] != supervisor_lease.principal_id
            ):
                raise TaskStoreConflict(
                    "DAG node transition differs from current durable state"
                )
            if event_type == "node_started" and (
                expected_node_state != "Queued"
                or task["status"] not in {"Queued", "Running"}
            ):
                raise TaskStoreConflict("DAG node start requires Queued state")
            if event_type == "node_progress" and (
                expected_node_state != "Running" or task["status"] != "Running"
            ):
                raise TaskStoreConflict("DAG node progress requires Running state")
            if terminal_state is not None and task["status"] not in {
                "Queued",
                "Running",
            }:
                raise TaskStoreConflict("DAG node terminal race was already lost")
            next_sequence = int(
                connection.execute(
                    "SELECT COALESCE(MAX(sequence), 0) + 1 FROM run_events "
                    "WHERE task_id = ?",
                    (task_id,),
                ).fetchone()[0]
            )
            if event.get("sequence") != next_sequence:
                raise TaskStoreConflict("run event sequence is not the next value")
            baseline = connection.execute(
                """
                SELECT fingerprint_hash FROM run_events
                WHERE task_id = ? AND node_id = ?
                ORDER BY sequence ASC LIMIT 1
                """,
                (task_id, intent.node_id),
            ).fetchone()
            if (
                baseline is not None
                and baseline["fingerprint_hash"] != event_fingerprint_hash
            ):
                raise TaskStoreConflict("run fingerprint changed within a DAG node")

            plan_row = connection.execute(
                """
                SELECT document_json, document_hash FROM plans
                WHERE task_id = ? AND plan_id = ?
                """,
                (task_id, intent.plan_id),
            ).fetchone()
            if plan_row is None:
                raise TaskStoreCorruption("DAG runtime plan cannot be read")
            plan_document = _decode_hashed_document(
                plan_row, label="DAG runtime aggregate plan"
            )
            aggregate_status = "Running"
            if terminal_state is not None:
                state_map = self._load_dag_node_state_map(
                    connection, task_id=task_id, plan=plan_document
                )
                if state_map is None:
                    raise TaskStoreCorruption("DAG runtime state map cannot be read")
                hypothetical_states = {
                    node.node_id: node.state for node in state_map.nodes
                }
                hypothetical_states[intent.node_id] = terminal_state
                try:
                    hypothetical_readiness = evaluate_dag_readiness(
                        plan_document, node_states=hypothetical_states
                    )
                except DagScheduleError as error:
                    raise TaskStoreCorruption(
                        "DAG terminal aggregate cannot be evaluated"
                    ) from error
                aggregate_status = self._dag_aggregate_status(
                    hypothetical_readiness
                )
            if event.get("task_status") != aggregate_status:
                raise TaskStoreConflict(
                    "DAG node event differs from the exact aggregate status"
                )
            connection.execute(
                """
                INSERT INTO run_events(
                    task_id, sequence, event_id, event_type, task_status,
                    node_id, fingerprint_hash, document_json, document_hash,
                    occurred_at, recorded_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    next_sequence,
                    event["event_id"],
                    event_type,
                    aggregate_status,
                    intent.node_id,
                    event_fingerprint_hash,
                    event_json,
                    event_hash,
                    event["occurred_at"],
                    recorded_at,
                ),
            )
            connection.execute(
                """
                INSERT INTO supervised_run_event_commits(
                    task_id, sequence, project_id, principal_id,
                    fencing_token, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    next_sequence,
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    recorded_at,
                    recorded_at_us,
                ),
            )
            if event_type == "node_progress":
                connection.execute(
                    "UPDATE tasks SET updated_at = ? WHERE task_id = ?",
                    (recorded_at, task_id),
                )
                snapshot = self._load_snapshot(connection, task_id)
                if snapshot is None or snapshot.status != "Running":
                    raise TaskStoreCorruption("DAG progress cannot be read")
                connection.commit()
                return snapshot

            node_revision = int(latest_state["revision"]) + 1
            new_state = "Running" if event_type == "node_started" else terminal_state
            assert new_state is not None
            reason = {
                "node_started": "dispatch_receipt_adopted",
                "node_succeeded": "adapter_succeeded",
                "node_cancelled": "adapter_cancelled",
            }.get(event_type)
            if event_type == "node_failed":
                error_code = (
                    event.get("error", {}).get("code")
                    if isinstance(event.get("error"), Mapping)
                    else None
                )
                reason = {
                    "dispatch_not_started": "dispatch_not_started",
                    "worker_exit": "worker_exit_no_retry",
                }.get(error_code, "adapter_failed")

            receipt_document: dict[str, Any] | None = None
            receipt_document_json: str | None = None
            receipt_document_hash: str | None = None
            attempt = None
            observation = None
            dispatch_outcome = None
            handle_json = None
            handle_hash = None
            if terminal_state is not None:
                attempt = connection.execute(
                    """
                    SELECT * FROM worker_launch_attempts
                    WHERE intent_id = ? ORDER BY attempt_number DESC LIMIT 1
                    """,
                    (intent_id,),
                ).fetchone()
                observation = (
                    None
                    if attempt is None
                    else connection.execute(
                        """
                        SELECT * FROM worker_attempt_observations
                        WHERE attempt_id = ?
                        ORDER BY observation_sequence DESC LIMIT 1
                        """,
                        (attempt["attempt_id"],),
                    ).fetchone()
                )
                dispatch_outcome = connection.execute(
                    """
                    SELECT outcome_document_hash AS document_hash
                    FROM effective_dispatched_intents WHERE intent_id = ?
                    """,
                    (intent_id,),
                ).fetchone()
                if (
                    intent.state != "dispatched"
                    or intent.handle is None
                    or attempt is None
                    or int(attempt["attempt_number"]) != 1
                    or observation is None
                    or dispatch_outcome is None
                ):
                    raise TaskStoreConflict(
                        "DAG terminal transition lacks exact attempt-1 receipt evidence"
                    )
                handle_json, handle_hash = encode_document(intent.handle)
            if terminal_state == "Succeeded":
                assert supplied_output is not None
                manifests = supplied_output.get("manifests")
                receipt_record_hash = supplied_output.get("receipt_record_hash")
                if (
                    supplied_output.get("schema_version") != "1.0.0"
                    or supplied_output.get("intent_id") != intent_id
                    or supplied_output.get("attempt_id") != attempt["attempt_id"]
                    or supplied_output.get("attempt_number") != 1
                    or not _is_sha256(receipt_record_hash)
                    or not isinstance(manifests, list)
                    or not manifests
                    or any(not isinstance(value, Mapping) for value in manifests)
                ):
                    raise TaskStoreConflict("DAG succeeded output proof is invalid")
                plan_row = connection.execute(
                    "SELECT document_json FROM plans WHERE task_id = ? AND plan_id = ?",
                    (task_id, intent.plan_id),
                ).fetchone()
                if plan_row is None:
                    raise TaskStoreCorruption("DAG terminal plan cannot be read")
                plan_document = _decode_document(
                    plan_row["document_json"], label="DAG terminal plan"
                )
                planned_node = next(
                    (
                        value
                        for value in plan_document.get("nodes", [])
                        if value.get("node_id") == intent.node_id
                    ),
                    None,
                )
                planned_outputs = (
                    planned_node.get("outputs")
                    if isinstance(planned_node, Mapping)
                    else None
                )
                if not isinstance(planned_outputs, list) or not planned_outputs:
                    raise TaskStoreCorruption("DAG terminal output plan is invalid")
                manifests_by_port: dict[str, Mapping[str, Any]] = {}
                for manifest in manifests:
                    extension = manifest.get("extensions", {}).get(
                        "org.agent_rpc.adapter"
                    )
                    port = (
                        extension.get("output_port")
                        if isinstance(extension, Mapping)
                        else None
                    )
                    if not isinstance(port, str) or port in manifests_by_port:
                        raise TaskStoreConflict("DAG output inventory is invalid")
                    manifests_by_port[port] = manifest
                output_documents: list[dict[str, Any]] = []
                for planned_output in planned_outputs:
                    port = planned_output.get("port")
                    manifest = manifests_by_port.get(port)
                    if (
                        manifest is None
                        or manifest.get("artifact_type")
                        != planned_output.get("data_type")
                    ):
                        raise TaskStoreConflict(
                            "DAG output inventory differs from the exact Plan"
                        )
                    manifest_json, manifest_hash = encode_document(manifest)
                    output_documents.append(
                        {
                            "output_port": port,
                            "data_type": planned_output["data_type"],
                            "artifact_manifest": copy.deepcopy(dict(manifest)),
                            "artifact_manifest_hash": manifest_hash,
                        }
                    )
                    if manifest_json != encode_document(manifest)[0]:
                        raise TaskStoreCorruption("DAG artifact canonicalization drifted")
                if set(manifests_by_port) != {
                    value.get("port") for value in planned_outputs
                }:
                    raise TaskStoreConflict("DAG output inventory has extra artifacts")
                receipt_document = {
                    "schema_version": "2.0.0",
                    "task_id": task_id,
                    "plan": {
                        "plan_id": intent.plan_id,
                        "plan_hash": intent.plan_hash,
                    },
                    "approval_id": intent.approval_id,
                    "node": {
                        "node_id": intent.node_id,
                        "input_binding_revision": admission["pending_revision"],
                        "succeeded_revision": node_revision,
                        "state": "Succeeded",
                    },
                    "input_binding_document_hash": admission[
                        "input_binding_document_hash"
                    ],
                    "scope": {
                        "project_id": task["project_id"],
                        "principal_id": task["principal_id"],
                    },
                    "input_supervisor_term": {
                        "fencing_token": admission["input_fencing_token"],
                        "owner_id": admission["input_owner_id"],
                        "acquired_at": admission["input_term_acquired_at"],
                    },
                    "completion_supervisor_term": {
                        "fencing_token": supervisor_lease.fencing_token,
                        "owner_id": supervisor_lease.owner_id,
                        "acquired_at": supervisor_lease.acquired_at,
                    },
                    "dispatch": {
                        "intent_id": intent_id,
                        "node_idempotency_key": intent.node_idempotency_key,
                        "handle_hash": handle_hash,
                        "attempt_id": attempt["attempt_id"],
                        "attempt_number": 1,
                        "worker_observation_sequence": observation[
                            "observation_sequence"
                        ],
                        "worker_observation_hash": observation["document_hash"],
                        "outcome_document_hash": dispatch_outcome[
                            "document_hash"
                        ],
                    },
                    "receipt_record_hash": receipt_record_hash,
                    "outputs": output_documents,
                    "succeeded_at": event["occurred_at"],
                }
                receipt_document_json, receipt_document_hash = encode_document(
                    receipt_document
                )

            if terminal_state is not None:
                connection.execute(
                    """
                    INSERT INTO dag_node_terminal_facts(
                        intent_id, task_id, plan_id, plan_hash, approval_id,
                        node_id, input_binding_node_revision,
                        input_binding_document_hash, input_fencing_token,
                        input_owner_id, input_term_acquired_at,
                        node_revision, node_state, event_sequence, event_hash,
                        attempt_id, attempt_number, worker_observation_sequence,
                        worker_observation_hash, dispatch_handle_json,
                        dispatch_handle_hash,
                        dispatch_outcome_document_hash,
                        adapter_status_json, adapter_status_hash,
                        project_id, principal_id, completion_fencing_token,
                        completion_owner_id, completion_term_acquired_at,
                        receipt_document_json, receipt_document_hash,
                        recorded_at, recorded_at_us
                    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?,
                              ?, 1, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        intent_id,
                        task_id,
                        intent.plan_id,
                        intent.plan_hash,
                        intent.approval_id,
                        intent.node_id,
                        admission["pending_revision"],
                        admission["input_binding_document_hash"],
                        admission["input_fencing_token"],
                        admission["input_owner_id"],
                        admission["input_term_acquired_at"],
                        node_revision,
                        terminal_state,
                        next_sequence,
                        event_hash,
                        attempt["attempt_id"],
                        observation["observation_sequence"],
                        observation["document_hash"],
                        handle_json,
                        handle_hash,
                        dispatch_outcome["document_hash"],
                        adapter_status_json,
                        adapter_status_hash,
                        task["project_id"],
                        task["principal_id"],
                        supervisor_lease.fencing_token,
                        supervisor_lease.owner_id,
                        supervisor_lease.acquired_at,
                        receipt_document_json,
                        receipt_document_hash,
                        recorded_at,
                        recorded_at_us,
                    ),
                )
            connection.execute(
                """
                INSERT INTO dag_node_execution_transition_facts(
                    intent_id, node_revision, previous_state, state,
                    event_sequence, event_hash, reason,
                    project_id, principal_id, fencing_token,
                    owner_id, term_acquired_at, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    intent_id,
                    node_revision,
                    expected_node_state,
                    new_state,
                    next_sequence,
                    event_hash,
                    reason,
                    task["project_id"],
                    task["principal_id"],
                    supervisor_lease.fencing_token,
                    supervisor_lease.owner_id,
                    supervisor_lease.acquired_at,
                    recorded_at,
                    recorded_at_us,
                ),
            )
            connection.execute(
                """
                INSERT INTO dag_node_state_events(
                    task_id, plan_id, plan_hash, node_id, revision,
                    previous_state, state, recorded_at, recorded_at_us
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    intent.plan_id,
                    intent.plan_hash,
                    intent.node_id,
                    node_revision,
                    expected_node_state,
                    new_state,
                    recorded_at,
                    recorded_at_us,
                ),
            )
            if (
                terminal_state == "Succeeded"
                and observation["heartbeat_state"] == "succeeded"
            ):
                # A cancellation race may prove that the Adapter already won
                # without a terminal Worker heartbeat.  Preserve that P2
                # terminal projection, but never promote its weaker evidence
                # into a reusable node-cache source.
                source_state = DagNodeStateSnapshot(
                    task_id=task_id,
                    plan_id=intent.plan_id,
                    plan_hash=intent.plan_hash,
                    node_id=intent.node_id,
                    revision=node_revision,
                    state="Succeeded",
                    recorded_at=recorded_at,
                )
                succeeded = self._load_v20_dag_node_succeeded_outputs(
                    connection,
                    task_id=task_id,
                    plan=plan_document,
                    approval_id=intent.approval_id,
                    source_state=source_state,
                    admission=admission,
                )
                self._insert_executed_node_cache_entry(
                    connection,
                    task_id=task_id,
                    plan=plan_document,
                    approval_id=intent.approval_id,
                    source_state=source_state,
                    admission=admission,
                    succeeded=succeeded,
                    recorded_at=recorded_at,
                    recorded_at_us=recorded_at_us,
                )
            if terminal_state is not None:
                self._materialize_dag_blocked_nodes(
                    connection,
                    task_id=task_id,
                    plan=plan_document,
                    approval_id=intent.approval_id,
                    project_id=task["project_id"],
                    principal_id=task["principal_id"],
                    supervisor_lease=supervisor_lease,
                    recorded_at=recorded_at,
                    recorded_at_us=recorded_at_us,
                )
                reconciled_states = self._load_dag_node_state_map(
                    connection, task_id=task_id, plan=plan_document
                )
                if reconciled_states is None:
                    raise TaskStoreCorruption(
                        "terminal DAG state map cannot be read"
                    )
                reconciled_readiness = evaluate_dag_readiness(
                    plan_document,
                    node_states={
                        node.node_id: node.state
                        for node in reconciled_states.nodes
                    },
                )
                if self._dag_aggregate_status(reconciled_readiness) != aggregate_status:
                    raise TaskStoreCorruption(
                        "materialized DAG blockers changed the aggregate result"
                    )
            connection.execute(
                "UPDATE tasks SET status = ?, updated_at = ? WHERE task_id = ?",
                (aggregate_status, recorded_at, task_id),
            )
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None or snapshot.status != aggregate_status:
                raise TaskStoreCorruption("DAG node transition cannot be read")
            connection.commit()
            if terminal_state is None:
                return snapshot
            return DagNodeTerminalFact(
                snapshot=snapshot,
                intent=intent,
                node_revision=node_revision,
                state=terminal_state,
                event_sequence=next_sequence,
                completion_fencing_token=supervisor_lease.fencing_token,
                receipt_document_hash=receipt_document_hash,
                replayed=False,
            )
        except (RuntimeSupervisorLeaseLost, TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if "active term" in str(error):
                raise RuntimeSupervisorLeaseLost(
                    "DAG node transition lost its supervisor lease"
                ) from error
            raise TaskStoreConflict(
                "DAG node transition conflicts with durable state"
            ) from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def commit_runtime_transition(
        self,
        *,
        task_id: str,
        expected_status: str,
        event: Mapping[str, Any],
        now: str | None,
        supervisor_lease: RuntimeSupervisorLease | None = None,
        supervisor_clock: Callable[[], str] | None = None,
    ) -> TaskSnapshot:
        """Atomically update status and append an already validated RunEvent.

        This is an internal persistence primitive, not a submission API.
        AwaitingApproval -> Queued is owned by ``submit_task`` and its atomic
        registry, approval-budget, Gate, intent, and idempotency transaction.
        This primitive therefore rejects every pre-runtime state.
        """

        document_json, document_hash = encode_document(event)
        _, fingerprint_hash = encode_document(event["fingerprint"])
        node_id = event.get("node_id")
        new_status = event["task_status"]
        supervised_recorded_at: str | None = None
        supervised_recorded_at_us: int | None = None
        if supervisor_lease is not None:
            if not isinstance(supervisor_lease, RuntimeSupervisorLease):
                raise TaskStoreConflict("runtime supervisor lease is invalid")
            if not callable(supervisor_clock):
                raise TaskStoreConflict("runtime supervisor clock is invalid")
        elif supervisor_clock is not None:
            raise TaskStoreConflict("runtime supervisor clock has no lease")
        elif not isinstance(now, str):
            raise TaskStoreConflict("runtime transition clock is invalid")
        connection = self._connect()
        try:
            connection.execute("BEGIN IMMEDIATE")
            if supervisor_lease is not None:
                if supervisor_clock is None:
                    raise TaskStoreCorruption(
                        "runtime supervisor clock is unavailable"
                    )
                supervised_recorded_at, supervised_recorded_at_us = (
                    _runtime_timestamp(supervisor_clock())
                )
                now = supervised_recorded_at
            task = connection.execute(
                """
                SELECT status, project_id, principal_id
                FROM tasks WHERE task_id = ?
                """,
                (task_id,),
            ).fetchone()
            if task is None:
                raise TaskStoreConflict("task does not exist")
            if supervisor_lease is not None:
                current_lease = self._load_runtime_supervisor_lease(
                    connection,
                    project_id=task["project_id"],
                    principal_id=task["principal_id"],
                )
                if (
                    current_lease is None
                    or current_lease.state != "active"
                    or supervisor_lease.project_id != task["project_id"]
                    or supervisor_lease.principal_id != task["principal_id"]
                    or not self._runtime_supervisor_identity_matches(
                        current_lease, supervisor_lease
                    )
                    or supervised_recorded_at_us is None
                    or _runtime_timestamp(current_lease.heartbeat_at)[1]
                    > supervised_recorded_at_us
                    or _runtime_timestamp(current_lease.expires_at)[1]
                    <= supervised_recorded_at_us
                ):
                    raise RuntimeSupervisorLeaseLost(
                        "runtime transition lost its supervisor lease"
                    )
            if task["status"] != expected_status:
                raise TaskStoreConflict("task status precondition failed")
            if expected_status in {"Draft", "NeedsInput", "AwaitingApproval"}:
                raise TaskStoreConflict(
                    "runtime transition is unavailable before validated submission"
                )
            if event["task_id"] != task_id:
                raise TaskStoreConflict("event task_id does not match the task")
            if new_status not in ALLOWED_TRANSITIONS.get(expected_status, frozenset()):
                raise TaskStoreConflict("invalid task status transition")
            next_sequence = connection.execute(
                "SELECT COALESCE(MAX(sequence), 0) + 1 FROM run_events WHERE task_id = ?",
                (task_id,),
            ).fetchone()[0]
            if event["sequence"] != next_sequence:
                raise TaskStoreConflict("run event sequence is not the next value")
            if node_id is not None:
                baseline = connection.execute(
                    """
                    SELECT fingerprint_hash FROM run_events
                    WHERE task_id = ? AND node_id = ?
                    ORDER BY sequence ASC LIMIT 1
                    """,
                    (task_id, node_id),
                ).fetchone()
                if (
                    baseline is not None
                    and baseline["fingerprint_hash"] != fingerprint_hash
                ):
                    raise TaskStoreConflict(
                        "run fingerprint changed within a plan node"
                    )
            connection.execute(
                """
                INSERT INTO run_events(
                    task_id, sequence, event_id, event_type, task_status,
                    node_id, fingerprint_hash, document_json, document_hash,
                    occurred_at, recorded_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    task_id,
                    event["sequence"],
                    event["event_id"],
                    event["event_type"],
                    new_status,
                    node_id,
                    fingerprint_hash,
                    document_json,
                    document_hash,
                    event["occurred_at"],
                    now,
                ),
            )
            if supervisor_lease is not None:
                if (
                    supervised_recorded_at is None
                    or supervised_recorded_at_us is None
                ):
                    raise TaskStoreCorruption(
                        "supervised runtime timestamp is unavailable"
                    )
                heartbeat = connection.execute(
                    """
                    UPDATE runtime_supervisor_leases
                    SET heartbeat_at = ?, heartbeat_at_us = ?
                    WHERE project_id = ? AND principal_id = ?
                      AND fencing_token = ? AND owner_id = ?
                      AND acquired_at = ?
                    """,
                    (
                        supervised_recorded_at,
                        supervised_recorded_at_us,
                        task["project_id"],
                        task["principal_id"],
                        supervisor_lease.fencing_token,
                        supervisor_lease.owner_id,
                        supervisor_lease.acquired_at,
                    ),
                )
                if heartbeat.rowcount != 1:
                    raise RuntimeSupervisorLeaseLost(
                        "runtime transition lost its supervisor lease"
                    )
                connection.execute(
                    """
                    INSERT INTO supervised_run_event_commits(
                        task_id, sequence, project_id, principal_id,
                        fencing_token, recorded_at, recorded_at_us
                    ) VALUES (?, ?, ?, ?, ?, ?, ?)
                    """,
                    (
                        task_id,
                        event["sequence"],
                        task["project_id"],
                        task["principal_id"],
                        supervisor_lease.fencing_token,
                        supervised_recorded_at,
                        supervised_recorded_at_us,
                    ),
                )
            connection.execute(
                "UPDATE tasks SET status = ?, updated_at = ? WHERE task_id = ?",
                (new_status, now, task_id),
            )
            snapshot = self._load_snapshot(connection, task_id)
            if snapshot is None:
                raise TaskStoreCorruption("transitioned task cannot be read")
            connection.commit()
            return snapshot
        except (TaskStoreConflict, TaskStoreCorruption):
            if connection.in_transaction:
                connection.rollback()
            raise
        except sqlite3.OperationalError as error:
            if connection.in_transaction:
                connection.rollback()
            _raise_operational_error(error)
        except sqlite3.IntegrityError as error:
            if connection.in_transaction:
                connection.rollback()
            if (
                supervisor_lease is not None
                and "supervised run event requires the active term" in str(error)
            ):
                raise RuntimeSupervisorLeaseLost(
                    "runtime transition lost its supervisor lease"
                ) from error
            raise TaskStoreConflict("run event conflicts with durable state") from error
        except Exception:
            if connection.in_transaction:
                connection.rollback()
            raise
        finally:
            connection.close()

    def list_run_events(
        self, task_id: str, *, after_sequence: int = 0, limit: int = 100
    ) -> list[dict[str, Any]]:
        if after_sequence < 0:
            raise ValueError("after_sequence must be non-negative")
        if limit < 1 or limit > 1000:
            raise ValueError("limit must be between 1 and 1000")
        connection = self._connect()
        try:
            if connection.execute(
                "SELECT 1 FROM tasks WHERE task_id = ?", (task_id,)
            ).fetchone() is None:
                raise TaskStoreConflict("task does not exist")
            rows = connection.execute(
                """
                SELECT task_id, sequence, event_id, event_type, task_status,
                       node_id, fingerprint_hash, occurred_at,
                       document_json, document_hash
                FROM run_events
                WHERE task_id = ? AND sequence > ?
                ORDER BY sequence ASC LIMIT ?
                """,
                (task_id, after_sequence, limit),
            ).fetchall()
            events: list[dict[str, Any]] = []
            for row in rows:
                events.append(_decode_run_event_row(row))
            return events
        finally:
            connection.close()

    def latest_run_event_sequence(self, task_id: str) -> int:
        """Return a stable high-water mark for one bounded event scan."""

        connection = self._connect()
        try:
            if connection.execute(
                "SELECT 1 FROM tasks WHERE task_id = ?", (task_id,)
            ).fetchone() is None:
                raise TaskStoreConflict("task does not exist")
            row = connection.execute(
                "SELECT COALESCE(MAX(sequence), 0) AS sequence "
                "FROM run_events WHERE task_id = ?",
                (task_id,),
            ).fetchone()
            sequence = row["sequence"] if row is not None else None
            if type(sequence) is not int or sequence < 0:
                raise TaskStoreCorruption("run event high-water mark is invalid")
            return sequence
        finally:
            connection.close()

    def draft_history(self, task_id: str) -> list[dict[str, Any]]:
        connection = self._connect()
        try:
            rows = connection.execute(
                """
                SELECT draft_id, revision, document_json, document_hash
                FROM draft_revisions
                WHERE task_id = ? ORDER BY revision ASC
                """,
                (task_id,),
            ).fetchall()
            values: list[dict[str, Any]] = []
            for row in rows:
                draft = _decode_hashed_document(row, label="draft")
                if (
                    draft.get("draft_id") != row["draft_id"]
                    or draft.get("revision") != row["revision"]
                ):
                    raise TaskStoreCorruption(
                        "persisted draft identity does not match its index"
                    )
                values.append(draft)
            return values
        finally:
            connection.close()

    def plan_history(self, task_id: str) -> list[dict[str, Any]]:
        connection = self._connect()
        try:
            rows = connection.execute(
                """
                SELECT plan_id, draft_id, draft_revision, plan_hash,
                       document_json, document_hash
                FROM plans
                WHERE task_id = ? ORDER BY recorded_at ASC, plan_id ASC
                """,
                (task_id,),
            ).fetchall()
            values: list[dict[str, Any]] = []
            for row in rows:
                plan = _decode_hashed_document(row, label="plan")
                if (
                    plan.get("plan_id") != row["plan_id"]
                    or plan.get("plan_hash") != row["plan_hash"]
                    or plan.get("draft")
                    != {
                        "draft_id": row["draft_id"],
                        "revision": row["draft_revision"],
                    }
                ):
                    raise TaskStoreCorruption(
                        "persisted plan identity does not match its index"
                    )
                values.append(plan)
            return values
        finally:
            connection.close()

    def approval_history(self, task_id: str) -> list[dict[str, Any]]:
        connection = self._connect()
        try:
            rows = connection.execute(
                """
                SELECT approval_id, plan_id, plan_hash, decision,
                       document_json, document_hash
                FROM approvals
                WHERE task_id = ? ORDER BY recorded_at ASC, approval_id ASC
                """,
                (task_id,),
            ).fetchall()
            values: list[dict[str, Any]] = []
            for row in rows:
                approval = _decode_hashed_document(row, label="approval")
                if (
                    approval.get("approval_id") != row["approval_id"]
                    or approval.get("plan_id") != row["plan_id"]
                    or approval.get("plan_hash") != row["plan_hash"]
                    or approval.get("decision") != row["decision"]
                ):
                    raise TaskStoreCorruption(
                        "persisted approval identity does not match its index"
                    )
                budget = connection.execute(
                    """
                    SELECT max_tasks, tasks_used FROM approval_budgets
                    WHERE task_id = ? AND approval_id = ?
                    """,
                    (task_id, row["approval_id"]),
                ).fetchone()
                scope = approval.get("scope")
                if (
                    budget is None
                    or not isinstance(scope, dict)
                    or type(budget["max_tasks"]) is not int
                    or type(budget["tasks_used"]) is not int
                    or budget["max_tasks"] != scope.get("max_tasks")
                    or budget["tasks_used"] < 0
                    or budget["tasks_used"] > budget["max_tasks"]
                ):
                    raise TaskStoreCorruption(
                        "persisted approval budget does not match its decision"
                    )
                values.append(approval)
            return values
        finally:
            connection.close()
